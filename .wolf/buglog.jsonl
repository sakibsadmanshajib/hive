{"id":"bug-001","timestamp":"2026-06-12T03:51:55.203Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a1d5f0532292929a3/apps/control-plane/cmd/server/main.go","root_cause":"2 lines replaced/restructured","fix":"Rewrote 13→23 lines (2 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-12T03:51:55.203Z"}
{"id":"bug-002","timestamp":"2026-06-12T03:51:59.669Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a1d5f0532292929a3/apps/control-plane/internal/litellmconfig/generator.go","root_cause":"2 lines replaced/restructured","fix":"Rewrote 7→12 lines (2 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-12T03:51:59.669Z"}
{"id":"bug-003","timestamp":"2026-06-12T03:52:01.513Z","error_message":"Incorrect value in code","file":".claude/worktrees/agent-a1d5f0532292929a3/apps/control-plane/internal/litellmconfig/restart.go","root_cause":"Had `^[a-zA-Z0-9][a-zA-Z0-9_./-]*$`","fix":"Changed to `^[a-zA-Z0-9][a-zA-Z0-9_.-]*$`","tags":["auto-detected","wrong-value","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-12T03:52:01.513Z"}
{"id":"bug-004","timestamp":"2026-06-12T03:55:00Z","error_message":"deploy-staging run 27392766395 failed; reported as staging 502 regression","file":"packages/sdk-tests/js/tests/chat-completions/chat-completions.test.ts","root_cause":"SDK replay tests still asserted pre-phase-20 unconditional 400 for tools/response_format; PR #206 shipped capability passthrough so requests succeed and tests fail with promise resolved instead of rejecting. Health endpoints were 200 the whole time.","fix":"PR #211 updates both tests to assert tool_calls finish_reason and parseable JSON output","tags":["deploy-staging","sdk-tests","phase-20","tools","stale-test"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-12T03:55:00Z"}
{"id":"bug-005","timestamp":"2026-06-13T08:44:00.078Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a33bf105ae05398d9/apps/control-plane/internal/litellmconfig/sync_integration_test.go","root_cause":"3 lines replaced/restructured","fix":"Rewrote 12→14 lines (3 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T08:44:00.078Z"}
{"id":"bug-006","timestamp":"2026-06-13T08:44:03.339Z","error_message":"Wrong reference: mockRestarter should be integMockRestarter","file":".claude/worktrees/agent-a33bf105ae05398d9/apps/control-plane/internal/litellmconfig/sync_integration_test.go","root_cause":"Used \"mockRestarter\" instead of \"integMockRestarter\"","fix":"Changed mockRestarter → integMockRestarter","tags":["auto-detected","wrong-reference","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T08:44:03.339Z"}
{"id":"bug-007","timestamp":"2026-06-13T08:51:42.784Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-aa27e96f8e15eb950/apps/control-plane/internal/providers/integration_test.go","root_cause":"3 lines replaced/restructured","fix":"Rewrote 18→35 lines (3 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T08:51:42.784Z"}
{"id":"bug-008","timestamp":"2026-06-13T09:37:37.499Z","error_message":"Missing error handling in unknown","file":"../.claude/projects/-home-sakib-hive/memory/MEMORY.md","root_cause":"Code path had no error handling — exceptions would propagate uncaught","fix":"Added try/catch block","tags":["auto-detected","error-handling","md"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T09:37:37.499Z"}
{"id":"bug-009","timestamp":"2026-06-13T10:28:29.437Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a2e35737dbb42549b/apps/control-plane/cmd/server/main.go","root_cause":"5 lines replaced/restructured","fix":"Rewrote 25→36 lines (5 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T10:28:29.437Z"}
{"id":"bug-010","timestamp":"2026-06-13T10:38:03.025Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a42a92a2b90af54a5/deploy/docker/buildx.sh","root_cause":"7 lines replaced/restructured","fix":"Rewrote 12→20 lines (7 removed)","tags":["auto-detected","refactor","sh"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T10:38:03.025Z"}
{"id":"bug-011","timestamp":"2026-06-13T10:38:10.262Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a42a92a2b90af54a5/deploy/oci/catch-a1.sh","root_cause":"3 lines replaced/restructured","fix":"Rewrote 11→23 lines (3 removed)","tags":["auto-detected","refactor","sh"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T10:38:10.262Z"}
{"id":"bug-012","timestamp":"2026-06-13T10:38:16.912Z","error_message":"Incorrect value in code","file":".claude/worktrees/agent-a42a92a2b90af54a5/deploy/oci/catch-a1.sh","root_cause":"Had \"{\\\"","fix":"Changed to \"$(jq -n --arg k \"","tags":["auto-detected","wrong-value","sh"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T10:38:16.912Z"}
{"id":"bug-013","timestamp":"2026-06-13T12:10:07.486Z","error_message":"Incorrect value in code","file":".claude/worktrees/agent-ac8486d870776d3c9/website/sovereign/package.json","root_cause":"Had \"^19.2.7\"","fix":"Changed to \"^19.2.17\"","tags":["auto-detected","wrong-value","json"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T12:10:07.486Z"}
{"id":"bug-014","timestamp":"2026-06-13T12:15:54.268Z","error_message":"Missing await","file":".claude/worktrees/agent-ac8486d870776d3c9/website/sovereign/src/pages/pricing.astro","root_cause":"Async call without await — returned Promise instead of value","fix":"Added await to async call","tags":["auto-detected","async-fix","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T12:15:54.268Z"}
{"id":"bug-015","timestamp":"2026-06-13T12:24:08.661Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-ae547274b88e323bf/website/sovereign/src/integrations/content-lint.ts","root_cause":"2 lines replaced/restructured","fix":"Rewrote 4→10 lines (2 removed) | Also: // 2. Sovereignty wording only in scope:onprem pag; if (scope !== 'onprem') {","tags":["auto-detected","refactor","ts"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-13T12:24:21.047Z"}
{"id":"bug-016","timestamp":"2026-06-13T12:24:29.677Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-ae547274b88e323bf/website/sovereign/SPEC.md","root_cause":"2 lines replaced/restructured","fix":"Rewrote 7→11 lines (2 removed)","tags":["auto-detected","refactor","md"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T12:24:29.677Z"}
{"id":"bug-017","timestamp":"2026-06-13T12:25:01.029Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-ae547274b88e323bf/website/sovereign/tailwind.config.ts","root_cause":"4 lines replaced/restructured","fix":"Rewrote 5→7 lines (4 removed)","tags":["auto-detected","refactor","ts"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T12:25:01.029Z"}
{"id":"bug-018","timestamp":"2026-06-13T12:25:58.109Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-ae547274b88e323bf/website/sovereign/astro.config.mjs","root_cause":"3 lines replaced/restructured","fix":"Rewrote 3→6 lines (3 removed) | Also: vite: {; plugins: [tailwindcss()],","tags":["auto-detected","refactor","mjs"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-13T12:26:02.331Z"}
{"id":"bug-019","timestamp":"2026-06-13T12:26:52.275Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-ae547274b88e323bf/website/sovereign/src/styles/global.css","root_cause":"9 lines replaced/restructured","fix":"Rewrote 13→55 lines (9 removed)","tags":["auto-detected","refactor","css"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T12:26:52.275Z"}
{"id":"bug-020","timestamp":"2026-06-13T20:05:06.574Z","error_message":"Wrong reference: production should be example","file":".claude/worktrees/agent-a6e2bb346c404e627/website/sovereign/.gitignore","root_cause":"Used \"production\" instead of \"example\"","fix":"Changed production → example","tags":["auto-detected","wrong-reference","unknown"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T20:05:06.574Z"}
{"id":"bug-021","timestamp":"2026-06-13T20:06:09.033Z","error_message":"Type error","file":".claude/worktrees/agent-a6e2bb346c404e627/website/sovereign/src/integrations/content-lint.ts","root_cause":"Missing or incorrect type annotation","fix":"Added type assertion/annotation","tags":["auto-detected","type-fix","ts"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T20:06:09.033Z"}
{"id":"bug-022","timestamp":"2026-06-13T20:07:20.089Z","error_message":"CSS fix: --font-mono","file":".claude/worktrees/agent-a6e2bb346c404e627/website/sovereign/src/styles/tokens.css","root_cause":"--font-mono: 'Geist Mono', ui-monospace, SFMono-Regular, monospace → 'Geist Mono', ui-monospace, 'SFMono-Regular', monospace","fix":"Changed --font-mono: 'Geist Mono', ui-monospace, SFMono-Regular, monospace → 'Geist Mono', ui-monospace, 'SFMono-Regular', monospace","tags":["auto-detected","style-fix","css"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T20:07:20.089Z"}
{"id":"bug-023","timestamp":"2026-06-13T20:25:05.835Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-ab5a490baec38f8dc/deploy/gcp/provision.sh","root_cause":"3 lines replaced/restructured","fix":"Rewrote 3→7 lines (3 removed)","tags":["auto-detected","refactor","sh"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T20:25:05.835Z"}
{"id":"bug-024","timestamp":"2026-06-13T20:25:47.541Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-ab5a490baec38f8dc/deploy/gcp/README.md","root_cause":"2 lines replaced/restructured","fix":"Rewrote 14→25 lines (2 removed)","tags":["auto-detected","refactor","md"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T20:25:47.541Z"}
{"id":"bug-025","timestamp":"2026-06-13T20:26:05.770Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-ade02a7f88703169c/scripts/seed-demo/main.go","root_cause":"3 lines replaced/restructured","fix":"Rewrote 10→18 lines (3 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T20:26:05.770Z"}
{"id":"bug-026","timestamp":"2026-06-13T22:48:03.953Z","error_message":"Wrong reference: span should be Technology","file":"website/sovereign/src/components/Footer.astro","root_cause":"Used \"span\" instead of \"Technology\"","fix":"Changed span → Technology","tags":["auto-detected","wrong-reference","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T22:48:03.953Z"}
{"id":"bug-027","timestamp":"2026-06-13T22:48:08.804Z","error_message":"Wrong reference: dark should be light","file":"website/sovereign/src/styles/global.css","root_cause":"Used \"dark\" instead of \"light\"","fix":"Changed dark → light","tags":["auto-detected","wrong-reference","css"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T22:48:08.804Z"}
{"id":"bug-028","timestamp":"2026-06-13T22:48:16.018Z","error_message":"Incorrect value in code","file":"website/sovereign/src/layouts/Base.astro","root_cause":"Had \"#0a0e16\"","fix":"Changed to \"#f8f7f4\"","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T22:48:16.018Z"}
{"id":"bug-029","timestamp":"2026-06-13T22:52:24.902Z","error_message":"Wrong reference: 1a1205 should be var","file":"website/sovereign/src/components/Nav.astro","root_cause":"Used \"1a1205\" instead of \"var\"","fix":"Changed 1a1205 → var","tags":["auto-detected","wrong-reference","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T22:52:24.902Z"}
{"id":"bug-030","timestamp":"2026-06-13T23:37:43.278Z","error_message":"Significant refactor of ","file":"website/sovereign/src/pages/index.astro","root_cause":"2 lines replaced/restructured","fix":"Rewrote 10→11 lines (2 removed)","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-13T23:37:43.278Z"}
{"id":"bug-031","timestamp":"2026-06-14T00:46:48.654Z","error_message":"Null/undefined access in ","file":"website/sovereign/src/layouts/Base.astro","root_cause":"Property access on potentially null/undefined value","fix":"Added null safety (optional chaining or null check)","tags":["auto-detected","null-safety","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T00:46:48.654Z"}
{"id":"bug-032","timestamp":"2026-06-14T01:10:48.505Z","error_message":"CSS fix: --text-muted, --text-faint","file":"website/sovereign/src/styles/tokens.css","root_cause":"--text-muted: #6b7585 → #4a515e; --text-faint: #8b93a1 → #707886","fix":"Changed --text-muted: #6b7585 → #4a515e; --text-faint: #8b93a1 → #707886","tags":["auto-detected","style-fix","css"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-14T01:10:52.414Z"}
{"id":"bug-033","timestamp":"2026-06-14T01:47:18.833Z","error_message":"Incorrect value in code","file":".tmp-gallery/logos.html","root_cause":"Had \"file:///home/sakib/hive/website/sovereign/src/ass","fix":"Changed to \"/website/sovereign/src/assets/brand/concepts/\"","tags":["auto-detected","wrong-value","html"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T01:47:18.833Z"}
{"id":"bug-034","timestamp":"2026-06-14T02:01:50.914Z","error_message":"Significant refactor of ","file":"website/sovereign/src/components/Nav.astro","root_cause":"4 lines replaced/restructured","fix":"Rewrote 6→16 lines (4 removed)","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T02:01:50.914Z"}
{"id":"bug-035","timestamp":"2026-06-14T02:02:32.013Z","error_message":"Incorrect value in code","file":"../../../tmp/gen-favicons.mjs","root_cause":"Had 'sharp'","fix":"Changed to 'node:module'","tags":["auto-detected","wrong-value","mjs"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T02:02:32.013Z"}
{"id":"bug-036","timestamp":"2026-06-14T02:02:44.611Z","error_message":"Incorrect value in code","file":"website/sovereign/src/pages/index.astro","root_cause":"Had '../assets/generated/slot1-hero3.jpg'","fix":"Changed to '../assets/generated/heroB/product-render-1-1.jpg'","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T02:02:44.611Z"}
{"id":"bug-037","timestamp":"2026-06-14T02:17:13.674Z","error_message":"Significant refactor of ","file":"website/sovereign/src/components/Figure.astro","root_cause":"2 lines replaced/restructured","fix":"Rewrote 8→16 lines (2 removed)","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T02:17:13.674Z"}
{"id":"bug-038","timestamp":"2026-06-14T02:17:26.196Z","error_message":"Significant refactor of ","file":"website/sovereign/src/pages/index.astro","root_cause":"4 lines replaced/restructured","fix":"Rewrote 13→19 lines (4 removed) | Also: /* Product render placed directly on the off-white; frame. The render already sits on a seamless off-w | Also: animation does not run. transform-origin keeps the; transform-origin: center; | Also: /* Hero collapses to a single column. The render m; runs full width on off-white at its natural height","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":4,"last_seen":"2026-06-14T02:17:54.508Z"}
{"id":"bug-039","timestamp":"2026-06-14T02:35:40.633Z","error_message":"Significant refactor of ","file":"website/sovereign/src/pages/pricing.astro","root_cause":"4 lines replaced/restructured","fix":"Rewrote 7→9 lines (4 removed)","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T02:35:40.633Z"}
{"id":"bug-040","timestamp":"2026-06-14T02:43:50.246Z","error_message":"Incorrect value in code","file":"website/sovereign/src/pages/security.astro","root_cause":"Had \"card\"","fix":"Changed to \"card card--static\"","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-14T02:43:53.856Z"}
{"id":"bug-041","timestamp":"2026-06-14T02:44:04.470Z","error_message":"Incorrect value in code","file":"website/sovereign/src/pages/pricing.astro","root_cause":"Had \"card\"","fix":"Changed to \"card card--static\"","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":4,"last_seen":"2026-06-14T02:44:13.427Z"}
{"id":"bug-042","timestamp":"2026-06-14T02:55:21.418Z","error_message":"Incorrect value in code","file":"website/sovereign/src/pages/index.astro","root_cause":"Had '../assets/generated/heroB/product-render-1-1.jpg'","fix":"Changed to '../assets/generated/heroB2/ink-cinematic-1.jpg'","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T02:55:21.418Z"}
{"id":"bug-043","timestamp":"2026-06-14T02:55:32.569Z","error_message":"Significant refactor of ","file":"website/sovereign/src/pages/index.astro","root_cause":"5 lines replaced/restructured","fix":"Rewrote 5→8 lines (5 removed)","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T02:55:32.569Z"}
{"id":"bug-044","timestamp":"2026-06-14T07:49:09.011Z","error_message":"Incorrect value in code","file":"website/sovereign/src/assets/diagrams/data-boundary.svg","root_cause":"Had \"d-surface\"","fix":"Changed to \"d-grey-stroke\"","tags":["auto-detected","wrong-value","svg"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T07:49:09.011Z"}
{"id":"bug-045","timestamp":"2026-06-14T07:51:50.098Z","error_message":"Significant refactor of ","file":"website/sovereign/src/pages/index.astro","root_cause":"4 lines replaced/restructured","fix":"Rewrote 8→14 lines (4 removed) | Also: <div class=\"ds__bleed\">; <Image | Also: // Curated photographic images, optimized by astro; import sectionImg from '../assets/generated/slot6- | Also: 06 CONTINUOUS IMPROVEMENT  (photo inside Figure fr; .improve__img {","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":4,"last_seen":"2026-06-14T07:55:42.930Z"}
{"id":"bug-046","timestamp":"2026-06-14T07:53:16.556Z","error_message":"Significant refactor of ","file":"website/sovereign/src/components/TrustStrip.astro","root_cause":"2 lines replaced/restructured","fix":"Rewrote 20→34 lines (2 removed)","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T07:53:16.556Z"}
{"id":"bug-047","timestamp":"2026-06-14T07:54:52.873Z","error_message":"Incorrect value in code","file":"website/sovereign/src/pages/index.astro","root_cause":"Had \"/use-cases\"","fix":"Changed to \"/use-cases#finance\"","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":5,"last_seen":"2026-06-14T07:55:07.248Z"}
{"id":"bug-048","timestamp":"2026-06-14T19:12:07.118Z","error_message":"Incorrect value in code","file":"website/sovereign/src/pages/index.astro","root_cause":"Had \"Sovereign On-Prem AI\"","fix":"Changed to \"On-Premises AI for Regulated Industries\"","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":5,"last_seen":"2026-06-14T19:12:33.438Z"}
{"id":"bug-049","timestamp":"2026-06-14T19:12:49.634Z","error_message":"Incorrect value in code","file":"website/sovereign/src/layouts/Base.astro","root_cause":"Had \"en\"","fix":"Changed to \"en-CA\"","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T19:12:49.634Z"}
{"id":"bug-050","timestamp":"2026-06-14T19:13:11.142Z","error_message":"Significant refactor of ","file":"website/sovereign/src/components/StructuredData.astro","root_cause":"2 lines replaced/restructured","fix":"Rewrote 19→28 lines (2 removed)","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T19:13:11.142Z"}
{"id":"bug-051","timestamp":"2026-06-14T19:13:14.478Z","error_message":"Incorrect value in code","file":"website/sovereign/src/components/StructuredData.astro","root_cause":"Had 'Worldwide'","fix":"Changed to 'Canada'","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T19:13:14.478Z"}
{"id":"bug-052","timestamp":"2026-06-14T19:14:51.744Z","error_message":"Incorrect value in code","file":"website/sovereign/src/pages/use-cases.astro","root_cause":"Had \"Use Cases\"","fix":"Changed to \"On-Prem AI for Finance, Healthcare and Legal\"","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T19:14:51.744Z"}
{"id":"bug-053","timestamp":"2026-06-14T19:15:03.686Z","error_message":"Incorrect value in code","file":"website/sovereign/src/pages/how-you-run-hive.astro","root_cause":"Had \"How You Run Hive\"","fix":"Changed to \"Self-Hosted AI Deployment Options\"","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T19:15:03.686Z"}
{"id":"bug-054","timestamp":"2026-06-14T19:19:44.076Z","error_message":"Incorrect value in code","file":"website/sovereign/src/pages/index.astro","root_cause":"Had '../assets/generated/heroB2/ink-cinematic-1.jpg'","fix":"Changed to '../assets/generated/heroC/hero-final.png'","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T19:19:44.076Z"}
{"id":"bug-055","timestamp":"2026-06-14T19:20:14.098Z","error_message":"Significant refactor of ","file":"website/sovereign/src/pages/index.astro","root_cause":"24 lines replaced/restructured","fix":"Rewrote 59→49 lines (24 removed) | Also: HERO  (cinematic ink band: real two-column grid, e; left, seamless full-bleed appliance on the right) | Also: /* Ink panel settles in: scale 1.02 to 1 over ~620; Starts near-full (0.85) so content is never hidden","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":3,"last_seen":"2026-06-14T19:21:43.439Z"}
{"id":"bug-056","timestamp":"2026-06-14T19:31:21.937Z","error_message":"Significant refactor of ","file":"website/sovereign/src/pages/index.astro","root_cause":"10 lines replaced/restructured","fix":"Rewrote 65→114 lines (10 removed)","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T19:31:21.937Z"}
{"id":"bug-057","timestamp":"2026-06-14T19:31:35.397Z","error_message":"Significant refactor of ","file":"website/sovereign/src/assets/diagrams/cost-crossover.svg","root_cause":"5 lines replaced/restructured","fix":"Rewrote 14→18 lines (5 removed)","tags":["auto-detected","refactor","svg"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T19:31:35.397Z"}
{"id":"bug-058","timestamp":"2026-06-14T19:36:22.631Z","error_message":"Significant refactor of ","file":"website/sovereign/src/components/Nav.astro","root_cause":"3 lines replaced/restructured","fix":"Rewrote 18→23 lines (3 removed) | Also: /* details becomes a block container that spans th; position: relative;","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-14T19:37:33.584Z"}
{"id":"bug-059","timestamp":"2026-06-14T19:56:17.663Z","error_message":"CSS fix: color-scheme","file":"website/sovereign/src/styles/global.css","root_cause":"color-scheme: light → dark","fix":"Changed color-scheme: light → dark","tags":["auto-detected","style-fix","css"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T19:56:17.663Z"}
{"id":"bug-060","timestamp":"2026-06-14T19:56:23.411Z","error_message":"Incorrect value in code","file":"website/sovereign/src/layouts/Base.astro","root_cause":"Had \"#f8f7f4\"","fix":"Changed to \"#0c0e12\"","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T19:56:23.411Z"}
{"id":"bug-061","timestamp":"2026-06-14T19:56:37.401Z","error_message":"Incorrect value in code","file":"website/sovereign/public/site.webmanifest","root_cause":"Had \"#f8f7f4\"","fix":"Changed to \"#0c0e12\"","tags":["auto-detected","wrong-value","webmanifest"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T19:56:37.401Z"}
{"id":"bug-062","timestamp":"2026-06-14T19:56:42.814Z","error_message":"Incorrect value in code","file":"website/sovereign/src/pages/index.astro","root_cause":"Had '../assets/generated/heroC/hero-final.png'","fix":"Changed to '../assets/generated/heroDark/hero-dark-final.png'","tags":["auto-detected","wrong-value","astro"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-14T19:56:46.648Z"}
{"id":"bug-063","timestamp":"2026-06-14T19:56:54.163Z","error_message":"Significant refactor of ","file":"website/sovereign/src/pages/index.astro","root_cause":"2 lines replaced/restructured","fix":"Rewrote 10→12 lines (2 removed) | Also: Inline attributes in the SVGs already paint the co; :global hooks let the assets re-tint cleanly from  | Also: color: var(--bg);; border-color: var(--text-faint);","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":3,"last_seen":"2026-06-14T19:58:23.485Z"}
{"id":"bug-064","timestamp":"2026-06-14T19:58:39.050Z","error_message":"Significant refactor of ","file":"website/sovereign/src/components/Nav.astro","root_cause":"6 lines replaced/restructured","fix":"Rewrote 33→54 lines (6 removed)","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T19:58:39.050Z"}
{"id":"bug-065","timestamp":"2026-06-14T20:15:35.852Z","error_message":"Significant refactor of ","file":"website/sovereign/src/pages/index.astro","root_cause":"4 lines replaced/restructured","fix":"Rewrote 12→23 lines (4 removed)","tags":["auto-detected","refactor","astro"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T20:15:35.852Z"}
{"id":"bug-066","timestamp":"2026-06-14T20:15:52.907Z","error_message":"Significant refactor of ","file":"website/sovereign/src/assets/diagrams/data-boundary.svg","root_cause":"3 lines replaced/restructured","fix":"Rewrote 7→10 lines (3 removed)","tags":["auto-detected","refactor","svg"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-14T20:15:52.907Z"}
{"id":"bug-067","timestamp":"2026-06-25T21:38:04.965Z","error_message":"Wrong reference: __E__ should be 231","file":"../../../tmp/claude-1001/-home-sakib-hive/4422c273-7623-4109-8443-8f64b8d0e4a1/scratchpad/issueB.md","root_cause":"Used \"__E__\" instead of \"231\"","fix":"Changed __E__ → 231","tags":["auto-detected","wrong-reference","md"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T21:38:04.965Z"}
{"id":"bug-068","timestamp":"2026-06-25T21:38:09.198Z","error_message":"Wrong reference: __B__ should be 232","file":"../../../tmp/claude-1001/-home-sakib-hive/4422c273-7623-4109-8443-8f64b8d0e4a1/scratchpad/issueE.md","root_cause":"Used \"__B__\" instead of \"232\"","fix":"Changed __B__ → 232","tags":["auto-detected","wrong-reference","md"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T21:38:09.198Z"}
{"id":"bug-069","timestamp":"2026-06-25T21:38:13.208Z","error_message":"Wrong reference: __B__ should be 232","file":"../../../tmp/claude-1001/-home-sakib-hive/4422c273-7623-4109-8443-8f64b8d0e4a1/scratchpad/issueF.md","root_cause":"Used \"__B__\" instead of \"232\"","fix":"Changed __B__ → 232","tags":["auto-detected","wrong-reference","md"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-25T21:38:17.733Z"}
{"id":"bug-070","timestamp":"2026-06-25T21:57:17.287Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a7b10e71ea2d33500/apps/edge-api/internal/anthropic/stream.go","root_cause":"6 lines replaced/restructured","fix":"Rewrote 15→30 lines (6 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T21:57:17.287Z"}
{"id":"bug-071","timestamp":"2026-06-25T21:59:27.073Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a7b10e71ea2d33500/apps/edge-api/cmd/server/main.go","root_cause":"3 lines replaced/restructured","fix":"Rewrote 9→6 lines (3 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T21:59:27.073Z"}
{"id":"bug-072","timestamp":"2026-06-25T22:09:09.901Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a442347ecfb509263/deploy/docker/docker-compose.yml","root_cause":"5 lines replaced/restructured","fix":"Rewrote 12→8 lines (5 removed) | Also: ANON_KEY: ${ENTERPRISE_ANON_KEY:-}; SERVICE_KEY: ${ENTERPRISE_SERVICE_ROLE_KEY:-} | Also: # These services activate ONLY under the `enterpri; # The cloud/local/chat profiles continue to use ho","tags":["auto-detected","refactor","yml"],"related_bugs":[],"occurrences":3,"last_seen":"2026-06-25T22:10:30.805Z"}
{"id":"bug-073","timestamp":"2026-06-25T22:09:31.285Z","error_message":"Wrong reference: entrypoint should be set","file":".claude/worktrees/agent-a442347ecfb509263/deploy/docker/docker-compose.yml","root_cause":"Used \"entrypoint\" instead of \"set\"","fix":"Changed entrypoint → set","tags":["auto-detected","wrong-reference","yml"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T22:09:31.285Z"}
{"id":"bug-074","timestamp":"2026-06-25T22:10:23.400Z","error_message":"Wrong reference: ActionLLMResponse should be audit","file":".claude/worktrees/agent-abd95b839d1d4f4e8/apps/control-plane/internal/audit/actions_test.go","root_cause":"Used \"ActionLLMResponse\" instead of \"audit\"","fix":"Changed ActionLLMResponse → audit","tags":["auto-detected","wrong-reference","go"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-25T22:10:31.124Z"}
{"id":"bug-075","timestamp":"2026-06-25T22:13:25.953Z","error_message":"Wrong reference: 20260625_01_audit_taxonomy_extension should be 20260625_03_audit_taxonomy_extension","file":".claude/worktrees/agent-abd95b839d1d4f4e8/supabase/migrations/20260625_03_audit_taxonomy_extension.sql","root_cause":"Used \"20260625_01_audit_taxonomy_extension\" instead of \"20260625_03_audit_taxonomy_extension\"","fix":"Changed 20260625_01_audit_taxonomy_extension → 20260625_03_audit_taxonomy_extension","tags":["auto-detected","wrong-reference","sql"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T22:13:25.953Z"}
{"id":"bug-076","timestamp":"2026-06-25T22:16:29.574Z","error_message":"Incorrect value in code","file":".claude/worktrees/agent-a442347ecfb509263/scripts/install.sh","root_cause":"Had '    docker compose --env-file %s/.env --profile e","fix":"Changed to '    docker compose -f %s/deploy/docker/docker-com","tags":["auto-detected","wrong-value","sh"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-25T22:16:36.861Z"}
{"id":"bug-077","timestamp":"2026-06-25T22:24:34.537Z","error_message":"Wrong reference: start_stack should be patch_sovereign_config","file":".claude/worktrees/agent-a9006a563f7dcf922/scripts/install.sh","root_cause":"Used \"start_stack\" instead of \"patch_sovereign_config\"","fix":"Changed start_stack → patch_sovereign_config","tags":["auto-detected","wrong-reference","sh"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T22:24:34.537Z"}
{"id":"bug-078","timestamp":"2026-06-25T22:25:29.254Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a9006a563f7dcf922/apps/control-plane/cmd/server/audit_sinks_gate_test.go","root_cause":"2 lines replaced/restructured","fix":"Rewrote 7→5 lines (2 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T22:25:29.254Z"}
{"id":"bug-079","timestamp":"2026-06-25T22:26:57.596Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a442347ecfb509263/deploy/docker/docker-compose.enterprise.yml","root_cause":"4 lines replaced/restructured","fix":"Rewrote 16→20 lines (4 removed)","tags":["auto-detected","refactor","yml"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T22:26:57.596Z"}
{"id":"bug-080","timestamp":"2026-06-25T22:29:43.665Z","error_message":"Missing error handling in unknown","file":".claude/worktrees/agent-a9006a563f7dcf922/scripts/install.sh","root_cause":"Code path had no error handling — exceptions would propagate uncaught","fix":"Added try/catch block","tags":["auto-detected","error-handling","sh"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T22:29:43.665Z"}
{"id":"bug-081","timestamp":"2026-06-25T22:30:06.081Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a7b10e71ea2d33500/apps/edge-api/internal/anthropic/handler_test.go","root_cause":"2 lines replaced/restructured","fix":"Rewrote 19→54 lines (2 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T22:30:06.081Z"}
{"id":"bug-082","timestamp":"2026-06-25T22:31:13.282Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a7b10e71ea2d33500/apps/edge-api/internal/anthropic/translate_request.go","root_cause":"12 lines replaced/restructured","fix":"Rewrote 32→45 lines (12 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T22:31:13.282Z"}
{"id":"bug-083","timestamp":"2026-06-25T22:44:16.745Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a0601818c191bc2f3/.github/workflows/ci.yml","root_cause":"2 lines replaced/restructured","fix":"Rewrote 7→11 lines (2 removed)","tags":["auto-detected","refactor","yml"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-25T22:44:16.745Z"}
{"id":"bug-084","timestamp":"2026-06-25T23:10:31.121Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a442347ecfb509263/deploy/docker/docker-compose.yml","root_cause":"6 lines replaced/restructured","fix":"Rewrote 11→3 lines (6 removed) | Also: # Enterprise profile: wait for local Postgres befo; # so the DB connection pool succeeds at boot.","tags":["auto-detected","refactor","yml"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-25T23:10:44.128Z"}
{"id":"bug-085","timestamp":"2026-06-26T00:09:46.716Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-af26bf44e2dbb6621/deploy/headscale/config.yaml","root_cause":"5 lines replaced/restructured","fix":"Rewrote 13→17 lines (5 removed) | Also: nameservers:; - 1.1.1.1","tags":["auto-detected","refactor","yaml"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-26T00:09:54.015Z"}
{"id":"bug-086","timestamp":"2026-06-26T00:10:10.393Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-af26bf44e2dbb6621/deploy/docker/docker-compose.relay.yml","root_cause":"3 lines replaced/restructured","fix":"Rewrote 5→8 lines (3 removed)","tags":["auto-detected","refactor","yml"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:10:10.393Z"}
{"id":"bug-087","timestamp":"2026-06-26T00:11:10.504Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a288cd6ab6f43002d/apps/edge-api/internal/audio/handler.go","root_cause":"2 lines replaced/restructured","fix":"Rewrote 5→17 lines (2 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:11:10.504Z"}
{"id":"bug-088","timestamp":"2026-06-26T00:11:29.922Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a288cd6ab6f43002d/apps/edge-api/internal/stt/client.go","root_cause":"6 lines replaced/restructured","fix":"Rewrote 14→51 lines (6 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:11:29.923Z"}
{"id":"bug-089","timestamp":"2026-06-26T00:15:01.690Z","error_message":"Wrong operator: != should be ==","file":".claude/worktrees/agent-a288cd6ab6f43002d/apps/edge-api/internal/audio/handler_test.go","root_cause":"Used \"!=\" instead of \"==\"","fix":"Changed operator != → ==","tags":["auto-detected","operator-fix","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:15:01.690Z"}
{"id":"bug-090","timestamp":"2026-06-26T00:22:41.177Z","error_message":"Incorrect value in code","file":".claude/worktrees/agent-af26bf44e2dbb6621/scripts/install.sh","root_cause":"Had \"http://$(hostname -I 2>/dev/null | awk '","fix":"Changed to \"$(hostname -I 2>/dev/null | awk '","tags":["auto-detected","wrong-value","sh"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:22:41.177Z"}
{"id":"bug-091","timestamp":"2026-06-26T00:22:48.937Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-af26bf44e2dbb6621/deploy/headscale/config.yaml","root_cause":"4 lines replaced/restructured","fix":"Rewrote 4→6 lines (4 removed)","tags":["auto-detected","refactor","yaml"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:22:48.937Z"}
{"id":"bug-092","timestamp":"2026-06-26T00:30:42.071Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a3df8e27ee45b377a/deploy/docker/docker-compose.enterprise.yml","root_cause":"4 lines replaced/restructured","fix":"Rewrote 8→16 lines (4 removed)","tags":["auto-detected","refactor","yml"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:30:42.071Z"}
{"id":"bug-093","timestamp":"2026-06-26T00:43:47.967Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-aa9cf13c698c444ac/apps/edge-api/internal/rag/repository.go","root_cause":"2 lines replaced/restructured","fix":"Rewrote 9→15 lines (2 removed) | Also: func encodeVector(v []float32) string {; return \"[]\" | Also: vec := encodeVector(queryVec); vec, topK, | Also: // encodeVector serialises []float32 to the pgvect; // This lets us use the '::vector' cast in SQL wit","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":4,"last_seen":"2026-06-26T00:44:31.394Z"}
{"id":"bug-094","timestamp":"2026-06-26T00:45:00.359Z","error_message":"Function not marked async","file":".claude/worktrees/agent-aa9cf13c698c444ac/apps/control-plane/internal/rag/uuidutil.go","root_cause":"Function uses await but wasn't declared async","fix":"Added async modifier","tags":["auto-detected","async-fix","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:45:00.359Z"}
{"id":"bug-095","timestamp":"2026-06-26T00:47:31.006Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-aa9cf13c698c444ac/apps/control-plane/internal/rag/ingest.go","root_cause":"2 lines replaced/restructured","fix":"Rewrote 4→10 lines (2 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:47:31.006Z"}
{"id":"bug-096","timestamp":"2026-06-26T00:47:48.372Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-aa9cf13c698c444ac/apps/edge-api/cmd/server/main.go","root_cause":"9 lines replaced/restructured","fix":"Rewrote 23→43 lines (9 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:47:48.373Z"}
{"id":"bug-097","timestamp":"2026-06-26T00:48:03.056Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-aa9cf13c698c444ac/apps/edge-api/internal/rag/handler.go","root_cause":"2 lines replaced/restructured","fix":"Rewrote 8→12 lines (2 removed) | Also: // Authorize before handing off; stt.TieredClient ; if _, ok := h.authorize(w, r); !ok {","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-26T00:52:09.637Z"}
{"id":"bug-098","timestamp":"2026-06-26T00:52:36.767Z","error_message":"Wrong reference: string should be apiKey","file":".claude/worktrees/agent-a288cd6ab6f43002d/apps/edge-api/internal/stt/client.go","root_cause":"Used \"string\" instead of \"apiKey\"","fix":"Changed string → apiKey","tags":["auto-detected","wrong-reference","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:52:36.767Z"}
{"id":"bug-099","timestamp":"2026-06-26T00:53:55.893Z","error_message":"Wrong return value","file":".claude/worktrees/agent-aa9cf13c698c444ac/apps/edge-api/internal/rag/handler_test.go","root_cause":"Was returning: nil","fix":"Now returns: false, nil","tags":["auto-detected","return-value","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:53:55.893Z"}
{"id":"bug-100","timestamp":"2026-06-26T00:54:19.740Z","error_message":"Wrong reference: handleGet should be handleGetDocument","file":".claude/worktrees/agent-aa9cf13c698c444ac/apps/edge-api/internal/rag/handler_test.go","root_cause":"Used \"handleGet\" instead of \"handleGetDocument\"","fix":"Changed handleGet → handleGetDocument","tags":["auto-detected","wrong-reference","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T00:54:19.740Z"}
{"id":"bug-101","timestamp":"2026-06-26T01:00:21.830Z","error_message":"Missing error handling in unknown","file":"../.claude/projects/-home-sakib-hive/memory/project_carl_sovereign_edge.md","root_cause":"Code path had no error handling — exceptions would propagate uncaught","fix":"Added try/catch block","tags":["auto-detected","error-handling","md"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T01:00:21.830Z"}
{"id":"bug-102","timestamp":"2026-06-26T01:02:34.453Z","error_message":"Incorrect value in code","file":".claude/worktrees/agent-a95daadb99dbeadf4/apps/edge-api/cmd/server/main.go","root_cause":"Had \"github.com/sakibsadmanshajib/hive/apps/edge-api/i","fix":"Changed to \"github.com/sakibsadmanshajib/hive/apps/edge-api/i","tags":["auto-detected","wrong-value","go"],"related_bugs":[],"occurrences":3,"last_seen":"2026-06-26T01:04:46.535Z"}
{"id":"bug-103","timestamp":"2026-06-26T01:12:08.238Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-ae621fbbf21eec699/apps/control-plane/internal/auditarchive/archiver.go","root_cause":"5 lines replaced/restructured","fix":"Rewrote 69→81 lines (5 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T01:12:08.238Z"}
{"id":"bug-104","timestamp":"2026-06-26T01:13:13.447Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-ae621fbbf21eec699/apps/control-plane/internal/auditarchive/storage_store.go","root_cause":"11 lines replaced/restructured","fix":"Rewrote 45→62 lines (11 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T01:13:13.447Z"}
{"id":"bug-105","timestamp":"2026-06-26T01:30:16.503Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-a64406852579252da/docs/enterprise-sso.md","root_cause":"10 lines replaced/restructured","fix":"Rewrote 18→53 lines (10 removed) | Also: create their `public.tenant_users` membership row.; 1. Admin creates or confirms the tenant row in `pu","tags":["auto-detected","refactor","md"],"related_bugs":[],"occurrences":2,"last_seen":"2026-06-26T01:34:36.372Z"}
{"id":"bug-106","timestamp":"2026-06-26T01:33:03.083Z","error_message":"Wrong operator: >= should be ==","file":".claude/worktrees/agent-ae621fbbf21eec699/apps/control-plane/internal/auditarchive/pg_repository.go","root_cause":"Used \">=\" instead of \"==\"","fix":"Changed operator >= → ==","tags":["auto-detected","operator-fix","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T01:33:03.083Z"}
{"id":"bug-107","timestamp":"2026-06-26T01:34:53.835Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-ae621fbbf21eec699/apps/control-plane/internal/auditarchive/archiver_test.go","root_cause":"3 lines replaced/restructured","fix":"Rewrote 14→17 lines (3 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T01:34:53.835Z"}
{"id":"bug-108","timestamp":"2026-06-26T01:40:52.291Z","error_message":"Significant refactor of ","file":".claude/worktrees/agent-ae621fbbf21eec699/apps/control-plane/internal/auditarchive/archiver_test.go","root_cause":"18 lines replaced/restructured","fix":"Rewrote 29→16 lines (18 removed)","tags":["auto-detected","refactor","go"],"related_bugs":[],"occurrences":1,"last_seen":"2026-06-26T01:40:52.291Z"}
{"date":"2026-07-03","error_message":"phase-19-owui-nightly: couldn't find env file /home/runner/work/hive/hive/.env.ci (exit 1, 7 consecutive nightly fails since 2026-06-26)","root_cause":"workflow referenced ../../.env.ci but no step created it; file never in tree, never gitignored, no other workflow uses it","fix":"PR #262: Materialize .env.ci step before compose boot, built from GH secrets (mirrors live-integration secret set), chmod 600, hard-fail loop on empty required externals, OWUI_SHIM_KEY generated per run","tags":["ci","nightly","env-file","github-actions","owui"]}
{"date":"2026-07-03","error_message":"OWUI Playwright nightly: all 14 specs ENOENT e2e/phase-19/owui/.auth/owui-user.json; suite never green in history","root_cause":"OWUI_E2E_EMAIL/OWUI_E2E_PASSWORD repo secrets never provisioned; owui.setup.ts setup.skip() on missing creds does not stop dependent projects, storageState file never written","fix":"PR #266: playwright.owui.config.ts gates owui/owui-perf projects with testMatch [] when creds absent (clean skip, exit 0) + workflow ::warning:: annotation. Real coverage still requires provisioning the two secrets","tags":["e2e","playwright","owui","nightly","secrets","ci"]}
{"date":"2026-07-03","error_message":"OWUI nightly setup: 'Sign in with Hive' OIDC button never renders; locator.click timeout 30s at owui.setup.ts:14 (run 28646650000)","root_cause":"OWUI OAUTH_CLIENT_ID/SECRET map to SUPABASE_OAUTH_CLIENT_ID/SECRET which default empty; nightly .env.ci materialization never writes them and no repo secret exists; empty client id means OWUI never registers OIDC provider, only native login card renders","fix":"Issue #269 filed. Needs owner: add SUPABASE_OAUTH_CLIENT_ID/SECRET repo secrets (values from GoTrue OAuth app registered for OWUI in SSO #259 setup), then wire into .env.ci block + readiness assert","tags":["e2e","owui","oidc","nightly","secrets","compose"]}
{"id":"bug-003","timestamp":"2026-07-07T08:12:21.335123Z","error_message":"TS2307 cannot find module 'recharts' in web-console CI (PR #314)","file":"apps/web-console/package.json","root_cause":"Dead-code grep scoped to apps/web-console/src only; recharts imports live in apps/web-console/components/analytics/{error,spend,usage}-chart.tsx, so the dep was wrongly judged unused and removed","fix":"Reverted package.json + package-lock.json to origin/main byte-identical; rule: dependency-usage grep must cover the whole package dir (components/, app/, tests/), never just src/","tags":["ci","web-console","dead-code","false-positive","grep-scope"],"related_bugs":[],"occurrences":1,"last_seen":"2026-07-07T08:12:21.335123Z"}
{"id":"bug-004","timestamp":"2026-07-07T08:12:21.335123Z","error_message":"gh pr edit / gh issue view fail: classic Projects GraphQL field deprecated (projectCards)","file":"gh CLI vs sakibsadmanshajib/hive","root_cause":"Repo linked to classic Projects; default gh commands query deprecated projectCards GraphQL field","fix":"Use gh issue view --json <fields> and gh api PATCH /repos/.../pulls/N instead of gh pr edit","tags":["gh-cli","github-api","workaround"],"related_bugs":[],"occurrences":2,"last_seen":"2026-07-07T08:12:21.335123Z"}
{"id":"bug-005","timestamp":"2026-07-07T08:12:21.335123Z","error_message":"git pull --ff-only aborts: untracked working tree files would be overwritten by merge (.wolf/, .planning/carl/, spec)","file":"/home/sakib/hive (shared checkout)","root_cause":"PR #314 committed snapshots of files that existed locally as untracked; pull refuses to overwrite untracked paths","fix":"Verify backup (diff -rq) then move collision paths aside, pull, restore live .wolf copy over tracked snapshot (drift shows as M, committed next .wolf pass)","tags":["git","pull-collision","wolf","workflow"],"related_bugs":[],"occurrences":1,"last_seen":"2026-07-07T08:12:21.335123Z"}
{"date":"2026-07-16","error_message":"gh pr merge failed: Pull Request has merge conflicts (mergePullRequest); second PR editing tools/lint-no-direct-tenant-id.mjs allowlist conflicted after first PR merged","root_cause":"Parallel builder PRs (#324, #326) both appended entries to the same ALLOWLIST_DIRS block in the tenant-id lint script; first squash merge made the second PR conflict.","fix":"Fresh worktree from remote branch, git merge origin/main, union both allowlist entries with their justification comments, push, re-gate, merge. Prevention: treat lint allowlist files as a known shared-file hotspot for parallel PR waves; assign allowlist edits line-disjoint or merge serially.","tags":["merge-conflict","parallel-builders","repo-policy-lint","allowlist","orchestration"]}
{"date":"2026-07-16","error_message":"cannot delete branch used by worktree / premature local cleanup while PR still OPEN after failed merge","root_cause":"Orchestrator compound command ran worktree remove + branch -D in the same command as gh pr merge, so cleanup executed even when the merge failed (conflict), destroying the builder local state early.","fix":"Sequence strictly: merge, verify state==MERGED via gh pr view, only then remove worktree and delete local branch. Remote branch preserved recovery this time.","tags":["git-worktree","merge-gate","orchestration","process"]}
{"date":"2026-07-16","error_message":"Cross-tenant data leak via permissive service-scan RLS policy USING(true) TO hive_app on agent_tasks (PR #333, caught in review pre-merge)","root_cause":"Postgres OR-combines all PERMISSIVE policies for a role+command. A blanket USING(true) SELECT policy added for a background worker cancels the tenant_isolation policy for EVERY hive_app SELECT, because Get/List filter tenant via RLS only (not in SQL WHERE). Third occurrence of this class (egress #308, RAG #232 prior).","fix":"Never add a blanket table RLS policy to the shared app role for a cross-tenant background job. Use a SECURITY DEFINER function scoped to exactly the job query, or a dedicated BYPASSRLS role/connection. Always add a regression test: a 2-tenant authenticated user still cannot Get/List another tenant row after the migration.","tags":["rls","cross-tenant","multi-tenant","postgres","permissive-policy","security","recurring"]}
{"date":"2026-07-16","error_message":"Web E2E (full stack) CI: container hive-control-plane-1 is unhealthy; air build fails: no required module provides package .../apps/agent-engine/engineapi; container exit 127 loop","root_cause":"PR #332 added agent-engine import to control-plane main.go (local go.mod replace), but deploy/docker/Dockerfile.control-plane copied only agent-engine go.mod/go.sum for cache warmup, never the source tree. Control-plane builds at container boot via air, so gap surfaced only at runtime in E2E, not in build-time images or go-tests.","fix":"Add COPY apps/agent-engine/ ./apps/agent-engine/ to Dockerfile.control-plane, matching existing pattern for control-plane/storage/audit-canonical local modules. PR #340.","tags":["ci","e2e","docker","go.work","control-plane","agent-engine","air","healthcheck"]}
{"date":"2026-07-17","error_message":"column \"tenant_id\" does not exist on clean supabase db push at 20260625_06_audit_cold_archive_manifest.sql (CREATE UNIQUE INDEX ... (tenant_id, partition_month))","root_cause":"audit_cold_archive_manifest defined twice with incompatible shapes: 20260516_04 (May, partition_name/parquet_path) and 20260625_06 (June, id/tenant_id/object_key). June used CREATE TABLE IF NOT EXISTS, so on a chained fresh apply it no-opped onto the May table, then its unique index on the June-only column tenant_id errored. A CI-only DROP TABLE band-aid in ci.yml masked it, and a stale compliance test (audit_retention_test.go) asserted May columns, so it only passed because the CI drop left the May table live. App code (auditarchive/pg_repository.go) and the 20260625_08 immutability trigger both use the June shape, so June is authoritative.","fix":"Made 20260625_06 self-healing (permitted: it was un-applied and always-failing): drop the stale May table and its May-era policies (manifest_service_role_all, manifest_auditor_select) then create the June table, with hive_app INSERT/SELECT/UPDATE and auditor_ro SELECT grants relocated in. Removed the CI DROP band-aid so CI validates the real chain. Fixed audit_retention_test.go to assert June columns. Stale May table provably empty everywhere (app writes June-only columns, so writes against the May shape always error; staging confirmed 0 rows), so the drop preserves all audit data. Validated: full migration chain applies cleanly on a fresh pgvector:pg17 with no drop hack, and the compliance + RLS suites pass against the June schema.","tags":["migration","rls","audit","compliance","ci-integrity","prod-only"]}
{"id":"preexec-allocator-unsafe","date":"2026-07-17","error_message":"Desktop sandbox launch() could hang the whole app (once a 15+ minute cargo test hang) inside the Command::pre_exec closure","root_cause":"The pre_exec closure ran after fork() and before execve() in a child of a multithreaded process but was not async-signal-safe: it built the Landlock ruleset (Ruleset build, PathFd::new), compiled the seccomp BPF program, ran codex pre_main_hardening (std::env::remove_var over vars_os), and used format! on error paths. Allocating in a forked child can deadlock forever if another thread held the allocator lock at fork, and std makes the parent spawn() block until the child execs or errors, so the parent hangs too.","fix":"Hoist all allocation and fd setup before the fork: build_landlock_ruleset returns a RulesetCreated (converted to OwnedFd) and build_seccomp_filter returns the compiled BpfProgram, both in the parent. Strip LD_* env on the Command before fork instead of via std::env in the child. The closure now performs only raw async-signal-safe syscalls on pre-built values: prctl(PR_SET_DUMPABLE) via codex disable_process_dumping, setrlimit(RLIMIT_CORE), prctl(PR_SET_NO_NEW_PRIVS), raw landlock_restrict_self on the pre-built fd, and seccompiler::apply_filter over the pre-compiled program; error paths map to allocation-free io::Errors. Added tests/launch_spawn.rs end-to-end regression guard. The #341 10s spawn timeout stays as defense in depth.","tags":["rust","sandbox","fork-safety","security"]}
{"date":"2026-07-17","id":"reservation-duplicate-key-23505","severity":"P2","error_message":"duplicate key value violates unique constraint \"idx_request_attempts_account_request_attempt\" (SQLSTATE 23505) on chat dispatch","root_cause":"Non-idempotent request-attempt insert. A single edge-api chat request starts the same attempt twice for one (account_id, request_id, attempt_number=1): the orchestrator calls StartAttempt directly (status dispatching), then CreateReservation internally calls usage.StartAttempt again (accounting/service.go). usage/repository.go CreateAttempt used a plain INSERT, so the second insert hit the unique index and raised 23505. Non-fatal to the served request (errors were logged as non-fatal), but the error aborted the reservation transaction, silently dropping the credit hold, and polluted logs.","fix":"Make CreateAttempt idempotent with INSERT ... ON CONFLICT (account_id, request_id, attempt_number) DO UPDATE SET status = request_attempts.status RETURNING ... The no-op SET returns the pre-existing attempt (first write wins); status transitions stay owned by UpdateAttemptStatus so an attempt never moves backward. Preserves append-only semantics and money-path correctness (single attempt, single reservation, no double-charge). Added DB-backed regression test TestCreateAttemptIsIdempotentOnAccountRequestAttempt in apps/control-plane/internal/usage/repository_test.go asserting no 23505, same row returned, exactly one row.","tags":["billing","idempotency","edge-api","db"]}
{"id":"matrix-missing-proprietary-endpoints","priority":"P0","date":"2026-07-17","error_message":"agent-subsystem /v1 endpoints (/v1/rag/*, /v1/agent/tasks*, /v1/artifacts*) 404 via UnsupportedEndpointMiddleware; entire agent-subsystem web surface dead (demo blocker)","root_cause":"support matrix supported_now never updated for Waves 2-4 endpoints. The edge-api UnsupportedEndpointMiddleware 404s any /v1/ path not marked supported_now in packages/openai-contract/matrix/support-matrix.json (loaded at boot, baked into the Docker image). The rag/agent/artifacts handlers shipped wired and feature-gated but were only covered by per-package isolation tests that bypass the full middleware chain, so CI stayed green while every request 404-ed in a real stack. Note: support-matrix.json is the hand-maintained source; overlays/hive-support-status.yaml is an OpenAI-only OpenAPI overlay consumed by nothing and does not feed the matrix.","fix":"Add supported_now entries for the 13 real proprietary routes (verified against the router registrations, not the ticket's guide) to support-matrix.json, then regenerate docs/support-matrix.md + generated/hive-openapi.yaml via generate-matrix.sh. Add apps/edge-api/internal/middleware/unsupported_integration_test.go, a full-chain integration test that drives all 13 paths through the real UnsupportedEndpointMiddleware + the real committed matrix: 404 before, 200 after.","tags":["contract","middleware","demo-blocker","test-gap"]}
{"id":"egress-e2e-loopback-502-ci","priority":"P1","date":"2026-07-17","error_message":"CI job 'Egress firewall e2e (bwrap)' failed deterministically: ALLOWED probe (127.0.0.2) returned '502 CONNECT tunnel failed' instead of 200 (disallowed 403 and direct-refused were fine). Passed locally because only the Rust unit tests were run, never the shell e2e, which executes only in CI where HIVE_EGRESS_E2E_REQUIRE=1.","root_cause":"egress-proxy-harness spawned AllowlistProxy via spawn(), which forces allow_local_dest=false. The e2e uses two loopback IPs (127.0.0.2 allowed vs 127.0.0.3 denied) as a throwaway stand-in internet. For the allowed target, is_allowed passes but resolve_pinned then hits the is_forbidden_ip loopback guard (all of 127.0.0.0/8 is loopback), returns Err, and the handler writes 502 Bad Gateway before dialing. No non-forbidden address answers on a CI runner, so the allowed probe could never reach 200 with that harness. Not a bwrap or allowlist-logic bug.","fix":"Add pub AllowlistProxy::spawn_allowing_local_dest (test-only wrapper over spawn_with_policy(..., true), the same opt-out the proxy's own unit tests use) and call it from the egress-proxy-harness example. Relaxes only the redundant destination-IP guard (separately unit-tested); the allowlist host check and netns isolation the e2e asserts stay unchanged. Verified: raw CONNECT over the socket gives allowed=200 / disallowed=403, and the full scripts/egress-bwrap-e2e.sh passes with HIVE_EGRESS_E2E_REQUIRE=1 inside a real bwrap sandbox (allowed=200, disallowed=403, direct refused).","tags":["ci","egress","desktop-sandbox","test-harness","loopback-guard"]}
{"id":"agent-engine-sif-playwright-missing","priority":"P1","date":"2026-07-17","error_message":"apptainer build agent-engine.sif FATAL in %post: '/usr/local/bin/python3: No module named playwright' at 'python3 -m playwright install --with-deps chromium'; exit status 1 (CI job Build agent-engine.sif, PR #351)","root_cause":"The vendored trio (openhands-sdk/-tools/-workspace) is installed with pip --no-deps to protect the patched workspace. That also drops openhands-tools' declared third-party runtime deps (binaryornot, cachetools, libtmux, browser-use, func-timeout, tom-swe). browser-use is what pulls playwright. openhands-agent-server is installed with deps but does not depend on openhands-tools, so pip never backfilled them (an already-installed --no-deps package is treated as satisfied, its missing deps never resolved). The subsequent playwright CLI invocation then failed because playwright was never installed.","fix":"In deploy/apptainer/agent-engine.def %post, after the trio and agent-server installs and before 'playwright install', add an explicit 'pip install' of openhands-tools' third-party runtime deps (binaryornot>=0.4.4, cachetools, libtmux>=0.53.0, browser-use>=0.8.0, func-timeout>=4.3.5, tom-swe>=1.0.3). These are plain PyPI packages, not the vendored trio, so the patched trio is left untouched (pip sees them already satisfied). Mirrors vendor/openhands/openhands-tools/pyproject.toml dependencies; keep in step with it.","tags":["ci","apptainer","agent-engine","playwright","packaging"]}
{"id":"toolchain-bash-c-silent-noop","priority":"P1","date":"2026-07-19","error_message":"docker compose --profile tools run toolchain bash -c \"...\" exits 0 with zero output, tests silently not run","root_cause":"toolchain image is Alpine with no bash and ENTRYPOINT [\"/bin/sh\",\"-c\"]; wrapping with bash -c or sh -c double-wraps so the nested shell reads EOF and exits 0 without executing","fix":"pass the command string directly as the single arg: docker compose --profile tools run toolchain \"cd /workspace && go test ...\"; CLAUDE.md testing section updated 2026-07-19","tags":["docker","ci","toolchain","silent-failure"]}
{"id":"console-caddy-host-mismatch-empty-200","priority":"P1","date":"2026-07-26","error_message":"https://console-hive.scubed.co/ returned HTTP 200 with Content-Length 0 (no body, no redirect) through the Cloudflare Tunnel right after the self-hosted web-console cutover (PR #456); web-console-prod itself worked fine when curled directly inside the box","root_cause":"Caddyfile.console's site address is http://{$CONSOLE_DOMAIN:console.localhost}, a Host-matched site block. The box's .env never set CONSOLE_DOMAIN or CONSOLE_EXTERNAL_SCHEME (PR #456 introduced the vars but the box's .env predated the PR), so Caddy only matched Host: console.localhost. Cloudflare Tunnel and any real client send Host: console-hive.scubed.co, which matched no site block; Caddy's fallback response for an unmatched Host in this build is 200 empty, not 404 or connection refused, which made the failure look like a working-but-blank origin instead of a routing miss.","fix":"Append CONSOLE_DOMAIN=console-hive.scubed.co and CONSOLE_EXTERNAL_SCHEME=https to the box's .env (matching the existing HIVE_CHAT_EXTERNAL_SCHEME/ARTIFACTS_DOMAIN pattern), then docker compose --env-file ../../.env up -d --force-recreate caddy-console to pick up the new env at container-create time (a plain restart does not re-resolve compose env substitution). Verified end to end: https://console-hive.scubed.co/ -> 307 to /auth/sign-in -> 200 Hive Console page.","tags":["caddy","cloudflare-tunnel","web-console","dns-cutover","silent-failure"]}
{"id":"signup-no-active-membership-500-no-webhook","priority":"P0","date":"2026-07-27","error_message":"Every password grant for a user with no ACTIVE public.tenant_users row returned HTTP 500 with {\"code\":\"P0001\",\"message\":\"no_active_membership\"}. Sign-up succeeded, first sign-in then failed permanently. 11 of 19 live users were stranded, 92 occurrences in 24 hours. The sign-in and OAuth consent forms rendered the raw upstream string 'Error running hook URI: pg-functions://postgres/public/custom_access_token_hook' to unauthenticated visitors.","root_cause":"Two faults stacked. First, public.custom_access_token_hook did RAISE EXCEPTION 'no_active_membership' whenever no ACTIVE tenant_users row joined to a non-archived tenant resolved, which denied the user the very request that would provision them, so the state was unrecoverable from the client. Second, and the reason such users existed at all, public.tenant_users was written by exactly one caller: apps/control-plane/internal/signup/webhook.go at POST /internal/auth/user-created, driven by a Supabase Database Webhook on auth.users insert. That webhook is dashboard state, not repository state, and it had never been created on the live project. A query for triggers on auth.users and for any supabase_functions.http_request trigger returned zero rows, so no signup had ever produced a membership. The demo owner only worked because scripts/seed-demo-owner.py had inserted their row directly. A third, separate fault surfaced while auditing the inert-token invariant: apps/control-plane/internal/auth/client.go read the tenant from user_metadata.selected_tenant_id, which GoTrue lets the user write via PUT /auth/v1/user, and never validated it against an ACTIVE membership the way the token hook does, so any signed-in caller could assert an arbitrary tenant id and read another tenant's model visibility list through /api/v1/catalog/models (OptionalRequire, not admin gated).","fix":"New migration supabase/migrations/20260727_01_token_hook_membershipless_no_raise.sql replaces the RAISE with an early return of the unmodified claims, so a membership-less user receives a valid token carrying no tenant_id, tenants, role or owui_role claim. Absent, never null, so no consumer can read the key and treat null as a wildcard. The member path is byte-for-byte unchanged including the owui_role OWNER to ADMIN remap. Resolution and the membership write were extracted from webhook.go into signup.Provisioner.Reconcile (apps/control-plane/internal/signup/reconcile.go), the single implementation both entry points now call; the webhook keeps working for deployments that do have the Database Webhook. A new authenticated route POST /api/v1/viewer/tenant-provision (apps/control-plane/internal/signup/viewer_http.go) lets the console reconcile on the first request from a tenant-less user; it derives identity solely from auth.ViewerFromContext, decodes no body, and accepts neither a tenant id nor an invite token, so a caller cannot name anybody or anything. Idempotency comes from an existing-ACTIVE-membership short-circuit plus the tenant_users (tenant_id, user_id) primary key with ON CONFLICT DO NOTHING; Reconcile never creates a tenant in either deployment posture, so no race can produce two. The escalation was closed with auth.Client.WithMembershipCheck, wired in main.go to tenantMembershipCheck(pool), which validates the self-asserted tenant against the same ACTIVE-and-not-archived predicate the hook uses and collapses anything unverified to uuid.Nil. Tests: 4 hook tests (static guard plus live-DB member, membership-less and revoked claim assertions), 9 reconcile and endpoint tests including an 8-way concurrency race and a body-cannot-name-a-user security test, 6 control-plane escalation tests, and 3 edge-api inert-token tests covering absent, empty, null, unparseable, all-zero and wrong-type tenant claims. Sanitization of the raw hook string is owned by the web-console client boundary (lib/auth/auth-error.ts), because GoTrue's error text is upstream and web-console is the last hop before the browser.","tags":["supabase-auth","custom-access-token-hook","tenant-provisioning","privilege-escalation","rls","signup","database-webhook","migration","inert-token"]}
{"id":"tenant-model-visibility-not-enforced-on-inference","priority":"P1","date":"2026-07-27","error_message":"A tenant could invoke any routed model alias regardless of the per-tenant model visibility an admin had configured. tenant_model_visibility was consulted only when listing the catalog; POST /v1/chat/completions resolved a route for a hidden alias and streamed a normal completion. Compounding it, /internal/catalog/visibility/* answered 404, so the admin control had no reachable write path at all.","root_cause":"Enforcement lived in the catalog listing query (catalog/repository.go ListAliasesForTenant) instead of in the path that grants access. edge-api chat dispatch called SelectRoute with only AliasID plus capability flags and never sent a tenant, and routing/service.go aliasAllowed returns true for an empty allowlist, so the empty list meant allow-all. /v1/models served the global ListPublicAliases snapshot for the same reason. Separately, catalog.NewVisibilityHandler was never constructed in control-plane cmd/server/main.go, so router.go skipped mounting the admin visibility routes.","fix":"Extracted the verdict into catalog.AliasVisibleToTenant and made it the single decider: one query (tenantVisibilityQuery) with an optional alias filter feeds one predicate, so ListAliasesForTenant and the new IsAliasVisibleToTenant cannot disagree. routing.Service takes a required TenantEntitlements dependency and refuses tenant-scoped selections for unentitled aliases with ErrModelNotEntitled (403), failing closed when the dependency is nil. edge-api RoutingClient.SelectRoute binds the tenant from auth.TenantID(ctx) and discards any caller-supplied value, so every JWT inference path (chat, RAG, audio, images) is covered at one seam. /v1/models reads a new tenant-scoped snapshot endpoint for JWT sessions. Mounted the visibility admin handler. Added tools/lint-tenant-model-visibility-single-source.mjs so a second copy of the rules cannot be written outside the catalog package. Empty-means-allow for public and preview aliases was preserved deliberately: tenant_model_visibility is empty on every deployment, so deny-by-default would have revoked chat for every tenant.","tags":["tenancy","authz","routing","catalog","control-plane","edge-api","entitlement","fail-closed"]}
{"id":"console-redirect-origin-leaks-bind-address","priority":"P1","date":"2026-07-27","error_message":"https://console-hive.scubed.co/auth/callback returned 307 with Location: https://0.0.0.0:3000/console, and https://chat-hive.scubed.co/agent-workspace/auth/callback returned 307 with Location: https://0.0.0.0:3000/agent-workspace/auth/sign-in. Users completing email verification or an OAuth code exchange landed on a dead host (ERR_CONNECTION_REFUSED) instead of the console.","root_cause":"Next.js does not build a request's URL from the Host header. With experimental.trustHostHeader unset, resolve-routes.js composes initUrl from the server's OWN bind address: `${protocol}://${formatHostname(opts.hostname || 'localhost')}:${opts.port}${req.url}`, and formatHostname('0.0.0.0') returns it verbatim. Both console containers start with `--hostname 0.0.0.0 --port 3000`, and Caddy/Cloudflare Tunnel supply the scheme via X-Forwarded-Proto, so any route handler resolving an absolute redirect against request.url emitted https://0.0.0.0:3000. The helper for this already existed: PR #157 added apps/web-console/lib/http/origin.ts, whose header comment documents this exact trap, but wired it into only 2 of 5 call sites. A second, compounding cause: the helper preferred NEXT_PUBLIC_APP_URL unconditionally, and .env.example ships http://localhost:3000, so a deployment carrying that value forward would have emitted localhost redirects and mailed localhost verification links.","fix":"Wired resolveCanonicalOrigin into the three remaining call sites: apps/web-console/app/auth/callback/route.ts, apps/web-console/app/console/account-switch/route.ts (all four targets), and apps/agent-console/app/auth/callback/route.ts (keeping PR #438's BASE_PATH prefix). Query params now come from request.nextUrl.searchParams, which derives from the request line and is unaffected. Corrected the helper's precedence: a loopback NEXT_PUBLIC_APP_URL is demoted below X-Forwarded-Host/Host while a real configured origin still outranks every header (preserving PR #157's anti-spoofing property), and wildcard bind addresses plus unparseable hosts are rejected from every source. agent-console got its own copy of the helper (separate npm package, separate Docker build context, so no shared import is possible) and needed no new build arg. Did NOT use experimental.trustHostHeader: it hardcodes https:// and pastes in the raw attacker-reachable Host header, which is the open-redirect shape origin.ts exists to prevent. Added tools/lint-no-request-url-origin.mjs, which fails CI if any route handler in either app reads request.url or nextUrl.origin, or if the two helper copies drift apart. Middleware is deliberately out of scope: verified live that Next.js emits a relative Location for a same-origin middleware redirect, so both middlewares are correct despite using the same expression. Proven live before/after with the real Dockerfiles behind the real Caddy configs; transcript and browser captures in docs/proof/console-origin-2026-07-27/.","tags":["next.js","redirect","0.0.0.0","bind-address","x-forwarded-host","web-console","agent-console","auth-callback","open-redirect","repo-policy-lint","same-family-as-agent-console-signin-drops-basepath"]}
{"id":"billing-profile-inner-join-500","priority":"P1","date":"2026-07-27","error_message":"/console/settings/billing returned HTTP 500 with a bare Next.js digest page and no recovery for any account that has zero rows in public.account_profiles (7 of 18 accounts on the dev project). Server-side throw: Error: profile not found at getBillingProfile (lib/control-plane/client.ts) from BillingSettingsPage's Promise.all.","root_cause":"profiles/repository.go GetBillingProfile inner-joined public.account_profiles while every column it selects is already COALESCEd against that table being absent, so a missing profile row produced pgx.ErrNoRows, mapped to ErrNotFound, and returned 404. web-console getBillingProfile threw on any non-OK response (its sibling getAccountProfile already special-cased 404), and there was no error.tsx anywhere under apps/web-console/app, so the throw escaped to the bare digest page.","fix":"Three layers. 1) LEFT JOIN public.account_profiles in GetBillingProfile so a profile-less account gets the coalesced empty profile (legal_entity_type falls back to individual or private_company by account_type); only a missing account is not-found. 2) getBillingProfile returns an empty billing profile on 404, mirroring getAccountProfile; other statuses still throw. 3) Added apps/web-console/app/console/error.tsx, the app's first error boundary, with an EmptyState and a working reset. Also split the 403 on this endpoint so a verified non-owner gets owner_role_required instead of the false email_verification_required. Verified live: pre-fix control-plane returns 404 and the page 500s, post-fix returns 200 with empty values and the page renders. Live-Postgres repository tests added and ./internal/profiles/... wired into the live-DB CI step.","tags":["control-plane","postgres","inner-join","web-console","server-components","error-boundary","authz-error-code"]}
{"id":"caddy-artifacts-auto-https-redirect-loop","priority":"P1","date":"2026-07-26","error_message":"https://artifacts-hive.scubed.co/ returns HTTP 308 with Location pointing back at itself, infinite redirect loop, curl -L hits 50-redirect cap","root_cause":"deploy/docker/Caddyfile.artifacts used a bare-hostname site address ({$ARTIFACTS_DOMAIN:artifacts.localhost} {), which triggers Caddy's automatic-HTTPS + HTTP-to-HTTPS auto-redirect. Behind Cloudflare Tunnel (or any external TLS terminator), the origin only ever receives plain HTTP, so Caddy redirects every request to https://<same-host>/ forever. A first-pass fix to bare `:80 {` stopped the loop but removed Caddy's host-header matching entirely, turning the container into a catch-all that would serve /artifacts/* content under ANY Host header -- a real regression against this file's own documented origin-isolation invariant (artifacts must stay on a separate origin from the chat/main-app domain so browser same-origin policy protects cookies). Caught by CodeRabbit + a second review bot on PR #434 before merge.","fix":"Use an explicit scheme in the site address instead: http://{$ARTIFACTS_DOMAIN:artifacts.localhost} { ... }. Caddy's automatic-HTTPS is triggered by the ABSENCE of an explicit scheme, not by the presence of a hostname -- naming http:// disables auto-HTTPS/redirect while keeping host-header matching intact. Fixed in PR #434 (sakibsadmanshajib/hive), merged 2026-07-26. Verified live against a real Cloudflare-Tunnel-fronted deployment both ways: correct Host = reaches reverse_proxy (no redirect); wrong Host = empty response, artifact content never served.","tags":["caddy","tls","cloudflare-tunnel","reverse-proxy","security-boundary","code-review-catch"]}
{"id":"caddy-bind-mount-needs-force-recreate","priority":"P2","date":"2026-07-26","error_message":"After `git pull` on a deployed box, a fixed Caddyfile still served the OLD behavior even after `docker compose build` + `up -d --no-deps` + `docker exec caddy reload`","root_cause":"Caddyfile.* is a single-file bind mount into the caddy-owui/caddy-artifacts containers (image is upstream caddy:2-alpine, no build step). `git pull` writes a NEW inode for the file; Docker's bind mount was already resolved to the OLD inode at container start, so neither `build` (no-op, nothing to build) nor a plain `up -d` (container already Running, not recreated) nor an in-container `reload` (re-reads the stale mounted path) pick up the new content.","fix":"Any Caddyfile-only change on a running box needs `docker compose ... up -d --no-deps --force-recreate <service>` specifically -- not build, not a bare up, not exec reload.","tags":["docker","caddy","bind-mount","deploy"]}
{"id":"agent-console-signin-drops-basepath","priority":"P1","date":"2026-07-26","file":"apps/agent-console/app/auth/sign-in/page.tsx","error_message":"Sign-in to https://chat-hive.scubed.co/agent-workspace succeeded but the browser landed on Open WebUI's login page (/auth?redirect=%2Ftasks) instead of the Cowork task console","root_cause":"page.tsx called window.location.assign(\"/tasks\"). Next.js applies basePath to its own primitives (next/link, useRouter, next/navigation redirect) but NOT to raw URLs given to window.location.* or NextResponse.redirect. The bare /tasks fell outside Caddy's /agent-workspace/* matcher, so Open WebUI's catch-all reverse_proxy answered it. app/auth/callback/route.ts had the same defect on both NextResponse.redirect targets. middleware.ts had already hit this and prefixed its own redirects, making this the third occurrence of one mistake.","fix":"Extracted the literal to apps/agent-console/lib/base-path.ts and imported BASE_PATH in all three call sites (middleware.ts, sign-in page, callback route). Regression tests: app/auth/sign-in/page.test.tsx, app/auth/callback/route.test.ts. PR #438.","tags":["nextjs","basepath","caddy","agent-console","auth-redirect","open-webui"]}
{"id":"edge-api-no-cors-breaks-browser-task-console","priority":"P1","date":"2026-07-26","file":"deploy/docker/Caddyfile.owui","error_message":"Task console showed \"Could not load tasks.\" with 10 browser CORS errors: no Access-Control-Allow-Origin on https://api-hive.scubed.co/v1/agent/tasks from origin https://chat-hive.scubed.co","root_cause":"edge-api implements NO CORS middleware at all (zero Access-Control-* headers anywhere in apps/edge-api). agent-console's task console fetches /v1/agent/tasks from the BROWSER using NEXT_PUBLIC_EDGE_API_BASE_URL, set to the api host while the page is served from the chat host, so every list, create and cancel died in preflight. This never worked in a browser in any environment, including local dev (default base http://localhost:8080 against a page on :3003). Unit tests mock fetch, so they never caught it.","fix":"Avoided opening a CORS surface on the gateway. Routed /v1/agent/* through the same Caddy listener that already fronts agent-console and built agent-console with NEXT_PUBLIC_EDGE_API_BASE_URL empty, so the client baseUrl falls back to \"\" and fetches are relative and same-origin. Auth unchanged, edge-api still validates the Supabase bearer token. PR #438.","tags":["cors","edge-api","agent-console","caddy","same-origin","browser-fetch"]}
{"id":"seed-demo-owner-rotates-shared-password","priority":"P2","date":"2026-07-26","file":"scripts/seed-demo-owner.py","error_message":"Supabase login returned 400 Invalid login credentials for demo@hive-demo.invalid using a password seed-demo-owner.py had printed seconds earlier","root_cause":"seed-demo-owner.py generates a fresh random password on EVERY run. It is idempotent for the tenant, role and feature-gate rows but not for the password. With several agents running it concurrently against one box, each run rotates the password out from under the others, so everyone sees Invalid login credentials even with a just-printed password.","fix":"Stopped relying on the shared account for concurrent verification: created a dedicated user with a stable password, granted tenant OWNER on the same demo tenant via tenant_users. For the shared account, reset a stable password through the Supabase admin API and told other agents to stop re-running the seed.","tags":["supabase","seeding","multi-agent","demo-box","password-rotation"]}
{"id":"bug-ms4dovjr-eb6402","timestamp":"2026-07-28T08:12:55.863Z","related_bugs":[],"occurrences":1,"last_seen":"2026-07-28T08:12:55.863Z","error_message":"POST /v1/messages dispatched a caller-supplied model straight to LiteLLM, so a raw route id such as route-groq-fast reached a provider with no alias resolution, no per-tenant entitlement check, no API-key alias allowlist check and no credit reservation","file":"apps/edge-api/internal/anthropic/handler.go","root_cause":"The Anthropic surface built its own upstream dispatch instead of delegating to the wired /v1/chat/completions chain. LiteLLM model names ARE route ids (deploy/litellm/config.yaml declares its model_list entries as route-openrouter-default, route-groq-fast and so on), so forwarding the client model unresolved let a caller address a route directly and skip every control that hangs off routing.Service.SelectRoute plus the whole accounting lifecycle. Two adjacent defects kept it latent and would have made it live: /v1/messages was never registered in the support matrix so UnsupportedEndpointMiddleware 404d it before the handler ran, and the chat dispatcher re-marshalled a three-field struct which silently dropped max_tokens, temperature, tools and stream_options.","fix":"Gave the handler a single dependency, the wired POST /v1/chat/completions handler chain, and made it translate and delegate: alias resolution, entitlement, the API-key allowlist, credit reservation and settlement, retry, tracing and audit are reused rather than duplicated. Response translation moved into a ResponseWriter that translates SSE incrementally, folds a stream back into one message for a non-streaming caller, and forwards a non-2xx response unchanged since the delegated chain already sanitizes it. Always dispatch downstream as a stream with stream_options.include_usage so the reservation settles at real token counts. Registered both Messages endpoints in the support matrix and stopped the chat dispatcher dropping caller fields. Guards: tools/lint-litellm-dispatch-requires-routing.mjs plus a Go structural test, both failing when an edge-api package dispatches to LiteLLM without SelectRoute.","tags":["edge-api","anthropic","routing","billing","authorization","provider-blind","support-matrix","guard-lint"]}
{"id":"ci-noop-echo-satisfies-required-checks","priority":"P1","date":"2026-07-27","file":".github/workflows/ci-noop.yml","error_message":"gh pr checks showed required contexts such as Go tests (control-plane) passing in 2 to 4 seconds on pull requests where those jobs genuinely take minutes, making the merge gate unreliable as a signal","root_cause":"ci.yml was path filtered on the trigger, and a workflow skipped by a trigger path filter never creates its check runs, so required checks stayed Pending and blocked the merge. The workaround was ci-noop.yml, a second workflow that fired on the inverse paths-ignore set and published job names identical to ci.yml's as echo steps. GitHub runs a paths-ignore workflow whenever ANY changed file falls outside the ignore set, so a pull request touching both code and docs fired both workflows and each required context received two check runs, one real and one echo. The repository's visual-proof rule commits screenshots under docs/ alongside code, so that mixed shape is the common case. Proven live on PR #559 commit d05b7ba1: every required context had two check runs, ci.yml run 30301329356 taking 13s to 2m38s and ci-noop.yml run 30301329536 taking 2s to 4s.","fix":"Deleted ci-noop.yml. Removed the trigger path filters from ci.yml and moved the decision into a leading changes job that lists the changed files through the GitHub API and emits one boolean. Jobs publishing a required check name use if: always() with per-step gating so they always report their own conclusion, which avoids depending on how branch protection treats a check run whose conclusion is skipped; non-required jobs use a plain job-level if:. The allow-list polarity is inverted so anything unrecognized, an API error, or an event with no diff runs the full suite, and any changed file with an executable extension runs the full suite regardless of directory. Removed the web-e2e-shim job, the last remaining pair of jobs sharing a check name, and added .github/ci/lint-workflow-check-names.mjs to the repo policy lints. The lint expands matrix job names against the real matrix values instead of a wildcard, so it fails on a duplicate producer for a required context, on a required context with no producer at all, and on a required job that can be skipped or whose workflow carries trigger path filters.","tags":["ci","github-actions","branch-protection","merge-gate","path-filtering","required-status-checks"]}
{"id":"cp-hive-stale-dns-522-no-tunnel-ingress","priority":"P1","date":"2026-07-27","file":"deploy/cloudflare/tunnel-ingress.json","error_message":"https://cp-hive.scubed.co returned HTTP 522 (origin connection timeout) on every path (/, /health, /healthz, /v1/health) while hive-control-plane-1 was Up and healthy on the box and api-hive.scubed.co/health returned 200 throughout","root_cause":"cp-hive.scubed.co was a leftover proxied A record pointing at 40.233.86.19, the retired 1GB VM that docker-compose.staging.yml was written for. It had NO Cloudflare Tunnel ingress rule, so it was never served by the demo box at all: Cloudflare kept trying to open a connection to a dead origin and timed out with 522. The control-plane was healthy the whole time under control-hive.scubed.co, the hostname the web-console Worker actually uses. The mapping was invisible to review because cloudflared on the box runs 'tunnel run' with only a TUNNEL_TOKEN and no --config, so the tunnel is remotely managed and its ingress lived only in the Cloudflare dashboard. Review of the first fix found the more dangerous half of the same defect: the value that feeds payment callbacks does not come from the staging overlay at all, since nothing references that file. It comes from docker-compose.yml, which defaulted CONTROL_PLANE_PUBLIC_URL to http://localhost:8081. resolveCallbackBaseURL already derives the callback base from the request Host when the variable is empty, which behind the tunnel yields the correct public hostname, so the compose default guaranteed the variable was always set and the correct fallback could never run. Live payment providers would have received an unreachable loopback callback URL.","fix":"Deleted the stale A record (zone record id 42db291d707ca4378f7c8efdc14ad773) so the name fails resolution instead of hanging. Brought the hostname map into the repo as deploy/cloudflare/tunnel-ingress.json plus deploy/cloudflare/reconcile-tunnel-dns.mjs, wired as npm run cloudflare:check and cloudflare:apply. Dropped the docker-compose.yml default for CONTROL_PLANE_PUBLIC_URL so the host-derived Go fallback runs, and documented the variable in .env.example. Labelled docker-compose.staging.yml as unreferenced and superseded. Extended apps/web-console/tests/unit/control-plane-host.test.ts to scan every tracked text file under deploy/ and fail on any scubed.co hostname that tunnel-ingress.json does not declare, which is the credential-free guard that would have caught this on the PR that introduced it.","tags":["cloudflare","tunnel","dns","ingress","522","control-plane","demo-box","host-only-config","drift","payment-callback","compose-default"]}
{"id":"bug-ms4daxxv-d70289","timestamp":"2026-07-28T08:02:05.779Z","related_bugs":[],"occurrences":1,"last_seen":"2026-07-28T08:02:05.779Z","error_message":"Paying customer lands on raw webhook JSON (SSLCommerz) or a 404 (Stripe) after completing payment","root_cause":"A payment rail has two outbound URLs that were conflated. SSLCommerz success_url/fail_url/cancel_url were set to CallbackBaseURL + /webhooks/sslcommerz/{success,fail,cancel}, which is the control-plane IPN handler, so the browser got {\"status\":\"ok\"} and a browser request could reach settlement. Stripe built ${CallbackBaseURL}/checkout/stripe?client_secret=..., a route present in no service. Both used the control-plane origin for a customer-facing landing page. Separately, deploy/docker/docker-compose.yml injected CONTROL_PLANE_PUBLIC_URL=http://localhost:8081 as a default, so the correct request-host fallback in resolveCallbackBaseURL never ran and providers were handed an unreachable callback base.","fix":"Split InitiateInput into CallbackBaseURL (control-plane, webhooks and settlement only) and ReturnBaseURL (console, browser only). ReturnBaseURL resolves from WEB_CONSOLE_PUBLIC_URL then NEXT_PUBLIC_APP_URL, with no hardcoded default, so a missing value refuses the checkout loudly instead of stranding a payer. Stripe moved to a hosted Checkout Session so both rails share one shape. Added one console return page with success/pending/failure/cancelled read from the payment intent via a new GET-only account-scoped endpoint; query params are never trusted as state. Removed the three SSLCommerz browser routes from the webhook surface. resolveCallbackBaseURL now demotes a loopback CONTROL_PLANE_PUBLIC_URL below the real request host.","tags":["payments","sslcommerz","stripe","browser-return","webhook","base-url","loopback-default","issue-538","bd-regulatory"]}
{"id":"bug-ms4egwv8-695878","timestamp":"2026-07-28T08:34:43.940Z","related_bugs":[],"occurrences":1,"last_seen":"2026-07-28T08:34:43.940Z","error_message":"Invitation acceptance impossible for a signed-out invitee: /auth/sign-in?next=/invitations/accept dropped the ?token, and later a member hitting /console/members hit the console error boundary while an already-member acceptance returned an opaque 500","root_cause":"The acceptance page built its sign-in bounce from a bare path and lib/auth/next-target.ts allow-listed /invitations/accept as an exact match only, so any query string collapsed to /console. Separately the accept handler mapped every service error to 400 with the raw internal string, the members page never read its own searchParams so invite feedback was computed and discarded, getMembers threw a bare Error on the owner-only 403 so a member crashed the page, and AcceptInvitation wrote the membership without checking for an existing one, surfacing the unique-constraint violation as a server error","fix":"Read the token before the auth check and carry it in next via appendNextParam; move /invitations/accept from the exact allow-list to the prefix arm beside /oauth/consent so path?query matches but /invitations/accept-evil still does not; map acceptance failures to stable codes (expired 410, already accepted 409, already member 409, email mismatch 403, not found 404) and render distinct truthful copy with a real next action for each; render invited/joined/role_updated/error searchParams as a status or alert banner; detect an existing membership before the write; catch the 403 from getMembers as an owner-only explanation and rethrow anything else; add a role selector at invite time, per-row role editors, member emails instead of UUIDs, and a PATCH /api/v1/accounts/current/members/{user_id} route gated on members.manage with no-self-change and last-owner guards enforced in the service and again in the UPDATE statement","tags":["invitations","rbac","web-console","control-plane","open-redirect","next-target","authz","truthful-errors","members","534","535","536"]}
{"id":"bug-ms4y99vw-a94f20","timestamp":"2026-07-28T17:48:39.883Z","related_bugs":[],"occurrences":1,"last_seen":"2026-07-28T17:48:39.883Z","error_message":"sqlalchemy.exc.ProgrammingError: (psycopg2.ProgrammingError) invalid dsn: invalid connection option \"pool_max_conns\"","root_cause":"PR #593 appended pool_max_conns to the shared Supabase DSNs. pool_max_conns is a pgx (Go) connection-string parameter, not a libpq one. Open WebUI connects via SQLAlchemy + psycopg2, which parses the DSN with libpq make_dsn and rejects any unknown option, so open-webui failed its healthcheck 19s after start and deploy-demo-box step Rebuild + recreate changed services failed. control-plane and edge-api both use pgx and went Healthy in the same run, which is why only chat-hive returned 502.","fix":"Keep pool_max_conns only on the pgx service DSNs (control-plane, edge-api). Give open-webui a clean libpq DSN with no pgx-only options; size its pool with Open WebUI env vars instead.","tags":["deploy","supabase","pooler","open-webui","dsn","pgx","psycopg2","pr-593","regression"]}
{"id":"bug-ms7kycjz-4dc1ff","timestamp":"2026-07-30T13:59:33.646Z","related_bugs":[],"occurrences":1,"last_seen":"2026-07-30T13:59:33.646Z","error_message":"PR #606 review: control-plane persisted raw err.Error() from agent-engine Launch failures into the customer-visible task error_message field","root_cause":"Service.CreateTask default branch (any Launch error besides ErrEngineNotConfigured) returned s.repo.Transition(...,err.Error()) directly, so an arbitrary Engine implementation error (dial failure with internal hostname, provider error body, etc.) could reach the task record returned by http.go taskResponse. ErrEngineNotConfigured already had a sanitized message (engineUnavailableMessage); the generic path never got the same treatment.","fix":"Added engineLaunchFailedMessage fixed string, same pattern as engineUnavailableMessage. Default branch now logs the real error via slog.Default().WarnContext (task_id + error) then persists the generic message, mirroring the existing poller.go sanitization pattern for engine status-check failures. No error_code field exists on Task/http.go yet, so a machine-readable code was deferred (would need a schema migration) rather than added here.","tags":["agenttask","provider-blind","error-sanitization","pr-606","code-review","control-plane"]}
{"id":"bug-2026-07-29-disconnect-settle","timestamp":"2026-07-29T09:29:37.000Z","error_message":"Mid-stream client disconnect settled nothing: no charge, no ledger entry, no usage event, reservation stuck active forever","file":"apps/edge-api/internal/inference/stream.go","root_cause":"executeStreaming/executeResponsesStreaming called FinalizeReservation with the same ctx the HTTP handler uses for the whole request. A client disconnect cancels that ctx, so accounting_client.go build the outbound request via http.NewRequestWithContext(ctx, ...) and it failed before leaving the process. The tail settle block then set finalized = true unconditionally regardless of whether the call actually succeeded, which suppressed the existing deferred release path too. Separately, the terminal-usage-missing fallback charged a hardcoded flat 10000 estimate (chat_completions.go / completions.go), a latent overcharge whenever no real usage block arrived.","fix":"Collapsed settlement into the one existing defer (already runs on every exit) via a new settleStream helper that always dispatches on context.WithTimeout(context.Background(), 5*time.Second), independent of the caller ctx a disconnect cancels. Deleted the tail settle block and its flat-10000 fallback; delivered tokens (usage-confirmed or, absent that, a content-based estimate, floored at 1 when anything was produced) settle via FinalizeReservation, nothing delivered releases in full via ReleaseReservation. finalized is set only after settleStream itself reports success, so a failed settle still falls through to the disconnect-release path on the next exit. terminal_usage_confirmed mirrors hasUsage so a content-estimate settle still routes to needs_reconciliation.","fix_pr":"fix/disconnect-settlement-context","tags":["billing","disconnect","streaming","reservation","edge-api","accounting","context-cancellation","overcharge","orphan-hold"],"related_bugs":[],"occurrences":1,"last_seen":"2026-07-29T09:29:37.000Z"}
{"id": "bug-2026-07-30-disconnect-settle-round2", "timestamp": "2026-07-30T14:00:00.000Z", "error_message": "PR #602's own fix still stranded holds and undercharged: three dispatch-failure branches released on the cancelled request context and discarded the error while forcing finalized=true anyway; recordCompletedEvent was skipped whenever CreateReservation had failed non-fatally; the no-confirmed-usage settlement estimate billed completion bytes only, ignoring prompt cost; and usage_events inserts for the new upstream_error/finalize_failed/interrupted event types were silently rejected by a stale CHECK constraint.", "file": "apps/edge-api/internal/inference/stream.go, stream_responses.go, supabase/migrations/20260730_01_usage_events_event_type_extend.sql", "root_cause": "Two independent reviewers on PR #602 found the settleStream fix from the first round did not cover every exit path: the three pre-SSE failure branches (dispatch error, non-2xx upstream status, missing http.Flusher) in both stream.go and stream_responses.go still called ReleaseReservation(ctx, ...) on the caller's request context (the same context a client disconnect cancels), discarded the returned error with `_ =`, and set finalized = true unconditionally -- reproducing the exact bug PR #602 exists to fix, just one step earlier in the lifecycle. Separately, settleStream's defer guard `if finalized || reservation.ID == \"\"` skipped recordCompletedEvent entirely whenever CreateReservation had failed non-fatally (an explicitly tolerated transient control-plane error), dropping completed-usage telemetry. settlementCredits' no-confirmed-usage fallback estimated completion tokens from streamed content only, so a client that disconnected right after the first output token billed roughly one credit regardless of prompt size, an abuse vector on a prepaid product since the client controls when to disconnect. Finally, usage_events.event_type's original CHECK constraint (20260330_02_usage_accounting.sql) never included upstream_error, finalize_failed, or interrupted, so recordInterruptedEvent's insert failed on every call and the error was discarded, hiding the gap.", "fix": "Added releaseReservationBackground(snapshot, reservationID, requestID, reason), a shared helper that releases on a fresh context.WithTimeout(context.Background(), accountingTimeout) and returns true only on real success; all six failure-branch sites (three per file) now gate finalized on that return value instead of assuming success, so a failed immediate release still falls through to the deferred settleStream retry. Removed the reservation.ID == \"\" short-circuit from both defer guards; settleStream now internally skips the ReleaseReservation/FinalizeReservation network calls when reservation.ID is empty but still calls recordInterruptedEvent or recordCompletedEvent so telemetry is never dropped just because CreateReservation failed. settlementCredits gained a promptBody parameter: when no confirmed usage exists but content was delivered, credits are now estimateCompletionTokens(promptBody) + estimateCompletionTokens(content), aligning the disconnect-estimate path with the confirmed path's prompt+completion billing (delivered stays gated on completion content only, so a true zero-content disconnect still releases in full). recordInterruptedEvent no longer discards RecordUsageEvent's error. Added migration 20260730_01_usage_events_event_type_extend.sql extending the CHECK constraint to include upstream_error, finalize_failed, and interrupted, idempotent via DROP CONSTRAINT IF EXISTS + re-ADD. Judged the reported over-release window (finalize charges then FinalizeReservation itself errors, fallback release could double-credit) as already closed: control-plane's releaseLocked (apps/control-plane/internal/accounting/service.go) checks reservation.Status == ReservationStatusFinalized before touching the ledger and returns a PolicyError instead of releasing, and finalize/release both run under the same per-account WithAccountLock, so no code change was needed there.", "fix_pr": "fix/disconnect-settlement-context", "tags": ["billing", "disconnect", "streaming", "reservation", "edge-api", "accounting", "context-cancellation", "undercharge", "orphan-hold", "usage-events", "check-constraint", "pr-602"], "related_bugs": ["bug-2026-07-29-disconnect-settle"], "occurrences": 1, "last_seen": "2026-07-30T14:00:00.000Z"}
{"id":"bug-ms76zn4y-3c10fb","timestamp":"2026-07-30T07:28:39.394Z","related_bugs":[],"occurrences":1,"last_seen":"2026-07-30T07:28:39.394Z","error_message":"Issue #592: resetProfilesAndInvitation deletes account_billing_profiles rows in a loop with no paired upsert immediately after, the same un-paired delete-then-write shape PR #583 already fixed for account_memberships and account_invitations","root_cause":"Audited and dismissed, not a real bug. GetBillingProfile (apps/control-plane/internal/profiles/repository.go) LEFT JOINs account_billing_profiles and COALESCEs every column, so a missing row reads back blank, never ErrNotFound: nothing observes the deleted-but-not-yet-reinserted window as broken. There is also no concurrent reader of that window: every spec beforeEach calls the fixture CLI via execFileSync, which blocks synchronously until the child process exits, and playwright.config.ts pins workers 1, so no page load can ever run during this function. Account ids are namespaced per E2E_RUN_KEY so two CI jobs never touch the same row either. supabase/functions/e2e-fixtures/index.ts, the other file the issue asked to audit, no longer exists: seeding moved in-process in PR #583.","fix":"Documented the audit conclusion as an inline comment above the account_billing_profiles delete loop in apps/web-console/tests/e2e/support/e2e-fixture-seed.mjs, referencing issue #592 directly, so the dismissal reasoning survives instead of living only in a PR body. No functional code change: the delete stays a bare delete on purpose. Revisit if playwright workers ever goes above 1 or the fixture CLI stops blocking synchronously.","tags":["e2e","fixture-seeding","race-audit","billing-profiles","issue-592","dismissed-non-bug","playwright"]}
{"id":"bug-ms778jnr-f3c347","timestamp":"2026-07-30T07:35:34.790Z","related_bugs":[],"occurrences":1,"last_seen":"2026-07-30T07:35:34.790Z","error_message":"CALL public.purge_metering_shadow_verdicts() fails with ERROR: invalid transaction termination","root_cause":"CREATE PROCEDURE ... SET search_path = public, pg_temp wraps every CALL in an implicit sub-transaction (to restore the GUC afterward); a COMMIT inside the procedure body cannot terminate that wrapper transaction, so any procedure that both sets a config GUC via the CREATE PROCEDURE SET clause and issues COMMIT/ROLLBACK internally fails at the first COMMIT.","fix":"Drop the SET search_path clause from the procedure. Every table reference inside was already fully schema-qualified (public.metering_shadow_verdicts, public.metering_shadow_verdicts_retention_config), so the clause bought nothing and removing it is free.","tags":["postgres","plpgsql","pg_cron","migrations","procedure"]}
{"id":"bug-ms778jzw-ba939b","timestamp":"2026-07-30T07:35:35.225Z","related_bugs":[],"occurrences":1,"last_seen":"2026-07-30T07:35:35.225Z","error_message":"Re-running supabase/migrations/20260729_02_metering_shadow_verdicts_retention.sql a second time aborts with relation already exists","root_cause":"CREATE TABLE public.metering_shadow_verdicts_retention_config had no IF NOT EXISTS guard while every other DDL statement in the same file did (extension, insert-on-conflict, create-or-replace-procedure), so the migration was not actually idempotent despite being written to look idempotent.","fix":"Add IF NOT EXISTS to the CREATE TABLE statement.","tags":["postgres","migrations","idempotency","supabase"]}
{"id":"bug-ms7ld1tc-8c5217","timestamp":"2026-07-30T14:10:59.567Z","related_bugs":[],"occurrences":1,"last_seen":"2026-07-30T14:10:59.567Z","error_message":"PR #609 review: purge_metering_shadow_verdicts had no search_path pinning after the SET clause was deleted (not relocated) from CREATE PROCEDURE","root_cause":"Postgres wraps a SET-clause procedure CREATE PROCEDURE ... SET search_path=... CALL in a sub-transaction to restore the setting afterward, and that wrapper rejects a COMMIT inside the body (invalid transaction termination). The earlier fix removed the SET clause outright to unblock the internal batch-loop COMMIT, leaving the procedure with an unpinned search_path (defense in depth gap) instead of relocating the pin.","fix":"Move SET search_path = pg_catalog, pg_temp; to the first statement inside the procedure body (mirrors the existing SET lock_timeout body-position pattern in the same procedure). Body-position SET coexists with the internal COMMIT. Verified live: two applies of both migrations with zero duplicate rows/errors, CALL with a forced multi-iteration batch size deletes stale rows and keeps fresh rows, and SHOW search_path in the same session reads pg_catalog, pg_temp immediately after CALL returns.","tags":["postgres","plpgsql","procedure","search_path","security","migration","pg_cron","hive-609"]}
{"id":"bug-ms778n8o-16075e","timestamp":"2026-07-30T07:35:39.432Z","related_bugs":[],"occurrences":1,"last_seen":"2026-07-30T07:35:39.432Z","error_message":"profile-completion.spec.ts billing settings save partial business profile fails intermittently in CI, roughly half the time, owner-only route 403 owner_role_required for the verified spec user","root_cause":"seedAccountsAndMemberships writes verifiedUser two memberships (owner of primary workspace, member of shared workspace) in one multi-row upsert. account_memberships.created_at defaults to now() and one INSERT statement stamps every row with the same transaction timestamp, so the two rows tie on created_at. ListMembershipsByUserID (apps/control-plane/internal/accounts/repository.go) orders ORDER BY created_at ASC, id ASC, with id documented there as a tiebreaker only for that pathological same-created_at case. The bulk seed hits that case every run, so which row sorts first depends on comparing two freshly random gen_random_uuid() ids: a coin flip on every reseed. EnsureViewerContext takes memberships[0] as the default workspace on every specs first navigation, so about half of CI runs land the verified user in the member-role workspace and its owner-only route 403s.","fix":"Stamped account_memberships.created_at explicitly in apps/web-console/tests/e2e/support/e2e-fixture-seed.mjs, one millisecond apart per array index in the seeds intended priority order (owned workspace before joined workspace), so the seeded rows never reach the tie case. Left the Go-side id tiebreak in ListMembershipsByUserID unchanged: it is correct general-purpose behavior for real simultaneous production writes and already has its own regression test (TestListMembershipsByUserID_TiebreaksByID), so the fix belongs at the seed source that manufactures the tie every run, not at the consumer that already resolves ties deterministically per call.","tags":["e2e","fixture-seeding","created-at-tie","nondeterministic","profile-completion-spec","issue-608","membership-ordering"]}
{"id":"bug-ms6gd0pm-9c3595","timestamp":"2026-07-29T19:03:13.881Z","related_bugs":[],"occurrences":1,"last_seen":"2026-07-29T19:03:13.881Z","error_message":"desktop entitlements tests: intermittent accept-thread stall and No rustls crypto provider is configured panic in cargo test","root_cause":"mock_server test helper only tried listener.accept() once and silently dropped the connection handler thread on any transient accept error under CI load; separately, reqwest rustls-no-provider client construction panics unless a process-wide rustls CryptoProvider is installed first, which main.rs does at real startup but cargo test never runs main() and test order/parallelism is unspecified","fix":"mock_server now loops on listener.incoming() retrying past transient accept errors instead of giving up after one Err; added a std::sync::Once-guarded ensure_crypto_provider() called from both mock_server and unreachable_origin so every fetch test installs rustls::crypto::ring::default_provider() before the first build_client() call regardless of run order","tags":["rust","desktop","tauri","rustls","reqwest","flaky-test","ci"]}
{"id":"bug-msapldi2-174bf4","timestamp":"2026-08-01T18:32:44.953Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-01T18:32:44.953Z","error_message":"releaseLocked accepted a needs_reconciliation reservation, so a settled hold could be released a second time: released_credits was recomputed from an already-zero remaining hold, overwriting 8997 back to 0, flipping status away from needs_reconciliation, marking a charged and delivered attempt cancelled, and leaving a row where reserved != consumed + released","root_cause":"the expand, finalize and release guards each enumerated the settled statuses they refused (a blocklist), so needs_reconciliation, added later for unconfirmed settlements, fell through the release and expand guards into the permitted path. Nothing consumes credit_reconciliation_jobs (issue 600), so those rows are terminal in practice and every one was a standing second-release target","fix":"replaced the three blocklists with one allowlist predicate, reservationOpen, permitting only active and expanded, so an unrecognized or newly added status fails closed with a typed PolicyError (HTTP 409) instead of falling through. Money was never at risk: the release amount recomputed to 0 and the unified release idempotency key (PR 662) dedupes at the credit_idempotency_keys primary key regardless","tags":["accounting","reservations","money-path","fail-closed","issue-672"]}
{"id":"bug-msaonukn-3d4b6a","timestamp":"2026-08-01T18:06:40.774Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-01T18:06:40.774Z","error_message":"audio billing charged flat hardcoded credits (1000 per speech request, 500 per transcription) and never read the model catalog, so a one second clip and a ten minute clip cost the same","root_cause":"apps/edge-api/internal/audio/handler.go set EstimatedCredits/ActualCredits to literals at every reservation site. The package never consulted alias pricing at all: routing.SelectionResult carried Pricing but edge-api inference.SelectRouteResult did not decode it, and no field existed for a price unit. Speech and transcription are not token priced upstream (Groq bills per character and per hour of audio), so there was also no unit in which the catalog price could have been applied even if it had been read.","fix":"Added model_aliases.price_unit (tokens|characters|seconds) with a CHECK that a non token unit prices only output_price_credits, repriced hive-tts and hive-stt from Groq published rates, carried pricing plus unit through routing.SelectionResult to inference.SelectRouteResult to audio.RouteResult, and derived every audio charge as quantity times catalog price via metering.ChargeCredits (per million units, math/big, round half up). Speech meters synthesized characters, transcription and translation meter audio seconds with the provider ten second minimum. The handler now asks upstream for verbose_json so a duration is always reported and reshapes the response back to the caller requested format; a 2xx with no duration is refused and the hold released rather than charged at a guess. An alias whose unit the endpoint cannot meter is refused before any hold is taken.","tags":["billing","audio","catalog-pricing","issue-627","fail-closed","edge-api","math-big"]}
{"id":"bug-msaq2khn-8e7fd3","timestamp":"2026-08-01T18:46:07.163Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-01T18:46:07.163Z","error_message":"audio release-on-error sites released holds on the request context and discarded the error, so a disconnected client stranded the hold silently (#616 mechanism, most exposed at the new unpriceable-response refusal)","root_cause":"every _ = h.accounting.ReleaseReservation(ctx, ...) in apps/edge-api/internal/audio/handler.go passed the request context. A release runs at the moment a request has already failed, which is when the client is most likely gone; a cancelled context aborts the accounting HTTP call before it is sent, and the dropped error hid the failure. settleReservation had already been fixed for finalize (#637) but the release sites were never converted.","fix":"Added Handler.releaseHold in reservation.go: fresh context.Background with its own releaseTimeout, logs a failed release, and takes no context argument so a caller cannot pass a request context. Converted all fifteen release sites in handler.go. settleReservation untouched (already separate bounded contexts for finalize and fallback release). Three red-first tests in release_context_test.go assert the release context is live and bounded, that a failed release is logged, and that a cancelled request context still yields a live release context.","tags":["billing","audio","issue-627","issue-616","context-cancellation","stranded-reservation","edge-api"]}
{"id": "bug-migbaseline-22d512", "timestamp": "2026-08-01T20:10:00.000Z", "related_bugs": [], "occurrences": 1, "last_seen": "2026-08-01T20:10:00.000Z", "error_message": "scripts/apply-migrations.sh seeded its baseline into public.hive_schema_migrations whenever that ledger was empty, so against a brand new database it recorded 61 of 84 migration files as already applied without executing any of them, then deployed against schema that was never created and reported success", "root_cause": "an empty ledger has two opposite meanings and looks identical in both: a database that predates the ledger and ran those migrations by hand, or a fresh database that has run nothing. The seed condition tested only the ledger, which cannot distinguish them, so the unknown state resolved to applied instead of to not applied", "fix": "apply-migrations.sh now calls probe-applied-migrations.py --baseline-state before seeding, which reads the schema and answers fresh, preexisting or ambiguous. Fresh skips the seed and runs the whole chain, preexisting seeds as before, ambiguous aborts with instructions and records nothing. HIVE_MIGRATION_BASELINE=seed|ignore is honoured only where the probe is inconclusive and is rejected when it contradicts the schema. Extensions are excluded from the decision because supabase/postgres ships pgcrypto and uuid-ossp pre-created. scripts/test-apply-migrations.sh proves the fresh case by counting executed migrations (84 of 84) against a real supabase/postgres 17.6.1.136", "tags": ["migrations", "deploy", "fresh-install", "fail-loud", "issue-676"]}
{"id": "bug-688-token-count-as-credits", "date": "2026-08-03", "title": "Chat, completions, responses and embeddings billed one credit per token, ignoring the alias catalog", "error_message": "credit_ledger_entries.usage_charge = -103 for a 72-input, 31-output request, exactly 72 + 31; hive-fast billed about 10.00 USD per million tokens against the 0.105 USD per million it carried in the catalog at the time, since repriced to 0.07 by PR #692. A 12000 input plus 3000 output request charged 15000 credits and settles at 118 credits at the live price ((12000 * 7000 + 3000 * 11200) / 1e6 = 117.6, round half up)", "root_cause": "settlementCredits in apps/edge-api/internal/inference/stream.go returned the provider's total_tokens as the credit figure, and both settlement sites (settleStream and executeSync) passed it through as FinalizeReservationInput.ActualCredits. model_aliases.input_price_credits and output_price_credits were never read on the text path. The correct per-million round-half-up math/big arithmetic existed in metering.ChargeCredits but only the audio path (PR #671) called it.", "fix": "Added apps/edge-api/internal/inference/pricing.go: CanPriceTokens gates on an explicit model_aliases.price_unit of tokens with at least one priced side, CreditsForTokens converts input and output counts through metering.ChargeCredits (single implementation, unchanged arithmetic) with a 1-credit floor, requireTokenPricing refuses a non-token-priced alias before any reservation exists. settlementCredits now takes the resolved route plus separate input and output token counts and returns its own confirmed verdict, so an unconfirmed estimate is catalog-priced too and a usage block reporting zero tokens is treated as absent rather than as a measured zero. settleStream carries the dispatched route. The JWT-session chat path records the same catalog figure in llm_traces.cost_credits.", "tags": ["billing", "issue-688", "edge-api", "catalog-pricing", "fail-closed", "d-031", "d-032", "d-034", "overcharge"]}
{"id":"bug-msaq3xcz-7ff846","timestamp":"2026-08-01T18:47:10.498Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-01T18:47:10.498Z","error_message":"deploy-demo-box reported success while no Caddyfile change ever reached the running proxy: PR #664 added a /v1/featuregate matcher to deploy/docker/Caddyfile.owui, run 30711940528 went fully green, and chat-hive.scubed.co kept answering /v1/featuregate with 200 text/html from Open WebUI","root_cause":"Two stacked layers. First, each Caddyfile is a bind-mounted file, so changing its contents does not change the container specification and docker compose up -d finds nothing to recreate; Caddy keeps serving the config it parsed at startup and the deploy step exits 0 either way (hive-caddy-owui-1 was 32 hours old while edge-api and open-webui were 15 minutes old). Second, a single-file bind mount resolves to an inode at container start and git pull replaces the file by rename rather than editing in place, so the container's /etc/caddy/Caddyfile stays pinned to the old inode forever (host inode 133458 5216 bytes versus guest inode 174599 4665 bytes). That makes the obvious fix, caddy reload --config /etc/caddy/Caddyfile inside the container, re-read the stale copy and exit 0 having changed nothing.","fix":"Added two steps to the deploy job in .github/workflows/deploy-demo-box.yml. The first validates each host Caddyfile by piping it into the container's caddy binary (caddy validate --config -), then compares the container's mounted copy with the host file: identical means an in-place caddy reload (zero downtime), different means the mount is inode-stale so the service is recreated with up -d --force-recreate --no-deps, which re-binds the mount and also stops the container reverting on its next restart. The second step asserts the outcome instead of trusting exit codes, probing localhost:3003/v1/featuregate and requiring an application/json body carrying a gateway shape, so a stale config fails the deploy instead of passing quietly. Verified live on the box: before 200 text/html, after 401 application/json UNAUTHENTICATED, with the four discriminator paths unchanged.","tags":["deploy","caddy","bind-mount","inode-staleness","silent-success","issue-619","demo-box","docker-compose"]}
{"id": "bug-701-litellm-sync-model-id-column", "date": "2026-08-04", "title": "LiteLLM config sync SELECT referenced pr.model_id, a column that does not exist on provider_routes, failing on every invocation", "error_message": "ERROR: column pr.model_id does not exist (SQLSTATE 42703), from litellmconfig: sync: query routes", "root_cause": "SyncService.Sync (apps/control-plane/internal/litellmconfig/service.go) selected pr.model_id AS litellm_name and separately concatenated cp.litellm_prefix onto it. provider_routes has no model_id column (real columns: route_id, alias_id, provider, provider_model, litellm_model_name, price_class, health_state, priority), so the query errored unconditionally and no invocation of Sync ever wrote a config. The one test that exercised the real query, sync_integration_test.go, was gated behind LITELLM_TEST_DB_URL, which appeared nowhere under .github/, so it always skipped in CI and the defect shipped invisibly.", "fix": "Select pr.provider_model directly as litellm_name and drop the litellm_prefix concatenation: every seeded provider_routes row (openrouter, groq, nvidia_nim) already stores provider_model pre-prefixed (e.g. openrouter/openai/gpt-4o-mini), so concatenating cp.litellm_prefix would have double-prefixed it. Wired LITELLM_TEST_DB_URL into ci.yml go-tests bootstrap step against the same ephemeral pgvector/pgvector:pg17 service container used by HIVE_TEST_DB_URL/ROUTING_TEST_DB_URL, and added internal/litellmconfig/... to the -tags integration test invocation so this suite actually runs on every PR. Strengthened the test to assert every seeded active route appears in the generated model_list by model_name with the exact litellm_params.model it was seeded with, so a future column rename fails loudly instead of shipping an empty or wrong model list again.", "tags": ["litellm", "sync", "provider_routes", "ci-gap", "skipped-test", "issue-701", "issue-684", "issue-689", "control-plane"]}
{"id":"bug-msapxlyj-522e62","timestamp":"2026-08-01T18:42:15.787Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-01T18:42:15.787Z","error_message":"TypeError: Failed to parse URL from /api/v1/accounts/current/api-keys/{id}/limits, web-console /console/api-keys/{id}/limits returned 500 for every key","root_cause":"getKeyLimits/updateKeyLimits in apps/web-console/lib/api-keys.ts fetched a bare relative path through an injected fetch client. A React Server Component has no origin, so Node fetch rejects the URL before any request is made, and the helpers also carried no session bearer. The page additionally passed a fetch client object down to a Client Component, which is not a serialisable prop.","fix":"Moved the transport into lib/control-plane/client.ts as getApiKeyLimits and updateApiKeyLimits, which use the existing getRequestContext (absolute CONTROL_PLANE_BASE_URL plus session headers). The page reads through the data layer and writes through an inline server action, so no fetch client crosses the Server/Client boundary and the 404 branch keys on ControlPlaneError.status instead of message text.","tags":["web-console","server-component","fetch","issue-552","rsc"]}
{"id":"bug-pr683-authz-limits-read","timestamp":"2026-08-03T15:00:00.000Z","related_bugs":["bug-msapxlyj-522e62"],"occurrences":1,"last_seen":"2026-08-03T15:00:00.000Z","error_message":"an unverified owner holding only api_keys.read got 403 on GET /api/v1/accounts/current/api-keys/{id}/limits and landed in the same React error boundary issue #552/#683 was filed to fix","root_cause":"resolveViewerContext in apps/control-plane/internal/apikeys/http.go gated every api-keys route, including the read-only handleGetKey, handleListKeys and handleGetLimits, on authz.PermAPIKeysWrite, which requires RequiresVerified=true. authz.PermAPIKeysRead requires no verification, so an unverified owner was refused a permission the registry explicitly grants them.","fix":"resolveViewerContext now takes an authz.Permission argument. Read handlers (handleGetKey, handleListKeys, handleGetLimits) pass authz.PermAPIKeysRead; mutating handlers keep authz.PermAPIKeysWrite. Added TestUnverifiedOwnerReadsLimitsButCannotWrite in limits_http_test.go: an unverified owner gets 200 on GET limits and 403 on PUT limits for the same principal.","tags":["control-plane","authz","api-keys","issue-552","issue-683","rsc"]}
{"id": "bug-708-phase19-testmatch-absolute-path", "timestamp": "2026-08-04T00:00:00.000Z", "related_bugs": ["bug-701-litellm-sync-model-id-column"], "occurrences": 1, "last_seen": "2026-08-04T00:00:00.000Z", "error_message": "npx playwright test --project=phase-19 exits 0 and reports only setup tests; all 7 per-push phase-19 spec files (tenant isolation, JWT expiry, cross-tenant attack, audit rows) are silently never collected, no error or warning", "root_cause": "apps/web-console/playwright.config.ts scoped the phase-19 project testMatch to a filename-only regex assuming Playwright tests it against the bare filename. Playwright actually tests testMatch regexes against the full absolute file path (runner/projectUtils.js collectFilesForProject), and an absolute path always contains path separators, so the anchored no-separator-from-position-0 pattern could never match any file. playwright test --list still exits 0 with zero collected specs and no diagnostic, the same silent-skip shape as issue 701.", "fix": "Rewrote testMatch to anchor on the phase-19 directory segment and stop before any further separator, so it matches the 7 direct-child spec files and deliberately excludes the e2e/phase-19/owui subtree, which is a separately configured nightly suite (own baseURL, own auth setup, documented in e2e/phase-19/README.md) that would otherwise get swept in under the wrong dependency chain. Added scripts/verify-phase19-collection.sh plus an npm script, which runs the real collector and fails loud if the project collection count is not exactly 7, verified to fail against the old regex and pass against the fix before commit. Did not wire --project=phase-19 into CI in this change: those specs need live Supabase auth and real upstreams whose pass state is unverified.", "tags": ["web-console", "playwright", "testMatch", "ci-gap", "silent-skip", "issue-708", "issue-701", "phase-19"]}
{"id": "bug-708-dark-go-tests", "date": "2026-08-04", "title": "Five Go DB-backed/integration tests never executed in CI; two had fixture bugs making them impossible to pass on any real schema", "error_message": "catalog: insert or update on table \"tenant_model_visibility\" violates foreign key constraint \"tenant_model_visibility_tenant_id_fkey\" (SQLSTATE 23503). providers: null value in column \"route_id\" of relation \"provider_routes\" violates not-null constraint (SQLSTATE 23502).", "root_cause": "Same failure shape as #701/#705, repeated five times. TestDispatchHappyPathWritesLLMTraceAndAuditsChatRequest (edge-api/internal/chat) and the four TestTenantFallback_* tests (edge-api/internal/auth) had HIVE_TEST_DB_URL correctly wired at the CI job level, but the RLS step's edge-api package list omitted ./internal/chat/... and ./internal/auth/.... TestChatCompletions_ToolCapable_AllowsTools plus four siblings (edge-api/internal/inference) needed only -tags integration, which the edge-api leg never passed. TestTenantVisibilityIntegration (control-plane/internal/catalog) and TestProviderCRUDIntegration (control-plane/internal/providers) were gated behind CATALOG_TEST_DB_URL/PROVIDERS_TEST_DB_URL, neither wired anywhere under .github/, so they never ran; their fixtures also had never been proven against a real schema. The catalog test hardcoded tenant UUIDs a0000000.../b0000000... into tenant_model_visibility without ever inserting them into public.tenants, so the FK on tenant_id was never satisfiable. The providers test's raw provider_routes INSERT omitted route_id, a text primary key with no default, so the not-null constraint was never satisfiable.", "fix": "Fixture fixes only, no assertions weakened: added seedTenant to catalog_integration_test.go inserting tenantA/tenantB into public.tenants (ON CONFLICT DO NOTHING) before any visibility row references them, with cleanup; added an explicit route_id value (route-<slug>) to the provider_routes INSERT in providers/integration_test.go. CI wiring: added CATALOG_TEST_DB_URL and PROVIDERS_TEST_DB_URL to the existing ephemeral-Postgres bootstrap step (same DSN as HIVE_TEST_DB_URL/ROUTING_TEST_DB_URL/LITELLM_TEST_DB_URL); added ./internal/catalog/... and ./internal/providers/... to control-plane's -tags integration invocation; added -tags integration plus ./internal/chat/..., ./internal/auth/..., ./internal/inference/... to edge-api's RLS step. Added a CI=true loud t.Fatal (matching #705's precedent) to every connect/skip helper touched (catalog, providers, chat, auth) so a future missing env var fails the build instead of silently skipping again. apps/edge-api/internal/audio/live_voice_integration_test.go deliberately left untouched: its round trip needs a real GROQ_API_KEY and belongs in the paid live-integration lane, not this free job.", "tags": ["ci-gap", "skipped-test", "integration-test", "fixture-bug", "issue-708", "issue-701", "issue-705", "control-plane", "edge-api", "catalog", "providers", "chat", "auth"]}
{"id": "bug-689-model-price-env-drift", "date": "2026-08-04", "title": "hive-default/hive-auto priced against paid models but OPENROUTER_DEFAULT_MODEL/OPENROUTER_AUTO_MODEL defaulted to OpenRouter's free-model router", "error_message": "provider_routes.provider_model read like the correct paid model (openrouter/openai/gpt-4o-mini, openrouter/openai/gpt-4.1-mini) while the box actually dispatched openrouter/free, an undisclosed arbitrary free model, so the catalog looked correct on audit while customers were billed the paid price for a free response.", "root_cause": "The model LiteLLM actually calls resolves at container start from an env var (deploy/litellm/config.yaml: model: os.environ/OPENROUTER_DEFAULT_MODEL / OPENROUTER_AUTO_MODEL), a mechanism completely independent of provider_routes.provider_model, which is descriptive metadata nothing in the dispatch path consults. .env.example defaulted both vars to openrouter/free with comments framing the free default as a deliberate cost-saving choice an operator opts out of, which is what made a real mispricing look intentional. No component compared the two, so the drift was invisible from the database alone.", "fix": "Live box state already corrected and verified by the owner (resolved model openai/gpt-4o-mini, 200 response, ledger charged 1 credit matching the catalog) before this PR. This PR prevents recurrence: .env.example now defaults OPENROUTER_DEFAULT_MODEL/OPENROUTER_AUTO_MODEL to the exact paid slugs provider_routes.provider_model names (matching comments corrected too), and deploy-demo-box.yml gained an 'Assert model catalog prices agree with the model LiteLLM will call' step that runs right after the recreate, queries provider_routes joined to model_aliases (price_unit='tokens', nonzero price, route not disabled/eol) via a disposable postgres:16-alpine psql container against the transaction-mode pooler DSN, reads LiteLLM's own GET /model/info (litellm_params.model is the env-var-substituted value per Router.set_model_list, not the os.environ/ template) and fails the deploy on any mismatch after stripping the leading provider/adapter segment from both sides (openrouter/ vs openai/ legitimately differ for hive-embedding-default's two routes, which use the openai/ generic adapter since LiteLLM's native openrouter/ provider does not map /embeddings). Proved locally with synthetic DB rows + /model/info payloads: the assertion logic fails (exit 1, correctly names both mispriced routes) against the openrouter/free state and passes (exit 0) against the fixed defaults, without falsely failing the multi-route embedding alias.", "tags": ["control-plane", "litellm", "pricing", "issue-689", "issue-701", "deploy-demo-box", "money-path", "one-model-one-price"]}
{"id":"bug-707-litellm-sync-drops-handtuned-params","date":"2026-08-04","title":"LiteLLM config sync reverted the embedding adapter workaround and dropped hand-tuned extra_body on every DB-managed route","error_message":"No runtime error. The next admin provider sync would have written litellm_params.model: openrouter/nvidia/llama-nemotron-embed-vl-1b-v2:free for route-openrouter-embedding (LiteLLM native openrouter/ provider does not map /embeddings, so RAG embeddings 404 upstream) and dropped extra_body.provider.allow_fallbacks/sort from route-openrouter-default and route-openrouter-auto.","root_cause":"PR #705 restored SyncService.Sync ability to write the live /etc/litellm/config.yaml, and mergeConfig only preserved (a) whole model_list entries with no provider_routes row and (b) model_info on DB-managed entries. Generate built litellm_params from exactly {model, api_base, api_key} straight out of provider_routes.provider_model, so any hand-tuned key on a DB-managed entry (extra_body.provider tuning, added for the flaky usage-token root cause) was dropped, and the deliberate generic openai/ adapter used for embeddings (deploy/litellm/config.yaml section 5) was replaced by the routing-canonical openrouter/ prefix the DB stores.","fix":"Two changes, no schema change and no new column. 1. Adapter awareness in the generator: SyncService.Sync now LEFT JOINs provider_capabilities and carries supports_embeddings into ModelEntry; litellmModel rewrites the leading provider segment to openai/ for an embeddings route, keeping the provider api_base, and Generate refuses an embeddings route with an empty api_base since openai/<model> without a base would send that provider key to api.openai.com. provider_routes.provider_model deliberately stays routing-canonical (openrouter/...) because the price catalog and deploy-demo-box.yml price assertion both read that column and canonicalize a live openai/X plus openrouter.ai api_base back to openrouter/X before comparing. 2. Field-level litellm_params preservation in mergeConfig: the generator owns model, api_base and api_key; every other key already present on the entry (and every other entry-level key, not just model_info) survives, mirroring the existing model_info rule.","verification":"Both regression tests were written first and watched fail against origin/main on a live pgvector/pgvector:pg17 with the full migration chain applied: TestWriteAndRestartPreservesHandTunedLiteLLMParams (extra_body and timeout absent after sync) and TestSyncKeepsEmbeddingAdapterForSeededRoute (model emitted as openrouter/nvidia/llama-nemotron-embed-vl-1b-v2:free). Both run in CI job Go tests (control-plane), the -short step and the -tags integration step respectively (LITELLM_TEST_DB_URL is wired into that job as of #705). Ad-hoc proof over a copy of the real deploy/litellm/config.yaml: after a sync the embedding entries carry openai/... plus the openrouter api_base, route-openrouter-default and route-openrouter-auto keep extra_body, route-doc-vlm and bge-m3 survive as orphans, litellm_settings and files_settings survive, and a replay of the deploy price assertion agrees on all 5 priced routes.","tags":["litellm","sync","config-drift","embeddings","rag","issue-707","issue-701","issue-705","issue-689","issue-713","control-plane"],"related_bugs":["bug-701-litellm-sync-model-id-column","bug-689-model-price-env-drift"]}
{"id":"bug-msf4rcs0-65a1ad","timestamp":"2026-08-04T20:48:22.895Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-04T20:48:22.895Z","error_message":"edge-api GET /v1/models answered 403 account_not_provisioned to the OWUI shim key that edge-api itself had just logged as active and able to authenticate model listing, RAG embeddings and TTS","root_cause":"Two defects. (1) The shim key account had no public.tenant_billing_accounts row because scripts/seed-owui-e2e-user.py never wrote one, and PR #620 made API-key tenant resolution fail closed, so /v1/models 403s. (2) checkOWUIShimKey asserted a WEAKER predicate than the request path: it read snapshot.Status and the model allowlist but never snapshot.TenantID, the one field handleModels requires, so the boot log asserted the opposite of the truth and the outage stayed invisible behind a false green.","fix":"Moved the tenant predicate onto authz.AuthSnapshot.TenantUUID and made all three consumers (handleModels, inference.Orchestrator.selectRoute, checkOWUIShimKey) call it, so probe and request path cannot drift. Probe failure now names the missing tenant_billing_accounts row and says a new key will not help. Seed script provisions the mapping before minting, refuses to repoint an existing one, and gained --tenant-slug because the mapping is 1:1 in both directions. Wired make test-scripts into the required repo-policy-lints CI job (nothing ran the python self-checks before) and made the nightly fail on a non-200 layer-2 status instead of printing 403 and exiting 0.","tags":["edge-api","owui","issue-717","false-green","probe-drift","tenant-billing-accounts","seed-script","ci-coverage"]}
{"id": "bug-712-owui-native-login-random-oauth-password", "date": "2026-08-04", "title": "OWUI nightly Playwright suite 400s on native /api/v1/auths/signin for the OAuth-only seeded fixture user", "error_message": "open_webui.models.auths:authenticate_user: owui-e2e@hive-e2e.invalid followed by \"POST /api/v1/auths/signin HTTP/1.1\" 400, every nightly run since 2026-07-26", "root_cause": "Open WebUI OAuth signup (backend/open_webui/utils/oauth.py, pinned v0.10.2) creates every OAuth-provisioned local account with password=get_password_hash(str(uuid.uuid4())), commented \"Random password, not used\". The e2e fixture user is OAuth-only (ENABLE_SIGNUP false), so its Open WebUI account password is never known. 09-agent-workspace-launcher.spec.ts deliberately signs in via the native email/password form with the freshly rotated OWUI_E2E_PASSWORD, which can never match the random local password, so it always 400s regardless of volume age or password rotation timing. The leading hypothesis in #712 (owui-data volume persisting across nightly runs) was checked and refuted: the job runs-on ubuntu-22.04, which resolves to GitHub hosted infra, not this repo self-hosted runner (labels self-hosted, Linux, X64, hive-demo, no ubuntu-22.04), confirmed via runner_group_name on the actual failing run.", "fix": "owui.setup.ts now calls POST /api/v1/users/{id}/update (admin-only, no old-password check) with the current OWUI_E2E_PASSWORD right after the real fixture OAuth login, using that same admin session (GET /api/v1/auths/ for the id). Runs every setup, reconciling Open WebUI local password to the rotated Supabase-side password every night. No assertion weakened or skipped.", "tags": ["web-console", "playwright", "owui-nightly", "open-webui", "oauth", "issue-712", "issue-704", "issue-684", "ci-gap"]}
{"id": "bug-712-owui-static-dir-missing-user-png", "date": "2026-08-04", "title": "OWUI nightly Playwright suite 500s on GET /api/v1/users/{id}/profile/image for every fixture user avatar request", "error_message": "RuntimeError: File at path /data/static/user.png does not exist. (15x per run, the only non-2xx response across 621+ requests once the #712 signin defect was fixed)", "root_cause": "open_webui.routers.users.get_user_profile_image_by_id falls back to FileResponse(f\"{STATIC_DIR}/user.png\") whenever profile_image_url is not an http(s) URL or a data: URI. Every OAuth-provisioned account gets exactly that default (\"/user.png\", set in utils/oauth.py). docker-compose.yml overrides STATIC_DIR to /data/static, bound read-only to deploy/docker/owui-static/ (a repo directory, not the owui-data named volume), and that directory never included a user.png file. Not a volume-persistence bug like #684: a read-only bind sourced from the repo has no first-boot seeding step at all, so the missing file was simply never shipped.", "fix": "Added deploy/docker/owui-static/user.png, the same badge-mark PNG already reused for logo.png/favicon.png/apple-touch-icon.png/splash.png/splash-dark.png (byte-identical, sha256 confirmed), matching the directory one-asset-every-purpose convention. Documented in the docker-compose.yml volumes comment for that mount.", "tags": ["web-console", "open-webui", "owui-nightly", "static-assets", "issue-712", "ci-gap"]}
{"id": "bug-556-hive-jwt-forward-never-installed-by-any-deploy", "date": "2026-08-05", "title": "Every chat completion on the demo box was refused because the hive_jwt_forward Open WebUI Function was never installed by any deploy path", "error_message": "edge-api owui unwrap: \"This chat session is not carrying a signed-in user token\" (missingUserTokenMessage, apps/edge-api/internal/auth/owui_unwrap.go:127) on every message a member sent from chat-hive.scubed.co; the request body carried no __metadata.upstream_auth", "root_cause": "hive_jwt_forward is an Open WebUI Function, and Open WebUI has no mount or env var that installs one; only its Functions REST API does, and that requires an authenticated admin session, which cannot exist at container boot. The only caller of that API in the repo was CI's Playwright setup (apps/web-console/e2e/phase-19/owui/owui.setup.ts), so no deployment ever had the filter and per-user attribution had never worked on the box. The absence was silent by construction: no error at boot, no banner, no deploy check, and the deploy went green. The compose comment recorded the manual step and flagged automating it as an open follow-up, which is where it stayed.", "fix": "scripts/install-owui-jwt-forward.py is now the single implementation: creates the Filter when absent, refreshes the stored body when it has drifted from the repo source, toggles is_active and is_global only when false (both endpoints flip rather than set, so an unconditional toggle switches the filter off on the second deploy), then re-reads the function from the API and exits nonzero unless it is present, active and global. scripts/owui-mint-admin-token.py obtains the admin session from inside the open-webui container for an admin that already exists, reading WEBUI_SECRET_KEY out of the running server's process environment because backend/start.sh exports it only into the process it execs and a docker exec shell cannot inherit it. deploy-demo-box.yml runs both after the Caddy verification and before the public smoke test. owui.setup.ts now calls the same script with the token of the admin session it just signed in as, so the nightly OWUI suite exercises the deploy's own code.", "tags": ["open-webui", "deploy-demo-box", "issue-556", "issue-269", "silent-absence", "idempotency", "chat"]}
{"id": "bug-722-owui-rag-embedding-model-frozen", "date": "2026-08-05", "title": "Open WebUI document RAG got 404 from /v1/embeddings because its persisted embedding model was frozen at a pre-catalog value", "error_message": "404, message='Not Found', url='http://edge-api:8080/v1/embeddings' (written onto every uploaded file record by Open WebUI)", "root_cause": "Open WebUI treats every rag.* setting as persistent config: the environment only seeds its own database on first boot, and Config.seed_defaults thereafter inserts nothing that already exists ('Existing DB values take precedence over defaults'). docker-compose.yml carried RAG_EMBEDDING_MODEL=text-embedding-3-small from 2026-05-18 (#143) until 2026-07-26 (#447), when it became the Hive catalog alias, so the demo box kept sending the OpenAI model id. No alias matches it in public.model_aliases, so route selection returned not-found and the gateway answered 404 model_not_found. The 404 body did name the model, but Open WebUI embeds through aiohttp raise_for_status, which discards the body and records only the bare reason phrase, so the failure read as an unregistered route and two investigations chased routing instead of the model name. Ruled out by evidence rather than assumption: allow_all_models is true on both live shim keys, both accounts have a tenant_billing_accounts row, hive-embedding-default has two healthy embedding-capable routes, and the deploy workflow's own sdk-replay job embedded successfully against the box with that alias the same day.", "fix": "deploy/docker/owui-patches/hive_rag_env_config.py, spliced into open_webui.config.seed_registered_defaults by apply_rag_env_config_patch.py at image build, reconciles exactly four persisted keys (rag.embedding_engine, rag.embedding_model, rag.openai.api_base_url, rag.openai.api_key) from the container environment on every start, and logs the reconciled model without the key value. Per-key rather than ENABLE_PERSISTENT_CONFIG=false, so every other setting an administrator changes in Open WebUI's UI still persists. The model is never hardcoded: it comes from RAG_EMBEDDING_MODEL, which compose derives from OWUI_RAG_EMBEDDING_ALIAS (D-001). Proven on the real image before and after: same persisted stale row and same environment, main sends text-embedding-3-small, patched sends hive-embedding-default.", "prevention": "Any Open WebUI setting expressed as a compose environment variable is inert on a deployment that has already booted. Reconcile it explicitly or expect the running system to diverge from the repo. CI cannot catch this class: owui-nightly boots a fresh Open WebUI every run, where first-boot seeding always agrees with the environment.", "tags": ["open-webui", "rag", "embeddings", "issue-722", "persistent-config", "demo-box", "ci-gap", "repo-is-not-the-running-system"]}
{"id": "bug-owui-launcher-spec-model-selector-aria-label", "date": "2026-08-05", "title": "The launcher clearance spec failed at 1440px because it located Open WebUI's model selector by an aria-label that only exists while no model is selected", "error_message": "Error: OWUI model selector at 1440px / expect(locator).toBeVisible() failed / Locator: locator('button[aria-label=\"Select a model\"]') / Error: element(s) not found, at apps/web-console/e2e/phase-19/owui/09-agent-workspace-launcher.spec.ts:114:7 (owui-nightly runs on dfc50f73 and the two before it, both attempts)", "root_cause": "Open WebUI labels one model-selector button two ways: aria-label 'Select a model' while nothing is selected and 'Selected model: <id>' once something is (i18n keys 'Select a model' and 'Selected model: {{modelName}}'). 09-agent-workspace-launcher.spec.ts waited on the first form before measuring header clearance, so its handle on the selector was only valid while the e2e catalogue came back empty, which is the state the spec's own comment records as normal. Once #735 and #737 made models resolve, Open WebUI auto-selected one, the label changed, and the wait failed on the first width of the loop, before any bounding box was read. The failure read as a 768px overlap defect because of the test name, but no geometry was ever measured and the CSS was never involved. 03-chat-model-switch.spec.ts and 06-tenant-model-visibility.spec.ts had already hit the identical rot at run 28688154897 and switched to a regex matching both forms; spec 09 was written without that fix.", "fix": "Locate the selector by the id upstream depends on itself: button[id^=\"model-selector-\"][id$=\"-button\"]. The pinned v0.10.2 bundle assigns that id unconditionally alongside aria-expanded, and its own model-selector keyboard shortcut calls document.getElementById(\"model-selector-0-button\").click(), so the id is present in every state the button has and cannot be renamed upstream without breaking upstream. Verified live in both states: id model-selector-0-button with aria-label 'Selected model: hive-auto' when the catalogue serves a model and with 'Select a model' when /api/models is stubbed empty. The text-node walk that widens the selector now skips whitespace-only nodes, which would otherwise leave the header at its original width and get reported as a launcher defect. custom.css's measurement table gained two corrections found while measuring: its right-edge figures are the selector's own and the add-model button adds about 22px past them (520 not 498 at 768px, so 94px of clearance not 116px), and a selected model makes upstream render a 'Set as default' button, which sits on its own line and never enters the launcher's row.", "prevention": "An aria-label that renders differently per state is not a locator. Before waiting on one, check whether the element also carries an id or role the application itself depends on. A spec whose handle is only valid in one state passes for as long as the environment happens to sit in that state, and the failure then surfaces far from its cause: this one failed at 1440px inside a test named for 768px, and a fix aimed at the test's name would have relaxed a working geometry assertion. Also: a green overlap assertion proves nothing without a forced-overlap control, so the proof capture includes one.", "tags": ["open-webui", "playwright", "e2e", "locator-rot", "owui-nightly", "aria-label", "tests-that-camouflage-bugs"]}
{"id":"bug-764-workspace-budget-routes-never-mounted","timestamp":"2026-08-06T17:29:19.323Z","error_message":"PUT /api/budget/{id} and POST /api/spend-alerts/{id} returned 404 from the console; a tenant owner could not save a hard spend cap or create a spend alert.","file":"apps/control-plane/internal/platform/http/router.go","root_cause":"budgets.Handler.ServeHTTP dispatched /api/v1/budgets/ and /api/v1/spend-alerts/ since Phase 14, but neither prefix was ever registered on the router, so every request fell through to the /api/v1/ catch-all and 404'd. Not an identifier mismatch: the console already sent viewer.current_account.id and the backend keys on public.accounts.id. The handler was correct and unreachable. The budgets package tests drove the handler directly with httptest.NewRequest, so an unmounted prefix was invisible to them, and the budget page swallowed the read with .catch(() => null) while the e2e spec deferred the save round trip by its own header comment.","fix":"Registered both prefixes on the router and added router_budgets_test.go, which asserts dispatch through NewRouter (not against the handler) and fails with the exact reported 404 when the mounts are reverted.","tags":["control-plane","routing","go","404","billing","handler-tested-but-unmounted","tests-that-camouflage-bugs"],"related_bugs":[],"occurrences":1,"last_seen":"2026-08-06T17:29:19.323Z"}
{"id":"bug-766-console-browser-fetches-control-plane-namespace","timestamp":"2026-08-06T17:29:19.323Z","error_message":"POST /api/v1/accounts/current/api-keys returned Next.js 404 HTML; API keys could not be created or revoked, and the checkout modal could not load rails or start a checkout.","file":"apps/web-console/components/api-keys/api-key-create-form.tsx","root_cause":"Four Client Components fetch a relative /api/v1/accounts/current/... path. That namespace belongs to the control-plane and has never existed in the console app: no route handler under app/api/v1/, no rewrites in next.config.ts, no middleware proxy. Next served its own 404 page. The typed server client already had correct createApiKey, revokeApiKey, getCheckoutRails and initiateCheckout; nothing browser-side was wired to them. Issue #552 fixed the identical mistake on the per-key limits page one call site at a time, which is how the siblings survived.","fix":"Added one console route handler with an explicit allowlist delegating to the typed client, plus browser-api-routes.test.ts, which walks the real files and asserts every relative /api/... a Client Component fetches has a matching route handler. It goes red on exactly the four broken paths.","tags":["web-console","nextjs","routing","404","api-keys","checkout","security","relative-fetch-to-foreign-namespace"],"related_bugs":["bug-552"],"occurrences":1,"last_seen":"2026-08-06T17:29:19.323Z"}
{"id": "bug-workspace-admin-areas-gated-on-platform-admin", "date": "2026-08-06", "title": "The console ADMIN group was unreachable for the account that owns the workspace, because feature gates and the marketplace were gated on platform admin", "error_message": "/console/feature-gates and /console/marketplace both rendered the EmptyState \"Admin access required\" for a tenant OWNER, after is_platform_admin was stripped from the demo accounts", "root_cause": "Two workspace-scoped surfaces were mounted behind RoleService.RequirePlatformAdmin in platform/http/router.go. Their data is per tenant (tenant_settings rows keyed by the caller selected tenant, marketplace_tenant_entries keyed the same way), so the authority they needed was OWNER of that tenant, not the platform-wide flag that also grants credit minting and provider base_url rewrites. While the demo account carried is_platform_admin the mismatch was invisible; stripping that flag, which was correct, made the organization owner unable to administer the organization.", "fix": "platform.WorkspaceAdminGate admits the OWNER of the tenant on the session (tenant_users, ACTIVE) or a platform admin, and stamps the platform-admin overlay into the request context. Feature gates and the marketplace mount behind it. Two carve-outs stay platform-only inside the handlers and are advertised on the wire so the console renders no refused control: feature gates in the billing and admin categories (manageable=false), and catalog curation, which writes the one global marketplace_entries table every tenant reads (can_curate=false). Provider CRUD and credit grants were not touched.", "prevention": "Gate on the scope of the data, not on the biggest flag that happens to be true for the person testing. A per-tenant row wants the tenant role; a global table wants the platform role. Also: viewer.TenantID is the only safe source of scope here, since deriving authority from any owner membership of any flagged account is the cross-workspace leak of issue #750.", "tags": ["authz", "rbac", "control-plane", "web-console", "issue-758", "issue-750", "tenant-scope"]}
{"id": "bug-workspace-admin-gate-mount-untested", "date": "2026-08-08", "title": "The workspace admin gate was unit tested, its attachment to the two routes was not, so deleting either mount kept the suite green", "error_message": "No error. Deleting the gate Require call from the feature-gate mount, and separately from the marketplace mount, left the whole control-plane short suite green", "root_cause": "Tests covered the predicate and the handler carve-outs; nothing dispatched through NewRouter, so no assertion depended on the middleware still being wired. The mutant lets any MEMBER with a selected tenant administer their own workspace gates. The same gap had been found once for the providers mount and the pattern was not carried to the new mounts.", "fix": "router_workspace_admin_test.go builds a router with a stub gate that records that it ran and refuses, and asserts all four workspace-scoped admin paths are answered by the gate, not the handler. A second test asserts a nil gate leaves the routes unmounted rather than falling back to a wider gate.", "prevention": "A middleware test proves the middleware. Only a router dispatch test proves it is installed. Verify by deleting the mount line, not by reading it.", "tags": ["authz", "control-plane", "router", "tests-that-camouflage-bugs", "issue-758", "pr-788"]}
{"id": "owui-772-dead-enable-admin-panel", "date": "2026-08-06", "title": "ENABLE_ADMIN_PANEL was a dead compose key on Open WebUI v0.10.2", "error_message": "No runtime error. docker-compose.yml set ENABLE_ADMIN_PANEL=false and two comments reasoned from it as if it were an active control.", "root_cause": "The name exists nowhere in the pinned ghcr.io/open-webui/open-webui:v0.10.2 image, verified by grepping the image's own backend rather than only the upstream tag. It arrived with the rest of the service block in #143, exactly as ENABLE_MODEL_FILTER did. Same class: a plausible-looking Open WebUI variable copied from an older release or a third-party hardening guide, never checked against the pinned source. ENABLE_PLUGINS, recommended by those guides for hiding the workspace, is dead in this release too.", "fix": "Deleted the key and rewrote the two comments in docker-compose.yml and Dockerfile.open-webui that cited it as the reason Open WebUI's model registry stays empty. Every Open WebUI variable introduced by #772 was checked against the pinned image first.", "tags": ["open-webui", "docker-compose", "dead-config", "772"]}
{"id":"bug-msi5lcsl-25f182","timestamp":"2026-08-06T23:35:01.125Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-06T23:35:01.125Z","error_message":"phase-19 spec 09-agent-workspace-launcher asserted a[href=\"/notes\"] is visible and clicked it, while the same PR removed Notes and 404d the route at Caddy","root_cause":"the spec used a sidebar entry as its drawer-opened fixture; the removal PR never ran the owui e2e project, and PR CI does not run it either, so the break would have surfaced only in the nightly","fix":"drawer-opened proof moved to a[href=\"/workspace\"], the nav loop dropped /notes, and an explicit toHaveCount(0) on a[href=\"/notes\"] added after the visible Workspace link so the absence is not vacuous. Verified live: old assertion FAILs on the patched stack, new assertion PASSes there and FAILs on a stack with ENABLE_NOTES back on","tags":["open-webui","playwright","e2e","phase-19","pr-779"]}
{"id":"bug-msi5lj5o-fee1e0","timestamp":"2026-08-06T23:35:09.372Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-06T23:35:09.372Z","error_message":"a Rewrite.find string in hive_ui_surfaces.py edited so it no longer matches the shipped Open WebUI bundle passed every test in scripts/test_owui_ui_surfaces.py","root_cause":"the bundle test built its input out of rewrite.find itself, so it could never notice that find had drifted off the real bundle; the only real guard runs inside the image build, and ci.yml runs make test-scripts without ever building Dockerfile.open-webui","fix":"checked in deploy/docker/owui-patches/pinned-bundle-excerpts.json, verbatim slices of the pinned digest bundle produced by dump_bundle_excerpts.py, and assert every find matches those real bytes exactly once plus that the fixture image equals the digest the Dockerfile pins, so a digest bump forces a regeneration instead of going green","tags":["open-webui","testing","bundle-patch","test-that-cannot-fail","pr-779"]}
{"id":"bug-mshupfeh-50bf08","timestamp":"2026-08-06T18:30:15.353Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-06T18:30:15.353Z","error_message":"Caddyfile.owui @blocked path_regexp anchored at ^/+ never covered the /api/v1 mount; GET /api/v1/configs/export and GET /openai/config returned the live gateway key and oauth.client_secret to any admin session","root_cause":"Every alternative in the regex sat behind the ^/+ anchor, so only bare SPA routes matched. POST /api/v1/auths/signup and the /api/v1/auths/admin/* family were never blocked. Separately the two credential-bearing config reads had no rule at all, and @adminMutation only inspects mutation verbs so a GET was never evaluated.","fix":"Added an api(?:/v[0-9]+)? arm covering auths/(signup|admin) and configs/export, plus a top-level openai/config arm. All segment-exact, never prefixes: a configs* prefix would catch /api/v1/configs/banners which the front end reads on every page load, and an openai* prefix would catch /openai/models and /openai/chat/completions. Must-stay-open list captured from a real signed-in session Caddy access log against the pinned image, not from source. Removed dead /api/v*/admin* and /api/v*/roles* globs from @adminMutation (no such OWUI routers). Pinned by scripts/test_caddy_owui_blocklist.py in make test-scripts.","tags":["caddy","security","open-webui","path-regexp","769","736","credential-leak"]}
{"id":"bug-770-771-owui-terminal-proxy-and-tool-server-forms","timestamp":"2026-08-06T20:35:25.692Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-06T20:35:25.692Z","date":"2026-08-06","title":"Open WebUI shipped an unauthenticated-to-admin terminal proxy and a settings panel that routed tool calls to any URL, and neither had a front-door block","error_message":"No runtime error. GET /api/v1/configs/tool_servers and GET /api/v1/configs/terminal_servers answered 200 on the public chat origin with a tenant-owner admin session, and every route on /api/v1/terminals answered on get_verified_user alone, including the any-verb proxy at /{server_id}/{path:path} and the websocket at /{server_id}/api/terminals/{session_id}.","root_cause":"Two halves that only look like one problem. Server side, routers/terminals.py in v0.10.2 gates nothing on get_admin_user, so a registered connection is usable by any verified user in its group scope, with the credential the connection was registered with attached server side; that is an authenticated request-forgery primitive against the compose network. Caddyfile.owui never listed /api/v1/terminals at all. Client side, the Settings > Integrations panel registers direct tool servers, which the browser calls at the configured URL itself, so no proxy rule on the origin can see that traffic at all. direct_tool_servers:false does not hide the panel because SettingsModal.svelte shows the tab when role is admin OR the permission is set, and every tenant owner is an Open WebUI admin here (#748). v0.10.2 has no ENABLE_ADMIN_PANEL key, so there is no configuration that removes it.","fix":"Caddyfile.owui @blocked gained a terminals arm and configs/(tool_servers|terminal_servers) arms, all segment-exact so /api/v1/configs/banners, /api/v1/configs/models, /api/v1/configs/connections and /api/v1/tools/ stay open. deploy/docker/owui-patches/remove_integrations_tab.py deletes the settings-tab descriptor from the built SPA bundle at image build, which closes the nav button, the settings search keyword route and the showSettings(tools) deep link at once, because all three are driven from that filtered descriptor list. scripts/test_caddy_owui_blocklist.py now also asserts the Dockerfile still runs that patch, so the two halves cannot drift apart in either direction.","prevention":"Match the descriptor, not the guard, when patching a minified bundle: the descriptor is built from source string literals and survives minification, the guard is optional chaining over mangled locals that a rebuild renames. And check what the client does with a non-2xx before adding a block: GET /api/v1/terminals/ is issued on every page load, so blocking it looked like an outage until its caller turned out to return an empty list on any non-2xx, which is why the block costs one console line and nothing else.","tags":["open-webui","caddy","security","ssrf","issue-770","issue-771","issue-748","tool-servers","bundle-patch"]}
{"id":"bug-msi4i4tx-dd6ba3","timestamp":"2026-08-06T23:04:31.220Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-06T23:04:31.220Z","error_message":"scripts/test_caddy_owui_blocklist.py asserted the substring remove_integrations_tab.py appeared somewhere in Dockerfile.open-webui, which the COPY line satisfies on its own","root_cause":"the check pinned a filename rather than an invocation, so neutering the RUN line while leaving the COPY in place kept the test green even though the image patch never executed and the Settings > Integrations panel shipped back","fix":"parse the Dockerfile into logical instructions across backslash continuations and assert separately that a COPY stages the script and a RUN executes it with python3; both mutations now fail the test","tags":["open-webui","testing","dockerfile","test-that-cannot-fail","pr-777"]}
{"id": "bug-2026-08-08-console-credits-unbounded", "date": "2026-08-08", "title": "Console checkout proxy forwarded credits above MAX_SAFE_INTEGER, silently rounded", "error_message": "checkout initiate with credits 9007199254740993 returned 200 and forwarded 9007199254740992", "root_cause": "The route checked credits for type, finiteness, integrality and sign but never for magnitude, and JSON numbers are float64 so anything past Number.MAX_SAFE_INTEGER arrives already rounded. The control-plane had no upper bound either: the per-rail maxima it advertises were never enforced.", "fix": "Bounded credits by Number.MAX_SAFE_INTEGER in the console route, matching the sibling budget proxy, and gave ValidatePurchaseAmount the rail so InitiateCheckout enforces the maximum it advertises.", "tags": ["money", "validation", "float64", "payments", "console"]}
{"id": "bug-2026-08-08-console-proxy-dead-error-branch", "date": "2026-08-08", "title": "Console proxy mapped every upstream refusal to 502 because its error branch was unreachable", "error_message": "A non-owner creating an API key received 502 Failed to create API key, identical to the control-plane being down", "root_cause": "The route branched on ControlPlaneError, but createApiKey, revokeApiKey, getCheckoutRails and initiateCheckout all threw a plain Error from readResponseError, so the branch could never be taken and statusSummary was dead code.", "fix": "Switched those four client functions to throwControlPlaneError, which is what the class exists for and what the sibling proxy routes already rely on.", "tags": ["error-handling", "console", "control-plane-client", "dead-code"]}
{"id":"bug-2026-08-10-workspace-switch-waitforurl-noop","timestamp":"2026-08-10T07:47:55.789Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-10T07:47:55.789Z","date":"2026-08-10","title":"E2E workspace switch helper waited on a URL that never changes, so it never waited at all","error_message":"page.reload: net::ERR_ABORTED; maybe frame was detached? (CI) and expect(locator).toHaveCount failed / waiting for navigation to finish (local); console-budgets toBeDisabled received enabled","root_cause":"tests/e2e/support/workspace-switch.ts awaited page.waitForURL(url => url.pathname.startsWith(\"/console\")) after selecting a workspace. The account-switch handler always 303s back to /console, so the URL is identical before and after the switch, and playwright-core Frame.waitForURL short-circuits to waitForLoadState whenever the current URL already matches the pattern. The wait resolved immediately without observing the navigation, so the next page.reload or page.goto raced the in-flight POST and the switch was lost. The product was correct throughout: the switch persists across a reload and the destination workspace enforces its owner gate.","fix":"Wait for the next page load event, which is per document rather than per URL, and short-circuit when the target workspace is already selected because React does not dispatch onChange for an unchanged select value so no navigation would occur. Raised the two specs to a 120s budget since each drives seven or eight server-rendered navigations.","tags":["e2e","playwright","navigation-race","false-red","web-console","issue-826"]}
{"id":"bug-2026-08-10-control-plane-single-shot-db-boot","timestamp":"2026-08-10T14:41:47.663Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-10T14:41:47.663Z","date":"2026-08-10","title":"Live integration went red on main because control-plane made exactly one attempt to reach a shared, contended pooler","error_message":"dependency failed to start: container hive-control-plane-1 is unhealthy; container log: WARNING: database not available at startup: failed to ping database: FATAL: (EMAXCONNSESSION) max clients reached in session mode - max clients are limited to pool_size: 15","root_cause":"The Supabase session-mode pooler is capped at 15 clients shared by CI, agents and the live deployment, and a push to main starts two full-stack CI jobs within one second of each other, each booting a control-plane with pool_max_conns=6. control-plane called platformdb.Open once, about two seconds into a 145 second healthcheck window, and captured DBReady as a static bool, so losing that one race left the process serving a 503 /health for its whole life with 143 seconds of the window unused.","fix":"Added db.OpenWithRetry, which retries the open for a 75 second budget that fits inside the compose start_period, and returns immediately on the new db.ErrConfig sentinel so a missing DSN or a transaction-mode pooler still fails fast. control-plane boot now uses it, and the redis probe context is created after the open so a slow open cannot expire it.","tags":["ci","control-plane","supabase","connection-pool","startup","healthcheck"]}
{"id":"bug-msnehe52-10e56a","timestamp":"2026-08-10T15:42:43.668Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-10T15:42:43.668Z","title":"Session-mode pooler exhausted by idle pgxpool connections","error_message":"FATAL: (EMAXCONNSESSION) max clients reached in session mode - max clients are limited to pool_size: 15","root_cause":"A Supavisor session slot is held for as long as the pgxpool connection that owns it exists, not for as long as it is used. pgxpool defaults pool_max_conn_idle_time to 30 minutes and pool_health_check_period to 1 minute, so any control-plane that ever burst to pool_max_conns=6 pinned six of the fifteen shared slots for the next half hour while idle. Three mostly-idle consumers (demo box, a developer stack, one CI job) exhausted the ceiling between them. Measured 2026-08-10 with no CI running: six parallel session-mode connections were all refused.","fix":"scripts/derive-pooler-dsn.py now adds pool_max_conn_idle_time=60s and pool_health_check_period=15s to the session DSN, and lists both in PGX_ONLY_PARAMS so the libpq flavour still strips them. Guarded by TestSessionPoolReleasesIdleConnections, which counts live server backends before and after the idle window.","tags":["ci","postgres","supavisor","pgxpool","connection-pool"]}
{"id":"bug-msnf3365-3e6777","timestamp":"2026-08-10T15:59:35.884Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-10T15:59:35.884Z","error_message":"Playwright web-e2e flaked at 7.7 percent: two profile-completion specs passed only on retry","root_cause":"The Supabase fixture reseed runs in test.beforeEach, which shares the test timeout, so a reseed measured between 2.6s and 30.2s left the product a random slice of its declared 30s budget. It also ran through execFileSync, freezing the worker event loop so Playwright timeout timer could not fire, and one test was reported passed at 30194ms under a 30000ms timeout.","fix":"Shared helper tests/e2e/support/fixture-reset.ts runs the reseed as an awaited async child with its own 120s ceiling and adds its measured wall time back via testInfo.setTimeout, so the product always gets the full declared budget. Applied to all nine specs that copy-pasted the hook.","tags":["e2e","playwright","flake","timeout","fixtures","ci"]}
{"id": "bug-746-session-chat-unmetered", "timestamp": "2026-08-11T07:45:44.260709Z", "error_message": "Session-authenticated chat served inference with no charge, no usage event and no recorded verdict; 80 traces across 4 tenants in 5 days all recorded zero tokens and zero credits", "file": "apps/edge-api/internal/chat/dispatch.go", "root_cause": "jwtAwareChatHandler routed a populated auth.UserFrom to chat.Handler, which wrote llm_traces and audit_log only: no hold, no settlement, no verdict. The budget middleware could not cover it either, since it resolves workspace identity from an hk_ bearer alone, so a zero-balance workspace still served. rewriteDispatchBody never set stream_options.include_usage, so the terminal usage frame never arrived and the token counts behind any charge were always zero. The branch dated to 2026-05-18; PR #735 moved all Open WebUI traffic onto it by fixing per-user identity injection, which turned a dormant design into live free serving.", "fix": "chat.Handler now resolves the tenant's billing account and deployment posture in one lookup, refuses an unmapped tenant, an unpriceable alias and an account that cannot pay before any provider call, takes a 10000 credit hold, forces stream_options.include_usage through metering.RewriteBody, and settles exactly once at the served route's catalog price with the real token counts, releasing the hold in full on every failure path. Control-plane carries the metered tokens onto the settlement usage event so console analytics reads real quantities (#856). ENTERPRISE_EDGE tenants stay explicitly non-billable per D-027.", "tags": ["billing", "metering", "edge-api", "chat", "fail-closed", "money-path", "746", "856"], "related_bugs": ["616", "626", "735", "743", "819"], "occurrences": 1, "last_seen": "2026-08-11T07:45:44.260747Z"}
{"id": "bug-855-2cb9c0", "timestamp": "2026-08-11T07:55:24.000Z", "related_bugs": [], "occurrences": 1, "last_seen": "2026-08-11T07:55:24.000Z", "error_message": "Pressing Enter in the deployed chat composer sent nothing: no POST /api/chat/completions, no error, prompt left in the composer, while the send arrow worked with the same text", "root_cause": "Not upstream Open WebUI and not the Hive patch layer. The shared demo account had the per-user preference settings.ui.ctrlEnterToSend set to true (Settings > Interface > Input, Enter Key Behavior). With it on, Enter inserts a newline and only Ctrl+Enter sends, while the send arrow submits the same form either way, which is exactly the reported asymmetry. Nothing in Hive writes that key; an automated sweep over the settings surface of the shared account left it set and nothing checked it afterwards. Both the stock open-webui v0.10.2 image and the Hive patched image sent on Enter when run locally, which ruled out the image before anything was changed.", "fix": "Reset the account's preference to false, and added apps/web-console/scripts/demo-chat-settings.mjs, which mints an audited live session, reads the account's Open WebUI settings, exits 1 on drift from the demo-safe values, and repairs them with --repair. Wired into the DEMO.md pre-demo verification list. Proof of the before and after states is in docs/proof/issue-855-enter-key.", "tags": ["open-webui", "chat", "demo", "settings", "shared-account", "issue-855"]}
{"id":"bug-mson4tep-93c712","timestamp":"2026-08-11T12:32:39.648Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-11T12:32:39.648Z","error_message":"agent task Blocked: engine: start conversation: Post http://agent-server.control/api/conversations: EOF","root_cause":"controlclient.WaitReady treated a dialable Unix socket as readiness. The socat shim inside the SIF creates that socket in the first line of the image runscript, while the Python agent-server behind it needs tens of seconds to bind, so the first request after the wait hit a shim whose TCP target was not listening and got a bare EOF.","fix":"WaitReady now requires a real HTTP response from /health before returning, and the default ControlReadyTimeout went from 30s to 3 minutes to cover a measured cold start. Separately, CreateTask ran the launch on the caller request context, so a closed browser tab cancelled a live launch; the launch and its transitions now run on context.WithoutCancel with their own timeout.","tags":["agent-engine","apptainer","readiness","demo-box","issue-780"]}
{"id":"bug-2026-08-11-owui-hybrid-retrieval-timeout","date":"2026-08-11","title":"Open WebUI knowledge retrieval always timed out at 60s on the demo box","error_message":"POST /api/v1/retrieval/query/collection returned 504 Gateway time-out after 60s for a single-chunk collection, so no knowledge-backed chat answer was ever possible","root_cause":"ENABLE_RAG_HYBRID_SEARCH was persisted true while no reranking model is configured. Open WebUI builds a RerankCompressor for every hybrid query and, with reranking_function None, re-embeds the query and every retrieved document to score them (backend/open_webui/retrieval/utils.py), so one question costs three gateway embedding round trips. Each takes 7 to 17 seconds through the gateway, which exceeds Caddyfile.owui response_header_timeout of 60s. The flag also lives in Open WebUI persisted config, so the compose value only ever seeded on first boot and no later compose edit could move it.","fix":"Default ENABLE_RAG_HYBRID_SEARCH to false in docker-compose.yml behind OWUI_RAG_HYBRID_SEARCH, and reconcile rag.enable_hybrid_search from the environment on every boot in owui-patches/hive_rag_env_config.py. Retrieval then returns 200 in about 32s and a knowledge-backed chat answer arrives in about 29s with its citation.","tags":["owui","rag","retrieval","timeout","persisted-config","demo-box","832"],"issue":832}
{"id":"bug-2026-08-11-native-validation-invisible-to-dom-gates","date":"2026-08-11","title":"A form blocked by HTML5 required validation looks like a dead button to a DOM coverage gate","error_message":"workspace:knowledge/create::button::Create Knowledge -> no effect (mutations=0)","root_cause":"Both fields carry the HTML5 required attribute and the form sets no novalidate, so the browser refuses the submit before any handler runs. The browser paints its validation bubble outside the DOM, so a MutationObserver based gate records zero mutations and zero requests and reports the control as silent when a person actually sees Please fill out this field.","fix":"No code change. Verified live with form.checkValidity() false and validationMessage populated, and captured the bubble in a screenshot. A DOM coverage gate that flags a control as doing nothing must first check checkValidity on the enclosing form before it can call the control dead.","tags":["e2e","coverage-gate","html5-validation","false-positive","owui","832"],"issue":832}
{"id":"bug-msol90k3-c748a6","timestamp":"2026-08-11T11:39:56.306Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-11T11:39:56.306Z","error_message":"Hive chat surface shipped the retired forest-green mark (#0d3b2e on #f8f7f4) and 404d on /static/favicon-dark.png, web-app-manifest-192x192.png, web-app-manifest-512x512.png and site.webmanifest","root_cause":"Two independent causes. First, the owui-static assets were drawn from the superseded hive.scubed.co brand; the current brand lives in a separate repo (scubedcombd/scubed) behind https://scubed.co/hive, so a sweep that checked this repo found nothing wrong. Second, an asset that is ABSENT cannot be found by auditing the assets that are present: every file in owui-static was on-brand while the files the browser actually requested (favicon-dark.png on every dark /auth load, the PWA manifest icons) did not exist at all. Size mismatches had the same shape: manifest.json declared logo.png as 500x500 while the file was 256x256, which is grounds for a browser to reject the icon and substitute its own.","fix":"Regenerated every raster from deploy/docker/owui-static/generate-brand-assets.py using the palette in scubedcombd/scubed src/app/(frontend)/styles.css and the hexagon from hive-hero.tsx. Added the four missing files, rendered each asset at the size its consumer declares, updated the two inline copies of the mark in loader.js and custom.css, and patched the Open WebUI manifest description in Dockerfile.open-webui. Verified by walking a running container with a real browser window: zero 4xx, favicon-dark.png now resolves.","tags":["branding","open-webui","white-label","static-assets","404","pwa","favicon"]}
{"id":"bug-msogokl1-5b8a9b","timestamp":"2026-08-11T09:32:04.020Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-11T09:32:04.020Z","title":"LiteLLM wiring lint attributed the credential to a sibling field, not the recipient","error_message":"apps/edge-api/internal/metering: is handed the LiteLLM address or master key in apps/edge-api/cmd/server/main.go but never calls SelectRoute","root_cause":"tools/lint-litellm-dispatch-requires-routing.mjs walked back from resolveLiteLLMBaseURL() to the nearest preceding package-qualified identifier on any line. Adding Billing: &metering.PGBillingAccountResolver{Pool: dbPool} above LiteLLMURL: inside the same chat.Deps literal moved attribution onto metering, which only ever receives a database pool. Worse than a false positive: it is a false negative on the package actually handed the credential, so the wiring rule can be pointed away from any real recipient by inserting one package-qualified sibling field.","fix":"Attribute to the type of the composite literal or call that lexically encloses the resolver, located with a brace and paren counter walking outward through nested literals. Self-test covers the sibling-field, nested-literal and positional-argument shapes alongside the original rogue case. Verified by injecting a rogue recipient into main.go and confirming the lint still exits non-zero.","tags":["lint","litellm","routing","metering","false-negative","pr-860","issue-746"]}
{"ts":"2026-08-25","error_message":"cannot load module /app/packages/dbtest listed in go.work file: open /app/packages/dbtest/go.mod: no such file or directory","root_cause":"Go Dockerfiles stage go.work member manifests layer by layer and packages/dbtest was added to go.work without adding its COPY pair, so every image bake died at go mod download","fix":"added the same manifest+source COPY pair packages/embedmodel already gets to all five Go Dockerfiles and added packages/dbtest/* to the ci.yml web_e2e path allowlist (PR #1159)","tags":["docker","go-work","ci"]}
{"ts":"2026-08-25","error_message":"TestAccountingConcurrentGetBalanceDuringReserve race: WARNING: DATA RACE read of l.barrier unsynchronized with write l.barrier=nil","root_cause":"deterministic test barrier stored nil under mutex but readers dereferenced it unlocked after the peer agent's 27ca9a1bc rewrite; equality fires at most once so the nil write is unnecessary","fix":"dropped the nil assignment in GetBalance, kept the bounded-wait equality barrier (PR #1159 commit 1ce8b197e)","tags":["race","go-test","concurrency"]}
{"ts":"2026-08-26","error_message":"150 deepseek-v4-pro calls, 30.2M credits burned by CI in one day on uat-test-hive-s-workspace","root_cause":"HIVE_TOOLS_MODEL defaulted to deepseek-v4-pro in ci.yml, deploy-demo-box sdk-replay env, docker-compose sdk-tests default and the js suite fallback because free-pool aliases lack tools capability; owner's free-tier instruction was implemented for the chat lane only","fix":"repointed all four sites to deepseek-v4-flash (tools_supported=true, healthy) and added a ci.yml declare-billing guard that exits 1 if the resolved tools model is deepseek-v4-pro (PR #1200)","tags":["ci","money","free-pool"]}
{"ts":"2026-08-26","error_message":"cache read/write tokens written as zero into api_key_usage_rollups while usage_events carried real counts","root_cause":"finalize HTTP request structs and FinalizeReservationInput had no cache token fields, so the rollup write received literal zeros; sync and stream paths additionally never sent input/output tokens","fix":"plumbed CacheReadTokens/CacheWriteTokens through edge finalize call, both services' input structs, the rollup write and the completed usage_event literal, with red-then-green tests at both surfaces (PR #1204)","tags":["money","accounting","cache"]}
{"id":"bug-mtdt9u40-4efe96","timestamp":"2026-08-29T03:18:45.935Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:45.935Z","date":"2026-08-28","tags":["auth","billing","provisioning","observability"],"error_message":"403 account_not_provisioned on every /v1/* call for a freshly minted API key with no operator-visible signal anywhere but the HTTP response","root_cause":"AuthSnapshot.TenantUUID (apps/edge-api/internal/authz/authz.go) returned ErrAccountNotProvisioned silently; qa-tester@hive.test specifically was a non-owner MEMBER of an old shared E2E fixture tenant (created 2026-04-25, before the tenant_billing_accounts table existed) whose billing mapping was already claimed by a different member's account, and both live provisioning entry points (signup.Provisioner.Reconcile, accounts.Service.EnsureViewerContext) short-circuit on any existing ACTIVE tenant_users row so neither could ever revisit it","fix":"added a log.Printf at TenantUUID's single choke point so every occurrence is operator-visible; live-repaired qa-tester by setting its stale tenant_users row to SUSPENDED and re-running the existing self-serve tenant-provision endpoint, which mapped its own account cleanly; verified with a freshly minted key against api-hive.scubed.co (200, real catalog); the check itself was extracted in review as exported authz.ParseTenantID(TenantLookup), which TenantUUID now delegates to and which the images and audio routing adapters (two further silent call sites found in the same review) also call, and the log line records account_id plus the offending tenant_id and deliberately omits key_id (CodeQL clear-text-logging alert #31)","verified_new_signups_unaffected":"traced signup.Provisioner.Reconcile + accounts.Service.provisionDefaultWorkspace on current main (c177972f4) and confirmed via the live control-plane boot log that both provisioning entry points are wired and running; no commit merged 2026-08-28 touched internal/signup, internal/accounts, or their main.go wiring","pr":1240}
{"id":"bug-2026-08-28-stream-include-usage-not-forced","timestamp":"2026-08-29T03:18:46.026Z","related_bugs":["1215","1220"],"occurrences":1,"last_seen":"2026-08-28","priority":"P1","date":"2026-08-28","error_message":"Direct inference streaming path (/v1/chat/completions, /v1/completions) never forced stream_options.include_usage upstream, so a caller that did not set it itself got no terminal usage frame from a provider requiring it and settlement captured the full reservation hold instead of the exact charge","file":"apps/edge-api/internal/inference/stream.go, apps/edge-api/internal/metering/bodyrewrite.go","root_cause":"metering.RewriteBody was wired into the chat-orchestrator path (internal/chat/dispatch.go) but never into internal/inference's executeStreaming, which dispatched the caller's original body untouched aside from a model-name rewrite in litellm_client.go's dispatch()","fix":"Force metering.RewriteBody before dispatch in executeStreaming, gated per provider via new metering.SupportsIncludeUsage (groq, openrouter allowlist). Strip the resulting usage-bearing frame from the client-visible stream when the caller's own request did not set include_usage, so billing consumes it internally without changing the public contract.","fix_pr":"fix/stream-include-usage-wiring","tags":["billing","streaming","edge-api","metering","fail-closed","include-usage","1226","1220","1215"],"pr":1251}
{"id":"bug-mtdt9u91-17bbe4","timestamp":"2026-08-29T03:18:46.116Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:46.116Z","error_message":"local batch executor's output.jsonl carried a raw upstream response body verbatim into a customer-retrievable file","root_cause":"apps/control-plane/internal/batchstore/executor/dispatcher.go's Dispatch success branch wrote InferencePort's ChatCompletion response body directly into OutputResponse.Body with no sanitization, unlike every sync/stream boundary edge-api normalizes (PR #1222)","fix":"extracted PR #1222's id-mint + frame-sanitize logic into shared module packages/sanitize (edge-api's mint_id.go/upstream_cost.go now delegate to it); dispatcher.go calls sanitize.VariablePriceFrame before constructing each line's OutputLine","tags":["security","provider-blind","batch","sanitization"],"pr":1253}
{"id":"bug-mtdt9uc5-ece0ca","timestamp":"2026-08-29T03:18:46.228Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:46.228Z","ts":"2026-08-28","error_message":"anon and authenticated held ALL privileges on all 82 public-schema tables on the demo box, and anon could EXECUTE the SECURITY DEFINER function public.agent_tasks_list_active()","root_cause":"an early revision of 20260828_01_service_role_public_schema_grant.sql granted ALL on all tables, sequences and functions in public to anon, authenticated and service_role plus matching ALTER DEFAULT PRIVILEGES, and rag-demo-readiness.yml applied it to the live box before review; narrowing the file afterwards did not withdraw the grants already made, and REVOKE FROM PUBLIC is no defence against a direct grant to a named role","fix":"migration now revokes tables, sequences, functions and the matching default privileges from all three API roles before re granting only service_role plus the five documented authenticated tenant-table grants, made idempotent and self-healing; ci-throwaway-db.sh asserts anon holds nothing, authenticated holds nothing outside those five tables, and anon cannot execute agent_tasks_list_active, with a negative control (PR #1257)","tags":["security","database","grants","rls","multi-tenant"],"pr":1257}
{"id":"bug-mtdt9ueq-a339e4","timestamp":"2026-08-29T03:18:46.322Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:46.322Z","ts":"2026-08-28","error_message":"rag-demo-readiness reported 'chunk relayed after finish_reason' against a branch that fixes exactly that leak","root_cause":"the job pointed EDGE_API_URL at the stack's own edge-api container, which serves the last deployed build, so it verified main and never the checkout under review","fix":"the job now starts a second edge-api on the same compose network with the checkout bind-mounted, runtime config copied from the live container, and verifies against that one; live stack untouched (PR #1257)","tags":["ci","verification","substrate"],"pr":1257}
{"id":"bug-mtdt9uh4-37fe58","timestamp":"2026-08-29T03:18:46.408Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:46.408Z","ts":"2026-08-28","error_message":"go build inside the PR edge-api container failed with 'error obtaining VCS status: exit status 128', then after that fix the container exited 126 with 'Permission denied' running the freshly built binary","root_cause":"the runner workspace is a git repo owned by the runner user while the container runs as root, so git refuses it and Go's default VCS stamping fails; separately, docker mounts a tmpfs noexec by default and that tmpfs is where air writes the binary it then executes","fix":"GOFLAGS=-buildvcs=false, and the tmpfs mount now requests exec explicitly; the wait loop also fails fast on 'failed to build' instead of burning the full timeout (PR #1257)","tags":["ci","docker","golang"],"pr":1257}
{"id":"bug-mtdt9uja-108d2d","timestamp":"2026-08-29T03:18:46.486Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:46.486Z","ts":"2026-08-28","error_message":"verify-rag-roundtrip.py reported '3 hits but no marker' on all three attempts against a healthy RAG pipeline","root_cause":"the script never deleted the documents it ingested, so nineteen near-identical fixture notes had accumulated in its throwaway tenant and crowded the newest marker out of a top_k=3 search; the check degraded the more often it ran","fix":"purge_stale_fixtures deletes its own prefixed leftovers older than any live run before ingesting, and the run's own document is deleted in a finally block so a failed run does not poison the next one; parse_created_at unit tested (PR #1257)","tags":["ci","rag","flaky-test"],"pr":1257}
{"id":"bug-mtdt9ulz-bc19f8","timestamp":"2026-08-29T02:30:00Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:46.582Z","error_message":"POST /v1/files answered a blind 500 with the storage error discarded, making every file and batch failure undiagnosable","root_cause":"apps/edge-api/internal/files/handler.go wrote the customer-facing 500 and dropped err on the floor, so nothing in the compose logs said why an upload failed. The underlying cause it was hiding: the CI S3_ENDPOINT secret still points at the Supabase Cloud project this repo left in August, and that hostname no longer resolves","fix":"log the error at the point of failure with bucket, size and content type, customer-facing message unchanged. Endpoint secret tracked as issue #1324","tags":["edge-api","storage","observability","ci"],"pr":1268}
{"id":"bug-mtdt9uoh-e45733","timestamp":"2026-08-29T02:30:00Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:46.673Z","error_message":"usage.prompt_tokens_details was present in one live run and absent in the next on the same alias","root_cause":"normalizeReasoningUsage existed for exactly this and had no caller anywhere in the tree, so the gateway relayed whatever the upstream sent instead of guaranteeing the OpenAI shape","fix":"called from normalizeChatCompletion before the response is marshalled, with a unit test that fails if the call goes missing again","tags":["edge-api","openai-compat","usage"],"pr":1268}
{"id":"bug-mtdt9usw-2315fd","timestamp":"2026-08-29T02:30:00Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:46.832Z","error_message":"live-integration metered 70048 tokens against a 6000 ceiling","root_cause":"the oversized-body probe in typed-exceptions.test.ts sent about 300KB of prompt, and nothing on the chat path rejects a body that size, so all of it was forwarded upstream and metered as prompt tokens. max_tokens bounded only the output","fix":"probe reduced to a tenth of the size, ceiling raised to 20000 with the measurement and the suite growth from 22 to roughly 50 completions written down beside it","tags":["ci","cost","sdk-conformance"],"pr":1268}
{"id":"bug-mtdt9uv1-7e2a42","timestamp":"2026-08-29T03:10:00Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:46.908Z","error_message":"multi-turn tool round trip intermittently returns an empty assistant message on the second turn, after the tool result goes back","root_cause":"deepseek-v4-flash spends most of its ceiling on hidden reasoning (measured live: 22 to 182 reasoning tokens out of 49 to 231 completion tokens at ceiling 256), and when reasoning overruns the ceiling the upstream answers content null with finish_reason length. The existing zero-content retry guard was gated on route.reasoning_reserve_tokens greater than zero, a column that enumerates three free-pool routes and nothing else, so it never ran for this route","fix":"gate the guard on the response shape (empty length completion) rather than the catalogue column, in apps/edge-api/internal/inference/orchestrator.go. The inverted regression test carries the measurement","tags":["edge-api","tool-calling","reasoning","claude-code-compat"],"pr":1268}
{"id":"bug-mtdt9uxw-f822f4","timestamp":"2026-08-29T03:10:00Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:47.011Z","error_message":"both max_tokens conformance assertions were incapable of failing","root_cause":"they asserted usage.completion_tokens against the caller ceiling, and clampUsageToCeiling rewrites that exact field to the caller ceiling on every fixed-price alias so the customer reads what they are billed. Both aliases under test are fixed price, so the assertion measured the clamp rather than the ceiling","fix":"both tests now also bound the visible content length, which the clamp does not touch","tags":["sdk-conformance","tests-that-cannot-fail"],"pr":1268}
{"id":"bug-mtdt9v0a-46ae74","timestamp":"2026-08-29T03:18:47.098Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:47.098Z","ts":"2026-08-28","error_message":"analytics-billing-page-wiring.test.tsx timed out under full-suite load after adding prior-period/cache-sample/top-keys fetches to the overview tab","root_cause":"five independent control-plane fetches were awaited sequentially on every overview page load instead of concurrently, adding real latency in production and pushing one test's wall time past the 5000ms default timeout under parallel CI load","fix":"restructured the analytics overview data-fetching, extracted in review to apps/web-console/lib/analytics/overview-fetch.ts, into five async helper functions (fetchMain, fetchPreviousUsage, fetchCacheSample, fetchPreviousCacheSample, fetchTopKeys), each swallowing its own errors, and run them all through one Promise.all instead of five sequential awaits","tags":["web-console","analytics","performance","test-flake"],"pr":1276}
{"id":"bug-mtdt9v2v-ce782e","timestamp":"2026-08-29T03:18:47.190Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:47.190Z","ts":"2026-08-28","error_message":"analytics overview tiles showed figures the account never measured: an unsampled sparkline bucket drawn as a 0 percent cache hit rate, and a measured zero prior period rendered as No prior data","root_cause":"Sparkline coalesced a null bucket to 0, and derivePeriodDelta merged fetch failure, absent prior figure and measured zero into one branch","fix":"nulls reach Recharts intact with connectNulls off, derivePeriodDelta takes the failure flag as its own argument and returns fromZero for a real move off a measured zero, blended price passes its null through, plus a component test and three page level tests (PR #1276)","tags":["web-console","analytics","ui"],"pr":1276}
{"id":"bug-mtdt9v5a-463b32","timestamp":"2026-08-29T03:18:47.278Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:47.278Z","date":"2026-08-28","error_message":"no runtime error: the console privacy page as first written rendered a blanket no-content-stored claim that is false for /v1/batches, /v1/files and /v1/rag, plus a product-wide provider-blindness claim disproved by catalogue summaries that name vendors (#1284), a metering claim anchored to the console-side UsageEventRow projection rather than the usage_events table, and a 1:1 alias-to-route claim that is a property of seed data rather than of SelectRoute","root_cause":"the console privacy page published four content-storage claims the system does not enforce: a blanket no-content-stored statement that was false for /v1/batches, /v1/files and /v1/rag, a structural claim resting on UsageEventRow (a console-side projection that omits internal_metadata, customer_tags and provider_request_id from the real usage_events table), a product-wide provider-blindness claim disproved by catalogue summaries that name vendors (#1284), and a 1:1 alias-to-route claim that is a property of seed data rather than of SelectRoute, which returns FallbackRouteIDs with no same-provider constraint","fix":"rescoped every claim to what the code enforces, named the three surfaces that do store content with their deletion routes, disclosed the litellm data_collection posture and the upstream-error-text logging exception, added a what-this-page-does-not-cover card, split catalog fetch failure from an empty catalog, and anchored the metering claim with a Go guard over every INSERT INTO public.usage_events column list plus negative page assertions that fail if any corrected sentence returns","tags":["web-console","privacy","compliance","parity","honesty"],"pr":1277}
{"id":"bug-mtdt9v7w-3cc0cd","timestamp":"2026-08-29T03:18:47.371Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:47.371Z","error_message":"deploy-drift-watchdog.yml's grace-window freshness check read main's tip commit date instead of the newest covered commit's date, so an unrelated uncovered commit landing shortly after a genuinely stale covered commit reset the apparent freshness and masked real deploy drift","root_cause":"the freshness step used gh api on steps.resolve.outputs.head_sha (main's tip) rather than determining which commit in the base..head range actually touched a path deploy-demo-box.yml's paths filter covers","fix":"added .github/ci/newest-covered-commit.mjs (reuses pushPaths/isCovered from lint-deploy-paths-filter.mjs) to find the newest covered commit in range; the workflow step now reads that commit's date via local git log instead of head_sha via gh api","tags":["ci","deploy-drift-watchdog","github-actions","freshness-check"],"pr":1281}
{"id":"bug-mtdt9val-4e15b5","timestamp":"2026-08-29T03:18:47.468Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:47.468Z","error_message":"scripts/set-compose-project-name.sh --check silently passed on a real compose-project-name collision when the colliding working directory's path happened to contain this worktree's compose_dir as a substring","root_cause":"the collision filter used grep -v -F (fixed-string substring match) instead of grep -vxF (whole-line match) to remove this worktree's own compose_dir from the docker-reported collision list, so any line merely containing compose_dir as a substring was wrongly treated as a self-match and dropped","fix":"changed grep -v -F to grep -vxF on line 81 of scripts/set-compose-project-name.sh so only an exact line match is removed","tags":["bash","docker-compose","set-compose-project-name","grep"],"pr":1281}
{"id":"bug-mtdt9vf6-c093d2","timestamp":"2026-08-29T03:18:47.634Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:47.634Z","date":"2026-08-28","tags":["security","authz","control-plane","tenant_users"],"error_message":"a demoted workspace owner keeps platform.WorkspaceAdminGate authority indefinitely after a Members-page demotion","root_cause":"public.tenant_users.role was written once at signup/reconciliation and never updated again; accounts.Service.UpdateMemberRole only ever wrote public.account_memberships.role, so the two role stores could diverge permanently and never converge","fix":"accounts.Service.UpdateMemberRole now propagates its role change onto tenant_users via the new signup.SyncTenantMembershipRole (resolved through public.tenant_billing_accounts); a backfill migration reconciles existing demotion drift; a second migration grants hive_app read access to tenant_billing_accounts","pr":1287}
{"id":"bug-mtdt9vhj-2b8ed2","timestamp":"2026-08-29T03:18:47.718Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:47.718Z","date":"2026-08-28","tags":["security","authz","control-plane","tenant_users","privilege-escalation"],"error_message":"the issue #1245 propagation fix could itself write tenant_users.role = OWNER onto a personal tenant's row, granting WorkspaceAdminGate and egress authority the personal-tenant MEMBER hardcode exists to withhold","root_cause":"the sync assumed UpdateMemberRole can never fire for a personal tenant because a single-member account rejects role changes, but Service.CreateInvitation has no account_type guard and 20260727_02 widened account_invitations.role to include owner, so a personal account can hold a second owner who then promotes the original user; the same CASE also flattened the four-value tenant_users role domain onto two values and could promote a non-ACTIVE row","fix":"gate the promotion arm of SyncTenantMembershipRole on tenants.personal_owner_user_id IS NULL and tenant_users.status = ACTIVE, preserve any non-OWNER tenant role with ELSE tu.role, leave the demotion arm unconditional, run the sync on context.WithoutCancel, and drop the same-role early return so a re-issued role repairs a failed propagation","pr":1287}
{"id":"bug-1292-inert-cadence-select","timestamp":"2026-08-29T03:18:47.830Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:47.830Z","date":"2026-08-28","title":"API key credit-limit cadence select had no observable effect and was unprovable by the interaction gate","error_message":"/console/api-keys select|#key-reset-cadence -> unproven: activation produced no request, no navigation, and no change to the rendered output (enabled by filling key-nickname, key-expires, key-credit-limit)","root_cause":"The select was disabled until the credit limit field was non-empty. The interaction gate's disabled branch satisfies the precondition and then re-activates the control with a click, and a click never changes a select's value, so the control was unreachable by the only means the gate has of exercising a select. Independently, changing the cadence altered React state and nothing rendered, so a customer setting a per-month cap got no acknowledgement that the amount they were typing meant per month rather than lifetime.","fix":"Removed the disabled gating and made the cadence drive rendered text: the credit limit field's hint restates what the amount means under the chosen cadence, and a summary line states the bound edge-api will enforce. Pinned by three tests in api-key-create-form.test.tsx.","tags":["web-console","interaction-coverage","api-keys","money-path","ui"],"pr":1292}
{"id":"bug-1292-unenforced-limit-displayed","timestamp":"2026-08-29T03:18:47.936Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:47.936Z","date":"2026-08-28","title":"Console reported a credit limit for API keys that were not capped","error_message":"no runtime error; the keys list rendered a dollar ceiling beside a key whose budget_kind was none","root_cause":"UpdatePolicy upserts budget_limit_credits with COALESCE, so a nil means leave unchanged rather than clear, and a key moved to budget_kind none keeps the ceiling it last carried. Enforcement ignores it (edge-api authz.CheckAccess requires a non-none kind AND a non-nil limit) but the keys list decided capped-or-not from the limit alone, so removing a cap left the old figure on screen next to a key that could spend the whole account balance. Reachable from the browser through the api-keys policy proxy route, which accepts budget_kind none.","fix":"buildKeyView reports no limit when the kind enforces nothing, fixing every consumer of the view at once. Pinned by TestKeyViewHidesALimitThatIsNotEnforced, asserted against buildKeyView directly because the in-package stub repository overwrites the column instead of COALESCEing it and a stub-driven test could not go red.","tags":["control-plane","api-keys","money-path","display-vs-enforcement"],"pr":1292}
{"id":"bug-1292-secret-lost-on-policy-failure","timestamp":"2026-08-29T03:18:48.061Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:48.061Z","date":"2026-08-28","title":"A network failure applying an API key credit cap discarded the key's one-time secret","error_message":"Failed to create key. Please try again.","root_cause":"The cap is applied in a second request after the key already exists, and that request sat inside the same try block as key creation. A transport failure threw into the outer catch, which sets the generic create-failed error and never renders the secret panel, so the customer was told nothing had been created while holding a real, live, uncapped key whose only copy of the secret had just been discarded.","fix":"The policy call has its own try block; a refusal and a transport failure both produce the same warning, which now says the key is live and uncapped, and the secret panel still renders. Pinned by a test that rejects the second fetch and asserts the secret is still shown.","tags":["web-console","api-keys","money-path","error-handling","data-loss"],"pr":1292}
{"id":"bug-2026-08-29-reqlog-latency-test-integrity","timestamp":"2026-08-29T03:18:48.164Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:48.164Z","date":"2026-08-29","title":"Latency column tests named guards they never asserted, and nothing crossed the wire seam","error_message":"Test named \"renders a formatted latency column and an em-dash for an unmeasured request\" asserted only getByText(\"1.8s\"); mutating the cell to formatLatencyMs(row.latency_ms ?? 0) left the suite 32/32 green. Separately, no test carried latency_ms across the JSON boundary: deleting the client decoder line left the suite 17/17 green and http_test.go asserted nothing about the key.","root_cause":"Two coverage shapes that cannot fail. The em-dash case had a fixture row but no assertion on it, and the three other columns that also render an em-dash would have made a naive text query pass anyway. The wire seam had a wiring test whose fixture omitted the new field entirely, so the only test running the real decoder never exercised the line under test.","fix":"Resolve the Latency cell by its header position and assert both the formatted value and the em-dash. Put latency_ms on the wiring test's mocked response body and assert the rendered cell. Add TestListEventsLatencyCrossesTheWire asserting the JSON key is emitted when measured and omitted when not. Verify each by mutation rather than by reading.","tags":["testing","test-integrity","mutation-testing","web-console","control-plane","usage-events","pr-1293"],"pr":1293}
{"id":"bug-mtdt9vzm-4cdfd0","timestamp":"2026-08-29T03:18:48.369Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:48.369Z","ts":"2026-08-28","error_message":"demo walkthrough harness: every wait-up-to-N-ms visibility check timed out identically regardless of timeout value (8s vs 40s made no difference), including chat sign-in itself","root_cause":"Playwright locator.isVisible({timeout}) does not poll: the timeout option is deprecated and ignored per playwright-core type declarations, so it is one immediate check that returns false whenever called before the element exists","fix":"added a waitVisible(locator, timeoutMs) helper using locator.waitFor({state:\"visible\", timeout}) and replaced all isVisible({timeout}) call sites in apps/web-console/tests/e2e/demo-walkthrough.mjs","tags":["playwright","e2e","harness"],"pr":1294}
{"id":"bug-mtdt9w2p-ef0692","timestamp":"2026-08-29T03:18:48.481Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:48.481Z","ts":"2026-08-28","error_message":"CodeQL js/incomplete-sanitization (high) on demo-walkthrough.mjs report writer: escaping only the pipe character left the backslash unescaped","root_cause":"the markdown table cell escaper ran .replace(/\\|/g, \"\\\\|\") so an input already containing a backslash produced a literal backslash followed by a live cell separator, and a newline in an error message ended the table row outright","fix":"extracted mdTableCell into apps/web-console/tests/e2e/support/md-table.mjs, escaping backslash and pipe in one pass, flattening line separators, and dropping an orphan backslash left by truncation; applied to every cell, with unit tests in tests/unit/md-table-cell.test.ts","tags":["codeql","sanitization","markdown","e2e","harness"],"pr":1294}
{"id":"bug-mtdt9w67-a34057","timestamp":"2026-08-29T03:18:48.607Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:48.607Z","ts":"2026-08-28","error_message":"latent credential leak: the walkthrough screenshotted the console right after minting an API key, and the console renders a newly created key in full exactly once, so the committed PNG would have carried a live key into a public repository","root_cause":"the capture happened with no masking pass, and the proof linter reads text files only, so nothing in CI, GitHub secret scanning or GitGuardian can inspect a credential burned into pixels; it never fired only because every create call in the evidence set failed","fix":"added maskLiveApiKeys (apps/web-console/tests/e2e/support/mask-api-keys.mjs, jsdom tested) sweeping every text node before the shutter and again after it with a retake on a late paint, registered the minted key as a run secret, filled the form's own expiry field, and taught tools/lint-no-token-in-proof-captures.mjs to catch a bare hk_ key","tags":["security","credential-leak","proof-capture","e2e","harness"],"pr":1294}
{"id":"bug-2026-08-28-anthropic-sdk-wire-conformance","timestamp":"2026-08-29T03:18:48.731Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:48.731Z","date":"2026-08-28","title":"Anthropic surface broke the real SDK four ways: content_block_start dropped text, empty completions serialized content null, count_tokens 401d API keys, /v1/models rejected x-api-key and answered OpenAI-shaped","error_message":"TypeError: unsupported operand type(s) for +=: 'NoneType' and 'str' in anthropic/lib/streaming/_messages.py accumulate_event; TypeError: 'NoneType' object is not iterable on msg.content; anthropic.AuthenticationError 401 {\"type\":\"error\",\"error\":{\"type\":\"authentication_error\",\"message\":\"missing user\"}} on count_tokens; anthropic.AuthenticationError 401 invalid_api_key on models.list()","root_cause":"Two families. (1) Go zero-value JSON encoding dropped values the Anthropic wire contract requires to be explicitly present: omitempty on StreamContentBlock.Text dropped the required \"text\":\"\" from every text content_block_start, and a nil []ResponseBlock in FromOAIResponse marshaled to null instead of []. Neither is visible to a Go-side assertion, since len() cannot distinguish nil from empty and a \"\" field looks correct until encoding/json runs. (2) The Anthropic surface's auth model only ever covered POST /v1/messages: count_tokens is the only route that does not delegate to the chat chain so it never gained an API-key authority and recognised session principals only, and GET /v1/models never got the leaf APIKeyNormalizer /v1/messages carries, which matters because the global one in authSelectorMiddleware only exists when jwtMW is non-nil.","fix":"StreamContentBlock.Text became *string and gained Input json.RawMessage set to {} on tool_use starts, matching the live streaming spec. FromOAIResponse seeds Content with an empty non-nil slice at construction so both exits are covered. anthropic.Deps.AuthorizeAPIKey added and count_tokens accepts either principal, fail-closed when nil, refusals reshaped from the shared WriteAuthFailure mapping. GET /v1/models registered through modelsHandler applying APIKeyNormalizer at the leaf plus a ModelsCompat wrapper that answers Anthropic-shaped callers with the Anthropic list and error envelopes while leaving OpenAI callers byte-identical.","tags":["anthropic","sdk-conformance","edge-api","json-encoding","omitempty","auth","streaming","provider-blind"],"issues":[1274,1260,1261,1259],"pr":"fix/anthropic-stream-content-block-start","discovered_by":"PR #1278's Anthropic Messages conformance suite, which caught the content_block_start case live against the real SDK and shipped it marked xfail against issue #1274; that PR's own buglog entry is folded in here rather than duplicated"}
{"id":"BUG-1284","timestamp":"2026-08-29T03:18:48.847Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:48.847Z","date":"2026-08-28","title":"GET /v1/models leaked upstream provider identity in hive-stt and hive-tts descriptions","error_message":"models.list() returned description \"Serverless speech-to-text (Groq Whisper) for /v1/audio/transcriptions.\" and \"Serverless text-to-speech (Groq PlayAI) for /v1/audio/speech.\" live against api-hive.scubed.co","root_cause":"public.model_aliases.summary was seeded with the provider name by 20260717_02_voice_groq_stt_tts.sql and is published verbatim as the description field of GET /v1/models and as summary on both catalogue endpoints. Every provider-blindness guard in the repo sat on an error path, so catalogue metadata was never examined. The hive-tts text was additionally stale: Groq deprecated playai-tts on 2025-12-31 and the route has pointed at canopylabs/orpheus-v1-english since that same migration.","fix":"Boundary guard in both modules: redactAlias in control-plane's buildCatalogSnapshot and buildPublicCatalogModels, redactSnapshot in edge-api's catalog client fetchSnapshot, scrubbing display name, summary, owned_by and capability badges. Vocabulary is serving-provider identity only, not model vendors, so shipped names such as Deepseek V4 Pro survive. Migration 20260828_01 repairs the two seeded rows behind a still-leaking predicate and raises a NOTICE for any other customer-facing row that names a provider.","tags":["provider-blindness","catalog","models-endpoint","security","issue-1284"],"pr":1300}
{"id":"BUG-2026-08-28-preauth-body-read","timestamp":"2026-08-29T03:18:49.050Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:49.050Z","date":"2026-08-28","title":"edge-api buffers request bodies before validating the credential on five routes with no backpressure","error_message":"no error surfaced: an unauthenticated caller sending Authorization: Bearer hk_x reaches io.ReadAll on /v1/chat/completions, /v1/completions, /v1/responses, /v1/embeddings and /v1/messages, at a measured 20.8 to 30.4 MiB live heap per request, with no rate limit, no concurrency limiter, no ingress proxy, no mem_limit and no GOMEMLIMIT in front of it","root_cause":"auth.Selector routes on the hk_ prefix alone and validates nothing, budgetGate passes through on a failed resolve, and Authorize (which carries the rate limiter) runs inside executeSync/executeStreaming, after the handler has already read and buffered the body. The alias needed by Authorize is only known after the body is parsed, which is why the check ended up downstream of the read.","fix":"Mitigated, not fixed, in PR #1301: a 60s read deadline armed around each pre-auth body read via internal/httpx.ReadBody and cleared only on a successful read (clearing it on the error path leaves net/http's post-handler body drain unbounded), one shared reader for the inference, anthropic and chat surfaces, http.MaxBytesReader handed the unwrapped *response so its close-after-reply half is not inert behind middleware, mem_limit 1536m plus a non-overridable GOMEMLIMIT 1280MiB with a boot-time cgroup comparison, files.ParseMultipartForm's maxMemory cut from 512 MiB to 32 MiB so one upload cannot pin half the container's memory limit, and Unwrap methods on statusRecorder and responseRecorder so http.ResponseController can reach the connection at all. The ordering fix, validating the credential before the read, is tracked in issue #1299.","tags":["security","availability","edge-api","dos","auth","memory","issue-1299"],"pr":1301}
{"id":"BUG-1291","timestamp":"2026-08-29T03:18:49.155Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:49.155Z","date":"2026-08-28","title":"Upstream error prose reached customers on the RAG chat relay and through the pre-stream sanitizer's forward-by-default tail","error_message":"You exceeded your current quota, please check your plan and billing details","root_cause":"Three defects. (1) /v1/rag/chat's streamGroundedChat sanitized by hand, rewriting model and id and deleting system_fingerprint while forwarding every other key of a parseable frame, so a top-level error object delivered inside a committed 200 reached the customer whole, provider brand and top-up URL included. (2) sanitizeProviderBlindMessage forwarded any upstream message no rule recognized, which is a deny list exactly as wide as its literals: DeepSeek's 402 'Insufficient Balance', 'monthly spend limit', 'past due ... payment information', 'Free tier limit reached' and 'run out of prepaid funds' all passed through verbatim, as did the reported quota sentence on any status other than 429. (3) packages/sanitize keyed its error check on the KEY being present, so an upstream that declares error on every chunk and leaves it null had every content frame refused: silent truncation on both SSE relays and needless retry-to-exhaustion in the batch executor. The session-chat relay did NOT leak an error frame: it already dropped one, which truncated the answer silently instead.","fix":"Routed the RAG relay through the shared packages/sanitize allowlist instead of its hand-rolled deny list. Inverted the tail of sanitizeProviderBlindMessage to forward an upstream message only when it is customer-actionable on a request-shaped 4xx status, and deleted the money-vocabulary deny list that had been added first. Keyed the frame error check on the VALUE, treating null, empty string, {}, [] and false as absent. Added sanitize.ReplaceErrorFrame as a separate opt-in call so both SSE relays render a gateway-owned error frame instead of truncating, while VariablePriceFrame's ok==false contract stays intact for the batch dispatcher, which would otherwise store and bill a synthetic error line.","tags":["provider-blind","chat","rag","streaming","sanitization","edge-api","control-plane","security","money"],"files":["packages/sanitize/sanitize.go","apps/edge-api/internal/errors/provider_blind.go","apps/edge-api/internal/chat/dispatch.go","apps/edge-api/internal/rag/chat_handler.go"],"related":["#744","#928","#965","#972","#1253","#1306","#1320","#1327"],"pr":1303}
{"id":"bug-2026-08-28-max-tokens-not-enforced","timestamp":"2026-08-29T03:18:49.264Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:49.264Z","date":"2026-08-28","title":"max_tokens silently inflated by the reasoning reserve, billing 206x the caller's ceiling; n>1 silently truncated to one choice","error_message":"max_tokens: 8 on hive-free returned usage.completion_tokens 1650, all billed; n: 2 returned HTTP 200 with choices.length 1 and no error","root_cause":"applyReasoningHeadroom (PR #1225, issue #1171) inflated every completion-limit field the caller had set by provider_routes.reasoning_reserve_tokens (4096 on three of four hive-free members) before dispatch, so max_tokens 8 went upstream as 4104. Its premise that hidden reasoning would burn the reserve while visible content stayed inside the caller's budget is unenforceable: nothing upstream separates the two, so the model spent the inflated ceiling on visible output. Because the rewrite skipped a ceiling the caller had NOT set, its entire effect was overriding ceilings callers HAD set. Separately, ChatCompletionRequest.N and CompletionRequest.N were declared and read by nothing, so n was forwarded, ignored upstream, and one choice returned with no error. Two hold-capture branches (sync zero-content, streaming missing-usage) also charged the flat DefaultHoldText authorization floor regardless of the caller's ceiling, a far larger breach of the same invariant.","fix":"Deleted applyReasoningHeadroom and its three call sites so the caller's ceiling reaches the provider unchanged. Added completion_ceiling.go: requestedCompletionCeiling reads the ceiling from the caller's original body before any outbound rewrite, clampUsageToCeiling caps usage.CompletionTokens at it on the single usage object both the response and the charge derive from, rewriteNormalizedUsage keeps the response body in step, and capCaptureAtCeiling bounds both hold-capture branches without ever reaching zero. Refused n other than 1 with 400 unsupported_parameter and param n on both chat and legacy completions.","tags":["billing","money-path","overcharge","max_tokens","openai-conformance","issue-1283","issue-1171","edge-api","inference","free-pool"],"pr":1305}
{"id":"bug-2026-08-28-completion-ceiling-review-defects","timestamp":"2026-08-29T03:18:49.447Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:49.447Z","date":"2026-08-28","title":"Completion-ceiling fix shipped a caller-triggerable revenue bypass, an unbounded content estimate, and a usage clamp on aliases it could not bill","error_message":"Request {max_tokens: 1, max_completion_tokens: 100000} was dispatched unchanged and settled at output_tokens 1, actual_credits 24 where the unclamped generation was worth 6620; a synchronous 200 carrying content and no usage block against max_tokens 8 charged 1323 credits versus 32 ceiling-priced; on an upstream_actual alias the caller read completion_tokens 8 while paying the upstream cost of 1650","root_cause":"Three separate causes in one change. First, requestedCompletionCeiling takes the smaller of the two chat spellings but nothing rewrote the outbound body, so after applyReasoningHeadroom was deleted both fields were forwarded verbatim and the request boundary enforced a different number than settlement; OpenAI and Groq both prefer max_completion_tokens, so the larger field governed generation while the smaller governed billing. Second, clampUsageToCeiling returns early when usage is nil, which is the same condition that routes settlement into the content-length estimate in settlementCredits, leaving the one path that guesses the completion count as the one path with no ceiling on it; the streaming path was masked because settleStream overwrites the estimate with a bounded hold capture. Third, clampUsageToCeiling was called unconditionally, but an upstream_actual alias settles from respBody or acc.RawUsageChunk, which the clamp never reads, so it lowered the number the caller reads without lowering the charge.","fix":"Added pinCompletionCeiling, which writes the settled minimum back over every ceiling field present in the outbound body before dispatch and only ever narrows, wired on executeSync, executeStreaming and executeResponsesStreaming ahead of EnforceVariablePriceBounds. Added a ceiling parameter to settlementCredits that bounds the content-length completion estimate, with ChatSettlementCredits reading the ceiling from the request bytes it already parses so the session chat surface is covered too. Gated clampUsageToCeiling on route.Pricing.IsUpstreamActual, the same test capCaptureAtCeiling applies. Refused best_of other than 1 on legacy completions. Qualified the support-matrix sentence for the variable-price case and documented that non-positive and non-integer ceiling spellings read as no ceiling.","tags":["billing","money-path","revenue-bypass","overcharge","max_tokens","max_completion_tokens","best_of","upstream-actual","issue-1283","pr-1305","edge-api","inference","code-review"],"pr":1305}
{"id":"bug-2026-08-28-completion-ceiling-second-stream-defects","timestamp":"2026-08-29T03:18:49.584Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:49.584Z","date":"2026-08-28","title":"Ceiling pinning kept unreadable fields, the variable-price bound invented a larger ceiling than the caller set, and hold captures priced the prompt at zero","error_message":"Request {max_tokens: 1, max_completion_tokens: 100000.5} was dispatched with the float intact; a variable-price request sending only {max_tokens: 1} was dispatched with max_completion_tokens 16384 written in beside it; a streaming request with a prompt of roughly 11334 tokens and max_tokens 8 whose upstream sent no usage block settled at 32 credits rather than the price of the prompt","root_cause":"Three causes in the fix for the first adversarial stream. First, pinCompletionCeiling skipped any present ceiling field that failed to unmarshal into int64, on a never-widen rule that does not hold for a value the gateway cannot read at all, since the provider may parse what encoding/json refuses and OpenAI treats max_completion_tokens as authoritative. Second, clampCompletionLimit sets every field the endpoint speaks to VariablePriceMaxCompletionTokens when absent, with no reference to a smaller ceiling already present, and an upstream_actual alias has no settlement clamp behind that. Third, capCaptureAtCeiling computes its bound as the catalog price of the whole request at the ceiling, but both callers pass the metered input count, which is zero exactly because those branches are reached when no usable usage block arrived, so the prompt was priced at nothing and the capture collapsed to the output side alone.","fix":"pinCompletionCeiling now keeps a field only when it reads as a positive integer at or below the ceiling and overwrites everything else present, logging the raw literal truncated. clampCompletionLimit derives an effective limit as the lower of VariablePriceMaxCompletionTokens and any smaller positive ceiling already in the body, and uses it both to fill absent fields and to narrow present ones. Added captureInputTokens, which falls back to the content-length prompt estimate settlementCredits already uses when the upstream reported no usage, and wired it into both capCaptureAtCeiling call sites. Added a byte-scan early-out to pinCompletionCeiling so an ordinary single-ceiling chat request pays no extra decode.","tags":["billing","money-path","revenue-bypass","free-serve","max_tokens","max_completion_tokens","upstream-actual","hold-capture","issue-1283","pr-1305","edge-api","inference","code-review"],"pr":1305}
{"id":"bug-2026-08-28-citation-hook-multiedit-blind","timestamp":"2026-08-29T03:18:49.755Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:49.755Z","date":"2026-08-28","title":"decision-citation-check hook exited 0 on every Claude Code MultiEdit","error_message":"No output, exit 0, on a MultiEdit payload citing a fabricated D-999; the settings matcher invoked the hook and it declined to look","root_cause":"The payload reader handled only Cursor's flat data.edits array. Claude Code nests a MultiEdit's edits under tool_input.edits, so ti.content, ti.new_string and the flat editsContent were all empty, content resolved to empty string, and the guard exited before auditing a single citation. The self-check could not see it because its claude payload shape was only ever tool_name Write with tool_input.content, so the MultiEdit shape was never exercised. Same defect as issue #1333 in secrets-scanner.js, inherited by copying the pattern.","fix":"Adopt the payload handling that fixed #1333 in secrets-scanner.js: read the edits array from either harness position, serialize any field that is not a string instead of coercing it, and audit every source rather than letting the first truthy one win. Run the decision-citation self-check cases over all three write shapes via the shared edit() helper, plus explicit payloads for content-and-edits together, a non-string new_string, and a fabrication in a later edit. Mutation tested: reverting the edits-array read turns nine cases red.","tags":["hooks","claude-code","payload-shape","fail-open","citation-check","pr-1313"],"related":["#1333"],"pr":1313}
{"id":"bug-mtdt9x8l-387c1b","timestamp":"2026-08-29T03:18:49.988Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:49.988Z","date":"2026-08-29","tags":["auth","billing","provisioning","api-keys","console"],"error_message":"The console minted an API key (201, secret shown in the copy-it-now panel, key listed active) for an account with zero public.tenant_billing_accounts rows, and edge-api then rejected that key with 403 account_not_provisioned and an unactionable contact-support message","root_cause":"apikeys.Service.CreateKey and RotateKey generated and persisted a secret without ever resolving the account's billing tenant, even though the exact lookup (Repository.GetTenantIDByAccountID) already existed in the same package for ResolveSnapshot; the unprovisioned state is permanent for any user whose tenant is already billed by a different workspace, since tenant_billing_accounts is UNIQUE on both columns and signup.EnsureTenantBillingAccount refuses to guess a mapping","fix":"added Service.requireBillingTenant as a precondition on both mints, returning a new ErrAccountNotProvisioned sentinel mapped to 409 with an actionable message and the machine code account_not_provisioned; the console proxy route now maps that code to its own customer-facing wording (it never forwards upstream error text) and ApiKeyCreateForm renders a 409 body verbatim instead of replacing it with generic retry text; ResolveSnapshot deliberately still tolerates uuid.Nil so pre-existing keys keep failing closed at the boundary","verification":"four new Go tests plus a mutation check that forces the guard false and confirms all four go red; two new console unit tests; visual proof captured against a branch-built control-plane and console over a throwaway Supabase with all 112 migrations, showing the refusal, zero api_keys rows written, and a positive control where a mapped workspace still issues a key","pr":1335}
{"id":"bug-mtdt9xel-580d70","timestamp":"2026-08-29T03:18:50.204Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T03:18:50.204Z","date":"2026-08-28","title":"Secrets scanner hook was blind to every MultiEdit and reported clean","error_message":"No output, exit 0, on a MultiEdit payload containing a hardcoded credential. Indistinguishable from a clean scan.","root_cause":"secrets-scanner.js resolved its edit array from the top-level data.edits field, which is Cursor's file-edit payload shape. Claude Code nests the array at tool_input.edits. On a MultiEdit all three content fallbacks (ti.content, ti.new_string, top-level data.edits) were empty, so content became the empty string and the early-return guard exited 0 with no output. The hooks.selfcheck.js harness fed both harness shapes but used Write for the Claude Code case, whose text lives in tool_input.content, so the nested-array path was never exercised; the harness was also not invoked by CI or any npm script, so even a covering case would not have run.","fix":"Consult both shapes, preferring tool_input.edits over data.edits. Add a claude-multiedit shape to hooks.selfcheck.js and run every secrets-scanner case under all three shapes. Wire hooks.selfcheck.js into the Repo policy lints required CI check so the harness actually runs.","tags":["hooks","secrets","claude-code","payload-shape","silent-failure","ci-coverage","issue-1333"],"pr":1337}
{"id":"sso-wave1-manifest-entry-outside-specs","date":"2026-08-29","title":"Playwright spec manifest entry landed outside the specs object","error_message":"scripts/verify-spec-collection.mjs would report UNDECLARED tests/e2e/sso-silent-consent.spec.ts runs but is not in the manifest","root_cause":"The manifest was rewritten with a JSON serializer that reformatted every array and appended the new spec as a key of the root object rather than of specs, and the guard reads document.specs only. The branch was never re-run against the guard after that edit because the pull request was already conflicting, and a conflicting pull request builds no refs/pull/N/merge, so GitHub created no pull_request run and the page showed only CodeQL, GitGuardian and CodeRabbit, which reads green at a glance.","fix":"Resolve the rebase conflict onto main's compact formatting and add the single line inside specs, then run npm run e2e:verify-collection to confirm 42 files collect into the pinned projects.","tags":["playwright","ci","manifest","rebase","web-console"]}
{"id":"sso-consent-redirect-guard-slash-loop","date":"2026-08-29","title":"OAuth consent landing redirect guard missed repeated slashes","error_message":"ERR_TOO_MANY_REDIRECTS between /oauth/consent and itself for an auto-approve target spelled //oauth/consent","root_cause":"isSafeRedirectTarget compared parsed.pathname with trailing slashes stripped and nothing else, so //oauth/consent and /oauth//consent matched neither the landing path nor its stripped form and were allowed. Next.js then 308-normalized the repeated slashes away and re-entered the landing, which asked GoTrue again and received the same redirect_url. The guard's comment reasoned about Next normalizing a trailing slash into the guarded path and stopped there, which is why it read as thorough and still missed the sibling case.","fix":"Collapse repeated slashes and decode percent escapes inside a try before comparing, and add a unit case that fails against the previous guard. Measured the real routing behaviour against a production console build rather than assuming it: encoded and case variants answer 404, repeated and trailing slashes answer 308.","tags":["oauth","redirect","auth","web-console","security-review"]}
{"date":"2026-08-28","error_message":"n/a","root_cause":"console had no privacy/data-policy surface at all (parity re-score, criterion weight 9, credit 0.25)","fix":"added /console/privacy grounded in verified backend state (UsageEventRow schema, routing.SelectionInput.AllowedProviders dead code path, provider-blind error handling), never rendering a claim or control that is not enforced; corrected mid-review to drop literal OpenRouter/Groq vendor names per the console-wide provider-blind convention, keeping the third-party-routing disclosure itself","tags":["web-console","privacy","compliance","parity"]}
{"timestamp":"2026-08-28T23:20:00Z","error_message":"Anthropic Messages request carrying top_k fails with 400 and the customer-facing message hive-free is not available.","root_cause":"edge-api forwards four non-OpenAI fields verbatim from the Anthropic surface (top_k, thinking, cache_control, session_id) because the OpenAI chat-completions shape has no equivalent. hive-free load balances across providers with different tolerances, so the request succeeded or hard-failed depending on which pool member answered, and the refusal reached the caller as model unavailability rather than as a refused field","fix":"dispatchWithRetry in apps/edge-api/internal/inference/retry.go now classifies a 400 that names one of those four fields, strips that single field, and retries. Bounded to one strip per field and to attempts that have a successor; a field the caller actually sent is never stripped. Known edge filed as issue #1323","tags":["anthropic","edge-api","free-pool","sdk-conformance"]}
{"date":"2026-08-29","title":"Pre-merge frontend gate compiled lib/hive only, so a Hive component added under lib/components was compiled and rendered by nothing","error_message":"make test-owui-frontend reported 16 files, 208 tests, 13/13 components compiled, exit 0 on a tree carrying a Svelte parse error, two transposed money figures and an inert settings tab","root_cause":"scripts/test-owui-hive-frontend.sh copies upstream components into its scratch tree as text fixtures and runs the svelte compile pass over lib/hive only, so a Hive authored component placed under lib/components/chat/Settings was read as text and never compiled, and no test rendered it; the image build that would have caught the parse error runs after merge in deploy-demo-box.yml","fix":"moved the component to vendor/open-webui/src/lib/hive/SettingsUsage.svelte where the compile guard already looks, taught the gate to install the lockfile pinned vite svelte plugin and write a vitest config so tests can import and server side render components, and re-ran the three mutations to confirm each one turns the suite red","tags":["ci","test-gate","open-webui","svelte","frontend","money-surface"]}
{"id":"bug-1329-anthropic-usage-zero","date":"2026-08-29","title":"/v1/messages reported usage input_tokens 0 and output_tokens 0 while billing charged correctly","error_message":"usage: {input_tokens: 0, output_tokens: 0} on every POST /v1/messages response","root_cause":"The post-finish chunk suppression in apps/edge-api/internal/inference/mint_id.go recognised a terminal usage frame as usage != nil AND len(choices) == 0. LiteLLM v1.98.0 sends that frame with one empty-delta choice, so the relay dropped the only usage frame on the stream. The Anthropic surface folds that same relay, so it reported zeros; settlement reads the accumulator, which sees the frame earlier, so billing stayed correct and hid the defect.","fix":"Key the suppression on whether a post-finish frame carries renderable payload rather than on the choice count, re-emit a forwarded terminal usage frame with an empty choices array, move the synthesized fallback usage frame ahead of the single [DONE] sentinel, and report input_tokens on the Anthropic message_delta.","tags":["anthropic","streaming","usage","litellm","sse","issue-1329"]}
{"date":"2026-08-29","area":"web-console","error_message":"Sign-up shows Something went wrong on our end. Reference AUTH-... after POST /auth/v1/signup returns a bare 404","root_cause":"The console shipped a sign-up route on deployments that refuse account creation at the gateway (Caddyfile.supabase 404) and at the GoTrue flag. The empty 404 body makes auth-js raise AuthUnknownError with no status, which the allow-list in lib/auth/auth-error.ts correctly withheld, so a deliberate policy rendered as an outage","fix":"Gate the sign-up route and its cross-links on NEXT_PUBLIC_DISABLE_SELF_SERVE_SIGNUP, fed from ENTERPRISE_DISABLE_SIGNUP so one variable drives GoTrue and the UI, and add toUserFacingSignUpMessage to report a 404, a 403, GoTrue signups-not-allowed copy, or the status-free AuthUnknownError shape as a policy refusal","tags":["web-console","auth","signup","issue-1328"]}
{"date":"2026-08-29","area":"web-console","error_message":"Rotate on /console/api-keys opens This page could not be found","root_cause":"api-key-list.tsx linked every active key to /console/api-keys/[id]/rotate, a route that was never built; the page header also promised rotation. The rotateApiKey client helper was dead and decoded the wrong response shape","fix":"Remove the link and the promise, name the working path (create a replacement, revoke the old) in the header, and delete the broken helper","tags":["web-console","api-keys","issue-1331"]}
{"date":"2026-08-29","area":"web-console","error_message":"Available credits renders 99,996,364,207 with no unit while the API keys table renders the same quantity as $0.000662","root_cause":"Two surfaces rendered the same credit quantity in different denominations, with no conversion shown on either","fix":"One CreditBalance component on the dashboard and the billing page, dollars as the headline with the credit figure and the conversion beneath, and a truncating formatUsdBalanceFromCredits so a balance never rounds up","tags":["web-console","billing","credits","issue-1332"]}
{"date":"2026-08-29","area":"web-console","error_message":"A negative credit balance displayed less debt than the account carried","root_cause":"formatUsdBalanceFromCredits rounded with Math.trunc, which rounds toward zero, so an available balance driven negative by reservations was flattered by one displayed unit","fix":"Round with Math.floor, identical for positive balances and conservative for negative ones, and state the direction in the docstring","tags":["web-console","billing","credits","issue-1332"]}
{"date":"2026-08-29","area":"chat","error_message":"The chat composer credit banner displayed more money than the account held, and its text disagreed with its own low-credit badge at the threshold","root_cause":"vendor/open-webui/src/lib/hive/credits.ts rendered the balance with formatUsdFromCredits, a port of the console PRICE formatter, which rounds to nearest; 9,996,364,207 credits rendered as $10.00 and 499,999,999 rendered as the $0.50 threshold while creditState called the same balance low","fix":"Port the console balance formatter as formatUsdBalanceFromCredits, rounding down in credit space, and move the two balance call sites onto it while today's spend keeps the price formatter","tags":["chat","open-webui","billing","credits","issue-1345"]}
{"id":"bug-2026-08-29-analytics-null-group-key","date":"2026-08-29","title":"Analytics summaries returned 500 when a grouped column was NULL","error_message":"usage: scan error summary row: cannot scan into dest[0] (col: group_key): cannot scan NULL into *string","root_cause":"GetErrorSummary, GetUsageSummary and GetSpendSummary scanned group_key into a non-nullable string. usage_events.api_key_id is nullable (NULL for chat traffic with no API key, for errors recorded before a key is resolved, and for keys deleted under ON DELETE SET NULL), so one unattributable row failed the scan for the entire result set and the console overview card rendered its error state on the first page after sign-in.","fix":"Scan group_key into a nullable destination in all three summaries and map NULL to an explicit unattributed bucket rather than dropping the row. The console Top API keys widget no longer labels that bucket Deleted key. Pinned by TestSummariesByAPIKey_NullKeyGroupsAsUnattributed, TestUnattributedGroupKeyMatchesTheConsole and a console unit case, each proven red by its own mutation.","tags":["control-plane","usage","analytics","postgres","null-scan","console-overview","web-console"],"files":["apps/control-plane/internal/usage/repository.go","apps/control-plane/internal/usage/repository_live_test.go","apps/control-plane/internal/usage/wire_contract_test.go","apps/web-console/lib/analytics/cache-metrics.ts","apps/web-console/lib/control-plane/contract.ts"]}
{"id":"bug-2026-08-29-agent-visual-proof-dependabot-secrets","date":"2026-08-29","title":"agent visual proof is permanently red on Dependabot pull requests because Dependabot events get no repository secrets","error_message":"HIVE_AGENT_ENGINE_LLM_API_KEY is required (value never printed)","root_cause":"A pull_request event raised by Dependabot is served from GitHub's separate Dependabot secrets store, so every secrets.* reference in agent-visual-proof.yml resolves to the empty string. The job's gate only excluded fork pull requests, and a Dependabot branch lives in the same repository, so it passed that check and then failed on the first secret read. Run 33234962802 shows the SIF correlation step succeeding and the launcher install step failing with LITELLM_MASTER_KEY, the whole S3 block and NEXT_PUBLIC_EDGE_API_BASE_URL blank in the same step log. Misdiagnosed twice as a trigger-path or artifact-correlation problem, which the step-level evidence rules out.","fix":"Skip the job for Dependabot pull requests alongside fork pull requests (github.actor != 'dependabot[bot]'), and route a bump that needs proof through the existing workflow_dispatch, which runs with real secrets.","tags":["ci","github-actions","dependabot","secrets","agent-visual-proof","false-red"]}
{"id":"bug-2026-08-29-sif-artifact-retention-clock","date":"2026-08-29","title":"agent visual proof gets less reliable the longer the sandbox image inputs stay unchanged","error_message":"no unexpired agent-engine-sif artifact was built from these image inputs","root_cause":"agent-engine-sif.yml builds only on a change to deploy/apptainer, apps/agent-engine/packs or vendor/openhands, while the uploaded artifact expires after 90 days regardless. The proof job refuses any artifact whose run did not build the same three trees, so a quarter without an image-input change expires the last match and every proof run hard-errors for a reason no pull request caused. A stable tree made CI less reliable, not more.","fix":"Add a monthly schedule to agent-engine-sif.yml so a matching unexpired artifact always exists, and name that schedule in the proof job's error message so a future hit is diagnosable.","tags":["ci","github-actions","artifact-retention","agent-visual-proof","latent"]}
{"date":"2026-08-29","title":"Supabase Storage refused every S3 request because the server had no S3 protocol credentials","error_message":"s3 PUT /storage/v1/s3/hive-files/... failed with status 403: AccessDenied: Missing S3 Protocol Access Key ID or Secret Key Environment variables","root_cause":"The self-hosted supabase-storage service was never given S3_PROTOCOL_ACCESS_KEY_ID, S3_PROTOCOL_ACCESS_KEY_SECRET or S3_PROTOCOL_PREFIX. In single tenant mode Storage recomputes the SigV4 signature from that pair, so with neither set it refuses every signed request while staying healthy with the buckets present. The documented recipe compounded it by telling operators to point both client variables at the service_role key, which is not an S3 credential and discloses itself through the access key id that SigV4 sends in the clear.","fix":"Hand supabase-storage S3_PROTOCOL_ACCESS_KEY_ID and S3_PROTOCOL_ACCESS_KEY_SECRET from the same S3_ACCESS_KEY and S3_SECRET_KEY the consumers sign with, plus S3_PROTOCOL_PREFIX=/storage/v1 to match the prefix Caddy strips. Corrected the credential guidance in .env.example and docker-compose.yml, and added two guards to scripts/test_selfhost_supabase_seam.py.","tags":["storage","supabase","s3","sigv4","compose","config","issue-1282"]}
{"id":"2026-08-29-demo-box-disk-90pct","date":"2026-08-29","area":"ops/deploy","error_message":"Demo box root filesystem at 90 percent (11 GB free of 98 GB) with deploy-demo-box.yml free to build into the remainder; run 33237021243 (job 99059755081) died at 05:55:11Z with 'Unhandled exception. System.IO.IOException: No space left on device' while the Actions runner was writing its own _diag worker log, reporting failure with an EMPTY failed-steps array because the runner process died rather than a step failing","root_cause":"The deploy job's two cleanup steps (docker image prune -f, docker builder prune -f --filter until=24h) both run only at the END of the job. `if: always()` covers a failed step but not the runner being killed, so a job that dies on ENOSPC or trips timeout-minutes mid build skips both. Separately, neither step targets what actually accumulated: tagged images that no container references and nothing in the repo names (three Playwright versions, superseded litellm, stale node and golang bases, 15 GB total). Dangling images were 0 and build cache was 0 B reclaimable, so both existing prunes were reclaiming approximately nothing.","fix":"Reclaimed 15 GB by removing 13 explicitly named unreferenced images with `docker rmi` (no prune, no --force, so docker refuses anything a container holds), taking the box from 12 GB to 27 GB free with all 22 running containers untouched, then a further 5.757 GB later that day with `docker builder prune -f` (no -a) once deploys had superseded those layers, ending at 25 GB free. Added scripts/check-deploy-disk.sh as the FIRST step of BOTH the migrate and deploy jobs, the only position a job timeout or ENOSPC death cannot skip: warn below 25 GB, hard fail below 15 GB (above the 11 GB at which the incident happened), printing docker system df and naming the unsafe reclaim moves. Floor overridable only via a workflow_dispatch input, never from a commit message. Guarded by scripts/test-deploy-disk-gate.sh in a required CI lane.","tags":["disk","docker","deploy-demo-box","prune","demo-box","timeout","ops"]}
{"id":"bug-mtdw1319-a41c02","timestamp":"2026-08-29T07:05:00.000Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T07:05:00.000Z","date":"2026-08-29","title":"images.generate answered 200 with an empty data array and billed the hold","error_message":"POST /v1/images/generations returned HTTP 200 with data: [], no image and no error, and the flat image reservation was finalized for it","root_cause":"Neither handleGeneration nor handleEdit checked the decoded ImageResponse for a usable payload before settling and writing 200; the only route with supports_image_generation is a text model carrying a legacy capability flag, so an imageless 2xx is its normal answer","fix":"Added hasImagePayload, counting entries that actually carry a url or b64_json rather than array length, and refused before settlement on both paths with the sanitized upstream error at 502 while releasing the hold; the upstream body handed to the sanitizer is bounded at 4 KiB so its operator log line cannot carry a multi-megabyte 2xx body","verification":"Six wire-level subtests over the three imageless shapes on both endpoints, asserting status, envelope, absence of a data key, hold released and not finalized; mutation with the guard disabled turns all six red","tags":["images","billing","fake-success","edge-api","issue-1319"],"pr":1371}
{"id":"bug-mtdw1318-7be519","timestamp":"2026-08-29T07:05:01.000Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T07:05:01.000Z","date":"2026-08-29","title":"audio.speech rejected every OpenAI voice name and answered 500","error_message":"POST /v1/audio/speech with the OpenAI default voice alloy failed with HTTP 500 and a bare internal error, making the endpoint uncallable by an unmodified OpenAI SDK","root_cause":"The handler rewrote only the model key and forwarded voice verbatim to groq/orpheus-v1-english, whose roster is six entirely different names; the upstream 400 then reached the caller through the provider-blind path as a sanitized 500, so a caller could not tell a bad parameter from a broken gateway and a retry could never succeed","fix":"Added resolveVoice, translating the eleven OpenAI stock names onto the six upstream voices and normalizing case and whitespace, with the resolved value written back into the dispatched body; a name in neither set is refused before route selection with a 400 invalid_request_error naming param voice, code invalid_value, and the supported roster, so no reservation is taken for a request that cannot succeed","verification":"Wire-level tests over all eleven stock names plus case and whitespace variants asserting the DISPATCHED body carries an accepted voice, an agreement test that every id advertised by GET /v1/audio/voices is accepted, and refusal tests for unknown, missing and blank voices; mutation disabling the guard and the rewrite turns sixteen subtests red","tags":["audio","tts","openai-compat","status-mapping","edge-api","issue-1318","issue-996","issue-1285"],"pr":1371}
{"id":"bug-mtdw1348-c93d44","timestamp":"2026-08-29T07:05:02.000Z","related_bugs":[],"occurrences":1,"last_seen":"2026-08-29T07:05:02.000Z","date":"2026-08-29","title":"Malformed chat requests were answered as a model availability problem","error_message":"An invalid message role and an oversized max_tokens both returned 400 with message hive-small is not available. and code upstream_error, pointing a customer debugging their own payload at model status","root_cause":"Two causes. Nothing validated the messages array, so a bad role was forwarded, refused upstream, and the LiteLLM fallback-group bookkeeping that came back was collapsed by sanitizeProviderBlindMessage into the is not available sentence regardless of status. Separately WriteProviderBlindUpstreamError labelled every non-429 non-503 upstream failure api_error with code upstream_error, including a 400 that means the caller request was refused. The pre-existing empty-messages check read len on a json.RawMessage, which is a byte length, so messages: [] passed it too","fix":"Added validateChatMessages refusing an unknown or missing role, an empty array and a non-object entry with invalid_request_error, the offending param and codes invalid_value, empty_array and invalid_type, matching the n refusal shape from PR 1305. Added providerBlindRequestShaped so an upstream 400 or 422 is relabelled invalid_request_error with code invalid_request and gets a request-shaped sentence instead of an availability verdict, while 404 and every 5xx keep theirs","verification":"Five wire-level subtests for the malformed shapes, a positive test that all six valid roles still pass, and two error-package tests pinning both sides of the status gate; mutations disabling the validator, cutting the role list, and forcing the status gate to false and to true each turn the matching test red, with the false mutation reproducing the reported body verbatim","tags":["error-mapping","chat-completions","validation","provider-blind","edge-api","issue-1348"],"pr":1371}
{"date":"2026-08-29","tags":["console","web","ui","dark-mode"],"title":"Analytics charts inverted in dark mode because their colours were hex literals","error_message":"Every analytics chart rendered a light panel with light-mode series colours against the dark console palette","root_cause":"The three chart components passed hex literals to recharts for the panel background and every series, and left the grid, axes, tooltip and legend at recharts light-tuned defaults. A colour handed to recharts as a prop is not reachable from a stylesheet, so the prefers-color-scheme override could not correct any of it.","fix":"Added a chart-theme module holding CSS variable references for the panel, grid, axes, tooltip, legend and every series, and pointed all three charts at it. A guard test reads the sources and fails on a quoted hex literal."}
{"date":"2026-08-29","tags":["console","web","ui","empty-state"],"title":"Console tables and charts reported an empty account while they were still loading","error_message":"Tables flashed 'No records yet.' during every fetch and empty charts rendered three bare axis frames with no explanation","root_cause":"DataTable had no pending state at all, so the empty branch was the only thing it could render before data arrived. ChartCard passed an empty series straight to recharts, which draws its axes regardless.","fix":"DataTable takes a loading prop and renders a pending row with aria-busy. ChartCard takes the rows it wraps and renders an empty state at the chart own height instead of rendering the chart."}
{"date":"2026-08-29","tags":["chat","owui","frontend","noise"],"title":"Chat front end reported two non-errors to the user and the console","error_message":"No token found in localStorage warned on every signed-out page load, and File not found. toasted whenever the file picker was cancelled","root_cause":"The socket connect handler treated an absent token as worth warning about when it is the normal anonymous state, and both composers treated a change event with an empty FileList as a missing file when it is also what pressing cancel produces.","fix":"Dropped the warning and kept the emit guard. Dropped the toast on an empty selection in both composers. A guard test pins all of it, plus the removal of the dead tools branches."}
{"id":"bug-1349-credit-precision-composer-overlap","date":"2026-08-29","title":"Balance rendered nine significant figures and the composer toolbar overlapped at 375px","error_message":"$0.000000858 shown as a credit balance in the chat banner and Settings > Usage; Cowork (x124-199) and Hive Auto (x145-272) drawn on top of each other at 375px","root_cause":"Two unrelated causes. (1) formatUsdBalanceFromCredits and the chat spend formatter both scaled decimal places to the value with Math.min(9, Math.max(2, 2 - magnitude)), a rule ported from the catalog PRICE formatter where nine decimals is correct, so a sub-cent balance rendered at the full width of one credit in chrome the customer cannot dismiss. The rule was hand-duplicated across two builds whose test suites cannot see each other, which is also how #1344 and #1345 happened. (2) The composer control row's left group was flex-1 min-w-0 while its children were shrink-0 and .hv-mode was flex-shrink: 0, so shrinking the group pushed its content out of the box and over the model chip.","fix":"Format balances and spend in whole cents, floored for balances and rounded for spend, with a sub-cent bound of \"< $0.01\" so a real non-zero figure still never renders as the empty-wallet string. Added tools/lint-credit-balance-formatter-parity.mjs, wired as a CI step, to fail the build when the two copies of formatUsdBalanceFromCredits diverge. Made the composer control row wrap, with the left group taking a full line below sm and the previous basis restored from sm up.","tags":["money","formatting","frontend","open-webui","web-console","layout","responsive","cross-build-duplication"]}
{"id":"bug-1372-hive-auto-flat-reservation-hold","date":"2026-08-29","title":"Hive Auto unusable below 2.00 USD: flat envelope-sized credit hold refused every request","error_message":"You exceeded your current quota, please check your plan and billing details. (429 insufficient_quota) shown while the account held 0.455 USD","root_cause":"hive-auto is priced upstream_actual, so the gateway takes an up-front credit hold instead of charging a catalog rate. reservation_estimate_credits was a flat 2000000000 (2.00 USD), the price of the largest request the variable-price bounds allow, and every request paid that authorization regardless of its actual size. control-plane's enforcePolicy refused any account whose balance was under it. Three silent enablers: the session chat path never ran EnforceVariablePriceBounds at all, so its hold covered a request with no size cap or completion ceiling; route-openrouter-auto-live carried no provider.max_price, so the rate half of the hold's coverage proof did not exist; and it carried no usage.include, so OpenRouter reported no cost and every settlement fell through to charging the full hold unconfirmed. The refusal text was OpenAI's canonical insufficient_quota sentence, which named a plan Hive does not sell.","fix":"Size the hold from the request: len(body) as a prompt-token upper bound plus the effective completion ceiling already written into the body, priced at the route's own rate ceiling through CreditsForUpstreamCost, capped by the catalog envelope and floored by the endpoint default. Run EnforceVariablePriceBounds on the session chat path. Add route-openrouter-auto-live to deploy/litellm/config.yaml with usage.include and provider.max_price. Split the refusal text by status and say what happened.","tags":["billing","reservation","upstream_actual","hive-auto","litellm-config","chat","error-message","money-path"],"files":["apps/edge-api/internal/inference/pricing.go","apps/edge-api/internal/chat/dispatch.go","apps/edge-api/internal/chat/billing.go","apps/edge-api/internal/inference/reservation_guard.go","deploy/litellm/config.yaml","apps/web-console/lib/quickstart-model.ts"],"issue":1372}
{"id":"bug-1367-console-mobile-nav","date":"2026-08-29","title":"Console had no navigation at all below the lg breakpoint, and three of its tables scrolled the whole document sideways at 375px","error_message":"At 375px on /console/api-keys the entire tabbable set was Documentation, Switch to EN, Switch to বাংলা, Sign out, Revoke: no route to any console section from any page. Separately, document.documentElement.scrollWidth was 803 on /console/api-keys and 818 on /console/catalog against a 375px viewport, and window.scrollX reached 428.","root_cause":"console-shell.tsx rendered the sidebar as `hidden lg:flex` with no small-screen replacement, so all thirteen nav links were display:none below 1024px. The overflow was two stacked faults in DataTable's wrapper: `overflow-hidden` clipped every column past the fold instead of scrolling it, and because the wrapper was not a containing block, Chromium propagated the over-wide table's layout overflow past that overflow:auto ancestor to the viewport, so the page scrolled sideways even though the scroller itself was the correct width.","fix":"Made the same aside element the drawer (fixed overlay plus scrim below lg, static grid column at lg and up) driven by a small client ConsoleFrame, keeping ConsoleShell a Server Component because it renders the async LocaleSwitcher. Closed state keeps `hidden` so the links stay out of the tab order. Changed DataTable's wrapper to `relative overflow-x-auto`.","tags":["console","responsive","accessibility","navigation","css-overflow","chromium","issue-1367"]}
{"id":"bug-1367-ledger-order","date":"2026-08-29","title":"Credit ledger returned a customer's money history in random order","error_message":"The billing overview's 'last five ledger events' rendered as 11, 16, 26, 16, 24 August, and the paginated Ledger tab was shuffled the same way.","root_cause":"ListEntriesWithCursor ordered by `id` and paginated on `id < cursor`. credit_ledger_entries.id is `gen_random_uuid()`, a v4 UUID, so it encodes no time order whatsoever. Both the overview and the Ledger tab read that one query, so a view-level sort would have fixed only half of it. An existing live test asserted descending id under the name 'default limit returns newest first', which encoded the bug as the requirement and kept it green.","fix":"ORDER BY created_at DESC, id DESC, with the keyset expressed as (created_at, id) < (SELECT created_at, id FROM credit_ledger_entries WHERE id = $cursor) so the opaque cursor stays the entry id and the API contract is unchanged. Corrected the existing assertion to compare created_at, and added three live Postgres tests using fixed UUIDs whose id order is the exact inverse of time order.","tags":["billing","ledger","postgres","pagination","keyset","control-plane","camouflaged-test"]}
{"id":"owui-skills-guard-on-page-not-layout","date":"2026-08-29","error_message":"/skills/create and /skills/edit rendered the full skill editor for a session without workspace.skills; the save then failed with 401 from POST /api/v1/skills/create","root_cause":"the permission guard was written in routes/(app)/skills/+page.svelte. A SvelteKit +page.svelte applies to its exact route only and is not inherited by nested routes, unlike +layout.svelte, so the two editor routes had no guard at all","fix":"moved the guard into routes/(app)/skills/+layout.svelte, which covers the index and both editor routes; skills-surface.test.ts now reads the layout rather than the page","tags":["owui","svelte","authz","sveltekit-routing"],"pr":1388}
{"id":"owui-workspace-components-need-workspace-chrome","date":"2026-08-29","error_message":"the skills index rendered underneath the sidebar with the New Skill button pinned to the window corner and the title overlapping the Hive wordmark","root_cause":"Skills.svelte and SkillEditor.svelte are upstream Workspace components authored for routes/(app)/workspace/+layout.svelte's container, which supplies both the md:max-w-[calc(100%-var(--sidebar-width))] constraint and the px-3 md:px-[18px] padding. Mounting them in a bare hv-panel-region flex region supplied neither","fix":"the /skills route layout reproduces that container minus the tab bar; a test pins all three class fragments so the chrome cannot silently go away again","tags":["owui","css","layout","fork"],"pr":1388}
{"id":"owui-group-access-grants-never-filtered","date":"2026-08-29","error_message":"a non-admin could attach an access grant naming any group id on the shared instance, including another tenant's, and have it stored","root_cause":"utils/access_control.filter_allowed_access_grants strips public grants and individual user grants for a non-admin and never inspects group grants at all. For skills that is cross-tenant prompt injection, because a skill body is appended verbatim to the chat request as a system message. Unreachable in practice only because GET /api/v1/groups filters to the caller's own memberships, which is secrecy of a UUID rather than an authorization check","fix":"owui-patches/apply_skill_group_grants_patch.py drops group grants naming a group the caller is not a member of, at all three skill write sites; the image build asserts all four markers landed. The general case across the other routers is filed as #1396","tags":["owui","security","authz","multi-tenancy","prompt-injection"],"pr":1388}
{"id":"agy-runs-outside-the-repo","date":"2026-08-29","error_message":"an Antigravity review returned detailed findings about a credit-formatting diff that does not exist on the branch under review","root_cause":"agy executes in ~/.gemini/antigravity-cli/scratch, not in the directory it is launched from. Measured by asking it to print pwd, git rev-parse --abbrev-ref HEAD and git diff --stat: the first returns the scratch path and both git commands fail with 'not a git repository'. Given no diff to read, it produced a plausible review of something else","fix":"paste the diff and the load-bearing surrounding code inline in the prompt rather than telling it to run git; discard any pass whose findings name files not in that paste","tags":["harness","review-streams","antigravity","false-green"],"pr":1388}
{"id":"shared-scratchpad-filename-collision-crossed-prs","date":"2026-08-29","error_message":"six review threads on PR #1083, another agent's pull request, were resolved by an agent that had never read them","root_cause":"agents in one session share a scratchpad directory. A helper written to the plain name resolve-threads.sh was overwritten by another agent's script of the same name, which targeted pull request 1083, and running it acted on their PR. Same failure class as the known parallel-agent report-path collision, but the blast radius is a GitHub mutation rather than a lost file","fix":"unresolved all six immediately to restore the state found, left a note on #1083 saying what happened and that the restore was blind rather than a judgement, and prefixed every later script with the worktree id","tags":["harness","parallel-agents","scratchpad","github"],"pr":1388}
{"id":"BUG-1198-upstream-actual-capture","date":"2026-08-29","title":"Zero-content capture replaced a reported upstream cost with the full authorization hold","error_message":"actual_credits = 100000000 where UpstreamActualSettlement had computed 560000 for the same generation","root_cause":"The zero-content branch in apps/edge-api/internal/inference/orchestrator.go overwrote actualCredits with capCaptureAtCeiling, whose contract is to return its credits argument untouched for an upstream_actual route; that argument is reservation.Held(). A 178x overcharge on hive-auto and openrouter-auto, the alias family with no catalog price to fall back on.","fix":"Guard the branch on !route.Pricing.IsUpstreamActual() at the call site rather than relying on the early return that caused it. UpstreamActualSettlement is already fail-closed for that route in all three of its outcomes.","tags":["money","billing","settlement","edge-api","overcharge","upstream-actual","issue-1198"]}
{"id":"BUG-1198-cached-prompt-double-charge","date":"2026-08-29","title":"A fully cached prompt was estimated back on top of its own cache tokens","error_message":"captureInputTokens returned a full-prompt estimate for a usage block whose fresh input count was a legitimate zero, and capCaptureAtCeiling then priced that estimate as fresh input alongside the same prompt's cache-read tokens","root_cause":"captureInputTokens tested freshInputTokens > 0 rather than whether the usage block reported any input quantity. NormalizeCacheUsage produces fresh = 0 for a 100 percent cache hit in the inclusive shape, so the real measurement was discarded and the prompt was billed twice, once at the full input rate and once at the cache rate.","fix":"Test hasUsage && freshInputTokens + cacheReadTokens + cacheWriteTokens > 0, which distinguishes nothing-reported from zero-reported, and pass the cache components from both call sites.","tags":["money","billing","settlement","edge-api","cache-tokens","double-charge","issue-1198"]}
{"id":"BUG-1198-hold-capture-overcharge","date":"2026-08-29","title":"Fail-closed hold capture billed the flat reservation hold when the caller set no max_tokens","error_message":"credit_reservations rows with consumed_credits = 100000000 and terminal_usage_confirmed = false, up to 355872x the median confirmed charge on the same alias","root_cause":"capCaptureAtCeiling in apps/edge-api/internal/inference/completion_ceiling.go short circuited on ceiling <= 0 and returned reservation.Held() unchanged, so both fail-closed capture branches (#1171 zero-content, #1215 missing usage block) charged the flat authorization floor as though it were a measurement. Reintroduced the exact defect issue #636 removed one layer down.","fix":"Apply the bound unconditionally, built from captureInputTokens plus the new captureCompletionTokens, capped by the caller ceiling where set, floored at one credit, still capped by the hold. Six tests that asserted the flat hold updated to the priced bound.","tags":["money","billing","settlement","edge-api","overcharge","issue-1198","D-034"]}
{"id":"bug-2026-08-29-ci-object-storage-dead-secret","date":"2026-08-29","title":"CI object storage endpoint pointed at a deleted Supabase Cloud project, so the file upload path could not fail","error_message":"file upload to object storage failed bucket=hive-files error=\"Put \\\"https://<project>.supabase.co/storage/v1/s3/hive-files/...\\\": dial tcp: lookup <project>.supabase.co: no such host\"","root_cause":"The S3_ENDPOINT repository secret was last written 2026-04-21, four months before the August self-hosted Supabase cutover, and still named the deleted Supabase Cloud project. The live-integration job read it at job level, so every POST /v1/files in CI died at DNS and the Files and Batches conformance assertions were carried as it.fails markers rather than as coverage. The same stale secret family also killed the OWUI nightly at its seeding step via SUPABASE_URL (#1370), which then skipped the entire Playwright suite and left a misleading missing-browser cascade as the only visible red. Issue #1380 attributed the blindness to the http://127.0.0.1:9 stub in ci.yml, but that stub is in two Playwright jobs that run no SDK suite.","fix":"Removed every secrets.S3_* reference from live-integration and agent-visual-proof. live-integration now boots its own throwaway supabase/storage-api at the digest docker-compose.enterprise.yml pins, on the throwaway Postgres it already stands up, whose 00-extensions.sql already creates the storage schema and roles. The fixture asserts a signed PUT and GET before printing its values. The it.fails markers came off. Guards added: a unit test asserting the job keeps the fixture and no S3 secret, and a seam-test assertion that the fixture and compose pin the same image.","tags":["ci","storage","s3","supabase","dark-detector","stale-secret","sigv4"],"issues":[1324,1380,1370,1282]}
{"id":"bug-2026-08-29-s3-absolute-form-request-target","date":"2026-08-29","title":"S3 client sent absolute-form request targets, which Supabase Storage refuses with an unnamed 500","error_message":"s3 PUT /s3/hive-files/<key> failed with status 500: <Code>InternalError</Code><Message>Internal Server Error</Message>; storage side: {\"code\":\"ERR_INVALID_URL\",\"input\":\"http://localhost:8080http://172.17.0.1:5000/s3/hive-files/<key>\"} TypeError: Invalid URL at SignatureV4.constructCanonicalRequest","root_cause":"packages/storage/signing.go SignHTTP sets req.URL.Opaque to \"//host/path\" so the AWS SigV4 signer receives an un-normalized path, and never cleared it. net/url.RequestURI returns Opaque verbatim and re-attaches the scheme when it starts with \"//\", so every signed S3 request went on the wire in absolute form (PUT http://host/s3/bucket/key HTTP/1.1). RFC 9112 reserves absolute form for a request to a proxy. The deployed box never saw it because every S3 request passes through Caddy, which normalizes the target before proxying. Reached directly, Supabase Storage builds its canonical URI as new URL(\"http://localhost:8080\" + prefix + request.url) and throws on the doubled origin.","fix":"Clear req.URL.Opaque after signer.SignHTTP returns. The signature is unaffected: the signer derives its canonical URI from Opaque while set, stripping the leading //host, and that string is byte identical to the EscapedPath the wire form falls back to once Opaque is empty. presignHTTP still keeps Opaque, because url.URL.String renders a bare-path Opaque as http:/s3/... and the //host form is what makes a presigned URL absolute. Four tests added, each verified red first.","tags":["storage","s3","sigv4","http","rfc9112","proxy-masked","dark-detector"],"issues":[1324,1380,1282]}
{"id":"bug-2026-08-29-checkout-rails-missing-profile","date":"2026-08-29","title":"Buy credits dead on the deployed console: checkout rails 500 on a missing account_profiles row, and a rails payload the console cannot decode","error_message":"GET /api/v1/accounts/current/checkout/rails -> 500 {\"error\":\"checkout temporarily unavailable\"} (console proxy reported {\"error\":\"Upstream service error\"})","root_cause":"Four call sites (payments.Service.GetCheckoutOptions, payments.Service.InitiateCheckout, and both through stubCountryAdapter into the demo stub) read the whole account profile purely to obtain CountryCode for AvailableRails, and each treated profiles.ErrNotFound as a server fault. country_code is already optional (the repository COALESCEs NULL to \"\") and AvailableRails(\"\") is defined, so a missing account_profiles row was a question the code could already answer. Issue #999 counted 14 such accounts live. Independently, RailOption never carried currency, label or enabled, which the console's decodeCheckoutRail requires, so the rail list reached the checkout modal empty and no payment method rendered even without the 500.","fix":"Added profiles.Service.CountryCode, which maps ErrNotFound to (\"\", nil) and propagates every other error, and narrowed payments.ProfileReader to take the bare country so the payment path can no longer fail on an absent row. stubCountryAdapter delegates to it. Extended RailOption with Label, Currency and Enabled and CheckoutOptions with CreditIncrement, MinCredits and MaxCredits, all built through one shared NewRailOption constructor; max_credits is the minimum ceiling across the rails on offer so the advertised bound is never looser than ValidatePurchaseAmount enforces.","tags":["money-path","checkout","payments","control-plane","web-console","profiles","500","wire-contract"],"issues":[1386,999,797],"files":["apps/control-plane/internal/profiles/service.go","apps/control-plane/internal/payments/service.go","apps/control-plane/internal/payments/stub/stub.go","apps/control-plane/cmd/server/main.go"]}
{"id":"BUG-2026-08-29-console-tenant-claim","date":"2026-08-29","title":"Console admin pages refused every real user because control-plane read a tenant field nothing writes","error_message":"GET /api/v1/admin/feature-gates -> 400 {\"error\":\"no tenant selected\"}; console renders \"Could not load feature gates\"","root_cause":"auth.Client resolved Viewer.TenantID only from raw_user_meta_data.selected_tenant_id. The only application writer of that field is POST /v1/tenants/switch, which the web console never calls: its workspace switcher sets an hive_account_id cookie the control-plane does not read, and signup writes tenant_users without it. Every account created through normal signup therefore carried uuid.Nil forever and platform.WorkspaceAdminGate refused it with 400. Only accounts seeded by scripts, which set the field through the admin API, could reach /console/feature-gates or /console/marketplace, which is why it was never caught. Meanwhile public.custom_access_token_hook had been stamping a correct, membership-validated tenant_id claim into every issued token the whole time, and control-plane discarded it.","fix":"auth.Client.LookupUser now reads the tenant_id claim from the bearer Supabase has just validated, sub-matched to the user Supabase resolved, preferring it over the metadata and keeping the metadata as a fallback for pre-hook tokens. The existing membershipCheck still runs on whichever candidate wins, so the claim cannot widen access and an errored check still fails closed.","tags":["auth","control-plane","web-console","featuregate","marketplace","tenant-scope","issue-543","issue-762"]}
{"id":"bug-2026-08-29-classifier-misattributes-gateway-cooldown","date":"2026-08-29","title":"CI upstream-refusal classifier reported a LiteLLM router cooldown on a paid alias as a whole-pool provider rate limit","error_message":"UPSTREAM REFUSAL, not a performance regression: the provider rate limited us (429). Check whether the window is per minute (transient, so a rerun may pass) or per day (an allowance that is spent).","root_cause":"The 'Name the real cause when the upstream refused' step in .github/workflows/ci.yml grepped a 2000-line window of the litellm and edge-api container logs for the bare token 'status=429' among others. Three log shapes matched that are not upstream refusals: LiteLLM's own 'No deployments available for selected model ... cooldown_list=[...]' router cooldown (no upstream call is made), its 'Cooldown Deployments=[...]' table (which quotes the causing exception verbatim and reprints it on every dispatch while the entry lives), and a bare 'No fallback model group found' (which LiteLLM emits on any failed dispatch to a group with no configured fallback, including three refusals the job deliberately tests for). The matched line also carried alias deepseek-v4-flash, so a paid alias's gateway cooldown was reported as a refusal of the hive-free pool. The free pool was healthy throughout: the job's own probe step reported 3 healthy 1 unhealthy in both runs, only the Gemini member was rate limited, and the actual job failures were '500 Failed to store file' and a strict XPASS on issue #1274. Only two of the seven consecutive failures on issue #1374 that morning classified at all, which is the signature of an incidental grep hit rather than a spent allowance.","fix":"Moved the classification from a bash heredoc into scripts/classify-upstream-refusal.py, which make test-scripts runs as a required check. Gateway cooldown lines in both shapes are dropped before any branch reads them. The dead-member branch now requires the NotFoundError shape together with model_group=route-free-pool. A classified refusal names the alias it saw. Fixtures are copied from run 33243396287's own output. This also removes a SIGPIPE exit 2 from the old 'grep ... | head -5' reporting pipeline under set -o pipefail. Separately, report-free-pool-health.py now distinguishes a rate-limited member from a retired one, whose remedies are opposites, and surfaces the provider's quota window and retry hint from the full error text instead of truncating them away at 300 characters.","tags":["ci","litellm","free-pool","signal-quality","false-positive","issue-1088","issue-1064","issue-1374"]}
{"date":"2026-08-29","title":"lint:proof-tokens skipped itself on the only pull requests that add proof captures","error_message":"Repo policy lints (tenant + audit) reported pass in 6 seconds on PR #1398, which committed 71 proof captures. No step in the job executed.","root_cause":"The changes job in .github/workflows/ci.yml puts docs/ on the inert-path allow-list, so a pull request whose changed paths are all under docs/proof/ sets run=false. Every step in repo-policy-lints was gated on needs.changes.outputs.run != 'false', including npm run lint:proof-tokens, whose scanner reads docs/proof/ and nothing else. The gate therefore disabled itself for its own subject matter, and published a green that a reviewer trusts. Nothing else backstops the text half of a proof capture: GitHub secret scanning, push protection and GitGuardian do not read release assets and read no pixels.","fix":"Run npm run lint:proof-tokens unconditionally, along with the checkout, setup-node and npm ci steps it needs. Every other step in the job keeps its docs-only gate. Rejected carving docs/proof/** out of the classification (would run the whole heavy suite over a screenshot pull request) and a dedicated changes output (copies the scanner's scope into the workflow where it can drift, and cannot cover a capture that landed earlier). Demonstrated red before green on a throwaway docs-only pull request: run 33247080029 failed on a planted synthetic token, run 33247152597 passed once redacted.","tags":["ci","docs-only","proof-captures","credential-leak","unfalsifiable-green","issue-1409","issue-797"],"pr":1417}
{"id":"BUG-1407","date":"2026-08-29","title":"Chat and console 502s during every container recreate, misattributed to per-request Docker DNS resolution","error_message":"dial tcp: lookup open-webui on 127.0.0.11:53: server misbehaving; dial tcp 172.18.0.x:8080: connect: connection refused","root_cause":"Caddy has no dial retry, so any request arriving while an upstream container is being recreated fails. The two error strings are the same window seen at two moments: while the old container's name is deregistered Docker's embedded DNS forwards the single-label name to the host resolver, which returns SERVFAIL and Go reports as 'server misbehaving'; once the new container has registered but is not listening the dial is refused. All 58 measured 502s across both origins fell inside a deploy run window and none against a healthy upstream.","fix":"lb_try_duration 30s and lb_try_interval 1s on the four reverse_proxy blocks in Caddyfile.owui and both in Caddyfile.console, bounded so a genuinely broken upstream still 502s. Guarded by scripts/test_caddy_upstream_retry.py.","tags":["caddy","docker-dns","deploy","502","demo-box","issue-1407"]}
{"id":"bug-2026-08-29-console-qa-cluster","date":"2026-08-29","title":"Unbounded API key nickname made the console key table unusable and unrepairable from any product surface","error_message":"API keys table renders 45,524 pixels wide; Revoke control of every key in the workspace is unreachable","root_cause":"The nickname field had no length bound at any layer (form, Next route, Go handler, Go service, Postgres column) and the table's name cell had no width constraint, so one 5000-character value sized the whole table. No rename or update route existed, so nothing in the product could shorten a stored value. The same shape recurred in three siblings: two CSV exports each hand-rolled their own cell escaping and neither neutralised a formula-leading cell, the analytics id-to-nickname join existed on one surface only, and an analytics grid item's default min-width of auto let one card size its track past the viewport.","fix":"Bound the nickname at 100 runes in a shared decodeMintBody used by both the create and rotate routes, and truncate the name cell with the full value on title so an already-stored row is repaired on render. Move CSV escaping into lib/csv.ts with formula neutralisation that exempts plain numbers. Move the api-key label join into lib/analytics/api-key-labels.ts and use it on the group tabs as well as the overview tile. Zero the analytics grid items' min-width.","tags":["console","validation","csv-injection","responsive","input-bounds","issue-1400","issue-1401","issue-1403","issue-1406","issue-1408"]}
{"date":"2026-08-29","title":"Demo box build cache grew unbounded, so the deploy disk gate refused three consecutive deploys","error_message":"demo box has 14G free on /var/lib/docker, below the 15G floor. Refusing to proceed.","root_cause":"Nothing reclaimed build cache in a way that reached the box. The deploy job's only cache prune carried --filter until=24h, and on a day of roughly 43 merges every cache record is younger than 24 hours, so the filter excluded exactly the records consuming the disk. Build cache reached 37.68GB with 12.42GB reclaimable while images were only 5 percent reclaimable.","fix":"Reclaim unused build cache inside scripts/check-deploy-disk.sh, below the warn line only, bounded by timeout 300, before the thresholds are compared. Inside the guard rather than as a preceding step so a failed reclaim cannot skip the check. Both the pre-reclaim and post-reclaim readings are reported so a non-cache disk problem still fails loudly.","tags":["deploy","disk","docker","build-cache","ci","issue-1419"]}
{"date":"2026-08-29","title":"Chat upload had no size or type limit: a 30 MB file stalled with no error and an .exe was accepted","error_message":"POST /api/v1/files/?process=true never returns for a 28.6 MB attachment; no progress, no timeout, no error. evil.exe uploads with 200.","root_cause":"Open WebUI enforces rag.file.max_size and rag.file.allowed_extensions in upload_file_handler, but both were unset. RAG_FILE_MAX_SIZE was in docker-compose.yml with an empty default and a comment claiming it fed the client-side guard, while the key was missing from hive_rag_env_config.py's RAG_CONFIG_ENV, so the persisted first-boot row outranked the environment and no value could ever reach a booted box. RAG_ALLOWED_FILE_EXTENSIONS was absent entirely.","fix":"Reconcile rag.file.max_size and rag.file.allowed_extensions through hive_rag_env_config.py, with an int coercion for the size and a list coercion for the allowlist (a persisted comma string turns upstream's not-in membership test into a substring test). Compose defaults 25 MB, matching RAG_MAX_UPLOAD_BYTES 26214400, and an allowlist derived from known_source_ext plus the loader's own document branches. A malformed size or an empty allowlist fails startup rather than booting unenforced.","tags":["open-webui","uploads","persistent-config","silent-failure","issue-1405","issue-722"]}
{"id":"1399-composer-smart-typography","date":"2026-08-29","title":"Chat composer rewrote typed characters and the rewritten text reached the model","error_message":"Typing `git push --force` into the chat composer sent `git push —force`; straight quotes became curly, `--` an em dash, `...` an ellipsis glyph, and `->`, `!=`, `<<`, `>>`, `2 * 3`, `^2` were also rewritten","root_cause":"vendor/open-webui/src/lib/components/common/RichTextInput.svelte registered @tiptap/extension-typography bare in the richText extension array. Its twenty-two entries are ProseMirror input rules, which rewrite the text buffer as a character is typed rather than its presentation, so the mutated string is what the editor serializes and what leaves the browser. Code inside a formed code block was exempt because input rules do not run there, which made the defect look narrower than it was and sent an earlier hunt to the marked renderer and to CSS ligatures, neither of which is the layer. A same-day QA pass could not reproduce it on the deployed box, most likely because richText is a per-user setting and the extension is registered only inside that branch.","fix":"Removed the import and the registration. The extension contributes nothing but input rules, so dropping it removes all twenty-two; the suggested configure() with six rules disabled would have left sixteen live, including -> and !=. Guarded by vendor/open-webui/src/lib/hive/composer-literal-input.test.ts, which types a corpus through a real ProseMirror input-rule pipeline with no DOM, asserts the serialized string, pins the harness against the measured corruption so it can go red, and keys on the imported module rather than the identifier so an aliased re-add is caught. Proven with a before/after wire capture on one pinned backend image differing only in the mounted frontend build.","tags":["chat","open-webui","composer","tiptap","prosemirror","frontend","issue-1399"]}
{"id":"bug-2026-08-29-owui-prompt-templates-unreachable","date":"2026-08-29","title":"Open WebUI's ten task and RAG prompt templates were unreachable on any booted deployment","error_message":"No error. Setting TITLE_GENERATION_PROMPT_TEMPLATE, RAG_TEMPLATE or any of the other eight in compose changed nothing on the demo box, silently.","root_cause":"All ten are Open WebUI persistent config. Config.seed_defaults only inserts keys that are absent, so the first boot seeded every one of them (confirmed on the demo box: rag.template holding upstream's full default text and nine *.prompt_template rows holding an empty string, all written at first boot) and the database outranked the environment from then on. The keys were not in deploy/docker/owui-patches/hive_rag_env_config.py's reconcile allowlist, which exists for exactly this failure. Compounding it, the surface that would otherwise edit them, Open WebUI's admin panel, is deleted from the fork and 404'd at the proxy, and every write verb under /api/v1/configs is denied, so the only remaining path was a hand-written SQLite UPDATE inside the owui-data volume on a live box.","fix":"Added the ten keys to RAG_CONFIG_ENV under upstream's own variable names, with a TEMPLATE_KEYS frozenset so a prompt's leading indentation and trailing newline are persisted unstripped while an all-whitespace value still counts as unset. Added the compose passthroughs with empty defaults on every profile. Proved delivery end to end by booting the pinned image twice against one volume and capturing the request body Open WebUI sent to the model.","tags":["open-webui","persisted-config","first-boot-wins","system-prompts","722","772","1405"]}
{"date":"2026-08-29","issue":491,"pr":null,"error_message":"Dark mode danger/warning/success text fails WCAG AA (danger badge 2.65:1)","root_cause":"globals.css dark media block redefined only the -soft semantic tokens; the three solid tokens (--color-success, --color-warning, --color-danger) fell through the cascade to their light-mode values, which are not readable against dark backgrounds","fix":"Added dark oklch(0.72 ...) overrides for the three solid tokens in globals.css, matching --color-accent's existing dark lightness; flipped button.tsx's danger variant label from hardcoded text-white to text-[var(--color-canvas)] since the lightened dark danger token drops white-on-danger to 2.72:1","tags":["web-console","theme","accessibility","wcag","css"]}
{"id":"BUG-1434-grafana-tiles-inert","date":"2026-08-29","title":"Analytics Grafana tiles gated on a GRAFANA_BASE_URL set nowhere, and wiring it would have leaked cross-tenant key ids","error_message":"Both Grafana tiles on /console/analytics render 'Not available on this deployment.' for every account; GRAFANA_BASE_URL is absent from every compose file, workflow, .env.example and from the running web-console container environment.","root_cause":"The tiles shipped with a runtime gate on an operator-provided variable that no deployment ever set, so the feature was inert from the first commit. The deeper defect is that completing the wiring was unsafe: Grafana runs with GF_AUTH_ANONYMOUS_ENABLED=true at the Viewer role bound on every interface, is fronted by no Caddyfile, and its rate-limit dashboard queries topk(10, sum by (key_id, tier) (...)) while the analytics page that renders the tiles carries no role guard, so every tenant member would have received an unauthenticated link naming other tenants' API key identifiers. Confirmed by capture: the pre-change component with GRAFANA_BASE_URL set renders two live Grafana links to a non-admin account.","fix":"Removed the two Grafana tiles and the nullable-href/disabled-state machinery that existed only to describe their absence, keeping the working first-party Request logs tile. Added a colocated regression test asserting the exact rendered link set, name-agnostic so a re-wiring under any variable name fails it, verified by mutation to go red on the pre-change component. Filed issue #1442 for hardening the Grafana instance itself, which this change deliberately does not touch.","tags":["console","analytics","grafana","observability","multi-tenant","information-disclosure","inert-code","dead-config"]}
{"id":"BUG-1428-upload-cap-divergence","date":"2026-08-29","title":"Chat upload cap and RAG ingest ceiling were two independently settable numbers, so the #1426 fix deployed inert","error_message":"Chat composer published a 100 MB attachment cap while edge-api and the markitdown sidecar enforced 26214400 bytes; a 30 MB attachment was accepted and stored by Open WebUI after a silent multi-minute upload","root_cause":"docker-compose.yml gave the open-webui service its own RAG_FILE_MAX_SIZE variable, in whole megabytes, settable independently of the RAG_MAX_UPLOAD_BYTES expression it passes edge-api and the sidecar in bytes. PR #1426 set that variable's compose default to 25, but the demo box's .env carried an explicit RAG_FILE_MAX_SIZE=100 and an explicit value beats a compose fallback, so the fix never reached the deployment it was written for.","fix":"Deleted RAG_FILE_MAX_SIZE as a settable knob. The open-webui service now takes the same ${RAG_MAX_UPLOAD_BYTES:-26214400} expression as the other two services, and hive_rag_env_config.derived_upload_cap floors it into whole megabytes, rounding down so the chat surface can never accept what the ingest path refuses. The container refuses to start if RAG_FILE_MAX_SIZE is present, and edge-api now fails the boot on a malformed ceiling instead of warning and falling back. A compose guard in scripts/test_owui_rag_env_config.py fails if a second knob is re-introduced.","tags":["config","docker-compose","open-webui","rag","uploads","silent-no-op","deploy-drift"]}
{"date":"2026-08-29","error_message":"OpenRouter 402 Payment Required (own account out of funds) forwarded verbatim as HTTP 402 to the Hive customer","root_cause":"WriteProviderBlindUpstreamError only sanitized the MESSAGE body for an upstream refusal; it never remapped the HTTP STATUS itself, so a provider funding refusal (about Hive's own OpenRouter balance) was indistinguishable, at the status-code level, from a caller quota refusal (about the customer's own Hive balance)","fix":"remap upstream 402 to 503/upstream_unavailable in WriteProviderBlindUpstreamError, reusing the existing 503/504 temporarily-unavailable message path; original status kept in the operator log","tags":["billing","provider-blind","accounting","reservations","issue-1411"]}
