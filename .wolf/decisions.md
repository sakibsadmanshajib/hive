# Decisions Ledger (lightweight index)

Terse locked-decision index. ONE or TWO lines each, pointing to the detail (Obsidian vault doc, issue, or in-repo ADR like VENDORING.md). Detail NEVER lives here, to keep .wolf light and out of the system prompt.

PROTOCOL:
- Read this before any spec, plan, design, or implementation. Inject the relevant decisions into every subagent brief (do not rely on agents rediscovering them).
- When the owner states a decision (a "decision:" moment), append a terse entry here immediately with a new D-ID and a pointer to where the detail lives.
- Supersede in place: mark the old entry SUPERSEDED by D-NNN, keep the line for history.

Format: `D-ID | decision (1 line) | source | date`

- D-001 | RAG embedding: admin selects the model AND its dimension; the system provisions the pgvector column/index to match and re-embeds existing docs; NO truncation band-aid | vault plan-admin-embedding-dim.md, issue #392 | 2026-07-19
- D-002 | Desktop sandbox = vendored OpenAI Codex Rust sandbox (bwrap + process-hardening + bubblewrap), NOT Tauri's default | apps/desktop-sandbox/VENDORING.md | 2026-07-16
- D-003 | Windows sandbox: ADOPT the elevated codex-windows-sandbox (dedicated sandbox OS user + UAC elevated runner + capability-restricted token + ACL + WFP egress scoped to sandbox SID + ConPTY), staged; the minimal restricted-token base variant is insufficient for the agentic + CLI flow. Step 1 (CreateProcessAsUserW launch fix) is a prerequisite under any option | vault plan-codex-crossplatform-desktop.md, issue #393, VENDORING.md | 2026-07-19
- D-004 | Owner reviews no PRs; agent owns the full loop (spec, impl, adversarial review, verify, merge); security boundaries merge only after real validation | memory feedback_agent_owns_full_pr_loop.md | 2026-07-19
- D-005 | No band-aid workarounds; solve the real design | .wolf/cerebrum.md Owner preferences | 2026-07-19
- D-006 | Gateway fronts all model types with per-model-type admin-controlled posture (self-host vs serverless), not a global switch | vault decision-2026-07-17-gateway-posture-and-network.md | 2026-07-17
- D-007 | One product, two modes: Hive (cloud SaaS) + Hive Enterprise (customer-hosted); single org = single tenant, departments via RBAC | vault project docs | 2026-07
- D-008 | Desktop must also provide a CLI environment (coding-agent CLI, Claude-Code-equivalent), following Codex's native cross-platform desktop model | owner 2026-07-19; research pending | 2026-07-19
- D-009 | Follow OpenAI Codex's native cross-platform (Windows/macOS/Linux) desktop + sandbox + CLI approach; research it and adopt or adapt rather than inventing a parallel design | owner 2026-07-19; vault plan-codex-crossplatform-desktop.md (pending research) | 2026-07-19
- D-010 | Windows desktop-sandbox UI isolation = DESKTOP-ONLY (private desktop on WinSta0, upstream/Chromium baseline), NOT a private window station: the least-priv account cannot CreateWindowStationW (ACCESS_DENIED gle 5), so per D-009 follow Codex; clipboard/global-atom isolation dropped (accepted tradeoff), hostile-child Low-integrity/SID-disable hardening deferred | vault decision-b1-desktop-only-2026-07-20.md, PR #403 (squash 61128551), issues #404-407 | 2026-07-20
- D-011 | Desktop sandbox stays NATIVE Windows for B2 (WFP egress, = Step 4): copy Codex's native account-SID (FWPM_CONDITION_ALE_USER_ID) WFP verbatim, cross-check vs Anthropic srt (Apache-2.0, same dedicated-user + WFP-SID + ACE model); reaffirms D-003/D-008/D-009 after buy-vs-build eval. We already fork codex-rs; Codex Desktop the product is NOT forkable (OpenAI-proprietary shell+backend). Codex's own Windows default is WSL2; native is the harder path we chose | owner 2026-07-20; memory reference_anthropic_srt.md + project_windows_sandbox_staged.md | 2026-07-20
- D-012 | Desktop sandbox backend is USER-selectable: Native Windows | WSL2, a Docker-Desktop-style toggle (cf. Docker's "Use the WSL 2 based engine"). WSL2 backend REUSES the existing linux.rs bwrap+seccomp+Landlock sandbox inside the user's WSL2 distro (= Codex's WSL2 model), NOT new confinement code. Same policy pair feeds both via a SandboxBackend seam; launch() dispatches on the user setting. Optional Hive-Enterprise admin pin to lock the choice (per D-006). Build order: finish B2 native FIRST, add seam + WSL2 backend AFTER | owner 2026-07-20; memory project_windows_sandbox_staged.md | 2026-07-20

- D-013 | Windows sandbox token follows ANTHROPIC SRT, not Codex, on the restricting-SID question: Codex's `RestrictingSids` set provably breaks Schannel/LSA RPC so no sandboxed HTTPS can work (openai/codex#17459 open; we inherited the bug by copying Codex). srt states it in code: "No RestrictingSids array - that breaks Schannel/LSA RPC". Adopt srt shape: `CreateRestrictedToken(LUA_TOKEN, SidsToDisable=[Administrators + logon SIDs], PrivilegesToDelete=all but SeChangeNotify, RestrictingSids=NULL)`, Medium IL, keep job + private desktop + WFP fence. Write containment therefore moves ENTIRELY to the dedicated account + NTFS ACEs, so hardening the sandbox-home ACLs (break inheritance, strip inherited Authenticated Users/Users) is a NON-OPTIONAL prerequisite that must land in the same change. Supersedes the copy-Codex-verbatim part of D-011 for the token only; WFP egress stays as-is. A TLS-terminating proxy does NOT avoid this (client still runs schannel to the proxy) | lab 2026-07-21 (row 5), vault lab-results-b2-wfp-egress-2026-07-21.md, srt vendor/srt-win-src/src/token.rs, openai/codex#17459 | 2026-07-21

- D-014 | PRIORITY PIVOT: native Windows sandbox hardening PAUSED, demoable desktop prototype first. Owner accepts the security posture Codex ships with and wants NO changes to how Codex or Claude Code themselves work. INVERTS D-012's build order: ship the WSL2 backend FIRST (reuses the existing working linux.rs bwrap sandbox inside the user's WSL2 distro, = Codex's own Windows default, no new confinement code), native B2 resumes later. PR #409 stays DRAFT and paused; row 5 (allowlisted-host TLS) was never measured, the validator harness blocks on a sandboxed whoami before any probe runs | owner 2026-07-21; supersedes the build-order half of D-012 | 2026-07-21
- D-015 | Windows sandbox restricted token HANGS LSA name resolution: a confined child's `whoami /user` blocks 10-20+ minutes, while the same account under the same WFP fence with an unrestricted token returns in 403ms, so the token is implicated and the fence is not. Our token matches upstream srt (vendor/srt-win-src/src/token.rs) EXACTLY, so this is not a divergence bug and D-013 stands as written. Any sandboxed code resolving a SID to a name by that path will block. NOTE: a third-party fork vendors a DIFFERENT srt shape (deny-only group SID, WFP keyed on that group) under vendor/srt-win, do not confuse the two | lab 2026-07-21 differential, PR #409 comment | 2026-07-21

(Older historical decisions to be backfilled from the vault + code ADR-comments in a lean sweep.)
