# Decisions Ledger (lightweight index)

Terse locked-decision index. ONE or TWO lines each, pointing to the detail (Obsidian vault doc, issue, or in-repo ADR like VENDORING.md). Detail NEVER lives here, to keep .wolf light and out of the system prompt.

PROTOCOL:
- Read this before any spec, plan, design, or implementation. Inject the relevant decisions into every subagent brief (do not rely on agents rediscovering them).
- When the owner states a decision (a "decision:" moment), append a terse entry here immediately with a new D-ID and a pointer to where the detail lives.
- Supersede in place: mark the old entry SUPERSEDED by D-NNN, keep the line for history.

Format: `D-ID | decision (1 line) | source | date`

- D-001 | RAG embedding: admin selects the model AND its dimension; the system provisions the pgvector column/index to match and re-embeds existing docs; NO truncation band-aid | vault plan-admin-embedding-dim.md, issue #392 | 2026-07-19
- D-002 | Desktop sandbox = vendored OpenAI Codex Rust sandbox (bwrap + process-hardening + bubblewrap), NOT Tauri's default | apps/desktop-sandbox/VENDORING.md | 2026-07-16
- D-003 | Windows sandbox: ADOPT the elevated codex-windows-sandbox (dedicated sandbox OS user + UAC elevated runner + capability-restricted token + ACL + WFP egress scoped to sandbox SID + ConPTY), staged; the minimal restricted-token base variant is insufficient for the agentic + CLI flow. Step 1 (CreateProcessAsUserW launch fix) is a prerequisite under any option | vault plan-codex-crossplatform-desktop.md, issue #393, VENDORING.md | 2026-07-19
- D-004 | Owner reviews no PRs; agent owns the full loop (spec, impl, adversarial review, verify, merge); security boundaries merge only after real validation | memory feedback_agent_owns_full_pr_loop.md | 2026-07-19
- D-005 | No band-aid workarounds; solve the real design | .wolf/cerebrum.md Owner preferences | 2026-07-19
- D-006 | Gateway fronts all model types with per-model-type admin-controlled posture (self-host vs serverless), not a global switch | vault decision-2026-07-17-gateway-posture-and-network.md | 2026-07-17
- D-007 | One product, two modes: Hive (cloud SaaS) + Hive Enterprise (customer-hosted); single org = single tenant, departments via RBAC | vault project docs | 2026-07
- D-008 | Desktop must also provide a CLI environment (coding-agent CLI, Claude-Code-equivalent), following Codex's native cross-platform desktop model | owner 2026-07-19; research pending | 2026-07-19
- D-009 | Follow OpenAI Codex's native cross-platform (Windows/macOS/Linux) desktop + sandbox + CLI approach; research it and adopt or adapt rather than inventing a parallel design | owner 2026-07-19; vault plan-codex-crossplatform-desktop.md (pending research) | 2026-07-19
- D-010 | Demo/staging box = owner's physical lab PC wiped to Ubuntu 26.04 server (12GB RAM, SSD, 24/7), ingress via Cloudflare Tunnel (no port forwarding); replaces the OCI-A1/Hetzner cloud-VM hunt | memory project_physical_demo_box.md | 2026-07-26
- D-011 | Redis on the new demo/staging box is self-hosted, not managed Upstash (owner: Upstash awkward/expensive to pay for) — SUPERSEDES the Upstash-only staging mandate (sized for a 1GB VM; this box has 12GB) | memory project_staging_redis_upstash.md | 2026-07-26
- D-012 | Server-side agent sandbox stays Apptainer rootless, no Firecracker/gVisor/E2B; cross-tenant isolation boundary moves to the worker VM (never co-schedule two tenants on one worker), not the sandbox layer, because rootless Apptainer cannot apply seccomp (host syscall surface stays full) | PR #465, issue #308 | 2026-07-27
- D-013 | REVOKED 2026-08-11 by D-036 (owner decision). Original entry, kept for history: Agent-workspace OWUI sidebar nav: NOT forking Open WebUI (confirmed zero plugin nav slot in compiled Svelte, permanent upstream-tracking burden disproportionate to one nav link); ship "seamless" by polishing the existing floating launcher (loader.js) to match OWUI's native header icon-button visual language instead of a real DOM nav item. The no-fork constraint no longer holds anywhere: Open WebUI IS forked and heavily modified. Do not cite this entry, and do not reconstruct the rule from the comments that used to restate it | owner 2026-07-27, PR #461 follow-up; revoked by owner 2026-08-11 | 2026-07-27, revoked 2026-08-11
- D-014 | Enterprise admin-settings split: web-console/control-plane is sole source of truth for model visibility, feature gates, RBAC, billing, API keys; OWUI's own admin panel stays disabled (env flags off), Caddy blocks mutation verbs on /api/v*/configs*, /admin*, /users/*, /groups*, /roles*, /functions*, /models* | owner 2026-07-27 audit | 2026-07-27
- D-015 | Public artifact link tier: build it, owner-toggled visibility; live-data-binding and public-visibility modeled as mutually exclusive (viewer-identity resolution is self-enforcing) | vault decision-2026-07-27-competitor-parity-build-rulings.md | 2026-07-27
- D-016 | Remote MCP by URL: build it behind tiered tenant settings (open / local-only / allowlist / admin-approval, default admin-approval); egress-proxy logging, SSRF/DNS-rebinding URL validation, per-tenant OAuth token storage retained | vault decision-2026-07-27-competitor-parity-build-rulings.md | 2026-07-27
- D-017 | Scheduler: build one UI, one radio at creation time, tiers are execution loci not deployment modes; server-side runs on whichever server the user is connected to (Hive Cloud or the customer's own Enterprise box), desktop-app execution runs on the user's own machine with local file access; Enterprise users get both, not on-box only | vault decision-2026-07-27-competitor-parity-build-rulings.md | 2026-07-27
- D-018 | bypassPermissions equivalent: build it behind tenant settings; default off, tenant-set ceiling, no user self-elevation, every elevation audited to the human principal | vault decision-2026-07-27-competitor-parity-build-rulings.md | 2026-07-27
- D-019 | Plugins: ONE registry, one manifest, one capability rule across developer console and chat/agent surfaces; do not fork into two systems | vault decision-2026-07-27-competitor-parity-build-rulings.md | 2026-07-27
- D-020 | Memory: ONE subsystem covering recall/forget/improve/remember scoped per person, chat-history search is a capability of it not a second store; build a thin memory schema on Hive's own Postgres+pgvector borrowing cognee's model (4-verb API, dataset-as-scope, session-cache-then-graph fallback), do NOT take cognee as a dependency (Apache-2.0 passes licence gate and Postgres-only mode works, but pure-Python second runtime + pre-1.0 churn + no verified entity-level purge API are the real reasons) | vault decision-2026-07-27-competitor-parity-build-rulings.md; scratchpad research-cognee-memory.md | 2026-07-27
- D-021 | Screen-driving Computer Use: DEFERRED to a future version, not dropped; audit/blast-radius story unresolved today | vault decision-2026-07-27-competitor-parity-build-rulings.md | 2026-07-27
- D-022 | Demo box web-console container running `npm run dev` (Dockerfile.web-console last line) is INTENTIONAL for now, not a defect; do not file it as a bug or switch it to a production build without owner sign-off | owner 2026-07-27 | 2026-07-27
- D-023 | Signup tenant provisioning must be guaranteed by shipped code, NOT a Supabase dashboard Database Webhook (none exists live; 11/19 users membership-less, 92 auth 500s in 24h). Design: custom_access_token_hook stops raising and issues a token with no tenant claim; control-plane reconciles idempotently via the existing signup/webhook.go logic refactored into one shared function; a tenant-less token must be inert everywhere except provisioning; auto-create-tenant respects deployment posture (cloud self-serve vs administered self-host) | owner 2026-07-27; scratchpad auth-failure-diagnosis.md | 2026-07-27
- D-024 | Absolute URLs in Next.js route handlers must derive origin from lib/http/origin.ts, never from request.url (Next builds it from the server's own --hostname/--port, leaking 0.0.0.0:3000 out through Caddy and Cloudflare Tunnel). experimental.trustHostHeader is REJECTED as the fix: it hardcodes https and trusts an attacker-reachable Host header, the open-redirect shape origin.ts exists to prevent. Fifth occurrence of this family (#157 added the helper, #438 fixed only the path half), so a guard test against future occurrences is required | owner 2026-07-27; scratchpad auth-failure-diagnosis.md | 2026-07-27
- D-025 | .wolf/ splits by writer: anything a hook rewrites on every tool call is gitignored (anatomy.md, memory.md, token-ledger.json, hooks/_session.json, buglog.json), only deliberately curated files stay tracked (cerebrum.md, decisions.md, buglog.jsonl, GOAL.md, fleet.json, cost-ledger.md, hooks/*.js). Bug memory becomes append-only .wolf/buglog.jsonl with merge=union in .gitattributes, so parallel branches merge appends automatically; buglog.json survives only as a generated aggregate so `openwolf bug search` keeps working. anatomy.md is untracked rather than throttled because it is fully derivable from the working tree and its per-call rewrite stamps a fresh timestamp, so no "only when materially changed" gate can stop the churn | owner 2026-07-27; branch chore/wolf-telemetry-untrack | 2026-07-27
- D-026 | `cp-hive.scubed.co` and `agent-hive.scubed.co` are DECOMMISSIONED (NXDOMAIN, verified 2026-07-28); canonical demo hosts are console-hive, chat-hive, api-hive, control-hive. The cp-hive strings still present on main are deliberate (the RETIRED_HOST guard in control-plane-host.test.ts, the deploy/cloudflare/README retired section, the buglog entry): they prevent reintroduction, so do not "clean them up" | PR #566, memory project_demo_subdomain_inventory.md | 2026-07-28

- D-027 | Backend chat metering enforced at the edge-api dispatch boundary (not middleware, not control-plane-only), tenant-settings precedence (ENABLE_USAGE_METERING beats principal-type defaults), fail-closed on unresolvable billing state except Enterprise shadow mode | vault spec-2026-07-28-backend-metering.md | 2026-07-28

- D-028 | RETIRED 2026-08-16 by D-040 (owner decision). Original entry, kept for history: Chat front-end: Open WebUI is REMOVED, LibreChat replaces it. Licensing is the gate: LibreChat is plain MIT (never relicensed, `spdx_id: MIT`) so it can be rebranded and resold inside Hive Enterprise, while LobeChat's LobeHub Community License clause 1(b) requires a paid licence from LobeHub LLC to distribute a derivative work, which is exactly what Hive Enterprise is (also `NOASSERTION`, relicensed twice, clause 2(a) reserves unilateral retightening). The incumbent is itself license-blocked: `Dockerfile.open-webui` documents that our branding patch is lawful only under a 50-users-per-30-days carve-out, which breaks on BD launch or any Enterprise sale. MongoDB entering an all-Postgres stack is the ACCEPTED cost, taken as isolation from the shared Supabase pooler. LibreChat's own RAG API and vector store are NOT deployed (rag_api pins its dimension at collection creation, contradicting D-001); `/v1/rag/chat` is exposed as a custom endpoint instead. The Supabase minimal-claims problem does NOT disappear, it moves from a vendored psycopg2 monkeypatch into `tenant_fallback.go` | vault decision-2026-07-29-chat-frontend-librechat.md, plan-2026-07-29-librechat-migration.md; retired by owner 2026-08-16 | 2026-07-29, retired 2026-08-16
- D-029 | MOOT since 2026-08-11: its subject D-013 is revoked outright by D-036, so there is no longer a no-fork rule to scope. Original entry, kept for history: D-013 (do not fork Open WebUI for a nav slot) is SCOPED TO OPEN WEBUI, not superseded: its rationale was Open WebUI's compiled Svelte plus its licence position, neither of which applies to MIT-licensed LibreChat, so the agent-workspace nav question is re-openable on the new front-end | vault plan-2026-07-29-librechat-migration.md section 1.4 | 2026-07-29

- D-030 | LibreChat authenticates to edge-api on API-key path (`Bearer hk_*`); chat lands on metered orchestrator. Per-user credentials REQUIRED (billing+visibility). Explicit Hive→LC user-id mapping, no tenant_id column. Tenant-on-key: add TenantID to AuthSnapshot. Task zero: assert api_key_id recorded, not chat success. Estimate: two-stage (cheap pre-check, then real via SelectionResult.Pricing + math/big). Below-threshold users: mechanical ledger query. Three prerequisites in order: seed+test (assert api_key_id), tenant-on-key PR, proportioned estimate. Unresolved blocking: credit unit (per-token vs per-million-tokens) | vault design-2026-07-30-lc7-principal-resolution.md | 2026-07-30

- D-031 | Credit unit: per million tokens, not per token. Formula: `(prompt_tokens * input_price + completion_tokens * output_price) / 1_000_000` with round-half-up via math/big. Already implemented in precedence.go:207-227. Per-token integer pricing misprices 347 of 365 models by 1000x+; per-million int64 pricing fits all real rates (0.01-150) and matches upstream canonical unit and console display. Follow-ups: issue #617 catalog reprice, numeric(20,6) column audit | vault decision-2026-07-31-credit-unit-per-million.md | 2026-07-31

- D-032 | AMENDED 2026-08-01 by owner ruling: one model, one price. Keep it as simple as possible, because complex solutions are difficult to maintain later; simplicity always wins. Credit price STAYS on `model_aliases`, at the ALIAS level, as bigint credits per million tokens with separate input/output columns. No `pricing_unit` column, and no price columns on `provider_routes`. Each alias maps to exactly ONE enabled route, so the price is unambiguous by construction rather than by lookup: `hive-fast` pins to Groq `openai/gpt-oss-20b` and its second route is disabled. Margin unchanged: stored price = provider list cost times 1.4, rounded up to a whole credit per million, with the multiplier and the rate fetch date recorded in the migration header; that rounding happens once at authoring time and does not govern per-request settlement, which stays D-031's round-half-up in precedence.go, so the two never compete. precedence.go arithmetic is untouched. Fail-closed is retained: an alias with no usable price is not selectable and never silently bills zero. SUPERSEDED design (price per route on `provider_routes` plus a `pricing_unit` column, keyed on `route_id`): it was wrong because it MODELLED an ambiguity that should have been REMOVED. `hive-fast` served two enabled routes whose real cost differs by roughly ten times, and the fix is to stop an alias having two prices, not to build schema that can express two. Justifying finding: there was never any failover to lose. `SelectRoute` populates `FallbackRouteIDs` but nothing reads it, no dispatcher retries a second route, and `health_state` has no automated writer, so a provider outage would not flip a route out of `healthy`. Groq already won on priority 10 against OpenRouter's 20, so the second route was never reached in practice; it only made the alias unpriceable. Shipped as PR #651 (`c91cee2b`), migration 20260801_01_alias_pricing_correction.sql; placeholder prices were roughly 1300x to 7500x too low | vault decision-2026-07-31-per-route-pricing.md, issue #617 | 2026-07-31, amended 2026-08-01
- D-033 | AMENDED 2026-08-01 to drop the reference to D-032's `pricing_unit` column, which was never built. Substance unchanged: non-token modalities (TTS priced per character, STT per audio duration) get NO invented per-token price, and a pricing row whose unit does not match what its handler meters must be REJECTED rather than silently mispriced. The mechanism that carries that unit is left open, since the per-route column D-032 originally proposed no longer exists; whatever carries it, the rule is the rejection, not the column. `internal/audio` still never reads the catalog at all, charging flat literals (1000 credits speech, 500 credits transcription), tracked in issue #627; migration 20260801_01 therefore deliberately left `hive-stt`, `hive-tts` and `hive-embedding-default` prices alone, since repricing them would move a console display without moving a charge. Embedding pricing needs separate determination since OpenRouter excludes embedding models from its listing | vault decision-2026-07-31-non-token-modality-pricing.md, issue #627 | 2026-07-31, amended 2026-08-01
- D-034 | Money path fails closed: never serve a request or claim an entitlement delivered unless the authorizing accounting step actually succeeded; failure classification switches on status codes, never string-matching; an unrecognized failure is fatal; a reservation reaches a terminal state exactly once; a payment intent is not complete unless the credit grant succeeded, and a failed grant never returns 2xx. Verified live 2026-07-31: three days of unmetered free usage (409 missed by string match), 154000 credits stranded across 32 abandoned holds (discarded finalize error), and a false-complete payment intent that suppressed a recoverable retry | vault decision-2026-07-31-money-path-fail-closed.md, issues #626 #616 #628 | 2026-07-31
- D-035 | REVOKED: the "never show FX rates, currency-exchange language, or USD equivalents to BD customers, omit amount_usd from BD responses" rule (CLAUDE.md Regulatory Rules, Phase 17/#137, FX-17-xx guard tests). Owner: it was never an actual regulatory requirement, and a USD amount reaching the customer is fine; do not reintroduce it or "hallucinate it back" from old comments/tests. Currency presentation to BD customers is now an unconstrained product decision. Scope of this decision: documentation (CLAUDE.md, README.md, apps/web-console/docs/STYLE_GUIDE.md) and test assertions that banned FX/USD strings on customer surfaces were updated/removed; production code that strips amount_usd or computes FX (payments/stub, invoices/pdf.go assertNoFXLeak+sanitize, lib/format/money.ts, budget_gate.go, checkout_return.go, client.ts) was deliberately left unchanged (behavior-neutral cleanup), relaxing that code is a separate future product decision | PR chore/revoke-fx-display-rule, memory feedback_bdt_no_fx_display.md (superseded) | 2026-08-08
- D-036 | Open WebUI IS FORKED and heavily modified. REVOKES D-013 and moots D-029, and with them the whole "no-fork, patch the pinned bundle only" posture: the exact-literal bundle rewrites under `deploy/docker/owui-patches/` and the `custom.css` plus `loader.js` static-hook pair are the OLD ceiling, not a rule, and no agent may cite them to refuse fork work. Target architecture in the owner's words: "one web, one shell, two embedded panels". The product is NOT keeping stock Open WebUI. Quality bar is explicit: a polished rebuild that reads as years of work, not something assembled in one night; polish is a merge requirement here, not a follow-up. Owner states this instruction was given before and not followed, and that is accepted as accurate. OPEN QUESTION FOR THE OWNER, not resolved here: this is in obvious tension with D-028, which records replacing Open WebUI with LibreChat. That epic is on hold until after the demo by separate owner instruction, and the owner has said it can be discussed later, so D-028 is deliberately NOT revoked. Whoever picks the fork work up must raise the conflict with the owner rather than pick a side: heavy investment in a forked Open WebUI and a later migration off it cannot both be cheap | owner 2026-08-11 | 2026-08-11
- D-037 | Formalized the coding pipeline every dispatched builder subagent must run: context, plan, spec, implement, open the PR before review (not after), adversarial review as two independent streams (CodeRabbit CLI as stream 1, domain matched peer review as stream 2), address every posted comment, verify, merge, deploy. Lives as a numbered section in `.claude/rules/orchestrator.md`; the shared `adversarial-pr-review` skill (`~/.claude/skills/adversarial-pr-review/SKILL.md`, used across all projects, not just hive) now names those two streams concretely and defaults to posting findings on the PR in pipeline mode instead of only when asked. SUPERSEDED in part by D-038, which replaced the fixed two-stream shape with a dynamic selector; the rest of the pipeline stands | PR #887, global `~/.claude/CLAUDE.md` Development Workflow Phase 2/4/5 | 2026-08-11
- D-038 | Superseded D-037's fixed two-stream adversarial review with a dynamic selector: always on, every PR: CodeRabbit CLI (skip and say SKIPPED if rate limited or auth fails, never treat a blocked run or the separate GitHub App bot's pass as clean), `ecc:code-review`, and a plain ticket-first adversarial pass with no specialized tool; added by diff shape: domain specialist skills plus the matching language reviewer, mandatory security/database review on auth or money or input-parsing paths; added only for diffs that already triggered that mandatory security review: `/codex:adversarial-review` (a genuinely different LLM's opinion, kept distinct from and not merged with `adversarial-pr-review`, see D-039). New "Gate Compliance" rule added to global `~/.claude/CLAUDE.md`: fulfill a fact-forcing or skill-prerequisite hook honestly and retry, never route around it or disable it without explicit owner instruction, escalate only on a genuine tool-capability gap (confirmed that session: `doc-updater` lacks the Skill tool and cannot satisfy the `skill-router.js` prerequisite gate on `CLAUDE.md`/`SKILL.md` paths, use `general-purpose` instead). Executed via `superpowers:subagent-driven-development`, hive half shipped as PR #889 | vault spec-2026-08-12-coding-harness-revamp.md, plan-2026-08-12-coding-harness-revamp.md, PR #889 | 2026-08-12
- D-039 | `skill-stocktake` verdict (scoped comparison, not a full inventory sweep): `adversarial-pr-review` and the `openai-codex` plugin's `adversarial-review`/`review` commands are kept, not merged or retired. Distinct roles: `adversarial-pr-review` is the orchestration framework (ticket-first, multi-stream selection, orchestrator verification, PR posting mechanics); `codex:adversarial-review` is one specific reviewer engine (a different LLM's opinion via `codex-companion.mjs`, review only, never fixes), now formally wired in as D-038's optional high-stakes stream rather than a competing, overlapping concept. `/harness-audit` baseline recorded same session: 26/39 (repo, consumer mode) on hive. That score is a session self-report with no artifact committed to this repo, so treat it as a baseline claim rather than a verified measurement. Zero on Memory Persistence and Eval Coverage are rubric-naming mismatches, not real gaps: the audit looks for `.claude/memory.md`/`docs/adr/`/`evals/` specifically, while hive's actual durable memory lives in `.wolf/` (cerebrum.md, decisions.md, buglog.jsonl) plus the Obsidian vault, and its actual test coverage is the Go/TS suites under `apps/*`, neither renamed to match the rubric's expected paths. Security Guardrails (7/10, missing `SECURITY.md`) and GitHub Integration (3/10) are the two categories with a plausible real gap, left as a future task, not actioned there (out of that plan's scope) | that session's task-4 run, no PR (informational) | 2026-08-12
- D-040 | The Open WebUI fork IS the product's shell, and D-028 (remove Open WebUI, LibreChat replaces it) is RETIRED, not merely on hold. This answers the open question the 2026-08-11 fork entry (D-036) deliberately left for the owner: that entry recorded the fork as real and heavy, declined to revoke D-028, and told whoever picked the fork work up to raise the conflict rather than pick a side. The conflict was raised on 2026-08-16, naming D-028 directly, and the owner chose the fork. What forced the question: the UI redesign spec puts the product's identity inside the fork across waves 1 through 5 (fork and identity, shell, runs become conversations, control, depth), which cannot be cheap if the front end is later replaced. Owner's stated reasoning: five users, a controlled deployment environment, and a licence position he considers permissive for what he wants. Consequence to plan around: the LibreChat migration is not deferred, it is expensive to revive; no agent should plan on it returning or hedge a design against it | owner 2026-08-16; vault spec-2026-08-16-hive-ui-redesign.md section 9 | 2026-08-16
- D-041 | The Open WebUI branding carve-out is ACCEPTED RISK, with no gate and no tracking issue. Constraint as documented in `deploy/docker/Dockerfile.open-webui`: the image strips the "(Open WebUI)" suffix upstream appends to any custom WEBUI_NAME, which Open WebUI License clause 4 permits only while a deployment stays at or under 50 end users per rolling 30 days, or holds written vendor permission, or holds an enterprise licence. That carve-out holds at today's five users, and stops holding on a Bangladesh launch or any Enterprise sale. Recommendation put to the owner on 2026-08-16: track it as a hard pre-launch gate. Owner's decision: no gate, accept the risk | owner 2026-08-16; deploy/docker/Dockerfile.open-webui header comment | 2026-08-16
- D-042 | Visual-proof screenshots posted to a PR must go through `scripts/post-pr-visual-proof.sh` (skill `.claude/skills/pr-visual-proof.md`), not a hand-committed `docs/proof/` image linked by a `raw.githubusercontent.com` URL pinned to the PR's own branch name. Empirically raced four candidates on scratch PR #959 with real browser screenshots, logged in and out, rather than assumed: a branch-name-pinned raw link renders fine until the branch is deleted, then 404s immediately (confirmed by `curl` showing a cache MISS 404 straight from origin seconds after `git push origin --delete`), which is what this repo's squash-and-delete-branch merge policy does to every PR branch on merge, and is the PR #867 failure repeated; a `blob/` link never renders inline at all, as a markdown image or a bare link (confirmed broken-image icon); a `data:` URI embedded in the comment body is stripped by GitHub's markdown sanitizer (confirmed broken-image icon); a release asset (`releases/download/...`) renders inline and is not reachable through any branch, so branch deletion cannot touch it. A commit-SHA-pinned raw link also survives branch deletion (GitHub retains a PR's head commit via its own internal ref indefinitely, open or merged), but was rejected as the standard since it depends on undocumented retention behavior and is one slip (branch name instead of SHA, exactly PR #867's mistake) from repeating the failure; the release asset has no such distinction to get wrong. The true drag-and-drop attachment upload (`github.com/user-attachments/assets/...`) is confirmed unreachable by REST or GraphQL (404 on the plausible REST path, no matching GraphQL `Issue` field): web-UI only, session-cookie and CSRF authenticated, not driven by this or any script. The script uploads to one permanent, never-deleted release tagged `visual-proof-assets`, created once and reused by every future proof; a leaked asset is removed individually with `gh release delete-asset`, never by deleting the whole release | PR test/proof-mechanism-race (#959, closed, scratch), scripts/post-pr-visual-proof.sh, .claude/skills/pr-visual-proof.md | 2026-08-17
- D-043 | The web-console production build already shipped and is already the live demo box service; the part of D-022 forbidding that switch is SUPERSEDED, not by this entry's own action but by prior merged work this entry only now records. PR #456 (2026-07-26) added `Dockerfile.web-console.prod` (`next build` + `next start`) and the `web-console-prod` compose service; PR #605 (2026-07-31) gated the old `npm run dev` `web-console` service behind an explicit `dev` compose profile, so it no longer starts under `local`, `chat`, or `enterprise`. `Caddyfile.console` proxies `console-hive.scubed.co` exclusively to `web-console-prod:3000`, and the demo box's only `docker compose up` invocation (`deploy-demo-box.yml`) passes `--remove-orphans`, so no leftover dev container can serve traffic. Re-verified live on 2026-08-18, read only: `/`, `/auth/sign-in`, and `/oauth/consent` all answered in 0.28 to 1.5 seconds across repeated requests with `cf-cache-status: DYNAMIC` (Cloudflare is not masking origin latency with a cached response), the served HTML carries no dev-mode HMR or `next-dev.js` markers, and the most recent `deploy-demo-box` run (2026-08-17 20:30 UTC) was green. Issue #967 documents a real intermittent 20 to 30 second stall on this same path from OWUI-nightly run 32112158077 (2026-08-18 07:46 to 07:48 UTC) and attributes it to dev-mode compile-on-demand, citing D-022 and the now-superseded `Dockerfile.web-console`; that attribution does not hold against the container actually serving traffic, since `web-console-prod` is a `next start` process with no compile-on-demand behavior. No code change was made in this session, because the dev-to-prod switch it was scoped to make had already shipped three weeks earlier; the real cause of the intermittent stall in #967 is still open and needs investigation that does not assume dev mode | PR #456, PR #605, issue #967, PR docs/web-console-prod-already-shipped (this session's live verification, docs-only) | 2026-08-18
- D-044 | Open WebUI is reduced to a view, not the system. Every controllable surface, every admin setting and every configuration knob moves to the control plane (code we own and review); only the user settings deliberately chosen for exposure stay visible in the Open WebUI UI, and even those are backend owned, with the backend as the source of truth rather than Open WebUI's own database. Open WebUI becomes one of several clients onto Hive instead of being the system itself, is to be dismantled gradually, and the product is to stop resembling Open WebUI structurally and visually; the owner delegated the specific sequencing and scope choices within those parameters. Concrete trigger and worked example, kept because it is the evidence: the owner raised a default token budget, and a repo wide search found it nowhere, no commit, no patch under `deploy/docker/owui-patches/`, no LiteLLM config parameter, no environment variable. It almost certainly lives in Open WebUI's own admin per model Advanced Params, a runtime database row invisible to git. That means a real configuration change to a running system was unreviewed, unversioned, unreproducible in a fresh environment, and silently revertible by a volume reset; PR #737's frozen embedding model bug had the same shape. Does NOT revoke D-036 or D-040: the fork stays the product's shell and the LibreChat migration stays retired. What changes is the division of responsibility inside that fork, not whether the fork exists. D-036 established the fork is real and heavy and that the old patch only ceiling does not bind; D-044 says the fork is a presentation layer whose state and control surfaces belong to the control plane. Standing constraint any implementer must know: `deploy/docker/Dockerfile.open-webui` builds only the frontend from `vendor/open-webui` and replaces `/app/build`, while the backend comes from a pinned upstream image, so backend edits under the vendored tree are inert unless applied through `deploy/docker/owui-patches/`. Implementation plan lives separately: vault plan-2026-08-18-openwebui-strangler.md | owner ruling, this session; D-036, D-040, PR #737; vault plan-2026-08-18-openwebui-strangler.md | 2026-08-18
- D-045 | The agent surface has NO navigation row of its own, and "Knowledge" is replaced by Projects plus Artifacts. Two owner rulings, 2026-08-23. Ruling 1: Cowork is a mode toggle inside the chat composer, rendered as `Chat | Cowork` immediately right of the plus button in the same pill row as the model selector. It is not a destination called "Agents" and not one called "Work". Switching it changes what the next message does; it does not navigate. An agent run IS a conversation: it appears in the same conversation list with the same row styling as a chat, opens in the same main pane, and renders in the same transcript component. Because there is no separate destination there is no separate origin, no separate session, no iframe and nothing to authenticate into. Agent progress renders as a right hand contextual panel in the same window with collapsible sections: Progress (a small step chain), Working folder (files created during the task), and Context (tools and referenced files). Tool calls appear inline in the transcript as muted lines, for example "Used Claude in Chrome (2 actions)", interleaved with the assistant's prose. Ruling 2: "Knowledge" is eliminated rather than renamed, and so is the Workspace container that held it. Context brought in lives in Projects, which is where RAG documents belong; things produced live in Artifacts, a first class navigation destination rendering a card grid with All, Yours and Shared tabs. There is no "Knowledge" nav item and no "Workspace" anywhere in the product. Supporting observation from the reference, used but not a third ruling: the sidebar is short and flat (New, Projects, Artifacts, Scheduled, Customize, then a More collapse, then pinned items, then conversations), and every destination renders in the SAME shell with the same sidebar and chrome, so nothing ever swaps the frame. Concrete trigger: the owner reviewed the live demo box and rejected the shipped surface outright, naming a left nav that still says "Agents", a page behind it that requires separate authentication, is still an iframe and does not look like the chat interface, and a "Knowledge" row that is Open WebUI's Workspace with a rename; his summary was that our designs are horrible. The reference is the Claude Desktop screenshot set the owner supplied, which is his personal desktop and is deliberately not copied into this repo. Consequences an implementer must plan around: the agent surface gets no route, no nav entry and no login of its own, so `apps/agent-console`, every `/agent-workspace` path and Caddy route, and the separate Supabase session go away rather than being restyled; the composer, the transcript and the conversation list are the only places agent work appears; the run panel is a panel in the same window and never a page; Projects and Artifacts are the only two content destinations. Does NOT revoke D-036 or D-040: the Open WebUI fork remains the product's shell and the LibreChat migration stays retired. Direct continuation of D-044's instruction that the product stop resembling Open WebUI structurally and visually, where D-044 moved control and configuration surfaces into the control plane and D-045 settles what the remaining user facing shell is shaped like | owner rulings, this session; D-036, D-040, D-044; owner supplied Claude Desktop reference screenshots (personal, not in repo); vault spec-2026-08-23-agent-surface-ia.md, corrected vault spec-2026-08-16-hive-ui-redesign.md | 2026-08-23

- D-046 | Credit unit rescale: 1 USD equals 1,000,000,000 Hive credits (was 1,000,000). Schema migration scales every credits column x10000, metadata stamp `credit_unit=v2-1usd-1e9` marks the boundary. Rationale owner: this will help calculate usage better. Shipped as PR #1106 with exact-magnitude ledger proof (2783 credits billed, arithmetic verified). Subsequent pricing work stays per-million-tokens formula per D-031 | PR #1106, supabase/migrations/20260823_40_credit_unit_rescale_billion.sql, digest section 3 | 2026-08-23

- D-047 | Model routing and pricing tiers, owner rulings 2026-08-23. hive-auto maps to OpenRouter Auto Router (real upstream routing, not hive-auto itself). hive-default maps to deepseek-v4-flash. Both hive-auto and hive-default were converted to fixed price at exactly half their current effective customer price per the free-route half-price bound (D-048). CI tests and all daily automated consumption MUST use hive-free alias, not hive-auto or hive-default. Rationale owner: use hive-free to combat the budget drain. Related: hive-auto previous behavior billed at actual upstream cost since #1012, so half of a free upstream was zero; the fixed-price conversion resolved that ambiguity. Before/after rate table in PR #1106 body. Consequence: chat CI lanes and daily inference caps no longer consume developer budget | PR #1106, digest section 3 item 3, digest section 5 items 1-3 | 2026-08-23

- D-048 | Free pool infrastructure and customer-facing pricing, owner rulings 2026-08-23. Free pool: four Hive-owned keys behind load-balanced failover (two free Groq text keys, OpenRouter free key, Gemini key) routed via a LiteLLM `route-free-pool` endpoint. Customer-facing Free Router endpoint charges 1,000,000 credits input / 4,000,000 credits output per million tokens (a few credits per typical request), priced to cover server cost. CI and daily lanes consume the free pool (hive-free alias per D-047), never the customer endpoint. Free-pool failover and per-key exhaustion handled by LiteLLM orchestration. Half-price bound applies only to pricing (halve the rate, never widen which token classes are billed; fail closed: no served request bills zero, no shape bills more than half the old rate, including cached reads). Shipped as PR #1115 with routing logic | PR #1115, digest section 3 item 4, digest section 5 item 4 | 2026-08-23

- D-049 | Agent concurrency ceiling: 200 active agents maximum (raised twice during session: 10 active -> 20 active -> 200 active, 200 total including idle). This is a resource and orchestration limit, not a per-user or per-tenant cap. No tracking issue opened; rule lives in the orchestrator brief language | digest section 5 item 7, concurrent dispatch capacity setting | 2026-08-23

- D-050 | Merge and review policy, owner ruling 2026-08-23. Merge the existing backlog before starting new work. Every PR must have adversarial review (two independent review streams per D-037/D-038/D-039, always on, dynamic selector, every PR). After review and green checks, merge with zero unresolved threads. Related to standing coding pipeline (D-037): context, plan, spec, implement, open PR before review, adversarial review, address comments, verify, merge, deploy. Standing test rule (D-051): new control -> test before merge; behavior change -> test changes; removal -> test removed. Related standing interaction coverage standard (D-051): 100 percent interaction coverage is the requirement | digest section 5 items 8-9, orchestrator.md coding pipeline section, .claude/rules/orchestrator.md | 2026-08-23

- D-051 | Test coverage and performance optimization, owner rulings 2026-08-23. Interaction coverage: every control surface in the application must be tested, everything clickable on a page must be tested and have a reason to exist, with tests written for it. 100 percent interaction coverage (per surface, achieved on web-console via gate #811 and live verification, 99.0% coverage, 326 interactions). Performance: investigate better indexing for database optimization; deploy local Redis read-through cache on hot routing and catalog reads (shipped PR #1130, 03:05Z session time); speed work in scope. Standing test rule: if we change a behavior we must change the test too; if we add something new we need tests; remove something then also need tests. Gate merged #811, coverage instrumentation #1131, Redis cache #1130, money-path tests #1137, behavioral money coverage #1142 (still open) | PR #811, #1130, #1131, #1137, #1142, digest section 4 | 2026-08-23

- D-052 | Chat frontend policy consolidation, three owner rulings on 2026-08-23. Ruling 1: Upstream tracking of vendor/open-webui is REVOKED. We are heavily modifying and gutting it, going ahead of upstream, so do not keep their package.json or upstream pins. Consequence: chat-frontend deps join the latest-stable sweep; the frontend-only image build mechanism stays true but the policy of avoiding backend edits to stay mergeable is gone. Patch drift risk is no longer a reason to refuse a backend change. Ruling 2: Open WebUI name goes away everywhere in phases (visible brand first via env if one exists, then docs, then internal identifiers bundled into waves already touching those files). Naming sweep inventoried 2026-08-23: 10,413 matches repo-wide. Visible brand already env-controlled (WEBUI_NAME compose var); residual leaks in constants, strings, manifests. Phases coordinated with D-044. Ruling 3: ALL dependencies go to latest stable before deploy. Owner cites exploitable old versions. Node 24 named as target; go.work 1.24 pin raised; @supabase/ssr breaking cookie changes handled deliberately, not swept into group bumps | 2026-08-23 owner rulings; cerebrum.md Rulings 108-113; charter decisions at wave scope in vault spec-2026-08-16-hive-ui-redesign.md | 2026-08-23

- D-053 | Spinifex deployment: plan-only, not adopted. AWS/Spinifex (github.com/mulgadc/spinifex) terraform/ansible approach is to understand scope and what needs doing; do NOT standardize on it or adopt it into the shipped product yet. Terraform/ansible plan targets real AWS first plus Spinifex second; demo box is already the target architecture (Ubuntu 26.04, physical lab PC, 12GB RAM, self-hosted). Rationale owner: understand the landscape. This decision leaves the door open for future adoption if the research changes the assessment | digest section 5 item 14 | 2026-08-23

- D-054 | Hook changes scoped to hive repository only, owner ruling 2026-08-25. Changes to `.claude/hooks/` and `.wolf/hooks/` are in scope for hive agents. User-level hooks in `~/.claude/` are off-limits. Plugin hooks under `~/.claude/plugins/cache/` are off-limits. ECC environment switches in `~/.bashrc` are off-limits. Rationale owner: that layer is shared across every project on the machine and stays under owner's personal control. Prohibition includes merely disabling a noisy gate via environment variable; if a gate is noisy the answer is to address the underlying issue in the project config (the repo-local hooks), not to silence it globally. Blocks: hook unification work, testing infrastructure refactor that touches the plugin layer, ECC profile customization | this session owner ruling, 2026-08-25 | 2026-08-25

(Older historical decisions to be backfilled from the vault + code ADR-comments in a lean sweep.)

- D-055 | Billing constraint on token classes, owner ruling 2026-08-23T18:46Z (free-route half-price dispatch brief). "Halve the rate only, never widen which token classes are billed. Fail closed: no served request bills zero, no shape bills more than half the old rate — including tool-call-only turns with no usage block, cached reads, and non-token modalities." This is the standing bound written because a prior vague money brief caused a 262x overcharge. Consequence: cache-aware pricing work (correcting the RATE applied to an already-billed class like input_tokens) is consistent with D-055. Beginning to bill a class that was previously unbilled (e.g. cache_read_tokens starting to generate a charge) would violate this constraint. Applies to in-flight work on feat/cache-aware-billing and feat/anthropic-cache-control branches. Per D-031 the unit is per-million-tokens; per D-032 simplicity is enforced; per D-033 non-token modalities are rejected when unit mismatches what the handler meters. This entry gates all future billing schema and handler changes | digest section 3, free-route brief timestamp 2026-08-23T18:46Z | 2026-08-23

- D-056 | `usage_events.cache_write_tokens` cutover, PR #1157. Before this PR's deploy, cache-write tokens were never metered anywhere in edge-api, so every stored 0 in that column is a bug artifact (nothing was measured), never a real zero. From this PR's deploy onward, a stored 0 in that column is a real measured zero. The two are byte-identical in the row itself with no version or epoch marker to tell them apart; a billing dispute on a request straddling the deploy must treat pre-deploy 0 as unknown, not verified-zero. This corrects the RATE applied to an already-billed class (cache tokens were already part of the flat-priced `prompt_tokens` total pre-fix, just at the wrong rate), so it is consistent with D-055's bound, not a violation of it: D-055 forbids beginning to bill a previously-unbilled class, and this PR bills no new class, only re-prices one already inside the charge. Detail: vault spec-2026-08-25-cache-aware-billing.md | PR #1157 | 2026-08-25

- D-057 | UI dependency evaluation for console and marketing surfaces: five libraries reviewed (Watermelon UI, motion-primitives, GSAP, Tremor, Aceternity), all five rejected, adopt zero. Tremor is Tailwind 3 JS config against our Tailwind 4 CSS first `@theme`, peers React 18 against our React 19.2.8, and would duplicate Recharts in the bundle; its stable line last published 2025-01-13. GSAP is rejected on licence, not on quality: version 3.15.0's npm `license` field is not an SPDX identifier (it reads "Standard 'no charge' license"); Webflow removed all paywalls in April 2025 so it is free, but the licence is not OSI approved and carries a field-of-use restriction plus a prior-written-consent requirement for prohibited uses, a bad trade when we sell data sovereignty to finance, legal, healthcare and government buyers whose counsel reads the dependency manifest. motion-primitives and Aceternity are decoration, and motion-primitives is self-declared beta. Watermelon UI is the best stack fit of the five (MIT, React 19, Tailwind 4, shadcn compatible) and is still rejected: every component pulls Radix and Framer Motion into a repo that has neither, and it is one vendor's registry at 92 stars; if overlay primitives are ever needed, take Radix directly rather than through an intermediary. The console's gap is not missing components, it is missing data and missing table behaviour, tracked in issues #1270, #1272 and #502 | this session evaluation | 2026-08-28

- D-058 | One visual system across all three web surfaces (Hive marketing site, sovereign/enterprise site, Bangladesh developer site), ground truth measured live 2026-08-28. `scubed.co/hive` returns 200 in cream `#f3eee4` and charcoal `#2b2b28` with a coral `#e8703a` accent, set in Hanken Grotesk. `hive.scubed.co` returns 200 in a different visual system entirely (near black ground, forest green accent, Fraunces typeface). `hive.scubed.com.bd` does not resolve (NXDOMAIN); its source sits unpublished at `website/bd/`. Two live sites presently sell the same product as two different companies, a credibility leak on the exact axis we ask buyers to trust. Decision: one visual system, the live `scubed.co` one, across all three surfaces. The dark Astro theme is retired; the content structure of its six pages survives. `scubed.co/hive` stays a single page, `hive.scubed.co` becomes the sovereign and enterprise site, `hive.scubed.com.bd` becomes the Bangladesh developer surface. Also recorded as a shipped defect, not just a plan: measuring the live brand found three accessibility failures already in production. `--muted` on cream measures 2.66:1 and fails even the 3:1 floor, so it can never be text on a light ground. `--line` at 1.34:1 cannot serve as a form control boundary under WCAG 1.4.11. Coral at 2.67:1 on cream is a fill colour and never ink | this session evaluation, live measurement 2026-08-28 | 2026-08-28
- D-059 | Correction to D-047: the hive-auto fixed price conversion was reversed the next day. Migration supabase/migrations/20260824_02_free_pool_router.sql (2026-08-24) set hive-auto's pricing_mode to upstream_actual with a flat reservation_estimate_credits of 2,000,000,000, putting hive-auto back on variable pricing. The schema is authoritative here: it is the later change, the live database confirms pricing_mode is upstream_actual, and the per request reservation mechanism PR #1378 repairs only exists in upstream_actual mode. hive-default is unaffected and remains fixed price exactly as D-047 records. Practical consequence: the flat 2,000,000,000 credit hold (2.00 USD equivalent) was taken against every hive-auto request regardless of size, making the alias unusable for any account holding less than 2.00 USD, filed as issue #1372 and repaired in PR #1378, which sizes the hold per request instead of a flat ceiling. Anyone reasoning about hive-auto pricing must read this correction alongside D-047, not D-047 alone | issue #1372, PR #1378, supabase/migrations/20260824_02_free_pool_router.sql | 2026-08-29

- D-060 | Partial reversal of issue #783: Hive ships a user-created Skills surface, and the stock Open WebUI Workspace > Skills tab stays removed. #783 removed the Skills tab in 2026-08 on two premises, both since falsified. Premise one, that the tab was "an empty, fully writable admin surface with no Hive product behind it", is answered by the owner asking on 2026-08-29 for "the ability to add new skills": there is a Hive product behind it now, and the storage, API and prompt-injection seam upstream ships (`backend/open_webui/models/skills.py`, `routers/skills.py` at `/api/v1/skills`, and the `<skill name="...">` system-message injection at `utils/middleware.py:2509-2556`) were never removed and still work. Premise two, that a permission-scoped hide hides nothing because every tenant OWNER is an Open WebUI admin (#748), was ended by `supabase/migrations/20260823_03_owui_role_never_admin.sql` and `owui-patches/tenant_role_from_db.py`, which now promote only a platform admin (`accounts.is_platform_admin`); every ordinary customer, tenant owners included, is Open WebUI `user`, so `workspace.skills` is a real gate over a real audience. The surface is a Hive route at `/skills` with its own shell navigation row, NOT a restored `/workspace/skills`: the proxy's `@removedSurfaces` rule in `Caddyfile.owui` keeps 404ing that path unchanged, D-045 ruling 2 deletes "Workspace" from the product outright, and `/skills` is unclaimed by upstream and unmatched by both Caddy blocks, so no proxy rule is reversed and the surface sits outside `routes/(app)/workspace/+layout.svelte`'s permission guard for the same reason #1109 moved Knowledge to `/knowledge`. Skills belongs under the "Customize" destination in D-045's target sidebar grammar; that container does not exist yet, so the row ships flat with a comment recording where it moves. Permission posture: `USER_PERMISSIONS_WORKSPACE_SKILLS_ACCESS` is true for the deployment, reconciled onto the already-booted database through `hive_rag_env_config.py` because `user.permissions` is one persisted config row seeded on first boot and the environment alone can never reach it (the #722 trap); sharing stays at its upstream defaults, both false, so a skill is private to the account that wrote it. Cross-tenant injection was checked rather than assumed and is not reachable: `filter_allowed_access_grants` strips public grants from a non-admin write, and `GET /api/v1/groups` filters to the caller's own memberships, so a member can target only their own tenant's group | owner ask 2026-08-29; issues #783, #772, #748, #1109; D-045; supabase/migrations/20260823_03_owui_role_never_admin.sql | 2026-08-29

- D-061 | Demo scope is locked to six capabilities, owner ruling 2026-08-11 in response to the no-go demo walkthrough (issue #858). The owner's own list: chat functionality, embeddings, voice to text, knowledge work, Cowork, and the coding agent. The same ruling holds that visual and structural parity between Cowork and the chat surface is a demo requirement, not a polish item. Operative consequence, and the reason this entry exists: anything outside that list is not a demo blocker, and anything inside it is. Until now the list survived only in one session's private memory, so every agent asked to judge "does this block the demo" was guessing or being hand-fed the answer in its brief. Related: D-036 and D-040 settle that the demo runs on the forked Open WebUI shell, so parity is fork work rather than a styling pass | owner 2026-08-11, issue #858 | 2026-08-11

- D-062 | Correction to D-048: the free pool stopped being four keys on 2026-08-30. D-048 described hive-free's pool as two free Groq text keys, an OpenRouter free key and a Gemini key, behind load-balanced failover. Migration `supabase/migrations/20260830_03_free_pool_capability_truth.sql` (PR #1556, issue #1563) set `health_state = 'disabled'` on both `route-free-pool-free` (the OpenRouter member) and `route-free-pool-gemini`, moved `alias_route_policies`' pinned fallback from `route-free-pool-free` to `route-free-pool-groq`, and repointed both surviving Groq routes' `provider_model` to `groq/qwen/qwen3.8-27b`. `litellmconfig.SyncService` (`apps/control-plane/internal/litellmconfig/service.go`) emits only routes with `health_state NOT IN ('disabled', 'eol')` on an enabled provider, and `deploy/litellm/config.yaml` carries no static `route-free-pool` entries of its own (it is a first-boot seed only; all four original pool members were DB-inserted by `supabase/migrations/20260824_02_free_pool_router.sql`). So the emitted LiteLLM config for `hive-free` has carried exactly two deployments, `route-free-pool-groq` and `route-free-pool-groq-2`, both Groq, since the first successful deploy after 20260830_03 merged; this was verified directly against the migration text and the SyncService active-route query, not assumed from D-048 or from this entry. Whether those two key slots (`GROQ_API_KEY`, `GROQ_API_KEY_2`) sit on one Groq organization or two, which decides whether the pool's real daily token ceiling is 2M shared or 4M, was the open question in issue #1413, now answered and closed by the owner on 2026-09-01: `GROQ_API_KEY` and `GROQ_API_KEY_2` are two separate Groq organizations, not two keys on one. So the two remaining routes carry two independent daily allowances, not one shared allowance; the pool's real ceiling is 4M tokens/day (2M per organization), the favourable reading, not the 2M single-allowance worst case this entry flagged as a demo-path risk. Anyone reasoning about hive-free's failover, vendor diversity or daily allowance must read this correction alongside D-048, not D-048 alone | PR #1556, issue #1563, issue #1566, issue #1413 (closed 2026-09-01), `supabase/migrations/20260830_03_free_pool_capability_truth.sql` | 2026-09-01

- D-063 | The internal `__metadata` carrier is sanitised at the connection boundary, rather than vendor task models being refused at configuration time. `__metadata` is a Hive invention, neither an Open WebUI field nor an OpenAI one, and it exists so a signed-in user's Supabase bearer can reach edge-api's `OWUIUnwrap` middleware through a request body; `deploy/docker/pipelines/hive_jwt_forward.py` and `deploy/docker/owui-patches/hive_upstream_auth.py` write it and nothing took it back off, so one administrator setting (`task.model.external` pointed at a second, vendor connection) shipped that bearer to the vendor on every conversation, with no user action and no second misconfiguration (issue #1578). Two shapes were available: refuse a vendor task model in configuration, or strip the carrier on the way out. Chosen: strip it at the boundary, because a refusal guards one door while boundary sanitising guards every door, and the destination is known only at the boundary. Vendor task models therefore stay permitted and no administrator choice is taken away. Recorded consequence, part of the decision rather than an implementation detail: the guard proving every outbound body is covered walks the AST for calls handing a `data` or `json` argument to the HTTP session and compares sends against guards per function, so a send that moves between functions is caught as well as one that appears. The first version counted the literal spellings `data=payload,` and `data=body,`, which is a count of two variable names and not of calls, and an independent security review disproved it by experiment: a sixth route spelled `data=outgoing,` left the count at five, the patch exited 0, the test printed all checks passed, and an unsanitised body would have shipped. That experiment now runs on every invocation of the regression test | PR #1654, issue #1578, `deploy/docker/owui-patches/hive_internal_metadata.py`, `apply_internal_metadata_boundary_1578_patch.py` | 2026-09-01
