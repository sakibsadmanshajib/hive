---
phase: 05-api-keys-hot-path-enforcement
plan: 06
type: execute
wave: 6
depends_on:
  - 05-03
  - 05-05
files_modified:
  - apps/edge-api/cmd/server/main.go
  - apps/edge-api/internal/authz/authz.go
  - apps/edge-api/internal/authz/authz_test.go
  - apps/edge-api/internal/authz/client.go
  - apps/edge-api/internal/authz/authorizer.go
  - apps/edge-api/internal/authz/authorizer_test.go
  - apps/edge-api/internal/authz/ratelimit.go
  - apps/edge-api/internal/authz/ratelimit_test.go
  - apps/edge-api/internal/authz/scripts/rpm_tpm.lua
  - apps/edge-api/internal/authz/scripts/window_score.lua
  - apps/edge-api/internal/errors/openai.go
  - apps/edge-api/internal/errors/openai_test.go
autonomous: true
gap_closure: true
requirements:
  - KEY-05
must_haves:
  truths:
    - "The edge enforces account-scope and key-scope RPM, TPM, 5-hour, and weekly limits from separate projected threshold objects."
    - "The edge no longer falls back to inline client-side limiter defaults; `client.go` only resolves auth snapshots."
    - "Only true rate-limit denials emit `x-ratelimit-*` and `retry-after`; invalid-key, disallowed-model, and insufficient-quota failures stay header-clean."
  artifacts:
    - path: "apps/edge-api/internal/authz/ratelimit.go"
      provides: "Atomic Redis limiter for account and key RPM, TPM, 5-hour, and weekly checks."
      contains: "func (l *Limiter) Check"
    - path: "apps/edge-api/internal/authz/scripts/rpm_tpm.lua"
      provides: "Sliding-window RPM/TPM Lua script."
      contains: "reset_ms"
    - path: "apps/edge-api/internal/authz/scripts/window_score.lua"
      provides: "Bucketed 5-hour and weekly quota Lua script."
      contains: "remaining"
    - path: "apps/edge-api/internal/errors/openai.go"
      provides: "Dedicated writer for 429 responses with retry metadata."
      contains: "func WriteRateLimitError"
  key_links:
    - from: "apps/edge-api/internal/authz/ratelimit.go"
      to: "apps/edge-api/internal/authz/authz.go"
      via: "Limiter checks must consume `account_rate_policy` and `key_rate_policy` separately; never substitute one scope for the other."
      pattern: "account_rate_policy"
    - from: "apps/edge-api/internal/authz/authorizer.go"
      to: "apps/edge-api/internal/errors/openai.go"
      via: "Only true rate-limit denials should use `WriteRateLimitError`; permanent failures stay on `WriteError`."
      pattern: "WriteRateLimitError"
---

<objective>
Add the missing edge limiter artifacts and 429 response wiring so the projected control-plane threshold data becomes real hot-path enforcement.

Purpose: Phase 5 still fails KEY-05 until the edge consumes separate account/key threshold objects, runs atomic Redis checks for both scopes, and emits rate-limit metadata only on actual retryable failures.
Output: Redis Lua limiter scripts, an edge limiter package, authorizer wiring for rate-limit headers, and removal of inline client-side limiter logic.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-CONTEXT.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-RESEARCH.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-VERIFICATION.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-03-PLAN.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-05-PLAN.md

<interfaces>
From apps/edge-api/internal/authz/authorizer.go:
```go
func (a *Authorizer) Authorize(ctx context.Context, authHeader string, aliasID string, estimatedCredits int64) (AuthSnapshot, *apierrors.OpenAIError)
```

From apps/control-plane/internal/apikeys/types.go:
```go
type RatePolicy struct {
	RateLimitRPM          int   `json:"rate_limit_rpm"`
	RateLimitTPM          int   `json:"rate_limit_tpm"`
	RollingFiveHourLimit  int64 `json:"rolling_five_hour_limit"`
	WeeklyLimit           int64 `json:"weekly_limit"`
	FreeTokenWeightTenths int   `json:"free_token_weight_tenths"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Redis Lua limiter artifacts that enforce separate account and key thresholds</name>
  <files>
    apps/edge-api/internal/authz/authz.go,
    apps/edge-api/internal/authz/authz_test.go,
    apps/edge-api/internal/authz/ratelimit.go,
    apps/edge-api/internal/authz/ratelimit_test.go,
    apps/edge-api/internal/authz/scripts/rpm_tpm.lua,
    apps/edge-api/internal/authz/scripts/window_score.lua
  </files>
  <behavior>
    - Sliding-window RPM and TPM checks run independently for account scope and key scope.
    - Long-window 5-hour and weekly checks use integer-only score math across account and key scopes.
    - Missing account policy is treated as a contract error, not a signal to reuse the key policy.
  </behavior>
  <action>
1. Update `apps/edge-api/internal/authz/authz.go` so the edge-side `AuthSnapshot` matches the control-plane resolver contract from `05-05`:
   - add `account_rate_policy`
   - add `key_rate_policy`
   - each policy object must include:
     - `rate_limit_rpm`
     - `rate_limit_tpm`
     - `rolling_five_hour_limit`
     - `weekly_limit`
     - `free_token_weight_tenths`

2. Create `apps/edge-api/internal/authz/ratelimit.go` and `apps/edge-api/internal/authz/ratelimit_test.go` with:
   - `type LimitResult struct { Allowed bool; Reason string; RequestLimit int; RequestRemaining int; RequestResetSeconds int; TokenLimit int; TokenRemaining int; TokenResetSeconds int }`
   - `type Limiter struct { redis *redis.Client; rpmTPMScript *redis.Script; windowScoreScript *redis.Script }`
   - `func NewLimiter(redisURL string) (*Limiter, error)`
   - `func (l *Limiter) Check(ctx context.Context, snapshot AuthSnapshot, aliasID string, estimatedCredits int64, billableTokens int64, freeTokens int64) (LimitResult, error)`
   - when `aliasID == ""`, use `"__all__"` as the alias segment for key construction
   - use these exact Redis key families:
     - `rl:{acct:<account_id>:<alias_id>}:rpm:current`
     - `rl:{acct:<account_id>:<alias_id>}:rpm:previous`
     - `rl:{acct:<account_id>:<alias_id>}:tpm:current`
     - `rl:{acct:<account_id>:<alias_id>}:tpm:previous`
     - `rl:{key:<key_id>:<alias_id>}:rpm:current`
     - `rl:{key:<key_id>:<alias_id>}:rpm:previous`
     - `rl:{key:<key_id>:<alias_id>}:tpm:current`
     - `rl:{key:<key_id>:<alias_id>}:tpm:previous`
     - `fraud:{acct:<account_id>}:5h:<bucket>`
     - `fraud:{key:<key_id>}:5h:<bucket>`
     - `fraud:{acct:<account_id>}:7d:<bucket>`
     - `fraud:{key:<key_id>}:7d:<bucket>`

3. Implement limiter math exactly:
   - RPM and TPM use a `60s` sliding-window counter
   - the 5-hour window uses `5m` buckets over `60` buckets total
   - the weekly window uses `1d` buckets over `7` buckets total
   - the long-window score increment is `estimatedCredits + billableTokens + (freeTokens * max(policy.free_token_weight_tenths, 1) / 10)`
   - all math must stay integer-only
   - run account-scope and key-scope checks independently using `snapshot.AccountRatePolicy` and `snapshot.KeyRatePolicy`
   - do not substitute `snapshot.KeyRatePolicy` when `snapshot.AccountRatePolicy` is nil; return an explicit error instead

4. Create `apps/edge-api/internal/authz/scripts/rpm_tpm.lua` and `apps/edge-api/internal/authz/scripts/window_score.lua`:
   - `rpm_tpm.lua` must implement a 60-second sliding-window counter across current and previous bucket keys and return `[allowed, remaining, reset_ms]`
   - `window_score.lua` must implement:
     - `5m` buckets across `60` buckets for the rolling 5-hour window
     - `1d` buckets across `7` buckets for the weekly window
     - integer-only score math
     - return `[allowed, remaining, reset_ms]`

5. Add tests:
   - `TestLimiterUsesSeparateAccountAndKeyThresholds`
   - `TestWindowScoreUsesWeightedFreeTokens`
   - `TestLimiterRejectsMissingAccountPolicy`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain "cd /workspace && go test ./apps/edge-api/internal/authz -count=1" && test -f apps/edge-api/internal/authz/scripts/rpm_tpm.lua && test -f apps/edge-api/internal/authz/scripts/window_score.lua && rg -n "account_rate_policy|key_rate_policy|func \\(l \\*Limiter\\) Check|fraud:\\{acct:<account_id>\\}:5h:<bucket>|rl:\\{key:<key_id>:<alias_id>\\}:rpm:current" apps/edge-api/internal/authz/authz.go apps/edge-api/internal/authz/ratelimit.go</automated>
  </verify>
  <acceptance_criteria>
    - `apps/edge-api/internal/authz/authz.go` contains `account_rate_policy`
    - `apps/edge-api/internal/authz/authz.go` contains `key_rate_policy`
    - `apps/edge-api/internal/authz/ratelimit.go` contains `func (l *Limiter) Check`
    - `apps/edge-api/internal/authz/ratelimit.go` contains `fraud:{acct:<account_id>}:5h:<bucket>`
    - `apps/edge-api/internal/authz/ratelimit.go` contains `rl:{key:<key_id>:<alias_id>}:rpm:current`
    - `apps/edge-api/internal/authz/ratelimit_test.go` contains `TestLimiterUsesSeparateAccountAndKeyThresholds`
    - `apps/edge-api/internal/authz/ratelimit_test.go` contains `TestLimiterRejectsMissingAccountPolicy`
  </acceptance_criteria>
  <done>The edge now has the missing limiter artifacts and enforces account and key scopes independently instead of conflating them inside one projected policy object.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove inline client-side limit logic and wire real 429 header handling through the authorizer</name>
  <files>
    apps/edge-api/cmd/server/main.go,
    apps/edge-api/internal/authz/client.go,
    apps/edge-api/internal/authz/authorizer.go,
    apps/edge-api/internal/authz/authorizer_test.go,
    apps/edge-api/internal/errors/openai.go,
    apps/edge-api/internal/errors/openai_test.go
  </files>
  <behavior>
    - `client.go` only hashes bearer tokens, reads/writes Redis snapshots, and falls back to the control-plane resolver.
    - The authorizer calls the limiter only after `05-03` access and projected-budget checks pass.
    - Only 429 responses get rate-limit headers.
  </behavior>
  <action>
1. In `apps/edge-api/internal/authz/client.go`, delete the inline rate-limit script and any `CheckRateLimit` helper. Keep the client responsible only for:
   - hashing bearer tokens
   - reading or writing `auth:key:{<token_hash>}` snapshots
   - falling back to `/internal/apikeys/resolve`

2. In `apps/edge-api/internal/authz/authorizer.go` and `apps/edge-api/cmd/server/main.go`:
   - change the authorizer constructor to `NewAuthorizer(client *Client, limiter *Limiter) *Authorizer`
   - change `Authorize` to `Authorize(ctx context.Context, authHeader string, aliasID string, estimatedCredits int64, billableTokens int64, freeTokens int64) (AuthSnapshot, map[string]string, *apierrors.OpenAIError)`
   - call the limiter only after key-status, alias, and projected-budget checks pass
   - use reason-specific 429 messages:
     - request limit: `Rate limit reached for requests. Limit: %d / min. Please try again in a little while.`
     - token limit: `Rate limit reached for tokens. Limit: %d / min. Please try again in a little while.`
     - long-window limit: `Rate limit reached for your current quota window. Please try again later.`
   - `/v1/models` should pass `0, 0, 0` for `estimatedCredits`, `billableTokens`, and `freeTokens`

3. In `apps/edge-api/internal/errors/openai.go`, add:
   - `func WriteRateLimitError(w http.ResponseWriter, message string, code *string, headers map[string]string)`
   - it must write every provided header before the JSON error body
   - use it only for `rate_limit_exceeded` paths so permanent failures still go through `WriteError`

4. Set only these headers when the limiter result is meaningful:
   - `x-ratelimit-limit-requests`
   - `x-ratelimit-remaining-requests`
   - `x-ratelimit-reset-requests`
   - `x-ratelimit-limit-tokens`
   - `x-ratelimit-remaining-tokens`
   - `x-ratelimit-reset-tokens`
   - `retry-after`
   - do not set any of those headers on invalid key, disabled key, expired key, revoked key, disallowed alias, or insufficient-quota denials

5. Add or update tests with these exact names:
   - `TestAuthorizeReturnsRateLimitHeadersOnlyFor429`
   - `TestPermanentFailuresOmitRetryHeaders`
   - `TestModelsRouteUsesLimiter`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain "cd /workspace && go test ./apps/edge-api/internal/authz ./apps/edge-api/internal/errors ./apps/edge-api/cmd/server -count=1" && rg -n "func WriteRateLimitError|rate_limit_exceeded|retry-after|x-ratelimit-limit-requests" apps/edge-api/internal/errors/openai.go apps/edge-api/internal/authz/authorizer.go && ! rg -n "func \\(c \\*Client\\) CheckRateLimit" apps/edge-api/internal/authz/client.go</automated>
  </verify>
  <acceptance_criteria>
    - `apps/edge-api/internal/errors/openai.go` contains `func WriteRateLimitError`
    - `apps/edge-api/internal/authz/authorizer.go` contains `Rate limit reached for your current quota window`
    - `apps/edge-api/internal/authz/authorizer.go` contains `retry-after`
    - `apps/edge-api/internal/authz/client.go` does not contain `func (c *Client) CheckRateLimit`
    - `apps/edge-api/internal/authz/authorizer_test.go` contains `TestAuthorizeReturnsRateLimitHeadersOnlyFor429`
    - `apps/edge-api/internal/errors/openai_test.go` contains `TestPermanentFailuresOmitRetryHeaders`
  </acceptance_criteria>
  <done>The edge hot path now enforces projected account and key rate limits with the missing Lua artifacts in place, and only real 429 denials emit retry metadata.</done>
</task>

</tasks>

<verification>
Run the edge auth, error, and server package tests, then confirm `client.go` no longer owns limiter behavior and that the limiter contract consumes separate `account_rate_policy` and `key_rate_policy` fields.
</verification>

<success_criteria>
- Account and key rate policies are enforced independently on the edge hot path.
- The Redis Lua limiter and long-window artifacts exist and are wired through the authorizer.
- Retry metadata appears only on actual rate-limit failures.
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-keys-hot-path-enforcement/05-06-SUMMARY.md`
</output>
