---
phase: 05-api-keys-hot-path-enforcement
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - supabase/migrations/20260331_03_api_key_policies.sql
  - apps/control-plane/cmd/server/main.go
  - apps/control-plane/internal/platform/http/router.go
  - apps/control-plane/internal/apikeys/types.go
  - apps/control-plane/internal/apikeys/repository.go
  - apps/control-plane/internal/apikeys/service.go
  - apps/control-plane/internal/apikeys/http.go
  - apps/control-plane/internal/apikeys/service_test.go
  - apps/control-plane/internal/apikeys/http_test.go
autonomous: true
requirements:
  - KEY-02
  - KEY-05
must_haves:
  truths:
    - "New keys start with the curated default model set and no key-specific budget cap until the owner changes policy."
    - "Customer-visible key list and key detail responses include expiration, budget, and allowlist summaries that reflect durable policy state without re-exposing the raw secret."
    - "The control plane can resolve a token hash into a Redis-safe auth snapshot for the edge without requiring Postgres reads from the public request path."
  artifacts:
    - path: "supabase/migrations/20260331_03_api_key_policies.sql"
      provides: "Durable key-policy storage for model groups, explicit alias overrides, and budget settings."
      contains: "create table public.api_key_policies"
    - path: "apps/control-plane/internal/apikeys/service.go"
      provides: "Default policy creation, policy mutation, key summary projection, and snapshot hydration."
      contains: "func (s *Service) ResolveSnapshot"
    - path: "apps/control-plane/internal/apikeys/http.go"
      provides: "Policy update, key detail, and internal resolve routes with customer-visible summaries."
      contains: "/internal/apikeys/resolve"
  key_links:
    - from: "apps/control-plane/internal/apikeys/service.go"
      to: "apps/control-plane/internal/routing/service.go"
      via: "Resolved key allowlists must stay in the Phase 4 alias namespace so disallowed aliases fail without remapping."
      pattern: "AllowedAliases"
    - from: "apps/edge-api/internal/authz/client.go"
      to: "apps/control-plane/internal/apikeys/http.go"
      via: "The edge rehydrates missing auth snapshots through `/internal/apikeys/resolve` instead of querying Postgres directly."
      pattern: "/internal/apikeys/resolve"
---

<objective>
Add durable key-policy storage, customer-visible key summaries, and the control-plane resolver contract that projects API-key policy into a hot-path auth snapshot.

Purpose: Phase 5 needs policy truth in the control plane before the edge can enforce model access and budget posture cheaply. This slice also closes the missing read surface for expiration, budget, and allowlist summaries required by the locked context.
Output: policy schema, default policy creation, key detail/list summary projection, and the internal `/internal/apikeys/resolve` contract.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-CONTEXT.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-RESEARCH.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-VALIDATION.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-01-PLAN.md

<interfaces>
From apps/control-plane/internal/apikeys/types.go:
```go
type APIKey struct {
	ID             uuid.UUID
	AccountID      uuid.UUID
	Nickname       string
	TokenHash      string
	RedactedSuffix string
	Status         KeyStatus
	ExpiresAt      *time.Time
	LastUsedAt     *time.Time
	CreatedAt      time.Time
	UpdatedAt      time.Time
}

type CreateKeyResult struct {
	Key    APIKey
	Secret string
}
```

From apps/control-plane/internal/apikeys/http.go:
```go
func (h *Handler) ServeHTTP(w http.ResponseWriter, r *http.Request)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable key-policy storage, default policy creation, and snapshot hydration</name>
  <files>
    supabase/migrations/20260331_03_api_key_policies.sql,
    apps/control-plane/internal/apikeys/types.go,
    apps/control-plane/internal/apikeys/repository.go,
    apps/control-plane/internal/apikeys/service.go,
    apps/control-plane/internal/apikeys/service_test.go
  </files>
  <behavior>
    - Every new API key gets a default durable policy row with the curated `default` group and `budget_kind = "none"`.
    - Policy resolution unions allowed groups and explicit aliases, subtracts denied aliases, and preserves the Phase 4 alias namespace.
    - The internal auth snapshot exposes key status, expiry, allowlist, budget metadata, and policy version in a Redis-safe JSON shape.
  </behavior>
  <action>
1. Create `supabase/migrations/20260331_03_api_key_policies.sql` with these exact durable objects:
   - `public.api_key_policies`
     - `api_key_id uuid primary key references public.api_keys(id) on delete cascade`
     - `allow_all_models boolean not null default false`
     - `allowed_group_names jsonb not null default '["default"]'::jsonb`
     - `allowed_aliases jsonb not null default '[]'::jsonb`
     - `denied_aliases jsonb not null default '[]'::jsonb`
     - `budget_kind text not null check (budget_kind in ('none', 'lifetime', 'monthly')) default 'none'`
     - `budget_limit_credits bigint`
     - `budget_anchor_at timestamptz`
     - `policy_version bigint not null default 1`
     - `updated_at timestamptz not null default now()`
   - `public.model_policy_groups`
     - `name text primary key`
     - `description text not null`
     - `auto_attach_new_aliases boolean not null default false`
   - seed group rows:
     - `default` / `Default launch-safe models` / `false`
     - `premium` / `Premium or higher-cost models` / `false`
     - `oss` / `Open-source public aliases` / `false`
     - `closed` / `Closed or hosted provider-backed aliases` / `true`
   - `public.model_policy_group_members`
     - `group_name text not null references public.model_policy_groups(name) on delete cascade`
     - `alias_id text not null references public.model_aliases(alias_id) on delete cascade`
     - primary key `(group_name, alias_id)`
   - seed memberships:
     - `default` -> `hive-default`
     - `default` -> `hive-fast`
     - `premium` -> `hive-auto`
     - `closed` -> `hive-default`
     - `closed` -> `hive-fast`
     - `closed` -> `hive-auto`

2. Extend `apps/control-plane/internal/apikeys/types.go` with:
   - `type KeyPolicy`
   - `type BudgetPolicy`
   - `type ExpirationSummary`
   - `type BudgetSummary`
   - `type AllowlistSummary`
   - `type KeyView`
   - `type UpdatePolicyInput`
   - `type AuthSnapshot`
   - `KeyView` must include the existing key metadata plus:
     - `ExpirationSummary ExpirationSummary`
     - `BudgetSummary BudgetSummary`
     - `AllowlistSummary AllowlistSummary`
   - `AuthSnapshot` must include exactly:
     - `KeyID uuid.UUID`
     - `AccountID uuid.UUID`
     - `Status KeyStatus`
     - `ExpiresAt *time.Time`
     - `AllowAllModels bool`
     - `AllowedAliases []string`
     - `BudgetKind string`
     - `BudgetLimitCredits *int64`
     - `BudgetConsumedCredits int64`
     - `BudgetReservedCredits int64`
     - `BudgetAnchorAt *time.Time`
     - `PolicyVersion int64`

3. Extend `apps/control-plane/internal/apikeys/repository.go` with:
   - `UpsertPolicy(ctx context.Context, accountID, keyID uuid.UUID, input UpdatePolicyInput) (KeyPolicy, error)`
   - `GetPolicy(ctx context.Context, accountID, keyID uuid.UUID) (KeyPolicy, error)`
   - `ListPolicies(ctx context.Context, accountID uuid.UUID) ([]KeyPolicy, error)`
   - `ListGroupMembers(ctx context.Context, groupNames []string) ([]string, error)`
   - `GetByTokenHash(ctx context.Context, tokenHash string) (APIKey, error)`
   - `GetPolicyByTokenHash(ctx context.Context, tokenHash string) (APIKey, KeyPolicy, error)`
   - Key creation from `05-01` must also insert a default policy row with `allowed_group_names = ["default"]` and `budget_kind = 'none'`.

4. Extend `apps/control-plane/internal/apikeys/service.go` with:
   - `func (s *Service) UpdatePolicy(ctx context.Context, accountID, actorUserID, keyID uuid.UUID, input UpdatePolicyInput) (KeyPolicy, error)`
   - `func (s *Service) ResolveSnapshot(ctx context.Context, tokenHash string) (AuthSnapshot, error)`
   - `func (s *Service) ListKeyViews(ctx context.Context, accountID uuid.UUID) ([]KeyView, error)`
   - `func (s *Service) GetKeyView(ctx context.Context, accountID, keyID uuid.UUID) (KeyView, error)`
   - `ResolveSnapshot` must:
     - load the key and policy by `token_hash`
     - return `KeyStatusExpired` when `expires_at < now()` and stored status is still `active`
     - build `AllowedAliases` by unioning `allowed_group_names` and `allowed_aliases`
     - remove anything present in `denied_aliases`
     - if `allow_all_models == true`, include every alias from `public.model_aliases`
     - set `BudgetConsumedCredits` and `BudgetReservedCredits` to `0` when no `api_key_budget_windows` row exists yet
     - carry `policy_version` directly from the durable row
   - `ListKeyViews` and `GetKeyView` must build customer-visible `expiration_summary`, `budget_summary`, and `allowlist_summary` from durable key plus policy data.

5. Use these exact default summary semantics until a key is customized:
   - `expiration_summary.kind = "never"` and `expiration_summary.label = "Never expires"` when `expires_at` is null
   - `budget_summary.kind = "none"` and `budget_summary.label = "No budget cap"` when `budget_kind = "none"`
   - `allowlist_summary.mode = "groups"`, `allowlist_summary.group_names = ["default"]`, and `allowlist_summary.label = "Default launch-safe models"` for a default key

6. Add tests in `apps/control-plane/internal/apikeys/service_test.go`:
   - `TestCreateKeyCreatesDefaultPolicyRow`
   - `TestUpdatePolicyResolvesGroupMembersAndOverrides`
   - `TestResolveSnapshotReturnsAllModelsWhenAllowAllModelsIsSet`
   - `TestResolveSnapshotReturnsExpiredWhenExpiresAtHasPassed`
   - `TestListKeyViewsExposeDefaultSummaries`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain "cd /workspace/apps/control-plane && go test ./internal/apikeys/... -count=1" && rg -n "create table public.api_key_policies|create table public.model_policy_groups|type KeyView|func \\(s \\*Service\\) ResolveSnapshot|Default launch-safe models|No budget cap" supabase/migrations/20260331_03_api_key_policies.sql apps/control-plane/internal/apikeys/types.go apps/control-plane/internal/apikeys/service.go</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260331_03_api_key_policies.sql` contains `create table public.api_key_policies`
    - `supabase/migrations/20260331_03_api_key_policies.sql` contains `create table public.model_policy_groups`
    - `apps/control-plane/internal/apikeys/types.go` contains `type KeyView`
    - `apps/control-plane/internal/apikeys/service.go` contains `func (s *Service) ResolveSnapshot`
    - `apps/control-plane/internal/apikeys/service.go` contains `Default launch-safe models`
    - `apps/control-plane/internal/apikeys/service_test.go` contains `TestListKeyViewsExposeDefaultSummaries`
  </acceptance_criteria>
  <done>Durable per-key policy state exists, every key gets a default launch-safe policy, and the control plane can project that state into both customer-visible summaries and an internal auth snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose policy update, key detail, and internal resolver routes with summary-rich responses</name>
  <files>
    apps/control-plane/cmd/server/main.go,
    apps/control-plane/internal/platform/http/router.go,
    apps/control-plane/internal/apikeys/http.go,
    apps/control-plane/internal/apikeys/http_test.go
  </files>
  <behavior>
    - The authenticated API-key surface now has a readable key-detail route in addition to the list route.
    - Create, rotate, list, and detail responses include expiration, budget, and allowlist summaries, but raw secrets still appear only on create and rotate.
    - The edge can hydrate Redis misses through `/internal/apikeys/resolve`.
  </behavior>
  <action>
1. Update `apps/control-plane/internal/apikeys/http.go` so `Handler` dispatches these exact routes:
   - `GET /api/v1/accounts/current/api-keys`
   - `GET /api/v1/accounts/current/api-keys/{key_id}`
   - `POST /api/v1/accounts/current/api-keys`
   - `POST /api/v1/accounts/current/api-keys/{key_id}/policy`
   - `POST /api/v1/accounts/current/api-keys/{key_id}/rotate`
   - `POST /api/v1/accounts/current/api-keys/{key_id}/disable`
   - `POST /api/v1/accounts/current/api-keys/{key_id}/enable`
   - `POST /api/v1/accounts/current/api-keys/{key_id}/revoke`
   - `POST /internal/apikeys/resolve`

2. Keep the verified-owner gate from `05-01` exactly as-is for all current-account routes. Do not widen mutation access and do not trust client-supplied account IDs.

3. Use these exact response shapes:
   - list response body:
     `{"items":[{"id":"...","nickname":"...","status":"active","redacted_suffix":"...","created_at":"...","updated_at":"...","expires_at":"...","last_used_at":"...","expiration_summary":{"kind":"never","label":"Never expires"},"budget_summary":{"kind":"none","label":"No budget cap"},"allowlist_summary":{"mode":"groups","group_names":["default"],"label":"Default launch-safe models"}}]}`
   - key detail response body: the same fields as a list item
   - create response body: key detail fields plus `"secret":"hk_..."`
   - rotate response body:
     `{"old_key_id":"...","new_key":{<key detail fields plus "secret":"hk_...">}}`
   - `POST /internal/apikeys/resolve` request and response:
     - request body: `{"token_hash":"<sha256 hex>"}`
     - response body: serialized `AuthSnapshot`

4. Update `apps/control-plane/cmd/server/main.go` and `apps/control-plane/internal/platform/http/router.go` so the same `apikeys.Handler` serves both:
   - the authenticated current-account API-key routes
   - the internal resolver route `/internal/apikeys/resolve`

5. Add tests in `apps/control-plane/internal/apikeys/http_test.go`:
   - `TestListKeysReturnsCustomerVisibleSummaries`
   - `TestGetKeyReturnsSummariesWithoutSecret`
   - `TestPolicyRouteUpdatesSummaries`
   - `TestInternalResolveRouteReturnsSnapshotJSON`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain "cd /workspace/apps/control-plane && go test ./internal/apikeys/... -count=1" && rg -n "/api/v1/accounts/current/api-keys/|/internal/apikeys/resolve|expiration_summary|budget_summary|allowlist_summary" apps/control-plane/internal/apikeys/http.go apps/control-plane/internal/platform/http/router.go</automated>
  </verify>
  <acceptance_criteria>
    - `apps/control-plane/internal/apikeys/http.go` contains `/api/v1/accounts/current/api-keys/{key_id}` handling
    - `apps/control-plane/internal/apikeys/http.go` contains `/internal/apikeys/resolve`
    - `apps/control-plane/internal/apikeys/http.go` contains `expiration_summary`
    - `apps/control-plane/internal/apikeys/http.go` contains `budget_summary`
    - `apps/control-plane/internal/apikeys/http.go` contains `allowlist_summary`
    - `apps/control-plane/internal/apikeys/http_test.go` contains `TestGetKeyReturnsSummariesWithoutSecret`
    - `apps/control-plane/internal/apikeys/http_test.go` contains `TestPolicyRouteUpdatesSummaries`
  </acceptance_criteria>
  <done>The control plane exposes policy updates, readable key details, and summary-rich key responses that satisfy the locked customer-visible context while also supplying the internal resolver contract the edge needs.</done>
</task>

</tasks>

<verification>
Run the `internal/apikeys` package tests, then confirm all customer-visible key responses now include `expiration_summary`, `budget_summary`, and `allowlist_summary`, while `/internal/apikeys/resolve` remains available for edge hydration.
</verification>

<success_criteria>
- Keys now have durable policy storage for model access and budget metadata.
- List/detail/create/rotate responses provide the required customer-visible summary fields without leaking raw secrets after issuance.
- The control plane can resolve token hashes into Redis-safe auth snapshots for later edge enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-keys-hot-path-enforcement/05-02-SUMMARY.md`
</output>
