---
phase: 05-api-keys-hot-path-enforcement
plan: 05
type: execute
wave: 5
depends_on:
  - 05-04
files_modified:
  - apps/control-plane/internal/apikeys/types.go
  - apps/control-plane/internal/apikeys/repository.go
  - apps/control-plane/internal/apikeys/service.go
  - apps/control-plane/internal/apikeys/service_test.go
  - apps/control-plane/internal/apikeys/http_test.go
  - apps/control-plane/internal/accounting/service.go
  - apps/control-plane/internal/accounting/service_test.go
  - apps/edge-api/cmd/server/main.go
  - apps/edge-api/internal/authz/authz.go
  - apps/edge-api/internal/authz/authz_test.go
  - apps/edge-api/internal/authz/client.go
  - apps/edge-api/internal/authz/authorizer.go
  - apps/edge-api/internal/authz/ratelimit.go
  - apps/edge-api/internal/authz/ratelimit_test.go
  - apps/edge-api/internal/authz/scripts/rpm_tpm.lua
  - apps/edge-api/internal/authz/scripts/window_score.lua
  - apps/edge-api/internal/errors/openai.go
  - apps/edge-api/internal/errors/openai_test.go
autonomous: true
gap_closure: true
requirements:
  - KEY-05
must_haves:
  truths:
    - "Hot-path budget denials use the API key's active lifetime or monthly budget window, including open reservations, instead of hard-coded zero totals."
    - "The edge consumes resolved rate-policy data from the control plane instead of hard-coded 60 RPM and 120000 TPM defaults."
    - "Account-scope and key-scope RPM, TPM, 5-hour, and weekly checks run as atomic Redis Lua decisions and only true rate-limit denials emit retry metadata."
  artifacts:
    - path: "apps/control-plane/internal/apikeys/service.go"
      provides: "AuthSnapshot projection with live budget totals and resolved rate-policy data."
      contains: "func (s *Service) ResolveSnapshot"
    - path: "apps/edge-api/internal/authz/ratelimit.go"
      provides: "Atomic Redis limiter for account/key RPM, TPM, 5-hour, and weekly checks."
      contains: "func (l *Limiter) Check"
    - path: "apps/edge-api/internal/authz/scripts/rpm_tpm.lua"
      provides: "Sliding-window RPM/TPM Lua script used by the edge limiter."
      contains: "reset_ms"
    - path: "apps/edge-api/internal/authz/scripts/window_score.lua"
      provides: "Bucketed long-window Lua script for 5-hour and weekly quota checks."
      contains: "remaining"
  key_links:
    - from: "apps/control-plane/internal/accounting/service.go"
      to: "apps/control-plane/internal/apikeys/service.go"
      via: "Reservation, finalize, and release paths must update the configured budget window kind instead of passing a hard-coded lifetime literal."
      pattern: "ApplyReservationDelta"
    - from: "apps/control-plane/internal/apikeys/service.go"
      to: "apps/edge-api/internal/authz/authz.go"
      via: "The internal resolver JSON must carry `AuthSnapshot.Policy` and budget window totals that the edge authorizer can consume directly."
      pattern: "rate_limit_rpm"
    - from: "apps/edge-api/internal/authz/authorizer.go"
      to: "apps/edge-api/internal/errors/openai.go"
      via: "429 denials must use the dedicated rate-limit writer so `x-ratelimit-*` and `retry-after` headers only appear on true rate-limit failures."
      pattern: "WriteRateLimitError"
---

<objective>
Close the remaining Phase 05 KEY-05 verification gap by projecting live budget and rate-policy state into the auth snapshot and by implementing the missing edge limiter and quota artifacts.

Purpose: Phase 05 is still incomplete because over-budget and long-window rate-limit denials are not wired through the current auth snapshot and edge hot path.
Output: control-plane snapshot projection that includes budget-window and rate-policy data, plus an edge limiter with the missing Redis Lua artifacts and 429 header handling.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-CONTEXT.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-RESEARCH.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-VERIFICATION.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-04-SUMMARY.md

<interfaces>
From apps/control-plane/internal/apikeys/types.go:
```go
type AuthSnapshot struct {
	KeyID                  uuid.UUID  `json:"key_id"`
	AccountID              uuid.UUID  `json:"account_id"`
	Status                 KeyStatus  `json:"status"`
	AllowAllModels         bool       `json:"allow_all_models"`
	AllowedAliases         []string   `json:"allowed_aliases"`
	BudgetKind             string     `json:"budget_kind"`
	BudgetLimitCredits     *int64     `json:"budget_limit_credits,omitempty"`
	BudgetConsumedCredits  int64      `json:"budget_consumed_credits"`
	BudgetReservedCredits  int64      `json:"budget_reserved_credits"`
	PolicyVersion          int64      `json:"policy_version"`
}
```

From apps/control-plane/internal/accounting/service.go:
```go
type apiKeyService interface {
	ApplyReservationDelta(ctx context.Context, apiKeyID uuid.UUID, budgetKind string, reservedDelta int64, consumedDelta int64, at time.Time) error
	RecordUsageFinalization(ctx context.Context, apiKeyID uuid.UUID, modelAlias string, inputTokens, outputTokens, cacheReadTokens, cacheWriteTokens, consumedCredits int64, at time.Time) error
	MarkLastUsed(ctx context.Context, apiKeyID uuid.UUID, at time.Time) error
}
```

From apps/edge-api/internal/authz/authorizer.go:
```go
func (a *Authorizer) Authorize(ctx context.Context, authHeader string, aliasID string) (AuthSnapshot, *apierrors.OpenAIError)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project the active budget window and resolved rate policy from control plane storage</name>
  <files>
    apps/control-plane/internal/apikeys/types.go,
    apps/control-plane/internal/apikeys/repository.go,
    apps/control-plane/internal/apikeys/service.go,
    apps/control-plane/internal/apikeys/service_test.go,
    apps/control-plane/internal/apikeys/http_test.go,
    apps/control-plane/internal/accounting/service.go,
    apps/control-plane/internal/accounting/service_test.go
  </files>
  <read_first>
    .planning/phases/05-api-keys-hot-path-enforcement/05-CONTEXT.md,
    .planning/phases/05-api-keys-hot-path-enforcement/05-RESEARCH.md,
    .planning/phases/05-api-keys-hot-path-enforcement/05-VERIFICATION.md,
    .planning/phases/05-api-keys-hot-path-enforcement/05-04-SUMMARY.md,
    apps/control-plane/internal/apikeys/types.go,
    apps/control-plane/internal/apikeys/repository.go,
    apps/control-plane/internal/apikeys/service.go,
    apps/control-plane/internal/apikeys/http.go,
    apps/control-plane/internal/apikeys/http_test.go,
    apps/control-plane/internal/accounting/service.go,
    apps/control-plane/internal/accounting/service_test.go,
    supabase/migrations/20260331_04_api_key_usage_and_limits.sql
  </read_first>
  <behavior>
    - `ResolveSnapshot` returns the current budget window totals for the key's configured `budget_kind` instead of `0` / `0`.
    - Reservation create, expand, finalize, and release update the configured `budget_kind` window rather than a hard-coded `"lifetime"` window.
    - The internal resolver JSON includes a `policy` object with the rate-limit fields the edge limiter consumes.
  </behavior>
  <action>
1. In `apps/control-plane/internal/apikeys/types.go`, add:
   - `type RatePolicy struct { RateLimitRPM int \`json:"rate_limit_rpm"\`; RateLimitTPM int \`json:"rate_limit_tpm"\`; RollingFiveHourLimit int64 \`json:"rolling_five_hour_limit"\`; WeeklyLimit int64 \`json:"weekly_limit"\`; FreeTokenWeightTenths int \`json:"free_token_weight_tenths"\` }`
   - `Policy *RatePolicy \`json:"policy,omitempty"\`` on `AuthSnapshot`

2. In `apps/control-plane/internal/apikeys/repository.go`, add exact repository methods:
   - `GetBudgetWindow(ctx context.Context, apiKeyID uuid.UUID, budgetKind string, at time.Time) (BudgetWindow, error)`
   - `GetRatePolicy(ctx context.Context, apiKeyID uuid.UUID) (RatePolicy, error)`
   - change `ApplyReservationDelta` to `ApplyReservationDelta(ctx context.Context, apiKeyID uuid.UUID, reservedDelta int64, consumedDelta int64, at time.Time) error`
   - `GetBudgetWindow` must read `public.api_key_budget_windows` for the active window:
     - `monthly` => `window_start = time.Date(at.Year(), at.Month(), 1, 0, 0, 0, 0, time.UTC)`
     - `lifetime` => `window_start = time.Time{}`
     - missing row => return zero totals without error
   - `GetRatePolicy` must read `public.api_key_rate_policies` and fall back to `60`, `120000`, `0`, `0`, `1` when no row exists
   - `ApplyReservationDelta` must look up `public.api_key_policies.budget_kind` for the key, no-op when it is `none`, and write deltas into the matching monthly or lifetime window instead of accepting a caller-supplied literal

3. In `apps/control-plane/internal/apikeys/service.go`, update `ResolveSnapshot` so it:
   - preserves the existing expiry and allowlist logic from `05-04`
   - calls `GetBudgetWindow` for `policy.BudgetKind`
   - sets `BudgetConsumedCredits` and `BudgetReservedCredits` from that row
   - calls `GetRatePolicy` and assigns the result to `AuthSnapshot.Policy`
   - stops returning `BudgetConsumedCredits: 0` and `BudgetReservedCredits: 0`

4. In `apps/control-plane/internal/accounting/service.go`, remove every hard-coded `"lifetime"` argument passed to `ApplyReservationDelta` and use the new signature so the API-key service resolves the real budget window itself on create, expand, finalize, and release.

5. Treat the rate-policy source as follows:
   - Use the existing `api_key_rate_policies` table as the resolved hot-path threshold source for both account-scope and key-scope limiter checks.
   - Do not add a new `account_rate_policies` schema in this gap-closure slice; the verifier identified missing projection and missing limiter artifacts, not missing durable tables.

6. Add or update tests with these exact names:
   - `TestResolveSnapshotIncludesBudgetWindowAndRatePolicy`
   - `TestApplyReservationDeltaUsesConfiguredMonthlyWindow`
   - `TestFinalizeReservationUsesConfiguredBudgetWindowKind`
   - `TestInternalResolveRouteIncludesPolicyFields`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain "cd /workspace && go test ./apps/control-plane/internal/apikeys ./apps/control-plane/internal/accounting -count=1" && rg -n "rate_limit_rpm|rolling_five_hour_limit|func \\(r \\*pgxRepository\\) GetRatePolicy|func \\(r \\*pgxRepository\\) GetBudgetWindow" apps/control-plane/internal/apikeys/types.go apps/control-plane/internal/apikeys/repository.go && ! rg -n "BudgetConsumedCredits: 0|BudgetReservedCredits: 0|ApplyReservationDelta\\(ctx, .*\"lifetime\"" apps/control-plane/internal/apikeys/service.go apps/control-plane/internal/accounting/service.go</automated>
  </verify>
  <acceptance_criteria>
    - `apps/control-plane/internal/apikeys/types.go` contains `json:"rate_limit_rpm"`
    - `apps/control-plane/internal/apikeys/types.go` contains `json:"rolling_five_hour_limit"`
    - `apps/control-plane/internal/apikeys/repository.go` contains `func (r *pgxRepository) GetBudgetWindow`
    - `apps/control-plane/internal/apikeys/repository.go` contains `func (r *pgxRepository) GetRatePolicy`
    - `apps/control-plane/internal/apikeys/service.go` does not contain `BudgetConsumedCredits: 0`
    - `apps/control-plane/internal/apikeys/service.go` does not contain `BudgetReservedCredits: 0`
    - `apps/control-plane/internal/accounting/service.go` does not contain `ApplyReservationDelta(ctx, *input.APIKeyID, "lifetime"`
    - `apps/control-plane/internal/apikeys/service_test.go` contains `TestResolveSnapshotIncludesBudgetWindowAndRatePolicy`
    - `apps/control-plane/internal/accounting/service_test.go` contains `TestFinalizeReservationUsesConfiguredBudgetWindowKind`
  </acceptance_criteria>
  <done>The control plane now emits live budget and rate-policy state for hot-path enforcement, and accounting updates the configured budget window instead of a hard-coded lifetime projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the missing Redis Lua limiter artifacts and wire real 429 header handling in the edge</name>
  <files>
    apps/edge-api/cmd/server/main.go,
    apps/edge-api/internal/authz/authz.go,
    apps/edge-api/internal/authz/authz_test.go,
    apps/edge-api/internal/authz/client.go,
    apps/edge-api/internal/authz/authorizer.go,
    apps/edge-api/internal/authz/ratelimit.go,
    apps/edge-api/internal/authz/ratelimit_test.go,
    apps/edge-api/internal/authz/scripts/rpm_tpm.lua,
    apps/edge-api/internal/authz/scripts/window_score.lua,
    apps/edge-api/internal/errors/openai.go,
    apps/edge-api/internal/errors/openai_test.go
  </files>
  <read_first>
    .planning/phases/05-api-keys-hot-path-enforcement/05-CONTEXT.md,
    .planning/phases/05-api-keys-hot-path-enforcement/05-RESEARCH.md,
    .planning/phases/05-api-keys-hot-path-enforcement/05-VERIFICATION.md,
    apps/edge-api/cmd/server/main.go,
    apps/edge-api/internal/authz/authz.go,
    apps/edge-api/internal/authz/authz_test.go,
    apps/edge-api/internal/authz/client.go,
    apps/edge-api/internal/authz/authorizer.go,
    apps/edge-api/internal/errors/openai.go
  </read_first>
  <behavior>
    - The edge authorizer uses projected `AuthSnapshot.Policy` values and no longer relies on the inline `CheckRateLimit` fallback in `client.go`.
    - Redis Lua scripts exist for both sliding-window RPM/TPM enforcement and bucketed 5-hour / weekly quota scoring.
    - Only true rate-limit denials emit `x-ratelimit-*` and `retry-after`; invalid key, disallowed model, and insufficient-quota failures do not.
  </behavior>
  <action>
1. Create `apps/edge-api/internal/authz/ratelimit.go` and `apps/edge-api/internal/authz/ratelimit_test.go` with:
   - `type LimitResult struct { Allowed bool; Reason string; RequestLimit int; RequestRemaining int; RequestResetSeconds int; TokenLimit int; TokenRemaining int; TokenResetSeconds int }`
   - `type Limiter struct { redis *redis.Client; rpmTPMScript *redis.Script; windowScoreScript *redis.Script }`
   - `func NewLimiter(redisURL string) (*Limiter, error)`
   - `func (l *Limiter) Check(ctx context.Context, snapshot AuthSnapshot, aliasID string, estimatedCredits int64, billableTokens int64, freeTokens int64) (LimitResult, error)`
   - when `aliasID == ""`, use `"__all__"` as the alias segment for key construction
   - use these exact Redis key families:
     - `rl:{acct:<account_id>:<alias_id>}:rpm:current`
     - `rl:{acct:<account_id>:<alias_id>}:rpm:previous`
     - `rl:{acct:<account_id>:<alias_id>}:tpm:current`
     - `rl:{acct:<account_id>:<alias_id>}:tpm:previous`
     - `rl:{key:<key_id>:<alias_id>}:rpm:current`
     - `rl:{key:<key_id>:<alias_id>}:rpm:previous`
     - `rl:{key:<key_id>:<alias_id>}:tpm:current`
     - `rl:{key:<key_id>:<alias_id>}:tpm:previous`
     - `fraud:{acct:<account_id>}:5h:<bucket>`
     - `fraud:{key:<key_id>}:5h:<bucket>`
     - `fraud:{acct:<account_id>}:7d:<bucket>`
     - `fraud:{key:<key_id>}:7d:<bucket>`
   - run both account-scope and key-scope checks from the same `AuthSnapshot.Policy` thresholds; do not introduce a new account-tier schema in this gap closure

2. Create `apps/edge-api/internal/authz/scripts/rpm_tpm.lua` and `apps/edge-api/internal/authz/scripts/window_score.lua`:
   - `rpm_tpm.lua` must implement a 60-second sliding-window counter across current/previous bucket keys and return `[allowed, remaining, reset_ms]`
   - `window_score.lua` must implement:
     - `5m` buckets across `60` buckets for the rolling 5-hour window
     - `1d` buckets across `7` buckets for the weekly window
     - integer-only score math
     - return `[allowed, remaining, reset_ms]`

3. In `apps/edge-api/internal/authz/authz.go`, expand `AuthSnapshot.Policy` to match the control-plane JSON contract:
   - `rate_limit_rpm`
   - `rate_limit_tpm`
   - `rolling_five_hour_limit`
   - `weekly_limit`
   - `free_token_weight_tenths`

4. In `apps/edge-api/internal/authz/client.go`, delete the inline `CheckRateLimit` Lua script and keep the client responsible only for:
   - hashing bearer tokens
   - reading or writing `auth:key:{<token_hash>}` snapshots
   - falling back to `/internal/apikeys/resolve`

5. In `apps/edge-api/internal/authz/authorizer.go` and `apps/edge-api/cmd/server/main.go`:
   - change the authorizer constructor to `NewAuthorizer(client *Client, limiter *Limiter) *Authorizer`
   - change `Authorize` to `Authorize(ctx context.Context, authHeader string, aliasID string, estimatedCredits int64, billableTokens int64, freeTokens int64) (AuthSnapshot, map[string]string, *apierrors.OpenAIError)`
   - call the limiter after `CheckAccess`
   - use score increment `estimatedCredits + billableTokens + (freeTokens * max(snapshot.Policy.FreeTokenWeightTenths, 1) / 10)` for the 5-hour and weekly checks
   - return reason-specific 429 messages:
     - request limit: `Rate limit reached for requests. Limit: %d / min. Please try again in a little while.`
     - token limit: `Rate limit reached for tokens. Limit: %d / min. Please try again in a little while.`
     - long-window limit: `Rate limit reached for your current quota window. Please try again later.`
   - `/v1/models` should pass `0, 0, 0` for `estimatedCredits`, `billableTokens`, and `freeTokens`

6. In `apps/edge-api/internal/errors/openai.go`, add:
   - `func WriteRateLimitError(w http.ResponseWriter, message string, code *string, headers map[string]string)`
   - it must write every provided header before the JSON error body
   - use it only for `rate_limit_exceeded` paths so permanent failures still go through `WriteError`

7. Add or update tests with these exact names:
   - `TestLimiterUsesProjectedPolicyValues`
   - `TestWindowScoreUsesWeightedFreeTokens`
   - `TestAuthorizeReturnsRateLimitHeadersOnlyFor429`
   - `TestPermanentFailuresOmitRetryHeaders`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain "cd /workspace && go test ./apps/edge-api/internal/authz ./apps/edge-api/internal/errors ./apps/edge-api/cmd/server -count=1" && test -f apps/edge-api/internal/authz/scripts/rpm_tpm.lua && test -f apps/edge-api/internal/authz/scripts/window_score.lua && rg -n "rolling_five_hour_limit|weekly_limit|free_token_weight_tenths|func WriteRateLimitError|func \\(l \\*Limiter\\) Check" apps/edge-api/internal/authz/authz.go apps/edge-api/internal/authz/ratelimit.go apps/edge-api/internal/errors/openai.go && ! rg -n "func \\(c \\*Client\\) CheckRateLimit" apps/edge-api/internal/authz/client.go</automated>
  </verify>
  <acceptance_criteria>
    - `apps/edge-api/internal/authz/ratelimit.go` contains `func (l *Limiter) Check`
    - `apps/edge-api/internal/authz/authz.go` contains `rolling_five_hour_limit`
    - `apps/edge-api/internal/authz/authz.go` contains `weekly_limit`
    - `apps/edge-api/internal/authz/authz.go` contains `free_token_weight_tenths`
    - `apps/edge-api/internal/errors/openai.go` contains `func WriteRateLimitError`
    - `apps/edge-api/internal/authz/client.go` does not contain `func (c *Client) CheckRateLimit`
    - `apps/edge-api/internal/authz/ratelimit_test.go` contains `TestWindowScoreUsesWeightedFreeTokens`
    - `apps/edge-api/internal/authz/authorizer.go` contains `Rate limit reached for your current quota window`
    - `apps/edge-api/internal/authz/scripts/rpm_tpm.lua` exists
    - `apps/edge-api/internal/authz/scripts/window_score.lua` exists
  </acceptance_criteria>
  <done>The edge hot path now enforces the projected rate-policy and quota windows with the missing Lua artifacts in place, and only real 429 denials emit retry metadata.</done>
</task>

</tasks>

<verification>
Run the control-plane and edge package tests from both tasks, then confirm the remaining `05-VERIFICATION.md` blocker strings are addressed:
- `ResolveSnapshot` no longer hard-codes zero budget totals.
- `apps/edge-api/internal/authz/ratelimit.go` and both Lua script files exist.
- `apps/edge-api/internal/authz/client.go` no longer owns the limiter logic.
</verification>

<success_criteria>
- The internal API-key resolver returns budget-window totals and rate-policy fields the edge can consume without Postgres reads.
- The edge limiter enforces account/key RPM, TPM, 5-hour, and weekly windows from projected policy data instead of fallback defaults.
- Only actual rate-limit denials emit `x-ratelimit-*` and `retry-after`; revoked, expired, disallowed, and insufficient-quota failures do not.
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-keys-hot-path-enforcement/05-05-SUMMARY.md`
</output>
