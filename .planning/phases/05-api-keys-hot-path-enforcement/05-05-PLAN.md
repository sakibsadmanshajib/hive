---
phase: 05-api-keys-hot-path-enforcement
plan: 05
type: execute
wave: 5
depends_on:
  - 05-02
  - 05-04
files_modified:
  - supabase/migrations/20260331_04_api_key_usage_and_limits.sql
  - apps/control-plane/internal/usage/types.go
  - apps/control-plane/internal/usage/repository.go
  - apps/control-plane/internal/usage/service.go
  - apps/control-plane/internal/accounting/types.go
  - apps/control-plane/internal/accounting/service.go
  - apps/control-plane/internal/accounting/service_test.go
  - apps/control-plane/internal/apikeys/types.go
  - apps/control-plane/internal/apikeys/repository.go
  - apps/control-plane/internal/apikeys/service.go
  - apps/control-plane/internal/apikeys/service_test.go
  - apps/control-plane/internal/apikeys/http_test.go
autonomous: true
gap_closure: true
requirements:
  - KEY-04
  - KEY-05
must_haves:
  truths:
    - "Hot-path budget denials use the API key's active lifetime or monthly budget window, including open reservations, instead of hard-coded zero totals."
    - "Account-tier and key-tier rate limits come from separate resolved sources; a per-key policy table is not allowed to stand in for account-scope thresholds."
    - "Per-key usage and budget projections extend the shared wallet and existing accounting flow; keys remain guardrails and attribution boundaries, not sub-wallets."
  artifacts:
    - path: "supabase/migrations/20260331_04_api_key_usage_and_limits.sql"
      provides: "Durable per-key usage rollups, budget windows, per-key rate policies, and an explicit account-tier rate-policy source."
      contains: "create table public.account_rate_policies"
    - path: "apps/control-plane/internal/apikeys/service.go"
      provides: "Live budget-window projection plus separate account/key rate-policy resolution for auth snapshots."
      contains: "func (s *Service) ResolveSnapshot"
    - path: "apps/control-plane/internal/accounting/service.go"
      provides: "Reservation create, expand, finalize, and release paths that update configured budget windows without hard-coded lifetime scope."
      contains: "ApplyReservationDelta"
  key_links:
    - from: "apps/control-plane/internal/accounting/service.go"
      to: "apps/control-plane/internal/apikeys/service.go"
      via: "Budget-affecting reservation and finalization paths must update the configured key budget window and invalidate the cached auth snapshot for that key."
      pattern: "ApplyReservationDelta"
    - from: "apps/control-plane/internal/apikeys/service.go"
      to: "apps/edge-api/internal/authz/authz.go"
      via: "The internal resolver JSON must emit separate `account_rate_policy` and `key_rate_policy` objects so the edge limiter can enforce both scopes independently."
      pattern: "account_rate_policy"
---

<objective>
Project live per-key usage and budget truth into the control plane and add the missing account-tier threshold source required for correct hot-path quota enforcement.

Purpose: The remaining Phase 5 verification gap is not just missing edge Lua scripts. The control plane still needs live budget-window totals and a distinct account-scope rate-policy source so the edge can enforce KEY-05 without conflating account and key limits.
Output: per-key usage rollups and budget windows, a new `account_rate_policies` source, and auth snapshots that expose separate account and key thresholds.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-CONTEXT.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-RESEARCH.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-VERIFICATION.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-04-SUMMARY.md
@.planning/phases/03-credits-ledger-usage-accounting/03-03-SUMMARY.md

<interfaces>
From apps/control-plane/internal/accounting/service.go:
```go
type apiKeyService interface {
	ApplyReservationDelta(ctx context.Context, apiKeyID uuid.UUID, budgetKind string, reservedDelta int64, consumedDelta int64, at time.Time) error
	RecordUsageFinalization(ctx context.Context, apiKeyID uuid.UUID, modelAlias string, inputTokens, outputTokens, cacheReadTokens, cacheWriteTokens, consumedCredits int64, at time.Time) error
	MarkLastUsed(ctx context.Context, apiKeyID uuid.UUID, at time.Time) error
}
```

From apps/control-plane/internal/apikeys/types.go:
```go
type AuthSnapshot struct {
	KeyID                 uuid.UUID  `json:"key_id"`
	AccountID             uuid.UUID  `json:"account_id"`
	Status                KeyStatus  `json:"status"`
	BudgetKind            string     `json:"budget_kind"`
	BudgetLimitCredits    *int64     `json:"budget_limit_credits,omitempty"`
	BudgetConsumedCredits int64      `json:"budget_consumed_credits"`
	BudgetReservedCredits int64      `json:"budget_reserved_credits"`
	PolicyVersion         int64      `json:"policy_version"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend accounting and usage to maintain live per-key budget windows and rollups</name>
  <files>
    supabase/migrations/20260331_04_api_key_usage_and_limits.sql,
    apps/control-plane/internal/usage/types.go,
    apps/control-plane/internal/usage/repository.go,
    apps/control-plane/internal/usage/service.go,
    apps/control-plane/internal/accounting/types.go,
    apps/control-plane/internal/accounting/service.go,
    apps/control-plane/internal/accounting/service_test.go
  </files>
  <behavior>
    - Reservation create, expand, finalize, and release all move the same key-budget window totals the edge will later enforce.
    - Per-key rollups remain derived from the shared-wallet accounting flow, not a second balance system.
    - Budget-affecting updates flow through the API-key service so the later snapshot projection layer can invalidate stale auth snapshots centrally.
  </behavior>
  <action>
1. Update `supabase/migrations/20260331_04_api_key_usage_and_limits.sql` so it contains exactly these schema elements:
   - `alter table public.usage_events add column api_key_id uuid references public.api_keys(id) on delete set null`
   - index `idx_usage_events_api_key_created_at` on `(api_key_id, created_at desc)`
   - `public.api_key_usage_rollups`
   - `public.api_key_budget_windows`
   - `public.api_key_rate_policies`
   - `public.account_rate_policies`
     - `account_id uuid primary key references public.accounts(id) on delete cascade`
     - `requests_per_minute integer not null default 60`
     - `tokens_per_minute integer not null default 120000`
     - `rolling_five_hour_limit bigint not null default 0`
     - `weekly_limit bigint not null default 0`
     - `free_token_weight_tenths integer not null default 1`
     - `updated_at timestamptz not null default now()`
   - Add an SQL comment explaining that `account_rate_policies` exists because account-tier and key-tier hot-path thresholds are separate policy sources; `api_key_rate_policies` must not be reused for account-scope checks.

2. Extend `apps/control-plane/internal/usage/types.go` and `apps/control-plane/internal/usage/repository.go` so `UsageEvent` and `RecordEventInput` include `APIKeyID *uuid.UUID`, and `RecordEvent` writes `api_key_id` into `public.usage_events`. Preserve the `05-04` attribution behavior if the field already exists; do not remove or rename it.

3. Extend `apps/control-plane/internal/accounting/types.go` and `apps/control-plane/internal/accounting/service.go` so:
   - `CreateReservationInput` carries `APIKeyID *uuid.UUID`
   - reservation create passes `APIKeyID` into usage attempt creation
   - reservation create, expand, finalize, and release all call the API-key service without a hard-coded budget-window literal
   - the API-key service resolves the real budget window kind from durable policy

4. Implement the accounting and rollup rules exactly:
   - `ApplyReservationDelta` updates `public.api_key_budget_windows` so the active lifetime or monthly window tracks both reserved and consumed credits
   - `RecordUsageFinalization` upserts `public.api_key_usage_rollups` for both `lifetime` and `monthly` windows
   - `RecordUsageFinalization` must not create a second wallet or balance table; it only updates projections derived from the shared-wallet accounting events
   - `MarkLastUsed` updates `public.api_keys.last_used_at` only when the new timestamp is later than the stored value

5. Add tests:
   - `TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup`
   - `TestBudgetProjectionCountsOpenReservations`
   - `TestFinalizeReservationUsesConfiguredBudgetWindowKind`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain "cd /workspace/apps/control-plane && go test ./internal/accounting/... ./internal/usage/... -count=1" && rg -n "create table public.account_rate_policies|api_key_budget_windows|api_key_usage_rollups|APIKeyID|ApplyReservationDelta" supabase/migrations/20260331_04_api_key_usage_and_limits.sql apps/control-plane/internal/accounting/types.go apps/control-plane/internal/accounting/service.go apps/control-plane/internal/usage/types.go</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260331_04_api_key_usage_and_limits.sql` contains `create table public.account_rate_policies`
    - `supabase/migrations/20260331_04_api_key_usage_and_limits.sql` contains `api_key_budget_windows`
    - `apps/control-plane/internal/accounting/service.go` no longer contains `ApplyReservationDelta(ctx, *input.APIKeyID, "lifetime"`
    - `apps/control-plane/internal/accounting/service_test.go` contains `TestFinalizeReservationUsesConfiguredBudgetWindowKind`
  </acceptance_criteria>
  <done>The control plane now keeps live per-key budget-window and usage-rollup truth in step with shared-wallet accounting, ready for snapshot projection without inventing a second wallet model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve live budget totals and separate account/key rate policies into the auth snapshot</name>
  <files>
    apps/control-plane/internal/apikeys/types.go,
    apps/control-plane/internal/apikeys/repository.go,
    apps/control-plane/internal/apikeys/service.go,
    apps/control-plane/internal/apikeys/service_test.go,
    apps/control-plane/internal/apikeys/http_test.go
  </files>
  <behavior>
    - `ResolveSnapshot` returns live consumed and reserved totals from the configured budget window instead of hard-coded zero values.
    - Account-tier thresholds and key-tier thresholds resolve through separate repository methods and serialize through separate JSON fields.
    - The internal resolve route exposes those separate fields for the edge limiter contract.
  </behavior>
  <action>
1. In `apps/control-plane/internal/apikeys/types.go`, add:
   - `type RatePolicy struct { RateLimitRPM int \`json:"rate_limit_rpm"\`; RateLimitTPM int \`json:"rate_limit_tpm"\`; RollingFiveHourLimit int64 \`json:"rolling_five_hour_limit"\`; WeeklyLimit int64 \`json:"weekly_limit"\`; FreeTokenWeightTenths int \`json:"free_token_weight_tenths"\` }`
   - `AccountRatePolicy *RatePolicy \`json:"account_rate_policy,omitempty"\`` on `AuthSnapshot`
   - `KeyRatePolicy *RatePolicy \`json:"key_rate_policy,omitempty"\`` on `AuthSnapshot`

2. In `apps/control-plane/internal/apikeys/repository.go`, add exact repository methods:
   - `GetBudgetWindow(ctx context.Context, apiKeyID uuid.UUID, budgetKind string, at time.Time) (BudgetWindow, error)`
   - `GetKeyRatePolicy(ctx context.Context, apiKeyID uuid.UUID) (RatePolicy, error)`
   - `GetAccountRatePolicy(ctx context.Context, accountID uuid.UUID) (RatePolicy, error)`
   - change `ApplyReservationDelta` to `ApplyReservationDelta(ctx context.Context, apiKeyID uuid.UUID, reservedDelta int64, consumedDelta int64, at time.Time) error`
   - `GetBudgetWindow` must read `public.api_key_budget_windows` for the active window:
     - `monthly` => `window_start = time.Date(at.Year(), at.Month(), 1, 0, 0, 0, 0, time.UTC)`
     - `lifetime` => `window_start = time.Time{}`
     - missing row => return zero totals without error
   - `GetAccountRatePolicy` must read `public.account_rate_policies` and fall back to `60`, `120000`, `0`, `0`, `1` when no row exists
   - `GetKeyRatePolicy` must read `public.api_key_rate_policies` separately and fall back to the same defaults when no row exists
   - do not read account-scope thresholds from `public.api_key_rate_policies`

3. In `apps/control-plane/internal/apikeys/service.go`, update `ResolveSnapshot` so it:
   - preserves the existing expiry and allowlist logic from `05-02`
   - calls `GetBudgetWindow` for `policy.BudgetKind`
   - sets `BudgetConsumedCredits` and `BudgetReservedCredits` from that row
   - calls `GetAccountRatePolicy` and `GetKeyRatePolicy`
   - assigns them to `AuthSnapshot.AccountRatePolicy` and `AuthSnapshot.KeyRatePolicy`
   - never reuses a generic `Policy` object for both scopes

4. Reuse the `05-04` snapshot-cache pattern on budget-affecting deltas:
   - after a successful reservation create, expand, finalize, or release for an attributed key, invalidate or refresh the cached auth snapshot for that key from the API-key service layer
   - if the durable budget update succeeds but snapshot invalidation fails, bubble the error instead of silently leaving stale budget truth in Redis

5. Keep the internal resolver route contract aligned by extending `apps/control-plane/internal/apikeys/http_test.go` with assertions that `/internal/apikeys/resolve` now emits:
   - `budget_consumed_credits`
   - `budget_reserved_credits`
   - `account_rate_policy`
   - `key_rate_policy`

6. Add or update tests with these exact names:
   - `TestResolveSnapshotIncludesLiveBudgetWindow`
   - `TestResolveSnapshotReturnsSeparateAccountAndKeyRatePolicies`
   - `TestApplyReservationDeltaUsesConfiguredMonthlyWindow`
   - `TestBudgetAffectingDeltaInvalidatesSnapshot`
   - `TestInternalResolveRouteIncludesSeparateRatePolicyFields`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain "cd /workspace/apps/control-plane && go test ./internal/apikeys/... -count=1" && rg -n "account_rate_policy|key_rate_policy|func \\(r \\*pgxRepository\\) GetAccountRatePolicy|func \\(r \\*pgxRepository\\) GetKeyRatePolicy|BudgetConsumedCredits" apps/control-plane/internal/apikeys/types.go apps/control-plane/internal/apikeys/repository.go apps/control-plane/internal/apikeys/service.go</automated>
  </verify>
  <acceptance_criteria>
    - `apps/control-plane/internal/apikeys/types.go` contains `json:"account_rate_policy,omitempty"`
    - `apps/control-plane/internal/apikeys/types.go` contains `json:"key_rate_policy,omitempty"`
    - `apps/control-plane/internal/apikeys/repository.go` contains `func (r *pgxRepository) GetAccountRatePolicy`
    - `apps/control-plane/internal/apikeys/repository.go` contains `func (r *pgxRepository) GetKeyRatePolicy`
    - `apps/control-plane/internal/apikeys/types.go` does not contain `Policy *RatePolicy`
    - `apps/control-plane/internal/apikeys/service_test.go` contains `TestResolveSnapshotReturnsSeparateAccountAndKeyRatePolicies`
    - `apps/control-plane/internal/apikeys/service_test.go` contains `TestBudgetAffectingDeltaInvalidatesSnapshot`
    - `apps/control-plane/internal/apikeys/http_test.go` contains `TestInternalResolveRouteIncludesSeparateRatePolicyFields`
  </acceptance_criteria>
  <done>The control plane now emits live budget totals plus distinct account-tier and key-tier rate-policy objects, and budget-affecting mutations invalidate stale auth snapshots instead of leaving Redis behind the durable truth.</done>
</task>

</tasks>

<verification>
Run the touched accounting, usage, and apikeys package tests. Then confirm the resolver contract no longer implies a single policy object can satisfy both account and key hot-path checks.
</verification>

<success_criteria>
- Budget totals in `AuthSnapshot` come from the active budget window and include open reservations.
- The repo now has an explicit durable account-tier threshold source instead of inferring account scope from the per-key table.
- The internal resolver emits separate account and key rate-policy objects for later edge enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-keys-hot-path-enforcement/05-05-SUMMARY.md`
</output>
