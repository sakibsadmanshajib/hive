---
phase: 05-api-keys-hot-path-enforcement
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - apps/control-plane/internal/usage/types.go
  - apps/control-plane/internal/usage/repository.go
  - apps/control-plane/internal/usage/service.go
  - apps/control-plane/internal/accounting/types.go
  - apps/control-plane/internal/accounting/service.go
  - apps/control-plane/internal/accounting/service_test.go
  - apps/control-plane/internal/apikeys/types.go
  - apps/control-plane/internal/apikeys/repository.go
  - apps/control-plane/internal/apikeys/service.go
  - apps/control-plane/internal/apikeys/service_test.go
  - apps/edge-api/internal/errors/openai.go
  - apps/edge-api/internal/errors/openai_test.go
  - apps/edge-api/internal/authz/authorizer.go
  - apps/edge-api/internal/authz/ratelimit.go
  - apps/edge-api/internal/authz/ratelimit_test.go
  - apps/edge-api/internal/authz/scripts/rpm_tpm.lua
  - apps/edge-api/internal/authz/scripts/window_score.lua
  - supabase/migrations/20260331_04_api_key_usage_and_limits.sql
autonomous: false
requirements:
  - KEY-04
  - KEY-05
must_haves:
  truths:
    - "Per-key spend attribution must extend the existing request-attempt, usage-event, and shared-wallet accounting model instead of inventing a second billing channel."
    - "Rate-limit and anti-fraud decisions must be atomic Redis operations; separate client-side read and write calls are not acceptable on the hot path."
    - "Retry and rate-limit headers are only meaningful for actual 429 limits and must not appear on revoked, disabled, expired, or over-budget denials."
  artifacts:
    - path: "supabase/migrations/20260331_04_api_key_usage_and_limits.sql"
      provides: "Durable per-key usage rollups, budget windows, and rate-policy configuration."
      contains: "create table public.api_key_usage_rollups"
    - path: "apps/control-plane/internal/apikeys/service.go"
      provides: "Per-key usage and budget projection updates on top of the existing accounting flow."
      contains: "func (s *Service) RecordUsageFinalization"
    - path: "apps/edge-api/internal/authz/ratelimit.go"
      provides: "Redis Lua-backed RPM, TPM, 5-hour, and weekly limit evaluation."
      contains: "func (l *Limiter) Check"
  key_links:
    - from: "apps/control-plane/internal/accounting/service.go"
      to: "apps/control-plane/internal/apikeys/service.go"
      via: "Reservation create, finalize, and release events must update per-key budget projections without turning keys into sub-wallets."
      pattern: "RecordUsageFinalization"
    - from: "apps/edge-api/internal/authz/ratelimit.go"
      to: "apps/edge-api/internal/errors/openai.go"
      via: "The limiter must surface OpenAI-style 429 responses with meaningful header metadata only when the denial is truly rate-limit based."
      pattern: "WriteRateLimitError"
---

<objective>
Finish Phase 5 by making usage and spend attributable per key and per model, then enforcing Redis-backed rate limits and long-window quotas with OpenAI-compatible 429 metadata.

Purpose: Phase 5 only reaches its goal when authorization, budget guardrails, and rate limits stay cheap on the hot path while the durable accounting and usage system still reconstructs spend by key and model.
Output: per-key usage rollups and budget windows in the control plane, plus Redis Lua limiters and response metadata in the edge authorization layer.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-CONTEXT.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-RESEARCH.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-VALIDATION.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-01-PLAN.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend accounting and usage to maintain per-key spend, budget windows, and usage rollups</name>
  <files>
    supabase/migrations/20260331_04_api_key_usage_and_limits.sql,
    apps/control-plane/internal/usage/types.go,
    apps/control-plane/internal/usage/repository.go,
    apps/control-plane/internal/usage/service.go,
    apps/control-plane/internal/accounting/types.go,
    apps/control-plane/internal/accounting/service.go,
    apps/control-plane/internal/accounting/service_test.go,
    apps/control-plane/internal/apikeys/types.go,
    apps/control-plane/internal/apikeys/repository.go,
    apps/control-plane/internal/apikeys/service.go,
    apps/control-plane/internal/apikeys/service_test.go
  </files>
  <read_first>
    .planning/phases/05-api-keys-hot-path-enforcement/05-02-PLAN.md,
    .planning/phases/05-api-keys-hot-path-enforcement/05-RESEARCH.md,
    apps/control-plane/internal/usage/types.go,
    apps/control-plane/internal/usage/repository.go,
    apps/control-plane/internal/accounting/types.go,
    apps/control-plane/internal/accounting/service.go,
    apps/control-plane/internal/apikeys/service.go,
    supabase/migrations/20260330_02_usage_accounting.sql,
    supabase/migrations/20260330_03_credit_reservations.sql
  </read_first>
  <action>
1. Create `supabase/migrations/20260331_04_api_key_usage_and_limits.sql` with these exact schema changes:
   - alter `public.usage_events` to add `api_key_id uuid references public.api_keys(id) on delete set null`
   - create index `idx_usage_events_api_key_created_at` on `(api_key_id, created_at desc)`
   - `public.api_key_usage_rollups`
     - `api_key_id uuid not null references public.api_keys(id) on delete cascade`
     - `model_alias text not null`
     - `window_kind text not null check (window_kind in ('lifetime', 'monthly'))`
     - `window_start timestamptz not null`
     - `request_count bigint not null default 0`
     - `input_tokens bigint not null default 0`
     - `output_tokens bigint not null default 0`
     - `cache_read_tokens bigint not null default 0`
     - `cache_write_tokens bigint not null default 0`
     - `consumed_credits bigint not null default 0`
     - `last_seen_at timestamptz not null default now()`
     - primary key `(api_key_id, model_alias, window_kind, window_start)`
   - `public.api_key_budget_windows`
     - `api_key_id uuid not null references public.api_keys(id) on delete cascade`
     - `window_kind text not null check (window_kind in ('lifetime', 'monthly'))`
     - `window_start timestamptz not null`
     - `window_end timestamptz`
     - `consumed_credits bigint not null default 0`
     - `reserved_credits bigint not null default 0`
     - `updated_at timestamptz not null default now()`
     - primary key `(api_key_id, window_kind, window_start)`
   - `public.api_key_rate_policies`
     - `api_key_id uuid primary key references public.api_keys(id) on delete cascade`
     - `requests_per_minute integer not null default 60`
     - `tokens_per_minute integer not null default 120000`
     - `rolling_five_hour_limit bigint not null default 0`
     - `weekly_limit bigint not null default 0`
     - `free_token_weight_tenths integer not null default 1`
     - `updated_at timestamptz not null default now()`

2. Extend `apps/control-plane/internal/usage/types.go` and `repository.go` so `UsageEvent` and `RecordEventInput` include `APIKeyID *uuid.UUID`, and `RecordEvent` writes `api_key_id` into `public.usage_events`.

3. Extend `apps/control-plane/internal/accounting/types.go` and `service.go` so `CreateReservationInput` includes `APIKeyID *uuid.UUID`, and `CreateReservation` passes that field into `usage.StartAttempt`.

4. Extend `apps/control-plane/internal/apikeys/types.go`, `repository.go`, and `service.go` with:
   - `type UsageRollupWindow`
   - `type BudgetWindow`
   - `func (s *Service) ApplyReservationDelta(ctx context.Context, apiKeyID uuid.UUID, budgetKind string, reservedDelta int64, consumedDelta int64, at time.Time) error`
   - `func (s *Service) RecordUsageFinalization(ctx context.Context, apiKeyID uuid.UUID, modelAlias string, inputTokens, outputTokens, cacheReadTokens, cacheWriteTokens, consumedCredits int64, at time.Time) error`
   - `func (s *Service) MarkLastUsed(ctx context.Context, apiKeyID uuid.UUID, usedAt time.Time) error`

5. Implement the budget and usage rules exactly:
   - `ApplyReservationDelta` updates `public.api_key_budget_windows` so per-key budgets track both reserved and consumed credits
   - `RecordUsageFinalization` upserts `public.api_key_usage_rollups` for both `lifetime` and `monthly` windows
   - `RecordUsageFinalization` must not create a second wallet or balance table; it only updates projections derived from the existing shared-wallet accounting events
   - `MarkLastUsed` updates `public.api_keys.last_used_at` only when the new timestamp is later than the stored value

6. Update `apps/control-plane/internal/accounting/service.go` so:
   - reservation create calls `ApplyReservationDelta(..., reservedDelta = estimatedCredits, consumedDelta = 0, ...)` when `APIKeyID` is present
   - reservation expand adds to `reservedDelta`
   - finalize subtracts released reserve, adds consumed credits, and calls `RecordUsageFinalization`
   - release subtracts remaining reserved credits from the budget window

7. Add tests:
   - `TestRecordEventPersistsAPIKeyID`
   - `TestCreateReservationPropagatesAPIKeyIDToAttempt`
   - `TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup`
   - `TestBudgetProjectionCountsOpenReservations`
   - `TestMarkLastUsedOnlyMovesForward`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain "cd /workspace/apps/control-plane && go test ./internal/apikeys/... ./internal/accounting/... ./internal/usage/... -count=1" && rg -n "alter table public.usage_events add api_key_id|create table public.api_key_usage_rollups|create table public.api_key_budget_windows|create table public.api_key_rate_policies|RecordUsageFinalization|ApplyReservationDelta" supabase/migrations/20260331_04_api_key_usage_and_limits.sql apps/control-plane/internal/usage/types.go apps/control-plane/internal/apikeys/service.go apps/control-plane/internal/accounting/service.go</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260331_04_api_key_usage_and_limits.sql` contains `alter table public.usage_events add api_key_id`
    - `supabase/migrations/20260331_04_api_key_usage_and_limits.sql` contains `create table public.api_key_usage_rollups`
    - `supabase/migrations/20260331_04_api_key_usage_and_limits.sql` contains `create table public.api_key_budget_windows`
    - `supabase/migrations/20260331_04_api_key_usage_and_limits.sql` contains `create table public.api_key_rate_policies`
    - `apps/control-plane/internal/usage/types.go` contains `APIKeyID`
    - `apps/control-plane/internal/apikeys/service.go` contains `func (s *Service) RecordUsageFinalization`
    - `apps/control-plane/internal/accounting/service.go` contains `ApplyReservationDelta`
    - `apps/control-plane/internal/apikeys/service_test.go` contains `TestMarkLastUsedOnlyMovesForward`
  </acceptance_criteria>
  <done>Usage, spend, and budget projections are now attributable per key and per model while still deriving from the shared-wallet accounting flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Redis Lua rate limiting and OpenAI-compatible 429 metadata to the edge authorizer</name>
  <files>
    apps/edge-api/internal/errors/openai.go,
    apps/edge-api/internal/errors/openai_test.go,
    apps/edge-api/internal/authz/authorizer.go,
    apps/edge-api/internal/authz/ratelimit.go,
    apps/edge-api/internal/authz/ratelimit_test.go,
    apps/edge-api/internal/authz/scripts/rpm_tpm.lua,
    apps/edge-api/internal/authz/scripts/window_score.lua
  </files>
  <read_first>
    .planning/phases/05-api-keys-hot-path-enforcement/05-RESEARCH.md,
    apps/edge-api/internal/authz/authorizer.go,
    apps/edge-api/internal/errors/openai.go,
    apps/control-plane/internal/apikeys/types.go,
    apps/control-plane/internal/apikeys/service.go,
    apps/control-plane/internal/platform/redis/client.go
  </read_first>
  <action>
1. Create `apps/edge-api/internal/authz/scripts/rpm_tpm.lua` implementing a sliding-window counter with exactly these inputs:
   - current-window key
   - previous-window key
   - limit value
   - now in milliseconds
   - current-window length in milliseconds
   - increment amount
   - the script must return:
     - `allowed` (`1` or `0`)
     - `remaining`
     - `reset_ms`

2. Create `apps/edge-api/internal/authz/scripts/window_score.lua` implementing bucketed long-window scoring with exactly these inputs:
   - a list of rolling bucket keys
   - per-bucket TTL
   - increment score
   - max score
   - the script must return:
     - `allowed`
     - `remaining`
     - `reset_ms`

3. Create `apps/edge-api/internal/authz/ratelimit.go` with:
   - `type LimitResult`
   - `type Limiter`
   - `func NewLimiter(redisURL string) *Limiter`
   - `func (l *Limiter) Check(ctx context.Context, snapshot Snapshot, aliasID string, billableTokens int64, freeTokens int64) (LimitResult, error)`
   - use these exact Redis key patterns:
     - `rl:{acct:<account_id>:<alias_id>}:rpm:current`
     - `rl:{acct:<account_id>:<alias_id>}:rpm:previous`
     - `rl:{acct:<account_id>:<alias_id>}:tpm:current`
     - `rl:{acct:<account_id>:<alias_id>}:tpm:previous`
     - `rl:{key:<key_id>:<alias_id>}:rpm:current`
     - `rl:{key:<key_id>:<alias_id>}:rpm:previous`
     - `rl:{key:<key_id>:<alias_id>}:tpm:current`
     - `rl:{key:<key_id>:<alias_id>}:tpm:previous`
     - `fraud:{acct:<account_id>}:5h:<bucket>`
     - `fraud:{key:<key_id>}:5h:<bucket>`
     - `fraud:{acct:<account_id>}:7d:<bucket>`
     - `fraud:{key:<key_id>}:7d:<bucket>`

4. Implement limiter math exactly:
   - RPM and TPM use a `60s` sliding-window counter
   - the 5-hour window uses `5m` buckets over `60` buckets total
   - the weekly window uses `1d` buckets over `7` buckets total
   - the long-window fraud score increment is `consumed_credits + billable_tokens + freeTokens/10`
   - all math must stay integer-only

5. Extend `apps/edge-api/internal/authz/authorizer.go` so successful authorization also runs the limiter and:
   - returns a `429` OpenAI-style error with code `rate_limit_exceeded` when RPM, TPM, 5-hour, or weekly limits deny
   - sets only these headers when the limiter result is meaningful:
     - `x-ratelimit-limit-requests`
     - `x-ratelimit-remaining-requests`
     - `x-ratelimit-reset-requests`
     - `x-ratelimit-limit-tokens`
     - `x-ratelimit-remaining-tokens`
     - `x-ratelimit-reset-tokens`
     - `retry-after`
   - does not set any of those headers on invalid key, disabled key, expired key, revoked key, disallowed alias, or insufficient-quota denials

6. Extend `apps/edge-api/internal/errors/openai.go` with:
   - `func WriteRateLimitError(w http.ResponseWriter, message string, code *string, headers map[string]string)`
   - it must write the provided headers before the JSON error body

7. Add tests:
   - `TestRateLimitLuaReturnsRemainingAndReset`
   - `TestWeeklyFraudWindowUsesWeightedFreeTokens`
   - `TestAuthorizerReturns429WithRateLimitHeaders`
   - `TestPermanentFailuresOmitRetryHeaders`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain "cd /workspace && go test ./apps/edge-api/internal/authz ./apps/edge-api/internal/errors -count=1" && rg -n "x-ratelimit-limit-requests|x-ratelimit-remaining-requests|x-ratelimit-reset-requests|retry-after|rate_limit_exceeded|fraud:\\{acct:<account_id>\\}:5h:<bucket>|rl:\\{key:<key_id>:<alias_id>\\}:rpm:current" apps/edge-api/internal/errors/openai.go apps/edge-api/internal/authz/authorizer.go apps/edge-api/internal/authz/ratelimit.go apps/edge-api/internal/authz/scripts/rpm_tpm.lua apps/edge-api/internal/authz/scripts/window_score.lua</automated>
  </verify>
  <acceptance_criteria>
    - `apps/edge-api/internal/authz/scripts/rpm_tpm.lua` contains `allowed`
    - `apps/edge-api/internal/authz/scripts/window_score.lua` contains `remaining`
    - `apps/edge-api/internal/authz/ratelimit.go` contains `rl:{acct:<account_id>:<alias_id>}:rpm:current`
    - `apps/edge-api/internal/authz/ratelimit.go` contains `fraud:{key:<key_id>}:7d:<bucket>`
    - `apps/edge-api/internal/authz/authorizer.go` contains `rate_limit_exceeded`
    - `apps/edge-api/internal/errors/openai.go` contains `WriteRateLimitError`
    - `apps/edge-api/internal/authz/ratelimit_test.go` contains `TestWeeklyFraudWindowUsesWeightedFreeTokens`
    - `apps/edge-api/internal/errors/openai_test.go` contains `TestPermanentFailuresOmitRetryHeaders`
  </acceptance_criteria>
  <done>The edge can now enforce key and account rate limits and long-window quotas with atomic Redis decisions and only emits retry metadata when the denial is actually retryable.</done>
</task>

</tasks>
