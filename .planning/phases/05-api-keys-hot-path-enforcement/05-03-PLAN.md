---
phase: 05-api-keys-hot-path-enforcement
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - deploy/docker/docker-compose.yml
  - apps/edge-api/go.mod
  - apps/edge-api/go.sum
  - apps/edge-api/cmd/server/main.go
  - apps/edge-api/cmd/server/main_test.go
  - apps/edge-api/internal/authz/authz.go
  - apps/edge-api/internal/authz/authz_test.go
  - apps/edge-api/internal/authz/client.go
  - apps/edge-api/internal/authz/authorizer.go
  - apps/edge-api/internal/authz/authorizer_test.go
autonomous: true
requirements:
  - KEY-02
  - KEY-05
must_haves:
  truths:
    - "The edge resolves API-key auth state from Redis snapshots and the narrow internal resolver, not from direct Postgres access."
    - "Disallowed Hive aliases fail immediately in the edge hot path; Hive must never silently remap to a different allowed alias."
    - "Budget admission on the edge denies when consumed + reserved + estimated request cost would exceed the key budget, instead of waiting for finalization to overshoot the budget."
  artifacts:
    - path: "apps/edge-api/internal/authz/authz.go"
      provides: "Edge-side auth snapshot contract and access checks aligned to the control-plane JSON."
      contains: "type AuthSnapshot struct"
    - path: "apps/edge-api/internal/authz/client.go"
      provides: "Redis-first snapshot resolution with fallback to `/internal/apikeys/resolve`."
      contains: "func (c *Client) Resolve"
    - path: "apps/edge-api/internal/authz/authorizer.go"
      provides: "OpenAI-compatible invalid-key, model access, and projected-budget denials."
      contains: "func (a *Authorizer) Authorize"
  key_links:
    - from: "apps/edge-api/internal/authz/client.go"
      to: "apps/control-plane/internal/apikeys/http.go"
      via: "Cache misses must hydrate through `/internal/apikeys/resolve` rather than inventing an edge-side database path."
      pattern: "/internal/apikeys/resolve"
    - from: "apps/edge-api/internal/authz/authz.go"
      to: "apps/control-plane/internal/routing/service.go"
      via: "Budget and model checks happen before routing, but they must use the same alias IDs Phase 4 routing expects."
      pattern: "AllowedAliases"
---

<objective>
Add the edge-side auth snapshot consumer and projected-cost budget admission layer that turns the control-plane key policy into hot-path denials.

Purpose: Phase 5 cannot claim cheap hot-path authorization until the edge can resolve snapshots from Redis, reject disallowed aliases in Hive's alias space, and stop budget overruns before a billable request starts.
Output: a Redis-backed edge auth client, a snapshot-aware authorizer, and server wiring that reuses the authorizer on existing public routes.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-CONTEXT.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-RESEARCH.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-VALIDATION.md
@.planning/phases/05-api-keys-hot-path-enforcement/05-02-PLAN.md

<interfaces>
From apps/control-plane/internal/apikeys/types.go:
```go
type AuthSnapshot struct {
	KeyID                 uuid.UUID  `json:"key_id"`
	AccountID             uuid.UUID  `json:"account_id"`
	Status                KeyStatus  `json:"status"`
	ExpiresAt             *time.Time `json:"expires_at,omitempty"`
	AllowAllModels        bool       `json:"allow_all_models"`
	AllowedAliases        []string   `json:"allowed_aliases"`
	BudgetKind            string     `json:"budget_kind"`
	BudgetLimitCredits    *int64     `json:"budget_limit_credits,omitempty"`
	BudgetConsumedCredits int64      `json:"budget_consumed_credits"`
	BudgetReservedCredits int64      `json:"budget_reserved_credits"`
	BudgetAnchorAt        *time.Time `json:"budget_anchor_at,omitempty"`
	PolicyVersion         int64      `json:"policy_version"`
}
```

From apps/edge-api/internal/authz/authorizer.go:
```go
func (a *Authorizer) Authorize(ctx context.Context, authHeader string, aliasID string) (AuthSnapshot, *apierrors.OpenAIError)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the edge auth snapshot contract and Redis-backed resolver client</name>
  <files>
    deploy/docker/docker-compose.yml,
    apps/edge-api/go.mod,
    apps/edge-api/go.sum,
    apps/edge-api/internal/authz/authz.go,
    apps/edge-api/internal/authz/authz_test.go,
    apps/edge-api/internal/authz/client.go
  </files>
  <behavior>
    - The edge keeps no durable auth state of its own; it reads `auth:key:{token_hash}` from Redis and rehydrates misses through the internal control-plane resolver.
    - Access checks preserve the Hive alias namespace from Phase 4 and never remap a denied alias.
    - Budget checks use projected request cost at admission time.
  </behavior>
  <action>
1. Update `deploy/docker/docker-compose.yml` so the `edge-api` service also receives:
   - `REDIS_URL=${REDIS_URL:-redis://redis:6379/0}`
   - keep `EDGE_CONTROL_PLANE_BASE_URL` and the existing `control-plane` dependency intact

2. Update `apps/edge-api/go.mod` and `apps/edge-api/go.sum` to ensure `github.com/redis/go-redis/v9` is available in the edge module.

3. Update `apps/edge-api/internal/authz/authz.go` so the edge-side `AuthSnapshot` mirrors the control-plane JSON contract from `05-02`. Keep the file responsible for:
   - `type AuthSnapshot`
   - `type CheckResult`
   - `func CheckAccess(snapshot AuthSnapshot, requestedAlias string, estimatedCredits int64) CheckResult`
   - `CheckAccess` must:
     - deny any non-`active` status as `invalid_api_key`
     - deny expired keys as `invalid_api_key`
     - deny a disallowed alias as `model_not_found`
     - deny a budget overrun as `insufficient_quota` when `BudgetLimitCredits != nil` and `BudgetConsumedCredits + BudgetReservedCredits + estimatedCredits > *BudgetLimitCredits`
     - never remap or widen aliases when access is denied

4. Update `apps/edge-api/internal/authz/client.go` with:
   - `func HashBearerToken(authHeader string) string`
   - `type Client`
   - `func NewClient(baseURL string, redisURL string) (*Client, error)`
   - `func (c *Client) Resolve(ctx context.Context, rawToken string) (AuthSnapshot, error)`
   - `Resolve` must:
     - trim the `Bearer ` prefix before hashing when a header-shaped value is passed in
     - hash the raw secret with lowercase hex SHA-256
     - lookup Redis key `auth:key:{<token_hash>}`
     - on cache miss, `POST {baseURL}/internal/apikeys/resolve` with `{"token_hash":"<hash>"}`
     - cache the returned snapshot JSON back into Redis under the same key
   - do not add rate-limit logic to this client; that lands in a later edge-only plan

5. Add tests:
   - `TestResolveHydratesRedisFromControlPlane`
   - `TestCheckAccessRejectsProjectedBudgetOverrun`
   - `TestCheckAccessRejectsDisallowedAliasWithoutRemap`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain "cd /workspace && go test ./apps/edge-api/internal/authz -count=1" && rg -n "REDIS_URL=.*redis://redis:6379/0|auth:key:\\{|/internal/apikeys/resolve|estimatedCredits|model_not_found|insufficient_quota" deploy/docker/docker-compose.yml apps/edge-api/internal/authz/authz.go apps/edge-api/internal/authz/client.go</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/docker/docker-compose.yml` contains `REDIS_URL=${REDIS_URL:-redis://redis:6379/0}`
    - `apps/edge-api/internal/authz/client.go` contains `auth:key:{`
    - `apps/edge-api/internal/authz/client.go` contains `/internal/apikeys/resolve`
    - `apps/edge-api/internal/authz/authz.go` contains `estimatedCredits`
    - `apps/edge-api/internal/authz/authz.go` contains `model_not_found`
    - `apps/edge-api/internal/authz/authz.go` contains `insufficient_quota`
    - `apps/edge-api/internal/authz/authz_test.go` contains `TestCheckAccessRejectsProjectedBudgetOverrun`
  </acceptance_criteria>
  <done>The edge now has a Redis-first auth snapshot reader and access checks that reject disallowed aliases and projected budget overruns before a request runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the edge authorizer into the server with projected-cost budget admission</name>
  <files>
    apps/edge-api/cmd/server/main.go,
    apps/edge-api/cmd/server/main_test.go,
    apps/edge-api/internal/authz/authorizer.go,
    apps/edge-api/internal/authz/authorizer_test.go
  </files>
  <behavior>
    - The edge server builds one reusable authorizer from the shared auth client.
    - Existing public routes can pass `estimatedCredits = 0`, but the helper contract makes future billable routes pass real estimated cost.
    - Budget denials stay OpenAI-compatible and happen before upstream execution.
  </behavior>
  <action>
1. Update `apps/edge-api/internal/authz/authorizer.go` with:
   - `type Authorizer`
   - `func NewAuthorizer(client *Client) *Authorizer`
   - `func (a *Authorizer) Authorize(ctx context.Context, authHeader string, aliasID string, estimatedCredits int64) (AuthSnapshot, *apierrors.OpenAIError)`
   - exact failure mapping:
     - missing or malformed bearer header -> HTTP `401`, type `invalid_request_error`, code `invalid_api_key`
     - unknown, revoked, disabled, or expired key -> HTTP `401`, type `invalid_request_error`, code `invalid_api_key`
     - alias not in `AllowedAliases` while `AllowAllModels` is false -> HTTP `404`, type `invalid_request_error`, code `model_not_found`
     - projected budget denial -> HTTP `429`, type `insufficient_quota`, code `insufficient_quota`

2. Update `apps/edge-api/cmd/server/main.go` so the server:
   - builds one authz client from `EDGE_CONTROL_PLANE_BASE_URL` and `REDIS_URL`
   - builds one authorizer from that client
   - keeps `/health`, `/docs`, and `/catalog/models` public
   - routes `/v1/models` through `authorizeAliasRequest(r, "", 0)`
   - adds one helper function `authorizeAliasRequest(r *http.Request, aliasID string, estimatedCredits int64)` that future billable handlers can reuse

3. Make the estimated-cost contract explicit in code comments and tests:
   - `/v1/models` passes `0` because it is not billable
   - any future billable `/v1/*` handler must pass the request's estimated Hive Credit cost into `Authorize` before upstream execution; do not default billable routes to `0`

4. Add tests:
   - `TestAuthorizeRejectsMalformedBearer`
   - `TestAuthorizeRejectsExpiredKey`
   - `TestAuthorizeReturnsInsufficientQuotaOnProjectedBudgetOverrun`
   - `TestModelsRouteRequiresValidAPIKey`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain "cd /workspace && go test ./apps/edge-api/internal/authz ./apps/edge-api/cmd/server -count=1" && rg -n "Authorize\\(ctx, authHeader, aliasID string, estimatedCredits int64\\)|authorizeAliasRequest\\(r \\*http.Request, aliasID string, estimatedCredits int64\\)|insufficient_quota" apps/edge-api/internal/authz/authorizer.go apps/edge-api/cmd/server/main.go</automated>
  </verify>
  <acceptance_criteria>
    - `apps/edge-api/internal/authz/authorizer.go` contains `estimatedCredits int64`
    - `apps/edge-api/internal/authz/authorizer.go` contains `insufficient_quota`
    - `apps/edge-api/cmd/server/main.go` contains `authorizeAliasRequest`
    - `apps/edge-api/cmd/server/main.go` contains `/v1/models`
    - `apps/edge-api/cmd/server/main_test.go` contains `TestModelsRouteRequiresValidAPIKey`
    - `apps/edge-api/internal/authz/authorizer_test.go` contains `TestAuthorizeReturnsInsufficientQuotaOnProjectedBudgetOverrun`
  </acceptance_criteria>
  <done>The edge server now enforces key validity, alias access, and projected-cost budget admission through a reusable authorizer contract that future billable endpoints can extend.</done>
</task>

</tasks>

<verification>
Run the edge auth and server package tests, then confirm the authorizer contract now accepts `estimatedCredits` and the access check uses `consumed + reserved + estimatedCredits` for budget denial.
</verification>

<success_criteria>
- The edge resolves auth snapshots from Redis with a narrow control-plane fallback.
- Disallowed aliases and invalid keys fail immediately with OpenAI-compatible errors in Hive's alias space.
- Budget admission now blocks projected overruns before billable work starts.
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-keys-hot-path-enforcement/05-03-SUMMARY.md`
</output>
