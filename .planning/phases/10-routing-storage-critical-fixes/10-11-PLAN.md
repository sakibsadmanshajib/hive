---
phase: 10-routing-storage-critical-fixes
plan: 11
type: execute
wave: 7
depends_on:
  - 10-10
files_modified:
  - apps/control-plane/internal/batchstore/worker.go
  - apps/control-plane/internal/batchstore/worker_test.go
  - apps/control-plane/cmd/server/main.go
  - scripts/phase10-smoke.sh
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - API-05
  - API-06
  - API-07
  - KEY-04
user_setup:
  - service: supabase-storage-and-live-smoke
    why: "The final live smoke requires local runtime credentials and a Hive API key."
    env_vars:
      - name: S3_ENDPOINT
        source: "Supabase Dashboard > Storage > S3 Access endpoint, including /storage/v1/s3"
      - name: S3_ACCESS_KEY
        source: "Supabase Dashboard > Storage > S3 Access Keys"
      - name: S3_SECRET_KEY
        source: "Supabase Dashboard > Storage > S3 Access Keys"
      - name: S3_REGION
        source: "Supabase Dashboard > Storage > S3 Access settings"
      - name: S3_BUCKET_FILES
        source: "Pre-created bucket name, default hive-files"
      - name: S3_BUCKET_IMAGES
        source: "Pre-created bucket name, default hive-images"
      - name: HIVE_API_KEY
        source: "A Hive API key for the running local stack"
    dashboard_config:
      - task: "Ensure hive-files and hive-images buckets exist before live smoke"
        location: "Supabase Dashboard > Storage > Buckets"
must_haves:
  truths:
    - "Any terminal batch with completed requests finalizes consumed credits before terminal status is written."
    - "Terminal batches with zero completed work release the reservation directly, while partial terminal batches rely on finalization to release only the unused remainder."
    - "Completed batch settlement writes usage/spend through existing accounting finalization so rollups are attributable by API key and model."
    - "KEY-04 is marked Complete only after terminal settlement tests and the accounting rollup proof pass."
    - "Live smoke is required when credentials exist, and skipped summaries list exact missing env vars when credentials are absent."
  artifacts:
    - path: "apps/control-plane/internal/batchstore/worker.go"
      provides: "Terminal reservation settlement"
      contains: "FinalizeReservation"
    - path: "apps/control-plane/internal/batchstore/worker_test.go"
      provides: "Worker settlement behavior tests"
      contains: "TestBatchWorkerFinalizesCancelledPartialBatches"
    - path: "apps/control-plane/internal/accounting/service_test.go"
      provides: "Existing real accounting finalization rollup proof"
      contains: "TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup"
    - path: "scripts/phase10-smoke.sh"
      provides: "Runtime accounting-contract smoke checks"
      contains: "Failed to reserve credits for batch"
    - path: ".planning/REQUIREMENTS.md"
      provides: "KEY-04 status"
      contains: "| KEY-04 | Phase 10 | Complete |"
  key_links:
    - from: "apps/control-plane/internal/batchstore/worker.go"
      to: "apps/control-plane/internal/accounting/service.go"
      via: "AccountingSettler.FinalizeReservation for completed or partial terminal batches"
      pattern: "accounting\\.FinalizeReservationInput"
    - from: "apps/control-plane/internal/batchstore/worker_test.go"
      to: "apps/control-plane/internal/batchstore/worker.go"
      via: "TestBatchWorkerFinalizesCancelledPartialBatches proves cancelled partial batches use finalization instead of direct release"
      pattern: "TestBatchWorkerFinalizesCancelledPartialBatches"
    - from: "apps/control-plane/internal/accounting/service_test.go"
      to: "KEY-04 rollup invariant"
      via: "TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup"
      pattern: "modelAlias != \"hive-fast\""
---

<objective>
Settle terminal batch reservations through accounting, prove the API-key/model rollup invariant, and close the final Phase 10 smoke and KEY-04 gates.

Purpose: Plan 10-10 makes attribution durable and propagated. This plan consumes those fields at terminal batch status, delegates usage/spend rollups to the existing accounting service, and updates KEY-04 only after the named accounting proof and terminal settlement tests pass.
Output: Worker settlement wiring, control-plane startup wiring, smoke failure checks for accounting-contract regressions, and KEY-04 completion status.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-routing-storage-critical-fixes/10-CONTEXT.md
@.planning/phases/10-routing-storage-critical-fixes/10-VERIFICATION.md
@.planning/phases/10-routing-storage-critical-fixes/10-10-SUMMARY.md
@apps/control-plane/internal/batchstore/worker.go
@apps/control-plane/internal/batchstore/types.go
@apps/control-plane/internal/accounting/service.go
@apps/control-plane/internal/accounting/service_test.go
@apps/control-plane/internal/accounting/types.go
@apps/control-plane/internal/apikeys/service.go
@apps/control-plane/internal/filestore/types.go
@apps/control-plane/cmd/server/main.go
@packages/openai-contract/upstream/openapi.yaml
@scripts/phase10-smoke.sh

<interfaces>
Existing accounting finalization records usage events and API-key/model rollups when the reservation has `APIKeyID` and `ModelAlias`. The invariant is already proven by:

```text
apps/control-plane/internal/accounting/service_test.go::TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup
```

The worker should use a narrow interface rather than importing the concrete accounting service:

```go
type AccountingSettler interface {
	FinalizeReservation(ctx context.Context, input accounting.FinalizeReservationInput) (accounting.Reservation, error)
	ReleaseReservation(ctx context.Context, input accounting.ReleaseReservationInput) (accounting.Reservation, error)
}
```

The `filestore.FileService` interface in `batchstore` must expose persisted attribution from Plan 10-10:

```go
GetBatch(ctx context.Context, id, accountID string) (*filestore.Batch, error)
```

Upstream batch cancel semantics must be honored exactly: `packages/openai-contract/upstream/openapi.yaml` documents `/batches/{batch_id}/cancel` as changing the batch to `cancelled`, where it can still have `partial results (if any) available in the output file`. Do not plan `cancelled` as an automatic full release path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settle terminal batch reservations through accounting</name>
  <files>apps/control-plane/internal/batchstore/worker.go, apps/control-plane/internal/batchstore/worker_test.go, apps/control-plane/cmd/server/main.go</files>
  <read_first>
    - apps/control-plane/internal/batchstore/worker.go
    - apps/control-plane/internal/batchstore/worker_test.go
    - apps/control-plane/internal/batchstore/types.go
    - apps/control-plane/internal/accounting/service.go
    - apps/control-plane/internal/accounting/types.go
    - apps/control-plane/internal/accounting/service_test.go
    - apps/control-plane/internal/apikeys/service.go
    - apps/control-plane/internal/filestore/types.go
    - apps/control-plane/cmd/server/main.go
    - .planning/phases/10-routing-storage-critical-fixes/10-10-SUMMARY.md
    - .planning/phases/10-routing-storage-critical-fixes/10-VERIFICATION.md
  </read_first>
  <behavior>
    - Completed batches call accounting `FinalizeReservation` before status is updated to `completed`.
    - Cancelled, failed, and expired batches that report one or more completed requests still call accounting `FinalizeReservation` before terminal status is updated.
    - `ReleaseReservation` is used only when terminal work consumed zero credits; partial terminal batches rely on `FinalizeReservation` so accounting releases only the unused remainder internally.
    - Completed batch status update includes `actual_credits`.
    - Accounting finalization receives the same account ID and reservation ID stored on the batch, the actual credit amount derived from completed request count, and the real terminal status string (`completed`, `cancelled`, `failed`, or `expired`).
    - The control-plane worker is constructed with the existing `accountingSvc`.
    - Worker tests prove the worker calls the settler for both release-only and cancelled-partial finalize paths; `apps/control-plane/internal/accounting/service_test.go::TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup` proves the real accounting finalization path updates API-key/model rollups.
  </behavior>
  <action>
    In `apps/control-plane/internal/batchstore/worker.go`:
    - Import `github.com/google/uuid` and `github.com/hivegpt/hive/apps/control-plane/internal/accounting`.
    - Extend `FileService` with `GetBatch(ctx context.Context, id, accountID string) (*filestore.Batch, error)`.
    - Add the `AccountingSettler` interface shown in this plan's `<interfaces>` block.
    - Add `accounting AccountingSettler` to `BatchWorker`.
    - Change `NewBatchWorker` to accept the settler as the final argument and store it.
    - In `HandleBatchPoll`, after unmarshalling payload and before terminal settlement, load the persisted batch with `w.fileService.GetBatch(ctx, payload.BatchID, payload.AccountID)`. Use persisted values first; payload fields are fallback for `api_key_id`, `model_alias`, and `estimated_credits`.
    - For any terminal status, derive an integral `completedRequests` count from upstream `request_counts.completed` first, then payload fallback data if needed.
    - Compute `actualCredits` from completed request count:
      - If `payload.ActualCredits > 0`, use it.
      - Else if upstream `request_counts.completed` is available, use `completed * 10` for `/v1/embeddings` and `completed * 1000` for other valid batch endpoints.
      - If computed actual credits exceed persisted or payload `estimated_credits`, cap at `estimated_credits`.
      - If no count is available but terminal status is `completed`, use `estimated_credits`.
      - If terminal status is `cancelled`, `failed`, or `expired` and no completed count exists, leave `actualCredits == 0`.
    - Keep credit math integral. When normalizing `request_counts` from JSON, reject non-integer numeric values before converting to `int64`; do not introduce float64 credit arithmetic. If any fractional money/FX math becomes necessary, use `math/big` per project convention.
    - Settle terminal reservations with this branch table before `UpdateBatchStatus(..., status, fields)`:
      - If `actualCredits > 0`, call `FinalizeReservation` for any terminal status and pass the upstream terminal status string through unchanged. Do not call `ReleaseReservation` afterward; `apps/control-plane/internal/accounting/service.go` already computes and releases `releasableCredits(...)` inside `FinalizeReservation`.
      - If `actualCredits == 0` and status is `completed`, call `ReleaseReservation` with reason `batch_completed_unused`.
      - If `actualCredits == 0` and status is `failed`, `cancelled`, or `expired`, call `ReleaseReservation` with reasons `batch_failed`, `batch_cancelled`, and `batch_expired`.
    - For the finalize branch, call:
```go
_, err := w.accounting.FinalizeReservation(ctx, accounting.FinalizeReservationInput{
	AccountID:              parsedAccountID,
	ReservationID:          parsedReservationID,
	ActualCredits:          actualCredits,
	TerminalUsageConfirmed: true,
	Status:                 status,
})
```
      Return an error containing `batchstore: finalize reservation` if settlement fails.
    - Include `fields["actual_credits"] = actualCredits`.
    - For the release-only branch, return an error containing `batchstore: release reservation` if `ReleaseReservation` fails.
    - If terminal settlement lacks an account ID or reservation ID, return an error containing `missing reservation_id for terminal batch settlement` or `invalid account_id for terminal batch settlement`; do not mark terminal status.

    In `apps/control-plane/cmd/server/main.go`, update `batchstore.NewBatchWorker(...)` to pass `accountingSvc` as the final argument.

    Extend `apps/control-plane/internal/batchstore/worker_test.go`:
    - Update fake file service to implement `GetBatch`.
    - Use valid UUID strings for `AccountID`, `ReservationID`, and `APIKeyID`.
    - Add fake accounting settler recording finalize and release calls.
    - Extend `TestBatchWorkerStoresCompletedOutputFiles` to assert one finalize call with `ActualCredits == 2000`, `Status == "completed"`, and `TerminalUsageConfirmed == true`.
    - Add `TestBatchWorkerFinalizesCancelledPartialBatches` using upstream status `cancelled` plus `request_counts.completed == 1`; assert one finalize call with `ActualCredits == 1000`, `Status == "cancelled"`, `TerminalUsageConfirmed == true`, zero direct release calls, and persisted `actual_credits == 1000` so partial terminal work is not dropped.
    - Add `TestBatchWorkerReleasesFailedCancelledAndExpiredReservations` for zero-work terminals only and assert release reasons `batch_failed`, `batch_cancelled`, and `batch_expired`.
    - Add `TestBatchWorkerRequiresReservationForTerminalSettlement` and assert the error contains `missing reservation_id for terminal batch settlement`.

    Do not add fake rollup assertions to worker tests. The worker only owns the settlement call. The real API-key/model rollup invariant is proven by `apps/control-plane/internal/accounting/service_test.go::TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup`, which this task must run and keep passing.
  </action>
  <verify>
    <automated>docker compose -f deploy/docker/docker-compose.yml run --rm --entrypoint /bin/sh toolchain -lc 'export PATH=/usr/local/go/bin:$PATH; cd /workspace && go test ./apps/control-plane/internal/batchstore ./apps/control-plane/cmd/server ./apps/control-plane/internal/accounting -run "TestBatchWorkerStoresCompletedOutputFiles|TestBatchWorkerFinalizesCancelledPartialBatches|TestBatchWorkerReleasesFailedCancelledAndExpiredReservations|TestBatchWorkerRequiresReservationForTerminalSettlement|TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup" -count=1'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/control-plane/internal/batchstore/worker.go` contains `type AccountingSettler interface`
    - `apps/control-plane/internal/batchstore/worker.go` contains `FinalizeReservation`
    - `apps/control-plane/internal/batchstore/worker.go` contains `ReleaseReservation`
    - `apps/control-plane/internal/batchstore/worker.go` contains `actual_credits`
    - `apps/control-plane/internal/batchstore/worker.go` contains `batch_completed_unused`
    - `apps/control-plane/internal/batchstore/worker.go` contains `missing reservation_id for terminal batch settlement`
    - `apps/control-plane/internal/batchstore/worker.go` does not contain `actualCredits := float64`
    - `apps/control-plane/cmd/server/main.go` contains `accountingSvc`
    - `apps/control-plane/internal/batchstore/worker_test.go` contains `TestBatchWorkerFinalizesCancelledPartialBatches`
    - `apps/control-plane/internal/batchstore/worker_test.go` contains `TestBatchWorkerReleasesFailedCancelledAndExpiredReservations`
    - `apps/control-plane/internal/batchstore/worker_test.go` contains `TestBatchWorkerRequiresReservationForTerminalSettlement`
    - `apps/control-plane/internal/accounting/service_test.go` contains `TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup`
    - The automated command exits 0, including the named accounting rollup test
  </acceptance_criteria>
  <done>Terminal batch statuses settle reservations through accounting without dropping cancelled partial results, and the real accounting finalization test proves API-key/model usage rollups after finalization.</done>
</task>

<task type="auto">
  <name>Task 2: Close KEY-04 and enforce honest final smoke semantics</name>
  <files>scripts/phase10-smoke.sh, .planning/REQUIREMENTS.md</files>
  <read_first>
    - scripts/phase10-smoke.sh
    - scripts/phase10-scrub-legacy-storage.sh
    - .planning/REQUIREMENTS.md
    - .planning/phases/10-routing-storage-critical-fixes/10-VALIDATION.md
    - .planning/phases/10-routing-storage-critical-fixes/10-VERIFICATION.md
    - apps/control-plane/internal/accounting/service_test.go
    - apps/control-plane/internal/batchstore/worker_test.go
  </read_first>
  <action>
    Update `scripts/phase10-smoke.sh` so image/audio/batch checks fail if any response body contains:
    - `policy_mode must be strict or temporary_overage`
    - `model_alias is required`
    - `Failed to reserve credits for batch`

    Run the focused terminal-settlement test command from Task 1, then run the full Go suite, smoke syntax check, and purge check. The focused command must include `TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup`; this is the existing real accounting service test that proves `FinalizeReservation` updates API-key/model usage rollups.

    After those tests pass, update `.planning/REQUIREMENTS.md`:
    - Change `- [ ] **KEY-04**: Hive tracks usage and spend per API key and per model.` to `- [x] **KEY-04**: Hive tracks usage and spend per API key and per model.`
    - Change traceability row `| KEY-04 | Phase 10 | Pending |` to `| KEY-04 | Phase 10 | Complete |`.

    For live smoke, use this rule:
    - If `.env` or the shell provides non-empty `S3_ENDPOINT`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`, `S3_REGION`, `S3_BUCKET_FILES`, `S3_BUCKET_IMAGES`, and `HIVE_API_KEY`, run `HIVE_BASE_URL="${HIVE_BASE_URL:-http://localhost:8080}" HIVE_CONTROL_PLANE_URL="${HIVE_CONTROL_PLANE_URL:-http://localhost:8081}" HIVE_API_KEY="$HIVE_API_KEY" sh scripts/phase10-smoke.sh`.
    - If any required var is missing, do not run the live smoke and do not claim it passed. The plan summary must contain `Live smoke skipped` and list every missing env var by name.
  </action>
  <verify>
    <automated>docker compose -f deploy/docker/docker-compose.yml run --rm --entrypoint /bin/sh toolchain -lc 'export PATH=/usr/local/go/bin:$PATH; cd /workspace && go test ./apps/control-plane/internal/batchstore ./apps/control-plane/cmd/server ./apps/control-plane/internal/accounting -run "TestBatchWorkerStoresCompletedOutputFiles|TestBatchWorkerFinalizesCancelledPartialBatches|TestBatchWorkerReleasesFailedCancelledAndExpiredReservations|TestBatchWorkerRequiresReservationForTerminalSettlement|TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup" -count=1'</automated>
    <automated>docker compose -f deploy/docker/docker-compose.yml run --rm --entrypoint /bin/sh toolchain -lc 'export PATH=/usr/local/go/bin:$PATH; cd /workspace && go test ./apps/control-plane/... ./apps/edge-api/... ./packages/storage/... -count=1'</automated>
    <automated>sh -n scripts/phase10-smoke.sh</automated>
    <automated>sh scripts/phase10-scrub-legacy-storage.sh --check</automated>
    <automated>bash -lc 'set -a; [ -f .env ] && . ./.env; set +a; missing=\"\"; for v in S3_ENDPOINT S3_ACCESS_KEY S3_SECRET_KEY S3_REGION S3_BUCKET_FILES S3_BUCKET_IMAGES HIVE_API_KEY; do eval val=\"\\${$v:-}\"; [ -n \"$val\" ] || missing=\"$missing $v\"; done; if [ -n \"$missing\" ]; then echo \"Live smoke skipped:$missing\"; else HIVE_BASE_URL=\"${HIVE_BASE_URL:-http://localhost:8080}\" HIVE_CONTROL_PLANE_URL=\"${HIVE_CONTROL_PLANE_URL:-http://localhost:8081}\" HIVE_API_KEY=\"$HIVE_API_KEY\" sh scripts/phase10-smoke.sh; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase10-smoke.sh` contains `policy_mode must be strict or temporary_overage`
    - `scripts/phase10-smoke.sh` contains `model_alias is required`
    - `scripts/phase10-smoke.sh` contains `Failed to reserve credits for batch`
    - `.planning/REQUIREMENTS.md` contains `- [x] **KEY-04**`
    - `.planning/REQUIREMENTS.md` contains `| KEY-04 | Phase 10 | Complete |`
    - Focused terminal-settlement command exits 0 and includes `TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup`
    - Full Go suite command exits 0
    - `sh -n scripts/phase10-smoke.sh` exits 0
    - Purge check exits 0
    - If live smoke runs, image/audio/batch outputs do not contain `policy_mode must be strict or temporary_overage`, `model_alias is required`, or `Failed to reserve credits for batch`
    - If live smoke is skipped, the SUMMARY contains `Live smoke skipped` and lists each missing env var by name
  </acceptance_criteria>
  <done>KEY-04 is marked complete only after terminal settlement and real accounting rollup tests pass, and final smoke output is either executed or explicitly skipped with exact missing env vars.</done>
</task>

</tasks>

<verification>
Final gate:

```bash
docker compose -f deploy/docker/docker-compose.yml run --rm --entrypoint /bin/sh toolchain -lc 'export PATH=/usr/local/go/bin:$PATH; cd /workspace && go test ./apps/control-plane/internal/batchstore ./apps/control-plane/cmd/server ./apps/control-plane/internal/accounting -run "TestBatchWorkerStoresCompletedOutputFiles|TestBatchWorkerFinalizesCancelledPartialBatches|TestBatchWorkerReleasesFailedCancelledAndExpiredReservations|TestBatchWorkerRequiresReservationForTerminalSettlement|TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup" -count=1'
docker compose -f deploy/docker/docker-compose.yml run --rm --entrypoint /bin/sh toolchain -lc 'export PATH=/usr/local/go/bin:$PATH; cd /workspace && go test ./apps/control-plane/... ./apps/edge-api/... ./packages/storage/... -count=1'
docker compose --env-file .env -f deploy/docker/docker-compose.yml config --services
sh -n scripts/phase10-smoke.sh
sh scripts/phase10-scrub-legacy-storage.sh --check
```

If `.env` or the shell has non-empty `S3_ENDPOINT`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`, `S3_REGION`, `S3_BUCKET_FILES`, `S3_BUCKET_IMAGES`, and `HIVE_API_KEY`, run:

```bash
HIVE_BASE_URL="${HIVE_BASE_URL:-http://localhost:8080}" HIVE_CONTROL_PLANE_URL="${HIVE_CONTROL_PLANE_URL:-http://localhost:8081}" HIVE_API_KEY="$HIVE_API_KEY" sh scripts/phase10-smoke.sh
```

If live smoke cannot run, the summary must say `Live smoke skipped` and list missing env vars by name. Do not claim live smoke passed unless the command ran.
</verification>

<success_criteria>
- Batch worker finalizes any terminal batch that consumed work and uses direct release only for zero-work terminal batches.
- `apps/control-plane/internal/accounting/service_test.go::TestFinalizeReservationUpdatesBudgetWindowAndUsageRollup` passes, proving real accounting finalization updates API-key/model rollups.
- `KEY-04` is checked and marked Complete in `.planning/REQUIREMENTS.md`.
- Full Go suite passes.
- Live smoke either runs successfully after runtime path fixes or is explicitly skipped with missing env vars listed.
</success_criteria>

<output>
After completion, create `.planning/phases/10-routing-storage-critical-fixes/10-11-SUMMARY.md`.
</output>
