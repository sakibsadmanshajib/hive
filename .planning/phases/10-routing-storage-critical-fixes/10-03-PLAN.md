---
phase: 10-routing-storage-critical-fixes
plan: 03
type: execute
wave: 1
depends_on:
  - 10-02
files_modified:
  - supabase/migrations/20260414_01_provider_capabilities_media_columns.sql
  - supabase/migrations/20260414_02_filestore_tables.sql
  - apps/control-plane/internal/routing/repository.go
  - apps/control-plane/internal/filestore/repository.go
  - apps/control-plane/internal/routing/repository_schema_test.go
  - apps/control-plane/internal/filestore/repository_test.go
autonomous: true
requirements:
  - ROUT-02
  - API-05
  - API-06
  - API-07
must_haves:
  truths:
    - "Routing repository no longer mutates schema at startup"
    - "provider_capabilities has five media capability columns through SQL migration"
    - "Existing approved hive-auto route data is backfilled so image, TTS, STT, and batch capability filters can select a route"
    - "Filestore tables and indexes are created by Supabase migrations only"
  artifacts:
    - path: "supabase/migrations/20260414_01_provider_capabilities_media_columns.sql"
      provides: "Media capability columns and route eligibility backfill on provider_capabilities"
      contains: "route-openrouter-auto"
    - path: "supabase/migrations/20260414_02_filestore_tables.sql"
      provides: "files/uploads/upload_parts/batches schema"
      contains: "create table if not exists public.batches"
    - path: "apps/control-plane/internal/routing/repository.go"
      provides: "Routing repository without runtime DDL"
      contains: "func NewPgxRepository"
    - path: "apps/control-plane/internal/filestore/repository.go"
      provides: "Filestore repository without runtime schema creation"
      contains: "func NewRepository"
  key_links:
    - from: "apps/control-plane/internal/routing/repository.go"
      to: "supabase/migrations/20260414_01_provider_capabilities_media_columns.sql"
      via: "ListRouteCandidates assumes migrated columns"
      pattern: "c\\.supports_image_generation"
    - from: "apps/control-plane/internal/filestore/repository.go"
      to: "supabase/migrations/20260414_02_filestore_tables.sql"
      via: "repository assumes migrated tables"
      pattern: "public\\.files"
---

<objective>
Move routing and filestore schema ownership out of Go constructors and into Supabase migrations.

Purpose: This closes the routing SQL failure and removes the runtime DDL pattern that caused the outage.
Output: Two SQL migrations plus simplified repositories with the Wave 0 schema tests passing.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-routing-storage-critical-fixes/10-CONTEXT.md
@.planning/phases/10-routing-storage-critical-fixes/10-RESEARCH.md
@.planning/phases/10-routing-storage-critical-fixes/10-VALIDATION.md
@.planning/phases/10-routing-storage-critical-fixes/10-02-SUMMARY.md
@supabase/migrations/20260331_02_routing_policy.sql
@apps/control-plane/internal/routing/repository.go
@apps/control-plane/internal/filestore/repository.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add routing and filestore Supabase migrations</name>
  <files>supabase/migrations/20260414_01_provider_capabilities_media_columns.sql, supabase/migrations/20260414_02_filestore_tables.sql</files>
  <read_first>
    - supabase/migrations/20260331_02_routing_policy.sql
    - apps/control-plane/internal/filestore/repository.go
    - apps/control-plane/internal/routing/repository.go
    - apps/control-plane/internal/filestore/types.go
    - .planning/phases/10-routing-storage-critical-fixes/10-RESEARCH.md
  </read_first>
  <action>
    Create `supabase/migrations/20260414_01_provider_capabilities_media_columns.sql` with this exact table target, columns, and backfill:

```sql
alter table public.provider_capabilities
  add column if not exists supports_image_generation boolean not null default false,
  add column if not exists supports_image_edit boolean not null default false,
  add column if not exists supports_tts boolean not null default false,
  add column if not exists supports_stt boolean not null default false,
  add column if not exists supports_batch boolean not null default false;

update public.provider_capabilities
set
  supports_image_generation = true,
  supports_image_edit = true,
  supports_tts = true,
  supports_stt = true,
  supports_batch = true
where route_id = 'route-openrouter-auto';
```

    This backfill is required because the new columns default false; without it `SelectRoute` filters out every media/batch candidate even after the migration succeeds. Use `route-openrouter-auto` because it is the existing approved `hive-auto` route in `supabase/migrations/20260331_02_routing_policy.sql`. Do not change routing logic and do not add new public endpoints.

    Create `supabase/migrations/20260414_02_filestore_tables.sql` by moving the Go `ensureSchema` DDL into SQL migrations with schema-qualified table names. It must create:
    - `public.files` with columns `id text primary key`, `account_id text not null`, `purpose text not null`, `filename text not null`, `bytes bigint not null`, `status text not null default 'uploaded'`, `storage_path text not null`, `created_at timestamptz not null default now()`, `expires_at timestamptz`.
    - `public.uploads` with columns `id text primary key`, `account_id text not null`, `filename text not null`, `bytes bigint not null`, `mime_type text not null`, `purpose text not null`, `status text not null default 'pending'`, `s3_upload_id text`, `storage_path text not null`, `created_at timestamptz not null default now()`, `expires_at timestamptz not null`.
    - `public.upload_parts` with columns `id text primary key`, `upload_id text not null references public.uploads(id)`, `part_num int not null`, `etag text not null`, `created_at timestamptz not null default now()`, and a unique constraint on `(upload_id, part_num)`.
    - `public.batches` with columns `id text primary key`, `account_id text not null`, `input_file_id text not null references public.files(id)`, `output_file_id text references public.files(id)`, `error_file_id text references public.files(id)`, `endpoint text not null`, `completion_window text not null default '24h'`, `status text not null default 'validating'`, `provider text not null default ''`, `upstream_batch_id text`, `reservation_id text`, `request_counts_total int not null default 0`, `request_counts_completed int not null default 0`, `request_counts_failed int not null default 0`, `created_at timestamptz not null default now()`, `in_progress_at timestamptz`, `completed_at timestamptz`, `failed_at timestamptz`, `cancelled_at timestamptz`, `expires_at timestamptz not null default (now() + interval '24 hours')`.
    - indexes `idx_files_account_id`, `idx_uploads_account_id`, `idx_upload_parts_upload_id`, and `idx_batches_account_id`.

    Use `create table if not exists` and `create index if not exists`. Do not add runtime DDL anywhere in Go.
  </action>
  <verify>
    <automated>docker compose -f deploy/docker/docker-compose.yml run --rm toolchain sh -lc 'cd /workspace && go test ./apps/control-plane/internal/routing ./apps/control-plane/internal/filestore -run "TestProviderCapabilitiesMigrationAddsMediaColumns|TestProviderCapabilitiesMigrationBackfillsMediaAndBatchRoutes|TestSelectRouteSucceedsForSeededMediaAndBatchCapabilities|TestFilestoreSchemaLivesInSupabaseMigration" -count=1'</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260414_01_provider_capabilities_media_columns.sql` contains `alter table public.provider_capabilities`
    - `supabase/migrations/20260414_01_provider_capabilities_media_columns.sql` contains `supports_image_generation boolean not null default false`
    - `supabase/migrations/20260414_01_provider_capabilities_media_columns.sql` contains `supports_batch boolean not null default false`
    - `supabase/migrations/20260414_01_provider_capabilities_media_columns.sql` contains `where route_id = 'route-openrouter-auto'`
    - `supabase/migrations/20260414_01_provider_capabilities_media_columns.sql` contains `supports_image_generation = true`
    - `supabase/migrations/20260414_01_provider_capabilities_media_columns.sql` contains `supports_tts = true`
    - `supabase/migrations/20260414_01_provider_capabilities_media_columns.sql` contains `supports_stt = true`
    - `supabase/migrations/20260414_01_provider_capabilities_media_columns.sql` contains `supports_batch = true`
    - `supabase/migrations/20260414_02_filestore_tables.sql` contains `create table if not exists public.files`
    - `supabase/migrations/20260414_02_filestore_tables.sql` contains `create table if not exists public.uploads`
    - `supabase/migrations/20260414_02_filestore_tables.sql` contains `create table if not exists public.upload_parts`
    - `supabase/migrations/20260414_02_filestore_tables.sql` contains `create table if not exists public.batches`
    - `supabase/migrations/20260414_02_filestore_tables.sql` contains `unique (upload_id, part_num)`
    - The automated command exits 0
  </acceptance_criteria>
  <done>Supabase migrations own the routing media columns and filestore schema.</done>
</task>

<task type="auto">
  <name>Task 2: Delete runtime DDL from routing and filestore repositories</name>
  <files>apps/control-plane/internal/routing/repository.go, apps/control-plane/internal/filestore/repository.go, apps/control-plane/internal/routing/repository_schema_test.go, apps/control-plane/internal/filestore/repository_test.go</files>
  <read_first>
    - apps/control-plane/internal/routing/repository.go
    - apps/control-plane/internal/filestore/repository.go
    - apps/control-plane/internal/routing/repository_schema_test.go
    - apps/control-plane/internal/filestore/repository_test.go
    - supabase/migrations/20260414_01_provider_capabilities_media_columns.sql
    - supabase/migrations/20260414_02_filestore_tables.sql
  </read_first>
  <action>
    In `apps/control-plane/internal/routing/repository.go`:
    - Remove import `fmt` only if it becomes unused after deleting runtime DDL; keep it if other functions still use it.
    - Change `NewPgxRepository` to exactly return `&pgxRepository{pool: pool}` with no schema mutation.
    - Delete the `ensureCapabilityColumns` function entirely.
    - Leave `ListRouteCandidates` selecting `c.supports_image_generation`, `c.supports_image_edit`, `c.supports_tts`, `c.supports_stt`, and `c.supports_batch` from `public.provider_capabilities`.

    In `apps/control-plane/internal/filestore/repository.go`:
    - Remove imports `context` and `fmt` if they become unused by constructor deletion.
    - Update the comment for `NewRepository` to `NewRepository creates a repository over migration-managed filestore tables.`
    - Change `NewRepository(pool *pgxpool.Pool) (*Repository, error)` to return `&Repository{pool: pool}, nil`.
    - Delete the entire `ensureSchema` function.
    - Keep all CRUD SQL statements unchanged except schema-qualified names are optional; do not add any `CREATE TABLE`, `CREATE INDEX`, or `ALTER TABLE` statements in Go.
  </action>
  <verify>
    <automated>docker compose -f deploy/docker/docker-compose.yml run --rm toolchain sh -lc 'cd /workspace && go test ./apps/control-plane/internal/routing ./apps/control-plane/internal/filestore -run "TestRoutingRepositoryDoesNotRunCapabilityDDL|TestFilestoreSchemaLivesInSupabaseMigration" -count=1'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/control-plane/internal/routing/repository.go` does not contain `ensureCapabilityColumns`
    - `apps/control-plane/internal/routing/repository.go` does not contain `ALTER TABLE`
    - `apps/control-plane/internal/routing/repository.go` contains `return &pgxRepository{pool: pool}`
    - `apps/control-plane/internal/filestore/repository.go` does not contain `ensureSchema`
    - `apps/control-plane/internal/filestore/repository.go` does not contain `CREATE TABLE`
    - `apps/control-plane/internal/filestore/repository.go` contains `return &Repository{pool: pool}, nil`
    - The automated command exits 0
  </acceptance_criteria>
  <done>Repository constructors no longer hide or mutate schema drift at runtime.</done>
</task>

</tasks>

<verification>
Run the narrow schema tests after each task, then run:

```bash
docker compose -f deploy/docker/docker-compose.yml run --rm toolchain sh -lc 'cd /workspace && go test ./apps/control-plane/internal/routing ./apps/control-plane/internal/filestore -count=1'
```
</verification>

<success_criteria>
- The provider capability migration targets `public.provider_capabilities`.
- The provider capability migration explicitly backfills `route-openrouter-auto` for image, TTS, STT, and batch route eligibility.
- Filestore schema exists only in Supabase migrations.
- Routing and filestore repositories have no runtime DDL helpers or call sites.
</success_criteria>

<output>
After completion, create `.planning/phases/10-routing-storage-critical-fixes/10-03-SUMMARY.md`.
</output>
