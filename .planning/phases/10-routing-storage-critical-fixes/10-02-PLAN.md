---
phase: 10-routing-storage-critical-fixes
plan: 02
type: tdd
wave: 0
depends_on: []
files_modified:
  - apps/control-plane/internal/routing/repository_schema_test.go
  - apps/control-plane/internal/filestore/repository_test.go
  - apps/control-plane/internal/filestore/http_test.go
  - apps/control-plane/internal/batchstore/worker_test.go
autonomous: true
requirements:
  - ROUT-02
  - API-07
must_haves:
  truths:
    - "Routing validation fails until provider_capabilities owns all five media capability columns"
    - "Filestore validation fails until runtime DDL is removed and migrated schema is the source of truth"
    - "Batch validation fails until output file IDs, error file IDs, and request counts persist"
  artifacts:
    - path: "apps/control-plane/internal/routing/repository_schema_test.go"
      provides: "Red test for provider_capabilities media columns and no runtime capability DDL"
      contains: "supports_image_generation"
    - path: "apps/control-plane/internal/filestore/repository_test.go"
      provides: "Red test for migration-owned filestore schema and UpdateBatchStatus fields"
      contains: "output_file_id"
    - path: "apps/control-plane/internal/filestore/http_test.go"
      provides: "Red test for internal response fields needed by edge clients"
      contains: "s3_upload_id"
    - path: "apps/control-plane/internal/batchstore/worker_test.go"
      provides: "Red test for completed batch storage upload and status field persistence"
      contains: "output_file_id"
  key_links:
    - from: "apps/control-plane/internal/routing/repository.go"
      to: "supabase/migrations"
      via: "repository source and migration assertions"
      pattern: "provider_capabilities"
    - from: "apps/control-plane/internal/filestore/http.go"
      to: "apps/edge-api/internal/files/client.go"
      via: "internal JSON fields"
      pattern: "storage_path"
    - from: "apps/control-plane/internal/batchstore/worker.go"
      to: "apps/control-plane/internal/filestore/repository.go"
      via: "UpdateBatchStatus fields"
      pattern: "request_counts_completed"
---

<objective>
Create the Wave 0 red validation scaffolding for routing schema ownership, filestore schema/response correctness, and batch output persistence.

Purpose: The routing and batch bugs are schema and internal-contract failures. This plan locks the expected contract before implementation plans modify database migrations and control-plane code.
Output: Failing control-plane tests that drive Plans 10-03 and 10-06.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-routing-storage-critical-fixes/10-CONTEXT.md
@.planning/phases/10-routing-storage-critical-fixes/10-RESEARCH.md
@.planning/phases/10-routing-storage-critical-fixes/10-VALIDATION.md
@apps/control-plane/internal/routing/repository.go
@apps/control-plane/internal/filestore/repository.go
@apps/control-plane/internal/filestore/http.go
@apps/control-plane/internal/filestore/service.go
@apps/control-plane/internal/batchstore/worker.go
@apps/edge-api/internal/files/client.go
@apps/edge-api/internal/batches/client.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red routing schema ownership test</name>
  <files>apps/control-plane/internal/routing/repository_schema_test.go</files>
  <read_first>
    - apps/control-plane/internal/routing/repository.go
    - supabase/migrations/20260331_02_routing_policy.sql
    - .planning/phases/10-routing-storage-critical-fixes/10-VALIDATION.md
  </read_first>
  <behavior>
    - Repository construction does not call `ensureCapabilityColumns`.
    - `apps/control-plane/internal/routing/repository.go` contains no `ALTER TABLE`.
    - A migration under `supabase/migrations` alters `public.provider_capabilities`, not any other table, to add `supports_image_generation`, `supports_image_edit`, `supports_tts`, `supports_stt`, and `supports_batch`.
    - `ListRouteCandidates` still selects all five media capability fields from alias `c`.
  </behavior>
  <action>
    Create `apps/control-plane/internal/routing/repository_schema_test.go` in package `routing`. The test may read source and migration SQL files from the repository root using `os.ReadFile` and `filepath`. Add tests named:

```go
func TestRoutingRepositoryDoesNotRunCapabilityDDL(t *testing.T)
func TestProviderCapabilitiesMigrationAddsMediaColumns(t *testing.T)
func TestListRouteCandidatesSelectsMediaColumns(t *testing.T)
```

    Concrete assertions:
    - `repository.go` must not contain `ensureCapabilityColumns`.
    - `repository.go` must not contain `ALTER TABLE`.
    - Migration text must contain `alter table public.provider_capabilities`.
    - Migration text must contain each column string exactly: `supports_image_generation`, `supports_image_edit`, `supports_tts`, `supports_stt`, `supports_batch`.
    - Migration text must not contain `alter table route_capabilities`.
    - `repository.go` must contain `c.supports_image_generation`, `c.supports_image_edit`, `c.supports_tts`, `c.supports_stt`, and `c.supports_batch`.
  </action>
  <verify>
    <automated>docker compose -f deploy/docker/docker-compose.yml run --rm toolchain sh -lc 'cd /workspace && go test ./apps/control-plane/internal/routing -run "TestRoutingRepositoryDoesNotRunCapabilityDDL|TestProviderCapabilitiesMigrationAddsMediaColumns|TestListRouteCandidatesSelectsMediaColumns" -count=1'</automated>
    Expected RED result for this Wave 0 task: command exits non-zero because `ensureCapabilityColumns` and runtime `ALTER TABLE` still exist or the provider-capabilities migration is absent.
  </verify>
  <acceptance_criteria>
    - `apps/control-plane/internal/routing/repository_schema_test.go` contains `TestRoutingRepositoryDoesNotRunCapabilityDDL`
    - `apps/control-plane/internal/routing/repository_schema_test.go` contains `alter table public.provider_capabilities`
    - `apps/control-plane/internal/routing/repository_schema_test.go` contains `supports_image_generation`
    - `apps/control-plane/internal/routing/repository_schema_test.go` contains `supports_batch`
    - The automated command exits non-zero before implementation and its output references `ensureCapabilityColumns`, `ALTER TABLE`, or `provider_capabilities`
  </acceptance_criteria>
  <done>Routing schema ownership is covered by failing tests before the migration/removal plan runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add red filestore migration and internal response tests</name>
  <files>apps/control-plane/internal/filestore/repository_test.go, apps/control-plane/internal/filestore/http_test.go</files>
  <read_first>
    - apps/control-plane/internal/filestore/repository.go
    - apps/control-plane/internal/filestore/http.go
    - apps/control-plane/internal/filestore/types.go
    - apps/edge-api/internal/files/client.go
    - apps/edge-api/internal/batches/client.go
    - .planning/phases/10-routing-storage-critical-fixes/10-RESEARCH.md
  </read_first>
  <behavior>
    - `NewRepository` no longer calls `ensureSchema`.
    - A Supabase migration creates `public.files`, `public.uploads`, `public.upload_parts`, and `public.batches`.
    - `fileToResponse` includes internal `storage_path` for control-plane internal clients.
    - `uploadToResponse` includes internal `s3_upload_id` and `storage_path`.
    - `batchToResponse` includes `output_file_id`, `error_file_id`, `in_progress_at`, `completed_at`, `failed_at`, and `cancelled_at` when values exist.
    - `UpdateBatchStatus` persists allowed fields: `upstream_batch_id`, `reservation_id`, `output_file_id`, `error_file_id`, `request_counts_total`, `request_counts_completed`, `request_counts_failed`, `in_progress_at`, `completed_at`, `failed_at`, `cancelled_at`.
  </behavior>
  <action>
    Create source/migration tests that fail against current code and pass after Plans 10-03 and 10-06:

    In `apps/control-plane/internal/filestore/repository_test.go`, add:
```go
func TestFilestoreSchemaLivesInSupabaseMigration(t *testing.T)
func TestUpdateBatchStatusPersistsAllowedFields(t *testing.T)
```

    In `apps/control-plane/internal/filestore/http_test.go`, add:
```go
func TestInternalFileResponseIncludesStoragePath(t *testing.T)
func TestInternalUploadResponseIncludesMultipartFields(t *testing.T)
func TestInternalBatchResponseIncludesOutputFieldsAndTimestamps(t *testing.T)
```

    Tests may call package-private helpers directly because they are in package `filestore`. For timestamp tests, construct `Batch` values with non-nil `OutputFileID`, `ErrorFileID`, `InProgressAt`, `CompletedAt`, `FailedAt`, and `CancelledAt`, then marshal the response to JSON and assert the exact JSON field names are present.
  </action>
  <verify>
    <automated>docker compose -f deploy/docker/docker-compose.yml run --rm toolchain sh -lc 'cd /workspace && go test ./apps/control-plane/internal/filestore -run "TestFilestoreSchemaLivesInSupabaseMigration|TestUpdateBatchStatusPersistsAllowedFields|TestInternal" -count=1'</automated>
    Expected RED result for this Wave 0 task: command exits non-zero because runtime `ensureSchema`, missing response fields, or incomplete `UpdateBatchStatus` remain.
  </verify>
  <acceptance_criteria>
    - `apps/control-plane/internal/filestore/repository_test.go` contains `TestFilestoreSchemaLivesInSupabaseMigration`
    - `apps/control-plane/internal/filestore/repository_test.go` contains `public.upload_parts`
    - `apps/control-plane/internal/filestore/repository_test.go` contains `request_counts_completed`
    - `apps/control-plane/internal/filestore/http_test.go` contains `storage_path`
    - `apps/control-plane/internal/filestore/http_test.go` contains `s3_upload_id`
    - `apps/control-plane/internal/filestore/http_test.go` contains `output_file_id`
    - `apps/control-plane/internal/filestore/http_test.go` contains `completed_at`
    - The automated command exits non-zero before implementation and its output includes `ensureSchema`, `storage_path`, `s3_upload_id`, or `output_file_id`
  </acceptance_criteria>
  <done>Filestore migration ownership and internal response contracts are locked by failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add red batch worker output-upload test</name>
  <files>apps/control-plane/internal/batchstore/worker_test.go</files>
  <read_first>
    - apps/control-plane/internal/batchstore/worker.go
    - apps/control-plane/internal/batchstore/types.go
    - apps/control-plane/internal/filestore/service.go
    - apps/control-plane/internal/filestore/repository.go
    - .planning/phases/10-routing-storage-critical-fixes/10-VALIDATION.md
  </read_first>
  <behavior>
    - A completed upstream batch with `output_file_id` downloads the upstream output file, uploads it to storage bucket `hive-files`, creates a file metadata record with purpose `batch_output`, and updates the batch with `output_file_id`.
    - A completed upstream batch with `error_file_id` stores an error output file and updates `error_file_id`.
    - Request counts from upstream response persist into `request_counts_total`, `request_counts_completed`, and `request_counts_failed`.
    - A nil storage uploader is never accepted by the worker startup path after Plan 10-06.
  </behavior>
  <action>
    Create `apps/control-plane/internal/batchstore/worker_test.go`. If needed for testability, define desired seams in the red test that Plan 10-06 must implement:

```go
type batchFileService interface {
	CreateFile(ctx context.Context, accountID, purpose, filename string, bytes int64, storagePath string) (filestore.File, error)
	UpdateBatchStatus(ctx context.Context, batchID, status string, updates map[string]interface{}) error
}
```

    Add a test named `TestBatchWorkerStoresCompletedOutputFiles` using:
    - an httptest LiteLLM server serving `/v1/batches/upstream-1` with JSON containing `status:"completed"`, `output_file_id:"file-out"`, `error_file_id:"file-err"`, and `request_counts:{"total":3,"completed":2,"failed":1}`;
    - `/v1/files/file-out/content` returning `{"ok":true}` with content type `application/jsonl`;
    - `/v1/files/file-err/content` returning `{"error":true}` with content type `application/jsonl`;
    - a fake storage uploader that records bucket, key, contentType, and body;
    - a fake file service that records `CreateFile` calls and `UpdateBatchStatus` fields.

    The test should fail before Plan 10-06 because `BatchWorker` currently stores a concrete `*filestore.Service` and control-plane startup wires nil storage.
  </action>
  <verify>
    <automated>docker compose -f deploy/docker/docker-compose.yml run --rm toolchain sh -lc 'cd /workspace && go test ./apps/control-plane/internal/batchstore -run TestBatchWorkerStoresCompletedOutputFiles -count=1'</automated>
    Expected RED result for this Wave 0 task: command exits non-zero because the worker lacks a test seam or because output fields are not persisted yet.
  </verify>
  <acceptance_criteria>
    - `apps/control-plane/internal/batchstore/worker_test.go` contains `TestBatchWorkerStoresCompletedOutputFiles`
    - `apps/control-plane/internal/batchstore/worker_test.go` contains `output_file_id`
    - `apps/control-plane/internal/batchstore/worker_test.go` contains `error_file_id`
    - `apps/control-plane/internal/batchstore/worker_test.go` contains `request_counts_completed`
    - `apps/control-plane/internal/batchstore/worker_test.go` contains `hive-files`
    - The automated command exits non-zero before implementation and its output includes `BatchWorker`, `output_file_id`, or a compile failure naming the desired seam
  </acceptance_criteria>
  <done>Batch output upload and status persistence are covered by a failing worker test.</done>
</task>

</tasks>

<verification>
Wave 0 intentionally creates red tests. The executor must record the expected non-zero test commands and the failure reasons that implementation plans must close.
</verification>

<success_criteria>
- Routing test fails on runtime DDL or missing provider-capabilities migration.
- Filestore tests fail on runtime schema creation, missing internal fields, or ignored update fields.
- Batch worker test fails on missing seam/wiring/output persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/10-routing-storage-critical-fixes/10-02-SUMMARY.md`.
</output>
