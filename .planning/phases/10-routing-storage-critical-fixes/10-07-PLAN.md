---
phase: 10-routing-storage-critical-fixes
plan: 07
type: execute
wave: 3
depends_on:
  - 10-03
  - 10-04
  - 10-05
  - 10-06
files_modified:
  - .env.example
  - CLAUDE.md
  - .planning/ROADMAP.md
  - scripts/phase10-scrub-legacy-storage.sh
autonomous: true
requirements:
  - ROUT-02
  - API-05
  - API-06
  - API-07
must_haves:
  truths:
    - "Environment docs include every storage variable required by edge-api and control-plane"
    - "ROADMAP owns the Phase 10 plan-count update and avoids forbidden legacy backend wording"
    - "Repository-wide legacy storage references are scrubbed by a repeatable generated-candidate script"
  artifacts:
    - path: ".env.example"
      provides: "Supabase Storage S3 env contract"
      contains: "S3_REGION="
    - path: "CLAUDE.md"
      provides: "Current storage convention and startup requirement"
      contains: "Supabase Storage is the only object storage backend"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 10 eight-plan list and purge ownership"
      contains: "10-08-PLAN.md"
    - path: "scripts/phase10-scrub-legacy-storage.sh"
      provides: "Mechanical generated-candidate purge script"
      contains: "--check"
  key_links:
    - from: ".env.example"
      to: "deploy/docker/docker-compose.yml"
      via: "S3 env vars"
      pattern: "S3_REGION"
    - from: "scripts/phase10-scrub-legacy-storage.sh"
      to: ".planning/ROADMAP.md"
      via: "generated candidate scan includes planning docs"
      pattern: "rg -uu"
---

<objective>
Document the required Supabase Storage environment and perform the repository-wide legacy storage reference purge as a focused mechanical cleanup.

Purpose: The final verification gate was too broad when combined with the purge. This plan owns docs/env cleanup and the repeatable purge script only; Plan 10-08 owns final verification.
Output: Updated storage docs, updated Phase 10 roadmap plan list, and a generated-candidate purge script that can prove the working tree has no forbidden legacy storage references outside `.git`.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UAT-REPORT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/10-routing-storage-critical-fixes/10-CONTEXT.md
@.planning/phases/10-routing-storage-critical-fixes/10-RESEARCH.md
@.planning/phases/10-routing-storage-critical-fixes/10-VALIDATION.md
@.planning/phases/10-routing-storage-critical-fixes/10-03-SUMMARY.md
@.planning/phases/10-routing-storage-critical-fixes/10-04-SUMMARY.md
@.planning/phases/10-routing-storage-critical-fixes/10-05-SUMMARY.md
@.planning/phases/10-routing-storage-critical-fixes/10-06-SUMMARY.md
@.env.example
@CLAUDE.md
@deploy/docker/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document required Supabase Storage env and update roadmap ownership</name>
  <files>.env.example, CLAUDE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .env.example
    - CLAUDE.md
    - .planning/ROADMAP.md
    - deploy/docker/docker-compose.yml
    - apps/edge-api/cmd/server/main.go
    - apps/control-plane/cmd/server/main.go
  </read_first>
  <action>
    Update `.env.example` Supabase Storage block to include all required variables:

```text
S3_ENDPOINT=https://yimgflllgdsbcibnaxqe.supabase.co/storage/v1/s3
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=us-east-1
S3_BUCKET_FILES=hive-files
S3_BUCKET_IMAGES=hive-images
```

    Add comments directly above the variables:

```text
# Enable S3 protocol in Supabase Dashboard > Storage > S3 Access.
# Create the buckets hive-files and hive-images before starting the stack.
# Copy the endpoint, region, access key, and secret key from Supabase Storage S3 settings.
```

    Update `CLAUDE.md` storage/conventions text so it says Supabase Storage is the only object storage backend, storage env vars are required for edge-api and control-plane startup, and buckets must be pre-created.

    Update Phase 10 in `.planning/ROADMAP.md` to list eight plans:
    - `10-01-PLAN.md` through `10-06-PLAN.md` unchanged except for any wording needed by the purge.
    - `10-07-PLAN.md — Env documentation and repository-wide legacy storage reference purge`.
    - `10-08-PLAN.md — Final route/media checks, full suite, live smoke, and purge verification`.

    Keep Phase 10 requirement IDs unchanged and update the progress table from `0/7` to `0/8`.
  </action>
  <verify>
    <automated>docker compose --env-file .env -f deploy/docker/docker-compose.yml config --services</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains `S3_ENDPOINT=https://yimgflllgdsbcibnaxqe.supabase.co/storage/v1/s3`
    - `.env.example` contains `S3_REGION=us-east-1`
    - `.env.example` contains `S3_BUCKET_FILES=hive-files`
    - `.env.example` contains `S3_BUCKET_IMAGES=hive-images`
    - `CLAUDE.md` contains `Supabase Storage is the only object storage backend`
    - `.planning/ROADMAP.md` contains `**Plans:** 8 plans`
    - `.planning/ROADMAP.md` contains `10-08-PLAN.md`
    - `.planning/ROADMAP.md` contains `0/8`
    - The automated command exits 0 and output includes `edge-api` and `control-plane`
  </acceptance_criteria>
  <done>Required Supabase Storage env, bucket setup, and Phase 10 plan ownership are documented.</done>
</task>

<task type="auto">
  <name>Task 2: Create and run generated-candidate purge script</name>
  <files>scripts/phase10-scrub-legacy-storage.sh, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/UAT-REPORT.md
    - .planning/v1.0-MILESTONE-AUDIT.md
    - .planning/phases/10-routing-storage-critical-fixes/10-CONTEXT.md
    - .planning/phases/10-routing-storage-critical-fixes/10-RESEARCH.md
    - .planning/phases/10-routing-storage-critical-fixes/10-VALIDATION.md
    - CLAUDE.md
  </read_first>
  <action>
    Create `scripts/phase10-scrub-legacy-storage.sh` as a POSIX `sh` script with `set -eu`. It must support `--apply` and `--check`.

    The script must generate its candidate list at runtime instead of hardcoding dozens of file paths:

```sh
term_a='mi'
term_b='nio'
forbidden="${term_a}${term_b}"
pattern="${forbidden}|github.com/${forbidden}|${forbidden}-go"
rg -uu -il "$pattern" . -g '!.git/**' > /tmp/hive-phase10-legacy-storage-candidates.txt
```

    `--apply` must rewrite every generated candidate file using neutral replacements that preserve audit meaning without preserving the forbidden term:
    - `legacy local object-store emulator`
    - `legacy S3-compatible client`
    - `old storage client`
    - `old object-storage dependency`

    Implement replacements carefully so the script itself does not introduce the contiguous forbidden term. It may use Perl, awk, or sed, but it must be deterministic and repeatable.

    `--check` must re-run the generated candidate scan and exit 1 if any matches remain outside `.git`; otherwise it exits 0. The check mode must print the remaining candidate list before failing.

    Run the script in apply mode:

```bash
sh scripts/phase10-scrub-legacy-storage.sh --apply
```

    This task intentionally allows the script to edit files not listed in frontmatter because the checker requested a tightly scripted mechanical purge with a generated candidate list instead of a 30+ file plan. Do not use this task for any semantic rewrite beyond replacing the forbidden legacy storage references.
  </action>
  <verify>
    <automated>sh -n scripts/phase10-scrub-legacy-storage.sh</automated>
    <automated>sh scripts/phase10-scrub-legacy-storage.sh --check</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase10-scrub-legacy-storage.sh` contains `--apply`
    - `scripts/phase10-scrub-legacy-storage.sh` contains `--check`
    - `scripts/phase10-scrub-legacy-storage.sh` contains `rg -uu -il`
    - `scripts/phase10-scrub-legacy-storage.sh` contains `hive-phase10-legacy-storage-candidates.txt`
    - `sh -n scripts/phase10-scrub-legacy-storage.sh` exits 0
    - `sh scripts/phase10-scrub-legacy-storage.sh --check` exits 0
  </acceptance_criteria>
  <done>Legacy storage references are purged through a repeatable generated-candidate script, and ROADMAP purge ownership is explicit.</done>
</task>

</tasks>

<verification>
Run after Task 1:

```bash
docker compose --env-file .env -f deploy/docker/docker-compose.yml config --services
```

Run after Task 2:

```bash
sh -n scripts/phase10-scrub-legacy-storage.sh
sh scripts/phase10-scrub-legacy-storage.sh --check
```
</verification>

<success_criteria>
- `.env.example` documents `S3_REGION` and both required buckets.
- `.planning/ROADMAP.md` lists eight Phase 10 plans and assigns purge to Plan 10-07.
- The generated-candidate purge script proves no forbidden legacy storage references remain outside `.git`.
- Plan 10-07 stays below the checker scope threshold; final suite/smoke verification is owned by Plan 10-08.
</success_criteria>

<output>
After completion, create `.planning/phases/10-routing-storage-critical-fixes/10-07-SUMMARY.md`.
</output>
