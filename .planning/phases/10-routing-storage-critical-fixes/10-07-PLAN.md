---
phase: 10-routing-storage-critical-fixes
plan: 07
type: execute
wave: 3
depends_on:
  - 10-03
  - 10-04
  - 10-05
  - 10-06
files_modified:
  - .env.example
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/UAT-REPORT.md
  - .planning/v1.0-MILESTONE-AUDIT.md
  - .planning/phases/07-media-file-and-async-api-surface/07-CONTEXT.md
  - .planning/phases/07-media-file-and-async-api-surface/07-01-PLAN.md
  - .planning/phases/07-media-file-and-async-api-surface/07-02-PLAN.md
  - .planning/phases/07-media-file-and-async-api-surface/07-03-PLAN.md
  - .planning/phases/07-media-file-and-async-api-surface/07-01-SUMMARY.md
  - .planning/phases/07-media-file-and-async-api-surface/07-03-SUMMARY.md
  - .planning/phases/07-media-file-and-async-api-surface/07-VERIFICATION.md
  - .planning/phases/07-media-file-and-async-api-surface/07-VALIDATION.md
  - .planning/phases/07-media-file-and-async-api-surface/07-RESEARCH.md
  - .planning/phases/09-developer-console-operational-hardening/09-RESEARCH.md
  - .planning/phases/10-routing-storage-critical-fixes/10-CONTEXT.md
  - .planning/phases/10-routing-storage-critical-fixes/10-RESEARCH.md
  - .planning/phases/10-routing-storage-critical-fixes/10-VALIDATION.md
  - .planning/phases/10-routing-storage-critical-fixes/10-01-PLAN.md
  - .planning/phases/10-routing-storage-critical-fixes/10-02-PLAN.md
  - .planning/phases/10-routing-storage-critical-fixes/10-03-PLAN.md
  - .planning/phases/10-routing-storage-critical-fixes/10-04-PLAN.md
  - .planning/phases/10-routing-storage-critical-fixes/10-05-PLAN.md
  - .planning/phases/10-routing-storage-critical-fixes/10-06-PLAN.md
  - .planning/phases/10-routing-storage-critical-fixes/10-07-PLAN.md
  - scripts/phase10-smoke.sh
autonomous: true
requirements:
  - ROUT-02
  - API-05
  - API-06
  - API-07
user_setup:
  - service: supabase-storage
    why: "Live storage smoke tests require Supabase Storage S3-compatible access and pre-created buckets"
    env_vars:
      - name: S3_ENDPOINT
        source: "Supabase Dashboard > Storage > S3 Access endpoint, including /storage/v1/s3"
      - name: S3_ACCESS_KEY
        source: "Supabase Dashboard > Storage > S3 Access Keys"
      - name: S3_SECRET_KEY
        source: "Supabase Dashboard > Storage > S3 Access Keys"
      - name: S3_REGION
        source: "Supabase Dashboard > Storage > S3 Access settings"
      - name: S3_BUCKET_FILES
        source: "Pre-created bucket name, default hive-files"
      - name: S3_BUCKET_IMAGES
        source: "Pre-created bucket name, default hive-images"
    dashboard_config:
      - task: "Create buckets hive-files and hive-images before live smoke"
        location: "Supabase Dashboard > Storage > Buckets"
must_haves:
  truths:
    - "Environment docs include every storage variable required by edge-api and control-plane"
    - "Historical planning docs are not approved exceptions for the zero-reference policy"
    - "Full Go suite passes across control-plane, edge-api, and packages/storage"
    - "Live smoke probes cover chat routing, image routing/storage, file upload, and batch listing"
  artifacts:
    - path: ".env.example"
      provides: "Supabase Storage S3 env contract"
      contains: "S3_REGION="
    - path: "scripts/phase10-smoke.sh"
      provides: "Live phase smoke probes"
      contains: "HIVE_API_KEY"
    - path: ".planning/phases/10-routing-storage-critical-fixes/10-07-SUMMARY.md"
      provides: "Final proof of purge and verification"
      contains: "rg -uu"
  key_links:
    - from: ".env.example"
      to: "deploy/docker/docker-compose.yml"
      via: "S3 env vars"
      pattern: "S3_REGION"
    - from: "scripts/phase10-smoke.sh"
      to: "edge-api runtime"
      via: "curl smoke"
      pattern: "/v1/files"
---

<objective>
Finish the phase by documenting required Supabase storage env, scrubbing repository-wide legacy storage references with no historical `.planning` exemption, and running the full phase verification gate.

Purpose: The user explicitly required zero references in code, config, and documentation. Historical planning artifacts are treated as mutable documentation for this cleanup, not approved exceptions.
Output: Complete docs/env cleanup, green full Go suite, smoke script execution when live credentials are present, and a clean repository-wide reference scan.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UAT-REPORT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/10-routing-storage-critical-fixes/10-CONTEXT.md
@.planning/phases/10-routing-storage-critical-fixes/10-RESEARCH.md
@.planning/phases/10-routing-storage-critical-fixes/10-VALIDATION.md
@.planning/phases/10-routing-storage-critical-fixes/10-03-SUMMARY.md
@.planning/phases/10-routing-storage-critical-fixes/10-04-SUMMARY.md
@.planning/phases/10-routing-storage-critical-fixes/10-05-SUMMARY.md
@.planning/phases/10-routing-storage-critical-fixes/10-06-SUMMARY.md
@.env.example
@CLAUDE.md
@deploy/docker/docker-compose.yml
@scripts/phase10-smoke.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document required Supabase Storage env and bucket setup</name>
  <files>.env.example, CLAUDE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .env.example
    - CLAUDE.md
    - .planning/ROADMAP.md
    - deploy/docker/docker-compose.yml
    - apps/edge-api/cmd/server/main.go
    - apps/control-plane/cmd/server/main.go
  </read_first>
  <action>
    Update `.env.example` Supabase Storage block to include all required variables:

```text
S3_ENDPOINT=https://yimgflllgdsbcibnaxqe.supabase.co/storage/v1/s3
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=us-east-1
S3_BUCKET_FILES=hive-files
S3_BUCKET_IMAGES=hive-images
```

    Add comments directly above the variables:
```text
# Enable S3 protocol in Supabase Dashboard > Storage > S3 Access.
# Create the buckets hive-files and hive-images before starting the stack.
# Copy the endpoint, region, access key, and secret key from Supabase Storage S3 settings.
```

    Update `CLAUDE.md` storage/conventions text so it says Supabase Storage is the only object storage backend, storage env vars are required for edge-api and control-plane startup, and buckets must be pre-created. Do not leave the forbidden legacy storage keyword in this file.

    Update Phase 10 in `.planning/ROADMAP.md` with the seven plan files and revised language that says "legacy local object-store implementation" instead of the forbidden legacy storage keyword. Keep requirement IDs unchanged.
  </action>
  <verify>
    <automated>docker compose --env-file .env -f deploy/docker/docker-compose.yml config --services</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains `S3_ENDPOINT=https://yimgflllgdsbcibnaxqe.supabase.co/storage/v1/s3`
    - `.env.example` contains `S3_REGION=us-east-1`
    - `.env.example` contains `S3_BUCKET_FILES=hive-files`
    - `.env.example` contains `S3_BUCKET_IMAGES=hive-images`
    - `CLAUDE.md` contains `Supabase Storage is the only object storage backend`
    - `.planning/ROADMAP.md` contains `10-07-PLAN.md`
    - The automated command exits 0 and output includes `edge-api` and `control-plane`
  </acceptance_criteria>
  <done>Required Supabase Storage env and bucket setup are documented without legacy backend references.</done>
</task>

<task type="auto">
  <name>Task 2: Scrub repository-wide legacy storage references with no planning-doc exception</name>
  <files>.planning/STATE.md, .planning/UAT-REPORT.md, .planning/v1.0-MILESTONE-AUDIT.md, .planning/phases/07-media-file-and-async-api-surface/07-CONTEXT.md, .planning/phases/07-media-file-and-async-api-surface/07-01-PLAN.md, .planning/phases/07-media-file-and-async-api-surface/07-02-PLAN.md, .planning/phases/07-media-file-and-async-api-surface/07-03-PLAN.md, .planning/phases/07-media-file-and-async-api-surface/07-01-SUMMARY.md, .planning/phases/07-media-file-and-async-api-surface/07-03-SUMMARY.md, .planning/phases/07-media-file-and-async-api-surface/07-VERIFICATION.md, .planning/phases/07-media-file-and-async-api-surface/07-VALIDATION.md, .planning/phases/07-media-file-and-async-api-surface/07-RESEARCH.md, .planning/phases/09-developer-console-operational-hardening/09-RESEARCH.md, .planning/phases/10-routing-storage-critical-fixes/10-CONTEXT.md, .planning/phases/10-routing-storage-critical-fixes/10-RESEARCH.md, .planning/phases/10-routing-storage-critical-fixes/10-VALIDATION.md, .planning/phases/10-routing-storage-critical-fixes/10-01-PLAN.md, .planning/phases/10-routing-storage-critical-fixes/10-02-PLAN.md, .planning/phases/10-routing-storage-critical-fixes/10-03-PLAN.md, .planning/phases/10-routing-storage-critical-fixes/10-04-PLAN.md, .planning/phases/10-routing-storage-critical-fixes/10-05-PLAN.md, .planning/phases/10-routing-storage-critical-fixes/10-06-PLAN.md, .planning/phases/10-routing-storage-critical-fixes/10-07-PLAN.md, CLAUDE.md</files>
  <read_first>
    - .planning/STATE.md
    - .planning/UAT-REPORT.md
    - .planning/v1.0-MILESTONE-AUDIT.md
    - .planning/phases/10-routing-storage-critical-fixes/10-CONTEXT.md
    - .planning/phases/10-routing-storage-critical-fixes/10-RESEARCH.md
    - .planning/phases/10-routing-storage-critical-fixes/10-VALIDATION.md
    - CLAUDE.md
  </read_first>
  <action>
    Treat historical `.planning` docs as not exempt. Rewrite all code comments, documentation, planning artifacts, summaries, validation docs, and plan files so the forbidden legacy storage keyword and its module path are absent from the working tree.

    Use neutral replacements that preserve audit meaning without preserving the forbidden term:
    - `legacy local object-store emulator`
    - `legacy S3-compatible client`
    - `old storage client`
    - `old object-storage dependency`

    The final scan command must construct the forbidden pattern without placing it contiguously in the plan file:
```bash
PATTERN='mi''nio|github.com/mi''nio|mi''nio-go'
rg -uu -i "$PATTERN" . -g '!.git/**'
```

    Because this task is allowed to edit Phase 10 plan files after they have been consumed, scrub this file and the earlier Phase 10 plan files as part of the task. Keep the files readable for historical traceability, but do not preserve the forbidden strings.
  </action>
  <verify>
    <automated>sh -lc 'PATTERN='\''mi'\'''\''nio|github.com/mi'\'''\''nio|mi'\'''\''nio-go'\''; ! rg -uu -i "$PATTERN" . -g '\''!.git/**'\'''</automated>
  </verify>
  <acceptance_criteria>
    - The automated command exits 0
    - `CLAUDE.md` does not contain the forbidden legacy storage keyword in any casing
    - `.planning/ROADMAP.md` does not contain the forbidden legacy storage keyword in any casing
    - `.planning/phases/10-routing-storage-critical-fixes/10-RESEARCH.md` does not contain the forbidden legacy storage keyword in any casing
    - `.planning/phases/07-media-file-and-async-api-surface/07-01-SUMMARY.md` does not contain the forbidden legacy storage keyword in any casing
    - `apps/edge-api/go.mod` does not contain the old storage module path
    - `apps/edge-api/go.sum` does not contain the old storage module path
  </acceptance_criteria>
  <done>No legacy storage keyword references remain in application code, Docker config, docs, or `.planning` artifacts outside `.git` internals.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 10 verification gate</name>
  <files>scripts/phase10-smoke.sh</files>
  <read_first>
    - scripts/phase10-smoke.sh
    - .planning/phases/10-routing-storage-critical-fixes/10-VALIDATION.md
    - apps/control-plane/cmd/server/main.go
    - apps/edge-api/cmd/server/main.go
    - deploy/docker/docker-compose.yml
  </read_first>
  <action>
    Run the full Go suite:
```bash
docker compose -f deploy/docker/docker-compose.yml run --rm toolchain sh -lc 'cd /workspace && go test ./apps/control-plane/... ./apps/edge-api/... ./packages/storage/... -count=1'
```

    Validate Docker Compose service config:
```bash
docker compose --env-file .env -f deploy/docker/docker-compose.yml config --services
```

    If `.env` has non-empty `S3_ENDPOINT`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`, `S3_REGION`, `S3_BUCKET_FILES`, `S3_BUCKET_IMAGES`, and `HIVE_API_KEY`, run:
```bash
HIVE_BASE_URL="${HIVE_BASE_URL:-http://localhost:8080}" HIVE_API_KEY="$HIVE_API_KEY" sh scripts/phase10-smoke.sh
```

    If live smoke cannot run because required secrets are missing, record that exact missing-variable list in the SUMMARY and do not claim live smoke passed. Unit/full-suite verification is still required.
  </action>
  <verify>
    <automated>docker compose -f deploy/docker/docker-compose.yml run --rm toolchain sh -lc 'cd /workspace && go test ./apps/control-plane/... ./apps/edge-api/... ./packages/storage/... -count=1'</automated>
    <automated>docker compose --env-file .env -f deploy/docker/docker-compose.yml config --services</automated>
    <automated>sh -n scripts/phase10-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - Full Go suite command exits 0
    - Compose config command exits 0 and output includes `edge-api`
    - Compose config command exits 0 and output includes `control-plane`
    - `sh -n scripts/phase10-smoke.sh` exits 0
    - If live smoke runs, its output does not contain `Failed to select a route`
    - If live smoke runs, its output contains `"object":"file"` for the file upload probe
    - If live smoke is skipped, the SUMMARY contains `Live smoke skipped` and lists each missing env var by name
  </acceptance_criteria>
  <done>Phase 10 has green automated verification and either a completed live smoke or an explicit missing-secret note for user action.</done>
</task>

</tasks>

<verification>
Final gate commands:

```bash
docker compose -f deploy/docker/docker-compose.yml run --rm toolchain sh -lc 'cd /workspace && go test ./apps/control-plane/... ./apps/edge-api/... ./packages/storage/... -count=1'
docker compose --env-file .env -f deploy/docker/docker-compose.yml config --services
sh -n scripts/phase10-smoke.sh
PATTERN='mi''nio|github.com/mi''nio|mi''nio-go'; ! rg -uu -i "$PATTERN" . -g '!.git/**'
```
</verification>

<success_criteria>
- `.env.example` documents `S3_REGION` and both required buckets.
- Full Go suite passes.
- Compose config includes both Go services.
- Smoke script is syntactically valid and live smoke is either run successfully or explicitly blocked by missing secrets.
- Repository-wide hidden-file scan has no forbidden legacy storage references outside `.git`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-routing-storage-critical-fixes/10-07-SUMMARY.md`.
</output>
