---
phase: 03-credits-ledger-usage-accounting
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/control-plane/cmd/server/main.go
  - apps/control-plane/internal/platform/http/router.go
  - apps/control-plane/internal/usage/types.go
  - apps/control-plane/internal/usage/repository.go
  - apps/control-plane/internal/usage/service.go
  - apps/control-plane/internal/usage/http.go
  - apps/control-plane/internal/usage/service_test.go
  - apps/control-plane/internal/usage/http_test.go
  - supabase/migrations/20260330_02_usage_accounting.sql
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - PRIV-01
must_haves:
  truths:
    - "Phase 3 must make usage reconstructable without storing prompts or responses at rest."
    - "Request accounting needs both a customer-visible request ID and a durable attempt identity so retries and interrupted streams remain reconcilable."
    - "Provider-facing debugging data may exist internally, but customer-facing records must stay provider-blind by default."
  artifacts:
    - path: "supabase/migrations/20260330_02_usage_accounting.sql"
      provides: "Request-attempt and privacy-safe usage-event schema."
      contains: "create table public.request_attempts"
    - path: "apps/control-plane/internal/usage/service.go"
      provides: "Metadata redaction and request-attempt accounting rules."
      contains: "func RedactMetadata"
    - path: "apps/control-plane/internal/usage/http.go"
      provides: "Authenticated current-account usage and request-attempt inspection APIs."
      contains: "/api/v1/accounts/current/usage-events"
  key_links:
    - from: "supabase/migrations/20260330_02_usage_accounting.sql"
      to: "apps/control-plane/internal/usage/repository.go"
      via: "The repository reads and writes `request_attempts` and `usage_events` using the schema defined by the migration."
      pattern: "public.request_attempts"
    - from: "apps/control-plane/internal/platform/http/router.go"
      to: "apps/control-plane/internal/usage/http.go"
      via: "The router exposes current-account request-attempt and usage-event routes."
      pattern: "/api/v1/accounts/current/request-attempts"
---

<objective>
Add privacy-safe usage events and request-accounting primitives that make Phase 3 meterable without transcript retention.

Purpose: reservation and settlement flows in the next plan need durable attempt identities and usage facts that explain what happened without storing prompts, responses, or SSE bodies.
Output: a `usage` package, a Supabase migration for request attempts and usage events, and authenticated read APIs for current-account request attempts and usage-event history.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-credits-ledger-usage-accounting/03-CONTEXT.md
@.planning/phases/03-credits-ledger-usage-accounting/03-RESEARCH.md
@.planning/phases/03-credits-ledger-usage-accounting/03-VALIDATION.md
@.planning/phases/03-credits-ledger-usage-accounting/03-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add request-attempt and privacy-safe usage-event schema</name>
  <files>
    supabase/migrations/20260330_02_usage_accounting.sql,
    apps/control-plane/internal/usage/types.go,
    apps/control-plane/internal/usage/repository.go
  </files>
  <read_first>
    .planning/phases/03-credits-ledger-usage-accounting/03-RESEARCH.md,
    .planning/phases/03-credits-ledger-usage-accounting/03-01-PLAN.md,
    supabase/migrations/20260330_01_credits_ledger.sql,
    apps/control-plane/internal/ledger/types.go,
    apps/control-plane/internal/ledger/repository.go,
    apps/control-plane/internal/profiles/repository.go
  </read_first>
  <action>
1. Create `supabase/migrations/20260330_02_usage_accounting.sql` with:
   - `public.request_attempts`
     - `id uuid primary key default gen_random_uuid()`
     - `account_id uuid not null references public.accounts(id) on delete cascade`
     - `request_id text not null`
     - `attempt_number integer not null check (attempt_number > 0)`
     - `endpoint text not null`
     - `model_alias text not null`
     - `status text not null check (status in ('accepted', 'dispatching', 'streaming', 'completed', 'failed', 'cancelled', 'interrupted'))`
     - nullable attribution columns `user_id uuid`, `team_id uuid`, `service_account_id uuid`, `api_key_id uuid`
     - `customer_tags jsonb not null default '{}'::jsonb`
     - `started_at timestamptz not null default now()`
     - `completed_at timestamptz`
     - unique index on `(account_id, request_id, attempt_number)`
   - `public.usage_events`
     - `id uuid primary key default gen_random_uuid()`
     - `account_id uuid not null references public.accounts(id) on delete cascade`
     - `request_attempt_id uuid not null references public.request_attempts(id) on delete cascade`
     - `request_id text not null`
     - `event_type text not null check (event_type in ('accepted', 'reservation_created', 'stream_update', 'completed', 'released', 'refunded', 'error', 'reconciled'))`
     - `endpoint text not null`
     - `model_alias text not null`
     - `status text not null`
     - token columns `input_tokens bigint not null default 0`, `output_tokens bigint not null default 0`, `cache_read_tokens bigint not null default 0`, `cache_write_tokens bigint not null default 0`
     - `hive_credit_delta bigint not null default 0`
     - `provider_request_id text`
     - `internal_metadata jsonb not null default '{}'::jsonb`
     - `customer_tags jsonb not null default '{}'::jsonb`
     - `error_code text`
     - `error_type text`
     - `created_at timestamptz not null default now()`
   - SQL comments stating that request or response bodies are forbidden in these tables
   - do not add columns named `prompt`, `prompts`, `messages`, `input_text`, `response_body`, `completion_text`, or `content`

2. Create `apps/control-plane/internal/usage/types.go` with:
   - `type RequestAttempt`
   - `type UsageEvent`
   - `type StartAttemptInput`
   - `type RecordEventInput`
   - `type ListEventsFilter`

3. Create `apps/control-plane/internal/usage/repository.go` with a pgx repository exposing:
   - `CreateAttempt(ctx context.Context, input StartAttemptInput) (RequestAttempt, error)`
   - `UpdateAttemptStatus(ctx context.Context, attemptID uuid.UUID, status string, completedAt *time.Time) error`
   - `RecordEvent(ctx context.Context, input RecordEventInput) (UsageEvent, error)`
   - `ListAttempts(ctx context.Context, accountID uuid.UUID, requestID string, limit int) ([]RequestAttempt, error)`
   - `ListEvents(ctx context.Context, filter ListEventsFilter) ([]UsageEvent, error)`
   - all list queries must filter by `account_id`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && rg -n "request_attempts|usage_events|internal_metadata|customer_tags" supabase/migrations/20260330_02_usage_accounting.sql apps/control-plane/internal/usage/types.go apps/control-plane/internal/usage/repository.go</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260330_02_usage_accounting.sql` contains `create table public.request_attempts`
    - `supabase/migrations/20260330_02_usage_accounting.sql` contains `create table public.usage_events`
    - `supabase/migrations/20260330_02_usage_accounting.sql` does not define columns named `prompt`, `messages`, `response_body`, or `completion_text`
    - `apps/control-plane/internal/usage/types.go` contains `type RequestAttempt`
    - `apps/control-plane/internal/usage/repository.go` contains `CreateAttempt`
    - `apps/control-plane/internal/usage/repository.go` contains `ListEvents`
  </acceptance_criteria>
  <done>The database and repository layer can persist request attempts and redacted usage-event facts without transcript storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add metadata redaction rules and current-account usage inspection APIs</name>
  <files>
    apps/control-plane/cmd/server/main.go,
    apps/control-plane/internal/platform/http/router.go,
    apps/control-plane/internal/usage/service.go,
    apps/control-plane/internal/usage/http.go,
    apps/control-plane/internal/usage/service_test.go,
    apps/control-plane/internal/usage/http_test.go
  </files>
  <read_first>
    apps/control-plane/cmd/server/main.go,
    apps/control-plane/internal/platform/http/router.go,
    apps/control-plane/internal/accounts/http.go,
    apps/control-plane/internal/accounts/service.go,
    apps/control-plane/internal/ledger/http.go,
    apps/control-plane/internal/ledger/service.go,
    apps/control-plane/internal/usage/repository.go,
    .planning/phases/03-credits-ledger-usage-accounting/03-RESEARCH.md
  </read_first>
  <action>
1. Create `apps/control-plane/internal/usage/service.go` with:
   - `func NewService(repo Repository) *Service`
   - `func RedactMetadata(input map[string]any) map[string]any`
   - `func (s *Service) StartAttempt(ctx context.Context, input StartAttemptInput) (RequestAttempt, error)`
   - `func (s *Service) RecordEvent(ctx context.Context, input RecordEventInput) (UsageEvent, error)`
   - `func (s *Service) ListAttempts(ctx context.Context, accountID uuid.UUID, requestID string, limit int) ([]RequestAttempt, error)`
   - `func (s *Service) ListEvents(ctx context.Context, filter ListEventsFilter) ([]UsageEvent, error)`
   - `RedactMetadata` must recursively remove keys named exactly `prompt`, `prompts`, `input`, `inputs`, `messages`, `response`, `responses`, `completion`, `completions`, `content`, and `output_text`
   - `RecordEvent` must persist only redacted metadata into `internal_metadata`

2. Create `apps/control-plane/internal/usage/http.go` following the same authenticated-current-account pattern used by `profiles.Handler`:
   - handler constructor `NewHandler(svc *Service, accountsSvc *accounts.Service) *Handler`
   - `GET /api/v1/accounts/current/request-attempts`
   - `GET /api/v1/accounts/current/usage-events`
   - both routes accept optional query params `request_id` and `limit`
   - `limit` defaults to `20`
   - both routes resolve `account_id` from viewer context, not query/body input
   - `GET /api/v1/accounts/current/usage-events` must return only provider-blind fields:
     - `request_id`, `event_type`, `endpoint`, `model_alias`, `status`, token counts, `hive_credit_delta`, `customer_tags`, `error_code`, `error_type`, and `created_at`
     - do not include `provider_request_id` or `internal_metadata` in the JSON response

3. Update `apps/control-plane/internal/platform/http/router.go` so it registers:
   - `/api/v1/accounts/current/request-attempts`
   - `/api/v1/accounts/current/usage-events`

4. Update `apps/control-plane/cmd/server/main.go` so when the DB pool is available it also:
   - builds `usageRepo`, `usageSvc`, and `usageHandler`
   - passes the usage handler into the router config

5. Add tests:
   - `apps/control-plane/internal/usage/service_test.go`
     - `TestRecordEventRedactsPromptFields`
     - `TestRedactMetadataStripsNestedMessageContent`
     - `TestStartAttemptRejectsBlankRequestID`
   - `apps/control-plane/internal/usage/http_test.go`
     - `TestListUsageEventsUsesCurrentAccount`
     - `TestListRequestAttemptsDefaultsLimit`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/usage/... -count=1 && rg -n "/api/v1/accounts/current/request-attempts|/api/v1/accounts/current/usage-events|func RedactMetadata|hive_credit_delta|customer_tags" apps/control-plane/internal/platform/http/router.go apps/control-plane/internal/usage/http.go apps/control-plane/internal/usage/service.go && ! rg -n "provider_request_id|internal_metadata" apps/control-plane/internal/usage/http.go</automated>
  </verify>
  <acceptance_criteria>
    - `apps/control-plane/internal/usage/service.go` contains `func RedactMetadata`
    - `apps/control-plane/internal/usage/service.go` contains `messages`
    - `apps/control-plane/internal/usage/http.go` contains `/api/v1/accounts/current/request-attempts`
    - `apps/control-plane/internal/usage/http.go` contains `/api/v1/accounts/current/usage-events`
    - `apps/control-plane/internal/usage/http.go` contains `hive_credit_delta`
    - `apps/control-plane/internal/usage/service_test.go` contains `TestRecordEventRedactsPromptFields`
    - `apps/control-plane/internal/usage/http_test.go` contains `TestListRequestAttemptsDefaultsLimit`
  </acceptance_criteria>
  <done>The control-plane can persist request-accounting facts and expose current-account usage history without storing prompts or responses at rest.</done>
</task>

</tasks>

<verification>
- Run `cd /home/sakib/hive && rg -n "request_attempts|usage_events" supabase/migrations/20260330_02_usage_accounting.sql` to confirm the schema exists.
- Run `cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/usage/... -count=1` to verify the usage package.
- Run `cd /home/sakib/hive && rg -n "/api/v1/accounts/current/request-attempts|/api/v1/accounts/current/usage-events" apps/control-plane/internal/platform/http/router.go apps/control-plane/internal/usage/http.go` to confirm the routes are registered.
</verification>

<success_criteria>
- Phase 3 has durable request attempts and usage events that explain billing outcomes without transcript storage.
- Usage-event metadata is recursively redacted for prompt, message, input, response, and completion body fields.
- Current-account usage and attempt history can be queried through authenticated control-plane endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/03-credits-ledger-usage-accounting/03-02-SUMMARY.md`
</output>
