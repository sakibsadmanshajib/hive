---
phase: 03-credits-ledger-usage-accounting
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - apps/control-plane/cmd/server/main.go
  - apps/control-plane/internal/platform/http/router.go
  - apps/control-plane/internal/ledger/types.go
  - apps/control-plane/internal/ledger/repository.go
  - apps/control-plane/internal/ledger/service.go
  - apps/control-plane/internal/usage/repository.go
  - apps/control-plane/internal/usage/service.go
  - apps/control-plane/internal/accounting/types.go
  - apps/control-plane/internal/accounting/repository.go
  - apps/control-plane/internal/accounting/service.go
  - apps/control-plane/internal/accounting/http.go
  - apps/control-plane/internal/accounting/service_test.go
  - apps/control-plane/internal/accounting/http_test.go
  - supabase/migrations/20260330_03_credit_reservations.sql
autonomous: false
requirements:
  - BILL-02
must_haves:
  truths:
    - "Reservations must happen before dispatch, and settlement must distinguish success, failure, cancellation, retry, and interrupted-stream cases."
    - "Ambiguous interrupted executions default in the customer's favor unless terminal upstream usage exists."
    - "Temporary overage is a bounded workspace policy overlay, not a new wallet type or an excuse to skip reservations."
  artifacts:
    - path: "supabase/migrations/20260330_03_credit_reservations.sql"
      provides: "Durable reservation, reservation-event, and reconciliation-job schema."
      contains: "create table public.credit_reservations"
    - path: "apps/control-plane/internal/accounting/service.go"
      provides: "Reservation, expansion, finalization, release, and reconciliation business logic."
      contains: "func (s *Service) FinalizeReservation"
    - path: "apps/control-plane/internal/accounting/http.go"
      provides: "Authenticated current-account reservation lifecycle endpoints."
      contains: "/api/v1/accounts/current/credits/reservations/finalize"
  key_links:
    - from: "apps/control-plane/internal/accounting/service.go"
      to: "apps/control-plane/internal/ledger/service.go"
      via: "Reservation actions post hold, release, charge, and refund ledger entries through the ledger service."
      pattern: "ChargeUsage"
    - from: "apps/control-plane/internal/accounting/service.go"
      to: "apps/control-plane/internal/usage/service.go"
      via: "Reservation lifecycle transitions emit usage-event records without storing transcript bodies."
      pattern: "RecordEvent"
---

<objective>
Build the reservation, finalization, release, and refund flows for streaming and retry scenarios on top of the Phase 3 ledger and usage primitives.

Purpose: the phase goal is not met until Hive can reserve before dispatch, settle correctly after terminal outcomes, and keep interrupted or ambiguous executions reconcilable without charging the full reserve by default.
Output: reservation and reconciliation schema, an `accounting` package, and authenticated current-account reservation lifecycle endpoints for create, expand, finalize, and release operations.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-credits-ledger-usage-accounting/03-CONTEXT.md
@.planning/phases/03-credits-ledger-usage-accounting/03-RESEARCH.md
@.planning/phases/03-credits-ledger-usage-accounting/03-VALIDATION.md
@.planning/phases/03-credits-ledger-usage-accounting/03-01-PLAN.md
@.planning/phases/03-credits-ledger-usage-accounting/03-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reservation and reconciliation schema plus accounting service rules</name>
  <files>
    supabase/migrations/20260330_03_credit_reservations.sql,
    apps/control-plane/internal/ledger/types.go,
    apps/control-plane/internal/ledger/repository.go,
    apps/control-plane/internal/ledger/service.go,
    apps/control-plane/internal/usage/repository.go,
    apps/control-plane/internal/usage/service.go,
    apps/control-plane/internal/accounting/types.go,
    apps/control-plane/internal/accounting/repository.go,
    apps/control-plane/internal/accounting/service.go,
    apps/control-plane/internal/accounting/service_test.go
  </files>
  <read_first>
    supabase/migrations/20260330_01_credits_ledger.sql,
    supabase/migrations/20260330_02_usage_accounting.sql,
    apps/control-plane/internal/ledger/types.go,
    apps/control-plane/internal/ledger/repository.go,
    apps/control-plane/internal/ledger/service.go,
    apps/control-plane/internal/usage/types.go,
    apps/control-plane/internal/usage/repository.go,
    apps/control-plane/internal/usage/service.go,
    .planning/phases/03-credits-ledger-usage-accounting/03-RESEARCH.md
  </read_first>
  <action>
1. Create `supabase/migrations/20260330_03_credit_reservations.sql` with:
   - `public.credit_reservations`
     - `id uuid primary key default gen_random_uuid()`
     - `account_id uuid not null references public.accounts(id) on delete cascade`
     - `request_attempt_id uuid not null references public.request_attempts(id) on delete cascade`
     - `reservation_key text not null unique`
     - `policy_mode text not null check (policy_mode in ('strict', 'temporary_overage')) default 'strict'`
     - `status text not null check (status in ('active', 'expanded', 'finalized', 'released', 'needs_reconciliation'))`
     - `reserved_credits bigint not null check (reserved_credits > 0)`
     - `consumed_credits bigint not null default 0`
     - `released_credits bigint not null default 0`
     - `terminal_usage_confirmed boolean not null default false`
     - `created_at timestamptz not null default now()`
     - `updated_at timestamptz not null default now()`
   - `public.credit_reservation_events`
     - `id uuid primary key default gen_random_uuid()`
     - `reservation_id uuid not null references public.credit_reservations(id) on delete cascade`
     - `event_type text not null check (event_type in ('reserved', 'expanded', 'finalized', 'released', 'refunded', 'marked_for_reconciliation'))`
     - `credits_delta bigint not null`
     - `reason text not null`
     - `metadata jsonb not null default '{}'::jsonb`
     - `created_at timestamptz not null default now()`
   - `public.credit_reconciliation_jobs`
     - `id uuid primary key default gen_random_uuid()`
     - `reservation_id uuid not null references public.credit_reservations(id) on delete cascade`
     - `request_attempt_id uuid not null references public.request_attempts(id) on delete cascade`
     - `reason text not null`
     - `status text not null check (status in ('pending', 'processing', 'resolved')) default 'pending'`
     - `run_after timestamptz not null default now()`
     - `created_at timestamptz not null default now()`

2. Extend `apps/control-plane/internal/ledger/types.go`, `repository.go`, and `service.go` with reservation-settlement helpers:
   - `ReserveCredits`
   - `ReleaseReservedCredits`
   - `ChargeUsage`
   - `RefundCredits`
   - each helper must write immutable `credit_ledger_entries` using entry types `reservation_hold`, `reservation_release`, `usage_charge`, and `refund`
   - `ChargeUsage` must post a negative `credits_delta`
   - `RefundCredits` must post a positive `credits_delta`

3. Extend `apps/control-plane/internal/usage/repository.go` and `service.go` so accounting can:
   - set request-attempt statuses to `streaming`, `completed`, `failed`, `cancelled`, or `interrupted`
   - record usage events `reservation_created`, `released`, `refunded`, and `reconciled`

4. Create `apps/control-plane/internal/accounting/types.go` with:
   - `type Reservation`
   - `type CreateReservationInput`
   - `type ExpandReservationInput`
   - `type FinalizeReservationInput`
   - `type ReleaseReservationInput`

5. Create `apps/control-plane/internal/accounting/repository.go` and `service.go` with:
   - `CreateReservation`
   - `GetReservation`
   - `ExpandReservation`
   - `FinalizeReservation`
   - `ReleaseReservation`
   - `CreateReconciliationJob`
   - `FinalizeReservation` must behave exactly like this:
     - if `terminal_usage_confirmed == true`, charge `actual_credits`, release any unused reserve, set `status = 'finalized'`
     - if `terminal_usage_confirmed == false` and `actual_credits == 0`, release the full reserve, create a reconciliation job, set `status = 'needs_reconciliation'`
     - if `terminal_usage_confirmed == false` and `actual_credits > 0`, charge `actual_credits`, release the remainder, create a reconciliation job, set `status = 'needs_reconciliation'`
   - `CreateReservation` and `ExpandReservation` must enforce:
     - `strict` policy rejects when the new reserved total exceeds `AvailableCredits`
     - `temporary_overage` allows the new reserved total to exceed `AvailableCredits` by at most `10000` credits
     - attempts beyond that `10000` credit buffer return a validation or policy error

6. Add tests in `apps/control-plane/internal/accounting/service_test.go`:
   - `TestCreateReservationStrictPolicyRejectsOverBalance`
   - `TestCreateReservationAllowsTemporaryOverageWithinBuffer`
   - `TestFinalizeReservationCreatesChargeAndRelease`
   - `TestFinalizeReservationMarksAmbiguousStreamForReconciliation`
   - `TestReleaseReservationWritesReleaseEventsOnlyOnce`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/accounting/... -count=1 && rg -n "credit_reservations|credit_reconciliation_jobs|FinalizeReservation|temporary_overage" supabase/migrations/20260330_03_credit_reservations.sql apps/control-plane/internal/accounting/service.go</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260330_03_credit_reservations.sql` contains `create table public.credit_reservations`
    - `supabase/migrations/20260330_03_credit_reservations.sql` contains `create table public.credit_reconciliation_jobs`
    - `apps/control-plane/internal/accounting/service.go` contains `FinalizeReservation`
    - `apps/control-plane/internal/accounting/service.go` contains `10000`
    - `apps/control-plane/internal/accounting/service_test.go` contains `TestFinalizeReservationMarksAmbiguousStreamForReconciliation`
    - `apps/control-plane/internal/accounting/service_test.go` contains `TestReleaseReservationWritesReleaseEventsOnlyOnce`
    - `apps/control-plane/internal/ledger/service.go` contains `ChargeUsage`
  </acceptance_criteria>
  <done>The backend can reserve, expand, finalize, release, and reconcile credit usage with customer-favoring semantics for ambiguous interruptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add current-account reservation lifecycle endpoints and wire the accounting package into the control-plane</name>
  <files>
    apps/control-plane/cmd/server/main.go,
    apps/control-plane/internal/platform/http/router.go,
    apps/control-plane/internal/accounting/http.go,
    apps/control-plane/internal/accounting/http_test.go
  </files>
  <read_first>
    apps/control-plane/cmd/server/main.go,
    apps/control-plane/internal/platform/http/router.go,
    apps/control-plane/internal/accounts/http.go,
    apps/control-plane/internal/accounts/service.go,
    apps/control-plane/internal/ledger/http.go,
    apps/control-plane/internal/usage/http.go,
    apps/control-plane/internal/accounting/service.go
  </read_first>
  <action>
1. Create `apps/control-plane/internal/accounting/http.go` following the same current-account resolution pattern used by `profiles.Handler` and `usage.Handler`:
   - handler constructor `NewHandler(svc *Service, accountsSvc *accounts.Service) *Handler`
   - `POST /api/v1/accounts/current/credits/reservations`
   - `POST /api/v1/accounts/current/credits/reservations/expand`
   - `POST /api/v1/accounts/current/credits/reservations/finalize`
   - `POST /api/v1/accounts/current/credits/reservations/release`

2. Use these exact request body fields:
   - create: `request_id`, `attempt_number`, `endpoint`, `model_alias`, `estimated_credits`, `policy_mode`, `customer_tags`
   - expand: `reservation_id`, `additional_credits`
   - finalize: `reservation_id`, `actual_credits`, `terminal_usage_confirmed`, `status`
   - release: `reservation_id`, `reason`

3. Update `apps/control-plane/internal/platform/http/router.go` so the protected routes include:
   - `/api/v1/accounts/current/credits/reservations`
   - `/api/v1/accounts/current/credits/reservations/expand`
   - `/api/v1/accounts/current/credits/reservations/finalize`
   - `/api/v1/accounts/current/credits/reservations/release`

4. Update `apps/control-plane/cmd/server/main.go` so when the DB pool is available it also:
   - builds `accountingRepo`, `accountingSvc`, and `accountingHandler`
   - passes the accounting handler into the router config alongside accounts, profiles, ledger, and usage handlers

5. Add `apps/control-plane/internal/accounting/http_test.go` with:
   - `TestCreateReservationUsesCurrentAccount`
   - `TestFinalizeReservationRequiresReservationID`
   - `TestReleaseReservationReturnsPolicyError`
   - `TestExpandReservationRejectsInvalidUUID`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/accounting/... -count=1 && rg -n "/api/v1/accounts/current/credits/reservations|/api/v1/accounts/current/credits/reservations/finalize" apps/control-plane/internal/platform/http/router.go apps/control-plane/internal/accounting/http.go</automated>
  </verify>
  <acceptance_criteria>
    - `apps/control-plane/internal/accounting/http.go` contains `/api/v1/accounts/current/credits/reservations`
    - `apps/control-plane/internal/accounting/http.go` contains `/api/v1/accounts/current/credits/reservations/expand`
    - `apps/control-plane/internal/accounting/http.go` contains `/api/v1/accounts/current/credits/reservations/finalize`
    - `apps/control-plane/internal/accounting/http.go` contains `/api/v1/accounts/current/credits/reservations/release`
    - `apps/control-plane/internal/accounting/http_test.go` contains `TestCreateReservationUsesCurrentAccount`
    - `apps/control-plane/internal/platform/http/router.go` contains `/api/v1/accounts/current/credits/reservations/finalize`
  </acceptance_criteria>
  <done>The control-plane exposes concrete current-account reservation endpoints that future edge execution and support flows can use and test.</done>
</task>

</tasks>

<verification>
- Run `cd /home/sakib/hive && rg -n "credit_reservations|credit_reconciliation_jobs" supabase/migrations/20260330_03_credit_reservations.sql` to confirm the schema exists.
- Run `cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/accounting/... -count=1` to verify reservation and settlement behavior.
- Run `cd /home/sakib/hive && rg -n "/api/v1/accounts/current/credits/reservations|/api/v1/accounts/current/credits/reservations/finalize" apps/control-plane/internal/platform/http/router.go apps/control-plane/internal/accounting/http.go` to confirm the endpoints are registered.
</verification>

<success_criteria>
- Hive can create reservations before dispatch, expand them during streaming when policy allows, and settle them accurately after success, failure, cancellation, or interruption.
- Ambiguous interrupted executions default to a customer-favoring release or partial charge plus a reconciliation marker.
- Reservation, charge, release, and refund transitions are represented as durable records instead of transient in-memory state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-credits-ledger-usage-accounting/03-03-SUMMARY.md`
</output>
