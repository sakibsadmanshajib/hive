---
phase: 04-model-catalog-provider-routing
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - apps/control-plane/internal/routing/normalizer.go
  - apps/control-plane/internal/routing/normalizer_test.go
  - apps/control-plane/internal/routing/sanitize.go
  - apps/control-plane/internal/routing/sanitize_test.go
  - apps/control-plane/internal/usage/http.go
  - apps/control-plane/internal/usage/http_test.go
  - apps/edge-api/internal/errors/provider_blind.go
  - apps/edge-api/internal/errors/provider_blind_test.go
autonomous: true
requirements:
  - ROUT-03
must_haves:
  truths:
    - "Cache-aware billing must flow into the existing `cache_read_tokens` and `cache_write_tokens` fields instead of creating a second usage-accounting shape."
    - "Customer-visible usage and error surfaces must stay provider-blind even when internal diagnostics preserve provider detail."
    - "Cache token fields should appear in customer-facing payloads only when the route/provider actually reported cache-aware semantics."
  artifacts:
    - path: "apps/control-plane/internal/routing/normalizer.go"
      provides: "Provider-usage normalization into Hive cache token categories."
      contains: "type ProviderUsageInput"
    - path: "apps/control-plane/internal/usage/http.go"
      provides: "Customer-visible usage response shaping that only emits cache fields when applicable."
      contains: "\"cache_read_tokens\""
    - path: "apps/edge-api/internal/errors/provider_blind.go"
      provides: "Edge-side provider-blind upstream error translation helpers."
      contains: "func WriteProviderBlindUpstreamError"
  key_links:
    - from: "apps/control-plane/internal/routing/normalizer.go"
      to: "apps/control-plane/internal/usage/http.go"
      via: "Normalized cache token categories are what the customer-visible usage handler should expose."
      pattern: "CacheCreationInputTokens"
    - from: "apps/control-plane/internal/routing/sanitize.go"
      to: "apps/edge-api/internal/errors/provider_blind.go"
      via: "The same provider-blind translation rules need to be mirrored in the edge error writer without importing control-plane internals directly."
      pattern: "SanitizeProviderMessage"
---

<objective>
Normalize cache-aware provider usage into Hive's existing accounting fields and add provider-blind error translation helpers.

Purpose: Phase 4 is not complete until cache-aware token categories can be attributed without leaking provider identity and upstream error messages can be translated into Hive/OpenAI-compatible failures.
Output: provider-usage normalization helpers, conditional cache-field emission in customer-visible usage responses, and reusable provider-blind error translation utilities.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-model-catalog-provider-routing/04-CONTEXT.md
@.planning/phases/04-model-catalog-provider-routing/04-RESEARCH.md
@.planning/phases/04-model-catalog-provider-routing/04-VALIDATION.md
@.planning/phases/04-model-catalog-provider-routing/04-01-PLAN.md
@.planning/phases/04-model-catalog-provider-routing/04-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize provider cache usage into Hive cache token categories and omit unsupported cache fields from customer responses</name>
  <files>
    apps/control-plane/internal/routing/normalizer.go,
    apps/control-plane/internal/routing/normalizer_test.go,
    apps/control-plane/internal/usage/http.go,
    apps/control-plane/internal/usage/http_test.go
  </files>
  <read_first>
    .planning/phases/04-model-catalog-provider-routing/04-CONTEXT.md,
    .planning/phases/04-model-catalog-provider-routing/04-RESEARCH.md,
    apps/control-plane/internal/usage/types.go,
    apps/control-plane/internal/usage/http.go,
    apps/control-plane/internal/usage/http_test.go,
    apps/control-plane/internal/usage/repository.go,
    apps/control-plane/internal/routing/types.go
  </read_first>
  <action>
1. Create `apps/control-plane/internal/routing/normalizer.go` with:
   - `type ProviderUsageInput`
   - fields:
     - `PromptTokens int64`
     - `CompletionTokens int64`
     - `PromptCachedTokens int64`
     - `CacheReadInputTokens int64`
     - `CacheCreationInputTokens int64`
   - `func NormalizeCacheUsage(input ProviderUsageInput) (cacheReadTokens int64, cacheWriteTokens int64)`

2. Implement `NormalizeCacheUsage` with these exact rules:
   - if `CacheReadInputTokens > 0`, use it as `cacheReadTokens`
   - if `CacheCreationInputTokens > 0`, use it as `cacheWriteTokens`
   - if `CacheReadInputTokens == 0` and `PromptCachedTokens > 0`, use `PromptCachedTokens` as `cacheReadTokens`
   - never map `PromptCachedTokens` into `cacheWriteTokens`

3. Update `apps/control-plane/internal/usage/http.go` so the customer-visible event response:
   - includes `cache_read_tokens` only when `event.CacheReadTokens > 0`
   - includes `cache_write_tokens` only when `event.CacheWriteTokens > 0`
   - preserves all existing non-cache fields

4. Add tests:
   - `apps/control-plane/internal/routing/normalizer_test.go`
     - `TestNormalizeCacheUsageMapsExplicitReadAndWriteFields`
     - `TestNormalizeCacheUsageFallsBackToPromptCachedTokens`
     - `TestNormalizeCacheUsageDoesNotInventWriteTokens`
   - `apps/control-plane/internal/usage/http_test.go`
     - `TestListEventsOmitsCacheFieldsWhenZero`
     - `TestListEventsIncludesCacheFieldsWhenPresent`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/usage/... ./internal/routing/... -count=1 && rg -n "CacheCreationInputTokens|PromptCachedTokens|cache_read_tokens|cache_write_tokens" apps/control-plane/internal/routing/normalizer.go apps/control-plane/internal/usage/http.go</automated>
  </verify>
  <acceptance_criteria>
    - `apps/control-plane/internal/routing/normalizer.go` contains `type ProviderUsageInput`
    - `apps/control-plane/internal/routing/normalizer.go` contains `CacheCreationInputTokens`
    - `apps/control-plane/internal/routing/normalizer.go` contains `PromptCachedTokens`
    - `apps/control-plane/internal/usage/http_test.go` contains `TestListEventsOmitsCacheFieldsWhenZero`
    - `apps/control-plane/internal/usage/http_test.go` contains `TestListEventsIncludesCacheFieldsWhenPresent`
  </acceptance_criteria>
  <done>Hive can now distinguish cache-read and cache-write usage when providers report them and avoid meaningless zero cache fields when they do not.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reusable provider-blind error translation helpers and leak tests</name>
  <files>
    apps/control-plane/internal/routing/sanitize.go,
    apps/control-plane/internal/routing/sanitize_test.go,
    apps/edge-api/internal/errors/provider_blind.go,
    apps/edge-api/internal/errors/provider_blind_test.go
  </files>
  <read_first>
    apps/edge-api/internal/errors/openai.go,
    apps/edge-api/internal/errors/openai_test.go,
    apps/edge-api/internal/middleware/unsupported_test.go,
    apps/control-plane/internal/usage/http.go,
    apps/control-plane/internal/routing/service.go,
    .planning/phases/04-model-catalog-provider-routing/04-RESEARCH.md
  </read_first>
  <action>
1. Create `apps/control-plane/internal/routing/sanitize.go` with:
   - `func SanitizeProviderMessage(alias string, raw string) string`
   - it must replace these exact provider/internal strings when they appear:
     - `openrouter`
     - `groq`
     - `openrouter/auto`
     - `openrouter/free`
     - `route-openrouter-default`
     - `route-openrouter-auto`
     - `route-groq-fast`
   - the sanitized message must mention the requested alias instead of the provider route when alias is non-empty

2. Create `apps/edge-api/internal/errors/provider_blind.go` with:
   - `func WriteProviderBlindUpstreamError(w http.ResponseWriter, alias string, httpStatus int, rawMessage string)`
   - exact status mapping:
     - `429` -> error type `rate_limit_error`, code `upstream_rate_limited`
     - `503` or `504` -> error type `api_error`, code `upstream_unavailable`
     - all other statuses -> error type `api_error`, code `upstream_error`
   - the message written to the client must apply the same exact replacement rules as `SanitizeProviderMessage` without importing `apps/control-plane/internal/...`

3. Add tests:
   - `apps/control-plane/internal/routing/sanitize_test.go`
     - `TestSanitizeProviderMessageRemovesProviderNames`
     - `TestSanitizeProviderMessageUsesAliasWhenAvailable`
   - `apps/edge-api/internal/errors/provider_blind_test.go`
     - `TestWriteProviderBlindUpstreamErrorStripsProviderStrings`
     - `TestWriteProviderBlindUpstreamErrorMaps429ToRateLimit`
     - `TestWriteProviderBlindUpstreamErrorMaps503ToUnavailable`
   - every test must assert the output does not contain `openrouter`, `groq`, `route-openrouter-default`, or `openrouter/auto`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/routing/... -count=1 && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain go test ./apps/edge-api/internal/errors/... -count=1 && rg -n "SanitizeProviderMessage|WriteProviderBlindUpstreamError|upstream_unavailable|upstream_rate_limited" apps/control-plane/internal/routing/sanitize.go apps/edge-api/internal/errors/provider_blind.go</automated>
  </verify>
  <acceptance_criteria>
    - `apps/control-plane/internal/routing/sanitize.go` contains `func SanitizeProviderMessage`
    - `apps/control-plane/internal/routing/sanitize.go` contains `route-openrouter-default`
    - `apps/edge-api/internal/errors/provider_blind.go` contains `func WriteProviderBlindUpstreamError`
    - `apps/edge-api/internal/errors/provider_blind.go` contains `upstream_unavailable`
    - `apps/edge-api/internal/errors/provider_blind.go` contains `upstream_rate_limited`
    - `apps/edge-api/internal/errors/provider_blind_test.go` contains `TestWriteProviderBlindUpstreamErrorStripsProviderStrings`
    - `apps/control-plane/internal/routing/sanitize_test.go` contains `TestSanitizeProviderMessageUsesAliasWhenAvailable`
  </acceptance_criteria>
  <done>Provider-originated failures and route-handle names can now be translated into Hive/OpenAI-compatible error messages without leaking vendor identity.</done>
</task>

</tasks>

<verification>
- Run `cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/usage/... ./internal/routing/... -count=1` to verify cache normalization and provider-message sanitization.
- Run `cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm toolchain go test ./apps/edge-api/internal/errors/... -count=1` to verify edge-side provider-blind error translation.
- Run `cd /home/sakib/hive && rg -n "SanitizeProviderMessage|WriteProviderBlindUpstreamError|cache_write_tokens|cache_read_tokens" apps/control-plane/internal/routing/sanitize.go apps/edge-api/internal/errors/provider_blind.go apps/control-plane/internal/usage/http.go` to confirm the new translation and cache-shaping paths exist.
</verification>

<success_criteria>
- Cache-aware provider usage can be normalized into Hive's existing accounting fields without creating new customer-visible token categories.
- Customer-facing usage responses only include cache fields when they are actually meaningful.
- Provider names and internal route handles are stripped from edge-facing upstream error messages.
</success_criteria>

<output>
After completion, create `.planning/phases/04-model-catalog-provider-routing/04-03-SUMMARY.md`
</output>
