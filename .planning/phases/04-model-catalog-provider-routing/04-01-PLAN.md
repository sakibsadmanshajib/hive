---
phase: 04-model-catalog-provider-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - deploy/docker/docker-compose.yml
  - apps/control-plane/cmd/server/main.go
  - apps/control-plane/internal/platform/http/router.go
  - apps/control-plane/internal/catalog/types.go
  - apps/control-plane/internal/catalog/repository.go
  - apps/control-plane/internal/catalog/service.go
  - apps/control-plane/internal/catalog/http.go
  - apps/control-plane/internal/catalog/service_test.go
  - apps/control-plane/internal/catalog/http_test.go
  - apps/edge-api/cmd/server/main.go
  - apps/edge-api/cmd/server/main_test.go
  - apps/edge-api/internal/catalog/client.go
  - apps/edge-api/internal/catalog/client_test.go
  - packages/sdk-tests/fixtures/golden/models-list.json
  - packages/sdk-tests/js/tests/models/list-models.test.ts
  - supabase/migrations/20260331_01_model_catalog.sql
autonomous: true
requirements:
  - ROUT-01
must_haves:
  truths:
    - "Public alias IDs are the customer contract; upstream provider model IDs must not appear in `/v1/models` or the public Hive catalog."
    - "`GET /v1/models` must stay OpenAI-compatible and lean even after Hive pricing and capability metadata become public."
    - "The richer Hive catalog and the OpenAI `/v1/models` projection must come from the same control-plane source of truth so they cannot drift."
  artifacts:
    - path: "supabase/migrations/20260331_01_model_catalog.sql"
      provides: "Durable alias-catalog schema and seeded public Hive aliases."
      contains: "create table public.model_aliases"
    - path: "apps/control-plane/internal/catalog/service.go"
      provides: "Control-plane catalog snapshot assembly for both public projections."
      contains: "type CatalogSnapshot"
    - path: "apps/edge-api/internal/catalog/client.go"
      provides: "Edge-side snapshot client used by `/v1/models` and `/catalog/models`."
      contains: "func (c *Client) FetchSnapshot"
    - path: "apps/edge-api/cmd/server/main.go"
      provides: "Public model list and richer catalog handlers backed by the control-plane snapshot."
      contains: "/catalog/models"
  key_links:
    - from: "apps/control-plane/internal/catalog/http.go"
      to: "apps/edge-api/internal/catalog/client.go"
      via: "The edge reads the control-plane snapshot from a single internal endpoint instead of duplicating catalog truth."
      pattern: "/internal/catalog/snapshot"
    - from: "packages/openai-contract/upstream/openapi.yaml"
      to: "packages/sdk-tests/fixtures/golden/models-list.json"
      via: "The seeded alias projection still has to satisfy the OpenAI list-model shape used by the SDK regression harness."
      pattern: "\"object\": \"list\""
---

<objective>
Create the Phase 4 source of truth for public Hive aliases, pricing metadata, and public model projections.

Purpose: Phase 4 cannot claim provider-blind public model listing until one control-plane-owned catalog feeds both the OpenAI `/v1/models` response and a richer Hive catalog projection.
Output: a seeded alias-catalog schema, a control-plane snapshot endpoint, an edge snapshot client, a non-empty `/v1/models` response, and a richer `/catalog/models` JSON surface that shares the same alias data.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-model-catalog-provider-routing/04-CONTEXT.md
@.planning/phases/04-model-catalog-provider-routing/04-RESEARCH.md
@.planning/phases/04-model-catalog-provider-routing/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the control-plane model catalog schema, seeded aliases, and internal snapshot endpoint</name>
  <files>
    supabase/migrations/20260331_01_model_catalog.sql,
    apps/control-plane/internal/catalog/types.go,
    apps/control-plane/internal/catalog/repository.go,
    apps/control-plane/internal/catalog/service.go,
    apps/control-plane/internal/catalog/http.go,
    apps/control-plane/internal/catalog/service_test.go,
    apps/control-plane/internal/catalog/http_test.go,
    apps/control-plane/internal/platform/http/router.go,
    apps/control-plane/cmd/server/main.go
  </files>
  <read_first>
    .planning/phases/04-model-catalog-provider-routing/04-CONTEXT.md,
    .planning/phases/04-model-catalog-provider-routing/04-RESEARCH.md,
    packages/openai-contract/upstream/openapi.yaml,
    apps/control-plane/cmd/server/main.go,
    apps/control-plane/internal/platform/http/router.go,
    apps/control-plane/internal/usage/types.go,
    apps/control-plane/internal/usage/http.go,
    apps/control-plane/internal/accounting/service.go
  </read_first>
  <action>
1. Create `supabase/migrations/20260331_01_model_catalog.sql` with exactly one durable catalog table named `public.model_aliases` and seed rows for the three public aliases `hive-default`, `hive-fast`, and `hive-auto`.

2. Define `public.model_aliases` with these exact columns:
   - `alias_id text primary key`
   - `owned_by text not null default 'hive'`
   - `display_name text not null`
   - `summary text not null`
   - `visibility text not null check (visibility in ('public', 'preview', 'internal'))`
   - `lifecycle text not null check (lifecycle in ('stable', 'preview', 'hidden'))`
   - `capability_badges jsonb not null default '[]'::jsonb`
   - `input_price_credits bigint not null`
   - `output_price_credits bigint not null`
   - `cache_read_price_credits bigint`
   - `cache_write_price_credits bigint`
   - `created_at timestamptz not null default now()`
   - `updated_at timestamptz not null default now()`

3. Seed the three aliases with these exact values:
   - `hive-default` / `Hive Default` / `visibility='public'` / `lifecycle='stable'` / badges `["stable","chat","responses"]`
   - `hive-fast` / `Hive Fast` / `visibility='public'` / `lifecycle='stable'` / badges `["fast","chat","responses"]`
   - `hive-auto` / `Hive Auto` / `visibility='preview'` / `lifecycle='preview'` / badges `["auto","fallback","preview"]`

4. Create `apps/control-plane/internal/catalog/types.go` with these exact types:
   - `type ModelAlias`
   - `type PublicModel`
   - `type PublicCatalogModel`
   - `type CatalogSnapshot`

5. Create `apps/control-plane/internal/catalog/repository.go` with a pgx-backed repository exposing:
   - `ListPublicAliases(ctx context.Context) ([]ModelAlias, error)`
   - `GetSnapshot(ctx context.Context) (CatalogSnapshot, error)`
   - `GetSnapshot` must return both:
     - `Models []PublicModel` for `/v1/models`
     - `Catalog []PublicCatalogModel` for `/catalog/models`

6. Create `apps/control-plane/internal/catalog/service.go` with:
   - `func NewService(repo Repository) *Service`
   - `func (s *Service) GetSnapshot(ctx context.Context) (CatalogSnapshot, error)`
   - `GetSnapshot` must map every alias row into:
     - OpenAI list-model entries with `id`, `object`, `created`, `owned_by`
     - richer catalog entries with `id`, `display_name`, `summary`, `capability_badges`, `pricing`, and `lifecycle`
   - internal aliases with `visibility='internal'` must never appear in either public projection

7. Create `apps/control-plane/internal/catalog/http.go` with:
   - `type Handler`
   - `func NewHandler(svc *Service) *Handler`
   - `GET /internal/catalog/snapshot`
   - JSON response shaped exactly like:
     - `{"models":[...],"catalog":[...]}`

8. Update `apps/control-plane/internal/platform/http/router.go` and `apps/control-plane/cmd/server/main.go` so the control-plane registers the catalog handler without auth on `/internal/catalog/snapshot` whenever the DB pool is available.

9. Add tests:
   - `apps/control-plane/internal/catalog/service_test.go`
     - `TestGetSnapshotReturnsHiveOwnedModels`
     - `TestGetSnapshotOmitsInternalAliases`
   - `apps/control-plane/internal/catalog/http_test.go`
     - `TestSnapshotHandlerReturnsModelsAndCatalog`
     - `TestSnapshotHandlerReturnsServerErrorOnRepositoryFailure`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/catalog/... -count=1 && rg -n "/internal/catalog/snapshot|create table public.model_aliases|hive-default|hive-fast|hive-auto" apps/control-plane/internal/catalog/http.go supabase/migrations/20260331_01_model_catalog.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260331_01_model_catalog.sql` contains `create table public.model_aliases`
    - `supabase/migrations/20260331_01_model_catalog.sql` contains `hive-default`
    - `supabase/migrations/20260331_01_model_catalog.sql` contains `hive-fast`
    - `supabase/migrations/20260331_01_model_catalog.sql` contains `hive-auto`
    - `apps/control-plane/internal/catalog/types.go` contains `type CatalogSnapshot`
    - `apps/control-plane/internal/catalog/http.go` contains `/internal/catalog/snapshot`
    - `apps/control-plane/internal/platform/http/router.go` contains `/internal/catalog/snapshot`
    - `apps/control-plane/internal/catalog/service_test.go` contains `TestGetSnapshotOmitsInternalAliases`
  </acceptance_criteria>
  <done>The control-plane can assemble one provider-blind catalog snapshot that drives both the OpenAI model list and the richer Hive catalog projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Back the public edge model endpoints with the control-plane snapshot and provider-blind SDK fixtures</name>
  <files>
    .env.example,
    deploy/docker/docker-compose.yml,
    apps/edge-api/internal/catalog/client.go,
    apps/edge-api/internal/catalog/client_test.go,
    apps/edge-api/cmd/server/main.go,
    apps/edge-api/cmd/server/main_test.go,
    packages/sdk-tests/fixtures/golden/models-list.json,
    packages/sdk-tests/js/tests/models/list-models.test.ts
  </files>
  <read_first>
    apps/edge-api/cmd/server/main.go,
    apps/edge-api/cmd/server/main_test.go,
    apps/edge-api/internal/errors/openai.go,
    apps/edge-api/internal/middleware/compat_headers_test.go,
    apps/control-plane/internal/catalog/http.go,
    packages/sdk-tests/js/tests/models/list-models.test.ts,
    packages/sdk-tests/fixtures/golden/models-list.json,
    .env.example,
    deploy/docker/docker-compose.yml
  </read_first>
  <action>
1. Update `.env.example` to add exactly one new edge runtime key:
   - `EDGE_CONTROL_PLANE_BASE_URL=http://control-plane:8081`

2. Update `deploy/docker/docker-compose.yml` so the `edge-api` service:
   - depends on `control-plane` with `condition: service_healthy`
   - gets environment variable `EDGE_CONTROL_PLANE_BASE_URL=${EDGE_CONTROL_PLANE_BASE_URL:-http://control-plane:8081}`

3. Create `apps/edge-api/internal/catalog/client.go` with:
   - `type Snapshot`
   - `type Client`
   - `func NewClient(baseURL string) *Client`
   - `func (c *Client) FetchSnapshot(ctx context.Context) (Snapshot, error)`
   - `FetchSnapshot` must call `GET {baseURL}/internal/catalog/snapshot`

4. Update `apps/edge-api/cmd/server/main.go` so the server:
   - creates one catalog client from `EDGE_CONTROL_PLANE_BASE_URL`
   - replaces the current empty `handleModels` implementation with a snapshot-backed handler that returns `snapshot.Models`
   - registers a new `GET /catalog/models` handler that returns `snapshot.Catalog`
   - when snapshot fetch fails, responds with `errors.WriteError(..., 503, "api_error", "The Hive model catalog is temporarily unavailable.", &code)` where `code == "catalog_unavailable"`

5. Update `packages/sdk-tests/fixtures/golden/models-list.json` so the fixture contains exactly three OpenAI model objects:
   - `hive-default`
   - `hive-fast`
   - `hive-auto`
   - each entry must have `object: "model"` and `owned_by: "hive"`

6. Extend `packages/sdk-tests/js/tests/models/list-models.test.ts` so it additionally asserts:
   - the returned IDs include `hive-default`
   - the response body does not contain provider strings matching `/openrouter|groq/i`

7. Add tests:
   - `apps/edge-api/internal/catalog/client_test.go`
     - `TestFetchSnapshotDecodesModelsAndCatalog`
     - `TestFetchSnapshotReturnsErrorForNon200`
   - `apps/edge-api/cmd/server/main_test.go`
     - `TestHandleModelsReturnsSeededHiveAliases`
     - `TestHandleCatalogModelsReturnsPricingMetadata`
     - `TestHandleModelsDoesNotLeakProviderNames`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm sdk-tests-js pnpm test -- --run tests/models/list-models.test.ts && rg -n "EDGE_CONTROL_PLANE_BASE_URL|/catalog/models|catalog_unavailable|hive-default|hive-fast|hive-auto" .env.example deploy/docker/docker-compose.yml apps/edge-api/cmd/server/main.go packages/sdk-tests/fixtures/golden/models-list.json</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains `EDGE_CONTROL_PLANE_BASE_URL=http://control-plane:8081`
    - `deploy/docker/docker-compose.yml` contains `EDGE_CONTROL_PLANE_BASE_URL`
    - `apps/edge-api/internal/catalog/client.go` contains `func (c *Client) FetchSnapshot`
    - `apps/edge-api/cmd/server/main.go` contains `/catalog/models`
    - `apps/edge-api/cmd/server/main.go` contains `catalog_unavailable`
    - `packages/sdk-tests/fixtures/golden/models-list.json` contains `hive-default`
    - `packages/sdk-tests/js/tests/models/list-models.test.ts` contains `openrouter|groq`
    - `apps/edge-api/cmd/server/main_test.go` contains `TestHandleModelsDoesNotLeakProviderNames`
  </acceptance_criteria>
  <done>The public edge now serves a non-empty OpenAI-compatible model list and a richer Hive catalog projection from the same provider-blind alias snapshot.</done>
</task>

</tasks>

<verification>
- Run `cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/catalog/... -count=1` to verify the control-plane catalog package.
- Run `cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm sdk-tests-js pnpm test -- --run tests/models/list-models.test.ts` to verify the public `/v1/models` SDK behavior.
- Run `cd /home/sakib/hive && rg -n "/internal/catalog/snapshot|/catalog/models|catalog_unavailable" apps/control-plane/internal/catalog/http.go apps/edge-api/cmd/server/main.go` to confirm the snapshot and public catalog routes exist.
</verification>

<success_criteria>
- `/v1/models` returns non-empty Hive alias model objects and stays within the OpenAI model-list shape.
- A richer public Hive catalog projection exposes pricing and capability badges without leaking provider identities.
- The control-plane, edge, and SDK fixtures all consume the same alias truth instead of drifting.
</success_criteria>

<output>
After completion, create `.planning/phases/04-model-catalog-provider-routing/04-01-SUMMARY.md`
</output>
