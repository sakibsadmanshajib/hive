---
phase: 04-model-catalog-provider-routing
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .env.example
  - deploy/docker/docker-compose.yml
  - deploy/litellm/config.yaml
  - apps/control-plane/cmd/server/main.go
  - apps/control-plane/internal/platform/http/router.go
  - apps/control-plane/internal/catalog/types.go
  - apps/control-plane/internal/catalog/repository.go
  - apps/control-plane/internal/catalog/http.go
  - apps/control-plane/internal/routing/types.go
  - apps/control-plane/internal/routing/repository.go
  - apps/control-plane/internal/routing/service.go
  - apps/control-plane/internal/routing/http.go
  - apps/control-plane/internal/routing/service_test.go
  - apps/control-plane/internal/routing/http_test.go
  - supabase/migrations/20260331_02_routing_policy.sql
autonomous: false
requirements:
  - ROUT-02
must_haves:
  truths:
    - "Hive must decide whether a route is eligible before LiteLLM fallback logic runs; LiteLLM is an adapter layer, not the public policy source of truth."
    - "Capability checks, allowlists, and fallback widening rules must be explicit and testable instead of being buried in ordered provider strings."
    - "Public aliases may map to multiple internal routes, but fallback must preserve the alias contract unless the alias policy explicitly allows widening."
  artifacts:
    - path: "supabase/migrations/20260331_02_routing_policy.sql"
      provides: "Durable route candidates, capability matrix, and alias policy schema."
      contains: "create table public.provider_routes"
    - path: "apps/control-plane/internal/routing/service.go"
      provides: "Deterministic route selection with capability, allowlist, and fallback policy enforcement."
      contains: "func (s *Service) SelectRoute"
    - path: "apps/control-plane/internal/routing/http.go"
      provides: "Internal route-selection endpoint for later edge execution phases."
      contains: "/internal/routing/select"
    - path: "deploy/litellm/config.yaml"
      provides: "LiteLLM model-group definitions keyed by internal route handles instead of public aliases."
      contains: "route-openrouter-default"
  key_links:
    - from: "apps/control-plane/internal/routing/service.go"
      to: "deploy/litellm/config.yaml"
      via: "The selected internal route handle must exist as a LiteLLM model group or deployment name."
      pattern: "litellm_model_name"
    - from: "apps/control-plane/internal/catalog/http.go"
      to: "apps/control-plane/internal/routing/http.go"
      via: "The alias snapshot defines what is public; the routing endpoint decides which internal route can satisfy that alias."
      pattern: "alias_id"
---

<objective>
Build the internal provider capability matrix, alias policy profiles, and LiteLLM-backed route selection contract for Phase 4.

Purpose: Hive cannot claim policy-driven provider routing until aliases resolve through explicit capability filters, allowlist checks, and ordered fallback rules instead of a hard-coded provider string.
Output: route-policy schema, routing service and internal endpoint, LiteLLM config for internal route handles, and deterministic tests proving fallback stays inside the alias contract by default.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-model-catalog-provider-routing/04-CONTEXT.md
@.planning/phases/04-model-catalog-provider-routing/04-RESEARCH.md
@.planning/phases/04-model-catalog-provider-routing/04-VALIDATION.md
@.planning/phases/04-model-catalog-provider-routing/04-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add routing-policy schema, capability records, and deterministic route selection logic</name>
  <files>
    supabase/migrations/20260331_02_routing_policy.sql,
    apps/control-plane/internal/catalog/types.go,
    apps/control-plane/internal/catalog/repository.go,
    apps/control-plane/internal/catalog/http.go,
    apps/control-plane/internal/routing/types.go,
    apps/control-plane/internal/routing/repository.go,
    apps/control-plane/internal/routing/service.go,
    apps/control-plane/internal/routing/service_test.go
  </files>
  <read_first>
    .planning/phases/04-model-catalog-provider-routing/04-CONTEXT.md,
    .planning/phases/04-model-catalog-provider-routing/04-RESEARCH.md,
    .planning/phases/04-model-catalog-provider-routing/04-01-PLAN.md,
    apps/control-plane/internal/catalog/types.go,
    apps/control-plane/internal/catalog/repository.go,
    apps/control-plane/internal/accounting/types.go,
    apps/control-plane/internal/usage/types.go
  </read_first>
  <action>
1. Create `supabase/migrations/20260331_02_routing_policy.sql` with these exact tables:
   - `public.provider_routes`
     - `route_id text primary key`
     - `alias_id text not null references public.model_aliases(alias_id) on delete cascade`
     - `provider text not null check (provider in ('openrouter', 'groq'))`
     - `provider_model text not null`
     - `litellm_model_name text not null`
     - `price_class text not null check (price_class in ('budget', 'standard', 'premium'))`
     - `health_state text not null check (health_state in ('healthy', 'degraded', 'disabled')) default 'healthy'`
     - `priority integer not null`
   - `public.provider_capabilities`
     - `route_id text primary key references public.provider_routes(route_id) on delete cascade`
     - `supports_responses boolean not null default false`
     - `supports_chat_completions boolean not null default false`
     - `supports_completions boolean not null default false`
     - `supports_embeddings boolean not null default false`
     - `supports_streaming boolean not null default false`
     - `supports_reasoning boolean not null default false`
     - `supports_cache_read boolean not null default false`
     - `supports_cache_write boolean not null default false`
   - `public.alias_route_policies`
     - `alias_id text primary key references public.model_aliases(alias_id) on delete cascade`
     - `policy_mode text not null check (policy_mode in ('pinned', 'cost', 'latency', 'stability', 'weighted'))`
     - `allow_price_class_widening boolean not null default false`
     - `fallback_order jsonb not null default '[]'::jsonb`

2. Seed these exact internal routes:
   - `route-openrouter-default` for alias `hive-default`
   - `route-openrouter-auto` for alias `hive-auto`
   - `route-groq-fast` for alias `hive-fast`
   - `route-openrouter-fast-fallback` as a same-contract fallback for `hive-fast`

3. Seed these exact policy records:
   - `hive-default` uses `policy_mode='stability'` and `allow_price_class_widening=false`
   - `hive-fast` uses `policy_mode='latency'` and `allow_price_class_widening=false`
   - `hive-auto` uses `policy_mode='weighted'` and `allow_price_class_widening=true`

4. Extend `apps/control-plane/internal/catalog/types.go` and `repository.go` so the catalog snapshot can also carry route metadata for internal consumers:
   - `type RouteSnapshot`
   - `type AliasPolicySnapshot`

5. Create `apps/control-plane/internal/routing/types.go` with these exact types:
   - `type SelectionInput`
   - `type RouteCandidate`
   - `type SelectionResult`

6. Create `apps/control-plane/internal/routing/repository.go` with repository methods:
   - `LoadAliasPolicy(ctx context.Context, aliasID string) (AliasPolicySnapshot, error)`
   - `ListRouteCandidates(ctx context.Context, aliasID string) ([]RouteCandidate, error)`

7. Create `apps/control-plane/internal/routing/service.go` with:
   - `func NewService(repo Repository) *Service`
   - `func (s *Service) SelectRoute(ctx context.Context, input SelectionInput) (SelectionResult, error)`
   - `SelectionInput` must include exact filter fields:
     - `AliasID string`
     - `NeedResponses bool`
     - `NeedChatCompletions bool`
     - `NeedEmbeddings bool`
     - `NeedStreaming bool`
     - `NeedReasoning bool`
     - `NeedCacheRead bool`
     - `NeedCacheWrite bool`
     - `AllowedAliases []string`
     - `AllowedProviders []string`
   - `SelectRoute` must apply filters in this exact order:
     1. alias exists
     2. alias passes `AllowedAliases` if the list is non-empty
     3. candidates match requested capability booleans
     4. candidates match `AllowedProviders` if the list is non-empty
     5. `health_state != 'disabled'`
     6. candidates are ordered by alias policy plus `priority`
     7. same-price-class fallback only, unless `allow_price_class_widening == true`

8. Add tests in `apps/control-plane/internal/routing/service_test.go`:
   - `TestSelectRouteHonorsCapabilityMatrix`
   - `TestSelectRouteRejectsDisallowedAlias`
   - `TestSelectRouteKeepsSamePriceClassByDefault`
   - `TestSelectRouteAllowsExplicitPriceClassWidening`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/routing/... -count=1 && rg -n "create table public.provider_routes|create table public.provider_capabilities|func \\(s \\*Service\\) SelectRoute|allow_price_class_widening" supabase/migrations/20260331_02_routing_policy.sql apps/control-plane/internal/routing/service.go</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260331_02_routing_policy.sql` contains `create table public.provider_routes`
    - `supabase/migrations/20260331_02_routing_policy.sql` contains `create table public.provider_capabilities`
    - `supabase/migrations/20260331_02_routing_policy.sql` contains `route-openrouter-default`
    - `apps/control-plane/internal/routing/types.go` contains `type SelectionInput`
    - `apps/control-plane/internal/routing/service.go` contains `func (s *Service) SelectRoute`
    - `apps/control-plane/internal/routing/service.go` contains `AllowedAliases`
    - `apps/control-plane/internal/routing/service_test.go` contains `TestSelectRouteKeepsSamePriceClassByDefault`
    - `apps/control-plane/internal/routing/service_test.go` contains `TestSelectRouteAllowsExplicitPriceClassWidening`
  </acceptance_criteria>
  <done>The control-plane can now decide which internal routes are eligible for an alias before any provider adapter or LiteLLM fallback is attempted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LiteLLM route-group config and expose internal route selection over HTTP</name>
  <files>
    .env.example,
    deploy/docker/docker-compose.yml,
    deploy/litellm/config.yaml,
    apps/control-plane/internal/routing/http.go,
    apps/control-plane/internal/routing/http_test.go,
    apps/control-plane/internal/platform/http/router.go,
    apps/control-plane/cmd/server/main.go
  </files>
  <read_first>
    .env.example,
    deploy/docker/docker-compose.yml,
    apps/control-plane/cmd/server/main.go,
    apps/control-plane/internal/platform/http/router.go,
    apps/control-plane/internal/routing/service.go,
    apps/control-plane/internal/catalog/http.go
  </read_first>
  <action>
1. Update `.env.example` to add these exact Phase 4 routing keys:
   - `LITELLM_MASTER_KEY=litellm-dev-key`
   - `OPENROUTER_API_KEY=`
   - `GROQ_API_KEY=`
   - `OPENROUTER_DEFAULT_MODEL=`
   - `OPENROUTER_AUTO_MODEL=`
   - `OPENROUTER_FAST_FALLBACK_MODEL=`
   - `GROQ_FAST_MODEL=`

2. Update `deploy/docker/docker-compose.yml` to add a `litellm` service with:
   - image `ghcr.io/berriai/litellm:main-stable`
   - port mapping `4000:4000`
   - env vars `LITELLM_MASTER_KEY`, `OPENROUTER_API_KEY`, `GROQ_API_KEY`, `OPENROUTER_DEFAULT_MODEL`, `OPENROUTER_AUTO_MODEL`, `OPENROUTER_FAST_FALLBACK_MODEL`, `GROQ_FAST_MODEL`
   - a volume mount `../../deploy/litellm/config.yaml:/app/config.yaml`
   - command `--config=/app/config.yaml`

3. Create `deploy/litellm/config.yaml` with these exact `model_list` entries:
   - `route-openrouter-default`
   - `route-openrouter-auto`
   - `route-openrouter-fast-fallback`
   - `route-groq-fast`
   - each entry must set `litellm_params.model` from the matching environment variable:
     - `route-openrouter-default` -> `OPENROUTER_DEFAULT_MODEL`
     - `route-openrouter-auto` -> `OPENROUTER_AUTO_MODEL`
     - `route-openrouter-fast-fallback` -> `OPENROUTER_FAST_FALLBACK_MODEL`
     - `route-groq-fast` -> `GROQ_FAST_MODEL`
   - the LiteLLM model-group IDs must never use the public alias names `hive-default`, `hive-fast`, or `hive-auto`

4. Create `apps/control-plane/internal/routing/http.go` with:
   - `type Handler`
   - `func NewHandler(svc *Service) *Handler`
   - `POST /internal/routing/select`
   - request JSON with exact fields:
     - `alias_id`
     - `need_responses`
     - `need_chat_completions`
     - `need_embeddings`
     - `need_streaming`
     - `need_reasoning`
     - `need_cache_read`
     - `need_cache_write`
     - `allowed_aliases`
     - `allowed_providers`
   - response JSON with exact fields:
     - `alias_id`
     - `route_id`
     - `litellm_model_name`
     - `provider`
     - `fallback_route_ids`

5. Update `apps/control-plane/internal/platform/http/router.go` and `apps/control-plane/cmd/server/main.go` so the routing handler is registered without auth on `/internal/routing/select`.

6. Add tests in `apps/control-plane/internal/routing/http_test.go`:
   - `TestSelectRouteHandlerReturnsRouteAndFallbacks`
   - `TestSelectRouteHandlerRejectsMissingAliasID`
   - `TestSelectRouteHandlerRejectsIneligibleRoute`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml config --services | grep -qx litellm && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/routing/... -count=1 && rg -n "route-openrouter-default|route-groq-fast|/internal/routing/select|LITELLM_MASTER_KEY" deploy/litellm/config.yaml apps/control-plane/internal/routing/http.go .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains `LITELLM_MASTER_KEY=litellm-dev-key`
    - `.env.example` contains `OPENROUTER_FAST_FALLBACK_MODEL=`
    - `deploy/docker/docker-compose.yml` contains `litellm:`
    - `deploy/docker/docker-compose.yml` contains `4000:4000`
    - `deploy/docker/docker-compose.yml` contains `../../deploy/litellm/config.yaml:/app/config.yaml`
    - `deploy/litellm/config.yaml` contains `route-openrouter-default`
    - `deploy/litellm/config.yaml` contains `route-groq-fast`
    - `apps/control-plane/internal/routing/http.go` contains `/internal/routing/select`
    - `apps/control-plane/internal/platform/http/router.go` contains `/internal/routing/select`
    - `apps/control-plane/internal/routing/http_test.go` contains `TestSelectRouteHandlerRejectsIneligibleRoute`
  </acceptance_criteria>
  <done>Hive now has an internal route-selection contract and a LiteLLM config surface keyed by private route handles rather than public alias names.</done>
</task>

</tasks>

<verification>
- Run `cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml config --services | grep -qx litellm` to confirm the LiteLLM service is wired into the Docker stack.
- Run `cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/routing/... -count=1` to verify capability filtering and fallback policy logic.
- Run `cd /home/sakib/hive && rg -n "/internal/routing/select|route-openrouter-default|allow_price_class_widening" apps/control-plane/internal/routing/http.go deploy/litellm/config.yaml supabase/migrations/20260331_02_routing_policy.sql` to confirm the route-selection surface exists.
</verification>

<success_criteria>
- Route selection is explicit, deterministic, and testable against capability flags, allowlists, health state, and fallback widening rules.
- Internal route handles are distinct from public aliases and map cleanly onto LiteLLM model groups.
- Phase 6 has a concrete internal contract for selecting routes without exposing providers on the public API.
</success_criteria>

<output>
After completion, create `.planning/phases/04-model-catalog-provider-routing/04-02-SUMMARY.md`
</output>
