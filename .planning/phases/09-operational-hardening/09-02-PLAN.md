---
phase: 09-operational-hardening
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: []
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "GitHub issues exist for each of the 7 deferred endpoint groups"
    - "Each issue has acceptance criteria for future implementation"
    - "Each issue references the stub implementation in v1-stubs.ts"
  artifacts: []
  key_links: []
---

<objective>
Create GitHub issues for each deferred OpenAI endpoint group so future work is tracked with clear acceptance criteria.

Purpose: Deferred endpoints are tracked in the project's issue tracker with actionable acceptance criteria, closing OPS-02.
Output: 7 GitHub issues created via `gh` CLI.
</objective>

<execution_context>
@/home/sakib/.claude/get-shit-done/workflows/execute-plan.md
@/home/sakib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-operational-hardening/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub issues for all 7 deferred endpoint groups</name>
  <files></files>
  <read_first>
    apps/api/src/routes/v1-stubs.ts (confirm stub routes exist to reference in issues)
    .planning/REQUIREMENTS.md (v2 requirements section for deferred endpoint details)
  </read_first>
  <action>
First verify `gh` CLI is authenticated: `gh auth status`. If not authenticated, create a markdown file `.planning/phases/09-operational-hardening/09-GITHUB-ISSUES.md` documenting all 7 issues instead, and flag for manual creation.

If authenticated, create 7 GitHub issues using `gh issue create`. Each issue follows this pattern:

**Issue 1: Audio**
- Title: `feat: implement /v1/audio (OpenAI compatibility)`
- Body:
  ```
  ## Endpoints
  - `POST /v1/audio/speech` -- text-to-speech
  - `POST /v1/audio/transcriptions` -- speech-to-text
  - `POST /v1/audio/translations` -- audio translation

  ## Reference
  - [OpenAI Audio API](https://platform.openai.com/docs/api-reference/audio)
  - Stub implementation: `apps/api/src/routes/v1-stubs.ts`

  ## Acceptance Criteria
  - [ ] Each endpoint returns valid OpenAI response schema
  - [ ] SDK clients (Python, Node, Go) work end-to-end
  - [ ] Stub route removed from `v1-stubs.ts` after implementation
  - [ ] Integration tests added
  ```

**Issue 2: Files**
- Title: `feat: implement /v1/files (OpenAI compatibility)`
- Endpoints: GET /v1/files, POST /v1/files, GET /v1/files/:file_id, DELETE /v1/files/:file_id, GET /v1/files/:file_id/content
- Reference: [OpenAI Files API](https://platform.openai.com/docs/api-reference/files)

**Issue 3: Uploads**
- Title: `feat: implement /v1/uploads (OpenAI compatibility)`
- Endpoints: POST /v1/uploads, GET /v1/uploads/:upload_id, POST /v1/uploads/:upload_id/cancel, POST /v1/uploads/:upload_id/parts
- Reference: [OpenAI Uploads API](https://platform.openai.com/docs/api-reference/uploads)

**Issue 4: Batches**
- Title: `feat: implement /v1/batches (OpenAI compatibility)`
- Endpoints: GET /v1/batches, POST /v1/batches, GET /v1/batches/:batch_id, POST /v1/batches/:batch_id/cancel
- Reference: [OpenAI Batch API](https://platform.openai.com/docs/api-reference/batch)

**Issue 5: Completions (Legacy)**
- Title: `feat: implement /v1/completions (OpenAI compatibility)`
- Endpoints: POST /v1/completions
- Reference: [OpenAI Completions API](https://platform.openai.com/docs/api-reference/completions)
- Note in body: "Legacy endpoint -- deprecated by OpenAI in favor of /v1/chat/completions. Consider whether to implement or permanently stub."

**Issue 6: Fine-tuning**
- Title: `feat: implement /v1/fine_tuning (OpenAI compatibility)`
- Endpoints: GET/POST /v1/fine_tuning/jobs, GET /v1/fine_tuning/jobs/:id, POST /v1/fine_tuning/jobs/:id/cancel, GET /v1/fine_tuning/jobs/:id/events, GET /v1/fine_tuning/jobs/:id/checkpoints
- Reference: [OpenAI Fine-tuning API](https://platform.openai.com/docs/api-reference/fine-tuning)

**Issue 7: Moderations**
- Title: `feat: implement /v1/moderations (OpenAI compatibility)`
- Endpoints: POST /v1/moderations
- Reference: [OpenAI Moderations API](https://platform.openai.com/docs/api-reference/moderations)

All issues should include the same acceptance criteria template (valid response schema, SDK compatibility, stub removal, integration tests) and reference `apps/api/src/routes/v1-stubs.ts` as the current stub implementation.
  </action>
  <verify>
    <automated>gh issue list --search "feat: implement /v1/" --json number,title --limit 10 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `gh issue list` output shows 7 issues with titles matching `feat: implement /v1/{group} (OpenAI compatibility)` for groups: audio, files, uploads, batches, completions, fine_tuning, moderations
    - Each issue body contains "Stub implementation: `apps/api/src/routes/v1-stubs.ts`"
    - Each issue body contains "Acceptance Criteria" section
    - If gh CLI is not authenticated, `.planning/phases/09-operational-hardening/09-GITHUB-ISSUES.md` exists with all 7 issues documented
  </acceptance_criteria>
  <done>7 GitHub issues created (or documented in markdown fallback), each referencing stub implementation and containing acceptance criteria for future implementation</done>
</task>

</tasks>

<verification>
1. `gh issue list --search "feat: implement /v1/"` shows 7 issues
2. Each issue has acceptance criteria and references v1-stubs.ts
</verification>

<success_criteria>
- 7 GitHub issues exist for deferred endpoint groups: audio, files, uploads, batches, completions, fine_tuning, moderations
- Each issue has endpoint list, OpenAI API reference link, acceptance criteria, and stub file reference
- Issues are findable via search
</success_criteria>

<output>
After completion, create `.planning/phases/09-operational-hardening/09-02-SUMMARY.md`
</output>
