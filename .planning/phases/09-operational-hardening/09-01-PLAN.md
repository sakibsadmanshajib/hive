---
phase: 09-operational-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/routes/v1-stubs.ts
  - apps/api/src/routes/v1-plugin.ts
  - apps/api/src/routes/__tests__/v1-stubs.test.ts
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "Requests to /v1/audio/speech return 404 with OpenAI error format and 'not yet supported' message"
    - "Requests to /v1/files return 404 with code 'unsupported_endpoint' instead of generic 'Unknown API route'"
    - "All 7 endpoint groups (audio, files, uploads, batches, completions, fine_tuning, moderations) return informative stub responses"
    - "Existing implemented endpoints (/v1/chat/completions, /v1/models, etc.) are unaffected"
  artifacts:
    - path: "apps/api/src/routes/v1-stubs.ts"
      provides: "Stub route handlers for all unsupported OpenAI endpoints"
      exports: ["registerV1StubRoutes"]
    - path: "apps/api/src/routes/__tests__/v1-stubs.test.ts"
      provides: "Compliance tests for stub error format and endpoint coverage"
      contains: "unsupported_endpoint"
  key_links:
    - from: "apps/api/src/routes/v1-stubs.ts"
      to: "apps/api/src/routes/api-error.ts"
      via: "sendApiError import"
      pattern: "sendApiError\\(reply, 404"
    - from: "apps/api/src/routes/v1-plugin.ts"
      to: "apps/api/src/routes/v1-stubs.ts"
      via: "registerV1StubRoutes call"
      pattern: "registerV1StubRoutes\\(app\\)"
---

<objective>
Register explicit stub routes for all unsupported OpenAI API endpoints so they return informative 404s in OpenAI error format, and verify with compliance tests.

Purpose: Users hitting unsupported endpoints get actionable "not yet supported" messages instead of a generic "Unknown API route" response. This closes OPS-01.
Output: `v1-stubs.ts` with all stub handlers, updated `v1-plugin.ts`, compliance test file.
</objective>

<execution_context>
@/home/sakib/.claude/get-shit-done/workflows/execute-plan.md
@/home/sakib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/api/src/routes/api-error.ts:
```typescript
export type OpenAIErrorType =
  | "invalid_request_error"
  | "authentication_error"
  | "permission_error"
  | "not_found_error"
  | "rate_limit_error"
  | "insufficient_quota"
  | "server_error";

export type ApiErrorOpts = {
  type?: OpenAIErrorType;
  param?: string | null;
  code?: string | null;
};

export function sendApiError(
  reply: FastifyReply,
  status: number,
  message: string,
  opts?: ApiErrorOpts,
): void;
```

From apps/api/src/routes/v1-plugin.ts:
```typescript
// Route registration pattern — stubs follow same style but without services arg:
registerChatCompletionsRoute(app, services);
registerModelsRoute(app, services);
registerEmbeddingsRoute(app, services);
registerImagesGenerationsRoute(app, services);
registerResponsesRoute(app, services);

// v1Plugin is registered with prefix "/v1", so routes inside register WITHOUT the /v1 prefix
// e.g., chat-completions registers "/chat/completions", not "/v1/chat/completions"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v1-stubs.ts and register in v1-plugin.ts</name>
  <files>apps/api/src/routes/v1-stubs.ts, apps/api/src/routes/v1-plugin.ts</files>
  <read_first>
    apps/api/src/routes/api-error.ts
    apps/api/src/routes/v1-plugin.ts
    apps/api/src/routes/chat-completions.ts (first 10 lines — confirm route registration pattern)
  </read_first>
  <action>
Create `apps/api/src/routes/v1-stubs.ts`:

1. Import `sendApiError` from `"./api-error"` and `FastifyInstance` from `"fastify"`.

2. Create a helper function `stubHandler(endpoint: string)` that returns a Fastify route handler calling:
   ```
   sendApiError(reply, 404,
     `The ${endpoint} endpoint is not yet supported. Please check our roadmap for availability.`,
     { code: "unsupported_endpoint" }
   )
   ```
   The `endpoint` parameter should include the `/v1` prefix in the message (e.g., `/v1/audio/speech`) even though routes are registered without it.

3. Export `registerV1StubRoutes(app: FastifyInstance): void` that registers ALL of these routes (paths WITHOUT `/v1` prefix since v1Plugin provides it):

   Audio (3 routes):
   - `app.post("/audio/speech", stubHandler("/v1/audio/speech"))`
   - `app.post("/audio/transcriptions", stubHandler("/v1/audio/transcriptions"))`
   - `app.post("/audio/translations", stubHandler("/v1/audio/translations"))`

   Files (5 routes):
   - `app.get("/files", stubHandler("/v1/files"))`
   - `app.post("/files", stubHandler("/v1/files"))`
   - `app.get("/files/:file_id", stubHandler("/v1/files/:file_id"))`
   - `app.delete("/files/:file_id", stubHandler("/v1/files/:file_id"))`
   - `app.get("/files/:file_id/content", stubHandler("/v1/files/:file_id/content"))`

   Uploads (4 routes):
   - `app.post("/uploads", stubHandler("/v1/uploads"))`
   - `app.get("/uploads/:upload_id", stubHandler("/v1/uploads/:upload_id"))`
   - `app.post("/uploads/:upload_id/cancel", stubHandler("/v1/uploads/:upload_id/cancel"))`
   - `app.post("/uploads/:upload_id/parts", stubHandler("/v1/uploads/:upload_id/parts"))`

   Batches (3 routes):
   - `app.get("/batches", stubHandler("/v1/batches"))`
   - `app.post("/batches", stubHandler("/v1/batches"))`
   - `app.get("/batches/:batch_id", stubHandler("/v1/batches/:batch_id"))`
   - `app.post("/batches/:batch_id/cancel", stubHandler("/v1/batches/:batch_id/cancel"))`

   Completions (1 route):
   - `app.post("/completions", stubHandler("/v1/completions"))`

   Fine-tuning (5 routes):
   - `app.get("/fine_tuning/jobs", stubHandler("/v1/fine_tuning/jobs"))`
   - `app.post("/fine_tuning/jobs", stubHandler("/v1/fine_tuning/jobs"))`
   - `app.get("/fine_tuning/jobs/:fine_tuning_job_id", stubHandler("/v1/fine_tuning/jobs/:fine_tuning_job_id"))`
   - `app.post("/fine_tuning/jobs/:fine_tuning_job_id/cancel", stubHandler("/v1/fine_tuning/jobs/:fine_tuning_job_id/cancel"))`
   - `app.get("/fine_tuning/jobs/:fine_tuning_job_id/events", stubHandler("/v1/fine_tuning/jobs/:fine_tuning_job_id/events"))`
   - `app.get("/fine_tuning/jobs/:fine_tuning_job_id/checkpoints", stubHandler("/v1/fine_tuning/jobs/:fine_tuning_job_id/checkpoints"))`

   Moderations (1 route):
   - `app.post("/moderations", stubHandler("/v1/moderations"))`

4. Modify `apps/api/src/routes/v1-plugin.ts`:
   - Add import: `import { registerV1StubRoutes } from "./v1-stubs";`
   - Add call AFTER existing route registrations (after `registerResponsesRoute(app, services);`):
     `registerV1StubRoutes(app);`
   - Note: no `services` argument needed since stubs don't call any backends.
  </action>
  <verify>
    <automated>cd /home/sakib/hive && pnpm --filter api exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/v1-stubs.ts` exists and contains `export function registerV1StubRoutes`
    - `v1-stubs.ts` contains `sendApiError(reply, 404` calls
    - `v1-stubs.ts` contains `code: "unsupported_endpoint"`
    - `v1-stubs.ts` contains `not yet supported. Please check our roadmap`
    - `v1-stubs.ts` registers all 7 endpoint groups: audio/speech, files, uploads, batches, completions, fine_tuning/jobs, moderations
    - `v1-plugin.ts` contains `import { registerV1StubRoutes } from "./v1-stubs"`
    - `v1-plugin.ts` contains `registerV1StubRoutes(app)`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>All 20+ stub routes registered in v1-stubs.ts, wired into v1-plugin.ts, TypeScript compiles cleanly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compliance tests for stub endpoints</name>
  <files>apps/api/src/routes/__tests__/v1-stubs.test.ts</files>
  <read_first>
    apps/api/src/routes/__tests__/differentiators-headers.test.ts (reference test pattern — how Fastify app is built for route testing)
    apps/api/src/routes/v1-stubs.ts (verify the routes just created)
    apps/api/src/routes/api-error.ts (confirm error shape)
  </read_first>
  <behavior>
    - Test 1: POST /v1/audio/speech returns 404 with body `{ error: { message: contains "not yet supported", type: "not_found_error", param: null, code: "unsupported_endpoint" } }`
    - Test 2: GET /v1/files returns same error shape (verifies non-POST methods work)
    - Test 3: DELETE /v1/files/file-123 returns same error shape (verifies parameterized routes)
    - Test 4: POST /v1/completions returns same error shape (legacy endpoint)
    - Test 5: POST /v1/fine_tuning/jobs returns same error shape (nested path)
    - Test 6: POST /v1/moderations returns same error shape
    - Test 7: POST /v1/uploads returns same error shape
    - Test 8: POST /v1/batches returns same error shape
    - Test 9: Error message includes the specific endpoint path (e.g., "/v1/audio/speech" appears in message for audio/speech request)
    - Test 10: Existing endpoint POST /v1/chat/completions does NOT return the stub error (regression check — may need valid auth or just verify status is not 404 with code "unsupported_endpoint")
  </behavior>
  <action>
Create `apps/api/src/routes/__tests__/v1-stubs.test.ts`:

1. Follow the test pattern from `differentiators-headers.test.ts` for building a Fastify test app (use `app.inject()` for route testing without a running server).

2. Build a test Fastify app that registers the v1Plugin (which now includes stubs). If the existing test pattern creates a minimal app, follow that pattern. The stubs only need the route registration — they do not need real services.

3. Write a describe block `"OPS-01: Stub endpoint error format"` with:
   - A parameterized test (test.each or similar) covering ALL 7 endpoint groups. For each group, test at least one representative route:
     - `["POST", "/v1/audio/speech"]`
     - `["GET", "/v1/files"]`
     - `["POST", "/v1/uploads"]`
     - `["GET", "/v1/batches"]`
     - `["POST", "/v1/completions"]`
     - `["POST", "/v1/fine_tuning/jobs"]`
     - `["POST", "/v1/moderations"]`
   - Each test asserts:
     - `response.statusCode === 404`
     - `body.error.type === "not_found_error"`
     - `body.error.code === "unsupported_endpoint"`
     - `body.error.param === null`
     - `body.error.message` contains `"not yet supported"`

4. Add a test for parameterized routes: `DELETE /v1/files/test-file-id` returns the same error shape.

5. Add a test that verifies the error message includes the specific endpoint path (e.g., request to `/v1/audio/speech` has message containing `/v1/audio/speech`).

6. Add a regression guard: `POST /v1/chat/completions` should NOT return status 404 with code "unsupported_endpoint" (it may return 400 or 401 due to missing body/auth, but not the stub response).
  </action>
  <verify>
    <automated>cd /home/sakib/hive && pnpm --filter api test run -- v1-stubs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/__tests__/v1-stubs.test.ts` exists
    - Test file contains `OPS-01` in a describe block
    - Test file covers all 7 groups: audio, files, uploads, batches, completions, fine_tuning, moderations
    - Test file asserts `statusCode` is `404`
    - Test file asserts `error.type` is `"not_found_error"`
    - Test file asserts `error.code` is `"unsupported_endpoint"`
    - Test file asserts message contains `"not yet supported"`
    - Test file includes regression check that /v1/chat/completions is NOT a stub
    - All tests pass: `pnpm --filter api test run -- v1-stubs` exits 0
  </acceptance_criteria>
  <done>All stub compliance tests pass, covering all 7 endpoint groups with correct error shape assertions</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd /home/sakib/hive && pnpm --filter api exec tsc --noEmit`
2. Stub tests pass: `cd /home/sakib/hive && pnpm --filter api test run -- v1-stubs`
3. Full test suite unbroken: `cd /home/sakib/hive && pnpm --filter api test run`
4. Manual spot check: `curl -X POST http://localhost:3000/v1/audio/speech` returns `{"error":{"message":"The /v1/audio/speech endpoint is not yet supported...","type":"not_found_error","param":null,"code":"unsupported_endpoint"}}`
</verification>

<success_criteria>
- All 20+ stub routes return 404 with OpenAI error format, type "not_found_error", code "unsupported_endpoint"
- Error messages include the specific endpoint path and "not yet supported" text
- Compliance tests cover all 7 endpoint groups and pass
- Existing endpoints are unaffected (regression test confirms)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-operational-hardening/09-01-SUMMARY.md`
</output>
