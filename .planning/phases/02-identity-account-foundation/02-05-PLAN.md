---
phase: 02-identity-account-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-04
files_modified:
  - apps/control-plane/internal/platform/http/router.go
  - apps/control-plane/internal/profiles/types.go
  - apps/control-plane/internal/profiles/repository.go
  - apps/control-plane/internal/profiles/service.go
  - apps/control-plane/internal/profiles/http.go
  - apps/control-plane/internal/profiles/service_test.go
  - apps/control-plane/internal/profiles/http_test.go
autonomous: true
requirements:
  - AUTH-04
must_haves:
  truths:
    - "Core profile completion stays limited to owner name, login email, account display name, account type, country, and state/province."
    - "Profile setup completion is independent from billing and tax completeness."
  artifacts:
    - path: "apps/control-plane/internal/profiles/http.go"
      provides: "Current-account core profile API."
      contains: "/api/v1/accounts/current/profile"
    - path: "apps/control-plane/internal/profiles/service.go"
      provides: "Profile persistence plus `profile_setup_complete` calculation."
      contains: "profile_setup_complete"
---

<objective>
Add the durable current-account core profile API for the minimal pre-billing identity data Phase 2 requires.

Purpose: Before any setup or settings UI exists, the control plane must own a durable API for the minimal core profile fields that onboarding is allowed to collect.
Output: Core profile DTOs, repository/service/http layers, route registration, and automated API tests.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-account-foundation/02-CONTEXT.md
@.planning/phases/02-identity-account-foundation/02-RESEARCH.md
@.planning/phases/02-identity-account-foundation/02-VALIDATION.md
@.planning/phases/02-identity-account-foundation/02-02-PLAN.md
@.planning/phases/02-identity-account-foundation/02-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the current-account core profile API</name>
  <files>
    apps/control-plane/internal/platform/http/router.go,
    apps/control-plane/internal/profiles/types.go,
    apps/control-plane/internal/profiles/repository.go,
    apps/control-plane/internal/profiles/service.go,
    apps/control-plane/internal/profiles/http.go,
    apps/control-plane/internal/profiles/service_test.go,
    apps/control-plane/internal/profiles/http_test.go
  </files>
  <read_first>
    supabase/migrations/20260328_01_identity_foundation.sql,
    apps/control-plane/internal/accounts/types.go,
    apps/control-plane/internal/accounts/http.go,
    apps/control-plane/internal/platform/http/router.go,
    .planning/phases/02-identity-account-foundation/02-CONTEXT.md,
    .planning/phases/02-identity-account-foundation/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test: updating the current-account profile persists owner name, login email, display name, account type, country, and state/province
    - Test: `profile_setup_complete` flips to true only after all required core profile fields are present
  </behavior>
  <action>
1. Create `apps/control-plane/internal/profiles/types.go` with DTOs for:
   - `AccountProfile`
   - `UpdateAccountProfileInput`

2. Implement `apps/control-plane/internal/profiles/service.go` so:
   - `GET /api/v1/accounts/current/profile` returns `owner_name`, `login_email`, `display_name`, `account_type`, `country_code`, `state_region`, and `profile_setup_complete`
   - `PUT /api/v1/accounts/current/profile` updates `public.account_profiles` plus `public.accounts.display_name` and `public.accounts.account_type`
   - `profile_setup_complete` becomes `true` only when all six core fields are present
   - validation requires `owner_name`, `login_email`, `display_name`, `account_type`, `country_code`, and `state_region`
   - `account_type` must be `personal` or `business`

3. Create `apps/control-plane/internal/profiles/repository.go` and `http.go`, then register:
   - `GET /api/v1/accounts/current/profile`
   - `PUT /api/v1/accounts/current/profile`

4. Add tests covering:
   - core profile persistence
   - `profile_setup_complete` transition
   - invalid `account_type`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/profiles/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - apps/control-plane/internal/profiles/http.go contains `/api/v1/accounts/current/profile`
    - apps/control-plane/internal/profiles/service.go contains `profile_setup_complete`
    - apps/control-plane/internal/profiles/service_test.go contains `TestUpdateAccountProfile`
  </acceptance_criteria>
  <done>The control plane can persist the minimal core profile without mixing in billing or tax completeness rules.</done>
</task>

</tasks>

<verification>
- Run `cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/profiles/... -count=1` to verify core profile persistence and completion logic.
</verification>

<success_criteria>
- The current-account profile API stores owner name, login email, display name, account type, country, and state/province durably.
- `profile_setup_complete` reflects only the minimal core profile fields, not billing or tax data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-account-foundation/02-05-SUMMARY.md`
</output>
