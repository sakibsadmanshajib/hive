---
phase: 02-identity-account-foundation
plan: 07
type: execute
wave: 6
depends_on:
  - 02-05
  - 02-06
files_modified:
  - supabase/migrations/20260328_02_billing_identity_profiles.sql
  - apps/control-plane/internal/platform/http/router.go
  - apps/control-plane/internal/profiles/types.go
  - apps/control-plane/internal/profiles/repository.go
  - apps/control-plane/internal/profiles/service.go
  - apps/control-plane/internal/profiles/http.go
  - apps/control-plane/internal/profiles/service_test.go
  - apps/control-plane/internal/profiles/http_test.go
  - apps/web-console/app/console/settings/billing/page.tsx
  - apps/web-console/components/profile/billing-contact-form.tsx
  - apps/web-console/components/profile/business-tax-form.tsx
  - apps/web-console/lib/control-plane/client.ts
  - apps/web-console/lib/profile-schemas.ts
  - apps/web-console/tests/e2e/profile-completion.spec.ts
autonomous: false
requirements:
  - AUTH-04
must_haves:
  truths:
    - "Billing contact, legal-entity, and tax fields are durable in Phase 2 but remain optional until checkout or invoicing."
    - "Billing settings must not turn into onboarding completion gates or dashboard reminders."
    - "Billing forms still branch by personal versus business account type."
  artifacts:
    - path: "supabase/migrations/20260328_02_billing_identity_profiles.sql"
      provides: "Durable billing-identity schema."
      contains: "create table public.account_billing_profiles"
    - path: "apps/control-plane/internal/profiles/http.go"
      provides: "Billing-profile API."
      contains: "/api/v1/accounts/current/billing-profile"
    - path: "apps/web-console/app/console/settings/billing/page.tsx"
      provides: "Optional billing settings screen."
      contains: "Optional until checkout or invoicing"
---

<objective>
Add the durable billing-profile storage and optional billing settings UI without making billing completeness a Phase 2 gate.

Purpose: Phase 2 must create the durable data hooks later billing flows need, but it must not force billing and tax completion during onboarding or general dashboard use.
Output: Billing-profile migration, API/storage, billing settings screen, and tests for optional partial saves.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-account-foundation/02-CONTEXT.md
@.planning/phases/02-identity-account-foundation/02-RESEARCH.md
@.planning/phases/02-identity-account-foundation/02-VALIDATION.md
@.planning/phases/02-identity-account-foundation/02-05-PLAN.md
@.planning/phases/02-identity-account-foundation/02-06-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add billing-profile persistence that allows partial data before checkout</name>
  <files>
    supabase/migrations/20260328_02_billing_identity_profiles.sql,
    apps/control-plane/internal/platform/http/router.go,
    apps/control-plane/internal/profiles/types.go,
    apps/control-plane/internal/profiles/repository.go,
    apps/control-plane/internal/profiles/service.go,
    apps/control-plane/internal/profiles/http.go,
    apps/control-plane/internal/profiles/service_test.go,
    apps/control-plane/internal/profiles/http_test.go
  </files>
  <read_first>
    supabase/migrations/20260328_01_identity_foundation.sql,
    apps/control-plane/internal/profiles/types.go,
    apps/control-plane/internal/profiles/repository.go,
    apps/control-plane/internal/profiles/service.go,
    apps/control-plane/internal/profiles/http.go,
    .planning/phases/02-identity-account-foundation/02-CONTEXT.md,
    .planning/phases/02-identity-account-foundation/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test: billing-profile updates persist billing contact, legal-entity, and tax fields durably
    - Test: business accounts can save partial billing identity data before checkout without a validation error
    - Test: personal accounts default `legal_entity_type` to `individual`
  </behavior>
  <action>
1. Create `supabase/migrations/20260328_02_billing_identity_profiles.sql` with `public.account_billing_profiles`:
   - `account_id uuid primary key references public.accounts(id) on delete cascade`
   - `billing_contact_name text`
   - `billing_contact_email text`
   - `legal_entity_name text`
   - `legal_entity_type text not null default 'individual' check (legal_entity_type in ('individual', 'sole_proprietor', 'private_company', 'public_company', 'non_profit'))`
   - `business_registration_number text`
   - `vat_number text`
   - `tax_id_type text`
   - `tax_id_value text`
   - `country_code text`
   - `state_region text`
   - `created_at timestamptz not null default now()`
   - `updated_at timestamptz not null default now()`

2. Extend `apps/control-plane/internal/profiles/types.go` with:
   - `BillingProfile`
   - `UpdateBillingProfileInput`

3. Extend `apps/control-plane/internal/profiles/service.go`, `repository.go`, `http.go`, and `router.go` so:
   - `GET /api/v1/accounts/current/billing-profile` returns stored billing-contact and tax fields
   - `PUT /api/v1/accounts/current/billing-profile` upserts the billing profile row
   - saving partial data is allowed for both personal and business accounts
   - for `personal`, write `legal_entity_type = "individual"` when no explicit legal entity type is provided
   - validate format only for provided values such as email shape; do not require completeness before checkout or invoicing

4. Add tests covering:
   - durable billing-profile persistence
   - partial business-profile save success
   - personal-profile default `legal_entity_type`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/profiles/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260328_02_billing_identity_profiles.sql contains `create table public.account_billing_profiles`
    - apps/control-plane/internal/profiles/http.go contains `/api/v1/accounts/current/billing-profile`
    - apps/control-plane/internal/profiles/service.go contains `legal_entity_type`
    - apps/control-plane/internal/profiles/service_test.go contains `TestPartialBusinessBillingProfile`
  </acceptance_criteria>
  <done>Billing identity data can be stored durably without turning completeness into a Phase 2 blocker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create optional billing settings UI without onboarding or dashboard pressure</name>
  <files>
    apps/web-console/app/console/settings/billing/page.tsx,
    apps/web-console/components/profile/billing-contact-form.tsx,
    apps/web-console/components/profile/business-tax-form.tsx,
    apps/web-console/lib/control-plane/client.ts,
    apps/web-console/lib/profile-schemas.ts,
    apps/web-console/tests/e2e/profile-completion.spec.ts
  </files>
  <read_first>
    apps/web-console/app/console/settings/profile/page.tsx,
    apps/web-console/lib/control-plane/client.ts,
    apps/web-console/lib/profile-schemas.ts,
    apps/control-plane/internal/profiles/http.go,
    .planning/phases/02-identity-account-foundation/02-CONTEXT.md,
    .planning/phases/02-identity-account-foundation/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test: `billing settings save partial business profile`
    - Test: `unverified billing settings redirect to profile`
    - Test: no dashboard billing reminder is introduced by this plan
  </behavior>
  <action>
1. Extend `apps/web-console/lib/control-plane/client.ts` with:
   - `getBillingProfile()`
   - `updateBillingProfile()`

2. Extend `apps/web-console/lib/profile-schemas.ts` with `billingProfileSchema` that:
   - branches by `accountType`
   - allows partial saves for both `personal` and `business`
   - validates only provided `billingContactEmail`, `vatNumber`, and `taxIdValue` shapes

3. Create `apps/web-console/app/console/settings/billing/page.tsx`, `billing-contact-form.tsx`, and `business-tax-form.tsx`:
   - render fields labeled `Billing contact name`, `Billing contact email`, `Legal entity name`, and `VAT / Tax ID`
   - display the exact helper copy `Optional until checkout or invoicing.`
   - if `viewer.user.email_verified === false`, redirect to `/console/settings/profile`
   - if `accountType === "personal"`, explain that business-specific fields matter only when later checkout or invoicing requires them
   - if `accountType === "business"`, allow saving partial data and show helper copy rather than blocking validation

4. Update `apps/web-console/tests/e2e/profile-completion.spec.ts` to cover:
   - `billing settings save partial business profile`
   - `unverified billing settings redirect to profile`
   - absence of a new billing reminder on `/console`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm web-console npm run test:e2e -- --grep "(billing settings save partial business profile|unverified billing settings redirect to profile)" && ! rg -n "billing reminder" apps/web-console/app/console/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web-console/app/console/settings/billing/page.tsx contains `Optional until checkout or invoicing.`
    - apps/web-console/app/console/settings/billing/page.tsx contains `redirect("/console/settings/profile")`
    - apps/web-console/lib/profile-schemas.ts contains `billingProfileSchema`
    - apps/web-console/tests/e2e/profile-completion.spec.ts contains `billing settings save partial business profile`
    - apps/web-console/tests/e2e/profile-completion.spec.ts contains `unverified billing settings redirect to profile`
  </acceptance_criteria>
  <done>Billing settings are available as durable optional storage, without becoming an onboarding or dashboard completion gate.</done>
</task>

</tasks>

<verification>
- Run `cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm control-plane go test ./internal/profiles/... -count=1` to verify billing-profile persistence.
- Run `cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm web-console npm run test:e2e -- --grep "(billing settings save partial business profile|unverified billing settings redirect to profile)"` to verify optional billing settings behavior.
</verification>

<success_criteria>
- Phase 2 stores billing contact, legal-entity, country, and VAT or business data durably.
- Billing identity completeness remains optional until checkout or invoicing.
- No new billing completeness reminder or onboarding gate is added to `/console`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-account-foundation/02-07-SUMMARY.md`
</output>
