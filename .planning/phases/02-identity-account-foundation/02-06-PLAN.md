---
phase: 02-identity-account-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - 02-05
  - 02-04
files_modified:
  - apps/web-console/app/console/page.tsx
  - apps/web-console/app/console/setup/page.tsx
  - apps/web-console/app/console/settings/profile/page.tsx
  - apps/web-console/components/profile/account-profile-form.tsx
  - apps/web-console/components/email-settings-card.tsx
  - apps/web-console/lib/control-plane/client.ts
  - apps/web-console/lib/profile-schemas.ts
  - apps/web-console/tests/unit/profile-schemas.test.ts
  - apps/web-console/tests/e2e/profile-completion.spec.ts
autonomous: false
requirements:
  - AUTH-04
must_haves:
  truths:
    - "Initial setup stays short and only asks for owner name, account name, account type, country, and state/province."
    - "Returning users land on `/console`; setup is a prompted action, not a mandatory redirect loop."
    - "Profile maintenance remains available to unverified users."
  artifacts:
    - path: "apps/web-console/app/console/setup/page.tsx"
      provides: "Short first-run setup flow."
      contains: "Owner name"
    - path: "apps/web-console/app/console/settings/profile/page.tsx"
      provides: "Account settings route with email maintenance plus core profile editing."
      contains: "Resend verification email"
    - path: "apps/web-console/lib/profile-schemas.ts"
      provides: "Shared validation for the core account-profile form."
      exports: ["accountProfileSchema"]
---

<objective>
Build the short setup flow plus the durable profile settings UI for the minimal pre-billing account data.

Purpose: The backend profile API is not enough on its own. Phase 2 needs a guided first-run setup and a profile settings page that remain within the locked minimal scope.
Output: `/console/setup`, `/console/settings/profile`, reusable core profile form, email maintenance card, and UI tests for setup completion.
</objective>

<execution_context>
@/home/sakib/.codex/get-shit-done/workflows/execute-plan.md
@/home/sakib/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-account-foundation/02-CONTEXT.md
@.planning/phases/02-identity-account-foundation/02-RESEARCH.md
@.planning/phases/02-identity-account-foundation/02-VALIDATION.md
@.planning/phases/02-identity-account-foundation/02-04-PLAN.md
@.planning/phases/02-identity-account-foundation/02-05-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the core profile form, setup route, and shared schema</name>
  <files>
    apps/web-console/app/console/setup/page.tsx,
    apps/web-console/components/profile/account-profile-form.tsx,
    apps/web-console/lib/control-plane/client.ts,
    apps/web-console/lib/profile-schemas.ts,
    apps/web-console/tests/unit/profile-schemas.test.ts
  </files>
  <read_first>
    apps/web-console/app/console/layout.tsx,
    apps/web-console/lib/control-plane/client.ts,
    apps/control-plane/internal/profiles/http.go,
    .planning/phases/02-identity-account-foundation/02-CONTEXT.md,
    .planning/phases/02-identity-account-foundation/02-RESEARCH.md
  </read_first>
  <action>
1. Extend `apps/web-console/lib/control-plane/client.ts` with:
   - `getAccountProfile()`
   - `updateAccountProfile()`

2. Create `apps/web-console/lib/profile-schemas.ts` with `accountProfileSchema` requiring:
   - `ownerName`
   - `accountName`
   - `accountType`
   - `countryCode`
   - `stateRegion`

3. Create `apps/web-console/components/profile/account-profile-form.tsx` and `app/console/setup/page.tsx` with these exact product rules:
   - show fields labeled `Owner name`, `Account name`, `Account type`, `Country`, and `State / Province`
   - submit through `updateAccountProfile()`
   - redirect to `/console` after success
   - keep `/console/setup` reachable when `viewer.user.email_verified === false`
   - do not render `Billing contact`, `Legal entity`, `VAT`, or `Tax ID` on this page

4. Add `apps/web-console/tests/unit/profile-schemas.test.ts` covering required core fields.
  </action>
  <verify>
    <automated>cd /home/sakib/hive && rg -n "Owner name|State / Province|accountProfileSchema" apps/web-console/app/console/setup/page.tsx apps/web-console/lib/profile-schemas.ts apps/web-console/components/profile/account-profile-form.tsx && docker compose -f deploy/docker/docker-compose.yml run --rm web-console npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - apps/web-console/app/console/setup/page.tsx contains `Owner name`
    - apps/web-console/app/console/setup/page.tsx contains `State / Province`
    - apps/web-console/app/console/setup/page.tsx does not contain `VAT`
    - apps/web-console/lib/profile-schemas.ts contains `accountProfileSchema`
  </acceptance_criteria>
  <done>The short setup flow exists and is constrained to the allowed core profile fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand profile settings and keep dashboard landing behavior intact</name>
  <files>
    apps/web-console/app/console/page.tsx,
    apps/web-console/app/console/settings/profile/page.tsx,
    apps/web-console/components/email-settings-card.tsx,
    apps/web-console/tests/e2e/profile-completion.spec.ts
  </files>
  <read_first>
    apps/web-console/app/console/page.tsx,
    apps/web-console/app/console/setup/page.tsx,
    apps/web-console/components/profile/account-profile-form.tsx,
    apps/web-console/lib/profile-schemas.ts,
    .planning/phases/02-identity-account-foundation/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test: `setup saves profile`
    - Test: `dashboard shows setup reminder instead of forcing setup after completion`
    - Test: `/console/settings/profile` is reachable for unverified users and exposes resend-verification/change-email controls
  </behavior>
  <action>
1. Create `apps/web-console/components/email-settings-card.tsx` that:
   - shows the current login email
   - provides `Resend verification email` via `supabase.auth.resend({ type: "signup", email })`
   - provides `Change email` via `supabase.auth.updateUser({ email: nextEmail })`

2. Create `apps/web-console/app/console/settings/profile/page.tsx` so it:
   - keeps the route accessible to unverified users
   - renders the email settings card
   - renders the same core account-profile form used in `/console/setup`

3. Update `apps/web-console/app/console/page.tsx` to:
   - show a setup reminder card when `profile_setup_complete` is false with a CTA to `/console/setup`
   - keep `/console` as the dashboard landing route after setup completion instead of redirecting users back into setup

4. Create `apps/web-console/tests/e2e/profile-completion.spec.ts` covering:
   - `setup saves profile`
   - `dashboard shows setup reminder instead of forcing setup after completion`
   - `profile settings stay reachable while unverified`
  </action>
  <verify>
    <automated>cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm web-console npm run test:e2e -- --grep "(setup saves profile|dashboard shows setup reminder instead of forcing setup after completion|profile settings stay reachable while unverified)"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web-console/app/console/settings/profile/page.tsx contains `Resend verification email`
    - apps/web-console/app/console/page.tsx contains `/console/setup`
    - apps/web-console/tests/e2e/profile-completion.spec.ts contains `setup saves profile`
    - apps/web-console/tests/e2e/profile-completion.spec.ts contains `profile settings stay reachable while unverified`
  </acceptance_criteria>
  <done>The console supports minimal setup and profile maintenance without forcing users through setup after completion.</done>
</task>

</tasks>

<verification>
- Run `cd /home/sakib/hive && docker compose -f deploy/docker/docker-compose.yml run --rm web-console npm run test:e2e -- --grep "(setup saves profile|dashboard shows setup reminder instead of forcing setup after completion|profile settings stay reachable while unverified)"` to verify the setup and profile UI behavior.
</verification>

<success_criteria>
- `/console/setup` stays minimal and excludes billing or tax fields.
- `/console/settings/profile` supports email maintenance and core profile editing for verified and unverified users.
- `/console` remains the landing page after setup completion and only shows a non-blocking setup reminder when needed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-account-foundation/02-06-SUMMARY.md`
</output>
