<script lang="ts">
	// The list, the editor, the import path and the access dialog are all
	// upstream code that works, reused rather than reimplemented. The guard and
	// the page chrome live in +layout.svelte beside this file.
	import Skills from '$lib/components/workspace/Skills.svelte';
</script>

<Skills />
