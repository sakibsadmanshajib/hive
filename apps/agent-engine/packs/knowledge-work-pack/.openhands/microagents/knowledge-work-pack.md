---
name: knowledge-work-pack
type: repo
---

Legacy-format mirror of `../../AGENTS.md` (current OpenHands SDK convention).
The vendored `openhands-sdk`/`openhands-agent-server` (vendor/openhands) do
not load this file — confirmed zero microagent-loading code in that repo.
Kept only for compatibility with any future tool that still reads the older
`.openhands/microagents/*.md` convention. Same sandbox trust tier as
`coding-pack`: arbitrary shell, build, and test commands allowed.
