---
// Security & Data Residency page stub (scope: onprem). The page builder fills
// the body from SPEC.md: jurisdiction diagram + two exposure vectors + the
// generic court sentence + how Hive removes it + designed-for controls +
// honest Hive-Cloud scope boundary + security disclaimer. No Pinterest here.
import Base from '../layouts/Base.astro';
---

<Base
  title="Security & Data Residency"
  description="Designed for on-prem data residency. Open model weights run entirely on your hardware; inference never calls an external API."
>
  <section class="shell py-section">
    <h1>Security and data residency</h1>
    <p class="placeholder">TODO: page builder fills this from SPEC.md.</p>
  </section>
</Base>

<style>
  .placeholder {
    color: var(--text-faint);
    font-size: 15px;
    margin-top: 24px;
  }
</style>
