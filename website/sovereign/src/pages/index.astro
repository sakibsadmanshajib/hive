---
// Home route for the Hive sovereign site (hive.scubed.co).
//
// Art direction: a confidential institutional dossier. The page reads like a
// meticulously typeset legal/financial briefing, not a generic SaaS landing
// page. Every numbered section uses a document grid: a left margin column holds
// a large Fraunces numeral and a short uppercase margin label, the right column
// holds content at a comfortable reading measure. Sections are separated by
// full-width 1px hairline rules and spacing, never by coloured background bands.
//
// Surfaces: warm off-white base throughout, near-black ink used on exactly two
// deliberate dark surfaces (the trust strip and the closing CTA) for rhythm. One
// deep forest accent (var(--accent)). No teal, no gold. Red appears only inside
// the data-boundary diagram's own markup (residency risk), never on page chrome.
//
// Dossier silhouette:
//   Hero (asymmetric editorial: meta-row + Fraunces H1 + value lines + CTAs,
//         hero image as a tall full-bleed ink panel on the right)
//   Trust strip (ink) -- frameworks + auditor footnote + quiet proof stats
//   01 The control problem        + control cards
//   02 What you are exposed to     + data-boundary svg (Fig. 1)
//   03 A spectrum of control       + tier-spectrum svg (Fig. 2) + tier cards
//   04 The ownership cost story     + cost-crossover svg (Fig. 3) + Pinterest proof
//   05 Who it is for                five audience cards
//   06 Continuous improvement       section photo (Fig. 4)
//   07 The difference in one line   the one Fraunces pull quote
//   08 What is actually true here   proof spine
//   Closing CTA (ink)               the second deliberate dark surface
//
// Copy rules honoured: never the word "compliant" with any framework;
// sovereignty and no-egress language scoped to the on-prem deployment in the
// same sentence; the court-order line stays generic, no date, no provider
// named; the cost wedge stays a multiple of the form "10x to 30x". Canadian
// English. Jurisdiction-neutral. LEGAL-REVIEW markers are invisible Astro
// comments only.
//
// Zero client JS. No <script> tags. The two photographic images go through
// astro:assets <Image>, which emits hashed webp/avif with width/height/srcset
// and no inline style (CSP-clean under img-src 'self' data:). The diagram SVGs
// are inlined at build time via ?raw + set:html, which ships no runtime and
// re-tints via currentColor and the diagram class hooks. This .astro page is not
// content-lint scanned (lint scans only MDX under src/content/pages), so no
// <Claim> tags belong here.
import { Image } from 'astro:assets';
import Base from '../layouts/Base.astro';
import CalloutCard from '../components/charts/CalloutCard.astro';
import SectionShell from '../components/SectionShell.astro';
import Figure from '../components/Figure.astro';
import TrustStrip from '../components/TrustStrip.astro';

// Curated photographic images, optimized by astro:assets at build time.
import heroImg from '../assets/generated/heroB/product-render-1-1.jpg';
import sectionImg from '../assets/generated/slot6-1.jpg';

// Build-time inlined SVGs. set:html keeps them CSP-clean and zero-JS, while
// currentColor + the .d-* class hooks let the host palette re-tint every stroke
// and fill.
import dataBoundary from '../assets/diagrams/data-boundary.svg?raw';
import tierSpectrum from '../assets/diagrams/tier-spectrum.svg?raw';
import costCrossover from '../assets/diagrams/cost-crossover.svg?raw';
---

<Base
  title="Sovereign On-Prem AI"
  description="Your AI runs on your hardware, not ours. Hive runs open-weight models on a box you own, so prompts and documents never leave your building."
  structuredData={{ organization: true, website: true, service: true, faq: true }}
>
  {/* ============ HERO (Apple-keynote two-column: lede + product render) ============
      A real two-column CSS grid. The left column carries the editorial lede; the
      right column places the product render directly on the off-white page (no
      ink panel, no frame). The render is on a seamless off-white background and
      carries its own soft shadow, so it sits in the page as a clean keynote shot. */}
  <section class="hero" aria-labelledby="hero-h1">
    <div class="shell hero__grid">
      <div class="hero__col load-rise">
        <p class="hero__meta">
          <span class="hero__tag">Sovereign AI</span>
          <span class="hero__metasep" aria-hidden="true">&#183;</span>
          <span class="hero__tag">On-prem</span>
          <span class="hero__metasep" aria-hidden="true">&#183;</span>
          <span class="hero__tag">Canada-led</span>
        </p>
        <h1 id="hero-h1" class="hero__h1">
          Your AI runs on your hardware. Not ours. Not anyone's.
        </h1>
        <ul class="hero__points" role="list">
          <li class="hero__point">Open-weight models on your own servers.</li>
          <li class="hero__point">No data leaves your building.</li>
          <li class="hero__point">No usage-based vendor bill.</li>
        </ul>
        <p class="hero__scope">
          Self-hosted deployment. Your hardware, your network, your data.
        </p>
        <div class="cta-row">
          <a class="cta cta--primary tap-target" href="/book-a-demo">Book a Demo</a>
          <a class="cta cta--ghost tap-target" href="/pricing">See Pricing</a>
        </div>
      </div>

      <div class="hero__media">
        <Image
          src={heroImg}
          alt="Hive on-prem AI appliance"
          widths={[768, 1100, 1376]}
          sizes="(max-width: 880px) 100vw, 50vw"
          width={1376}
          height={768}
          format="webp"
          loading="eager"
          fetchpriority="high"
          class="hero__img"
        />
      </div>
    </div>
  </section>

  {/* ============ TRUST STRIP (ink, one of two deliberate dark surfaces) ============ */}
  <TrustStrip />

  {/* ============ 01 THE CONTROL PROBLEM ============ */}
  <SectionShell numeral="01" label="Control, not trust" id="control-h2">
    <h2 class="section-h2 section-h2--lead">
      A data-protection promise is only as strong as the jurisdiction that
      can override it.
    </h2>
    <div class="prose">
      <p>
        Every cloud AI provider will tell you your data is protected, and
        most of them mean it. The limit is structural, not a question of
        intent. Any provider is subject to the laws of the jurisdiction it
        answers to, so a contractual promise can be overridden by a lawful
        order from that jurisdiction. This is true of every cloud, in every
        country. It is the nature of handing data to a third party.
      </p>
      <p>
        Hive changes the mechanism rather than the promise. The open-weight
        model runs on hardware you control. Your prompts, your documents, and
        the model's responses stay inside your own network boundary. On the
        on-prem deployment, inference runs locally and the request never
        calls an external API, so there is no third-party endpoint to
        subpoena and no external data path to audit. The data does not move,
        so there is nothing to compel.
      </p>
      <p>
        That is what we mean by sovereignty, scoped precisely: the on-prem
        Hive deployment keeps every inference inside your own infrastructure,
        under your physical and legal control.
        {/* LEGAL-REVIEW */}
      </p>
    </div>

    <ul class="ds__bleed cards cards--3" role="list">
      <li class="card">
        <h3 class="card__h3">You hold the hardware.</h3>
        <p class="card__body">
          The box sits on your premises, or in an in-region datacentre you
          select, under your own jurisdiction. You hold the keys, the logs, and
          the machine.
        </p>
      </li>
      <li class="card">
        <h3 class="card__h3">No request leaves the boundary.</h3>
        <p class="card__body">
          On the on-prem deployment, inference runs locally and never calls an
          external API. There is no provider in the loop, so there is no
          provider to be ordered to produce anything.
          {/* LEGAL-REVIEW */}
        </p>
      </li>
      <li class="card">
        <h3 class="card__h3">One vendor relationship, no metered data path.</h3>
        <p class="card__body">
          You license software and support from us. Your data never reaches us,
          because there is no place in the architecture for it to go.
          {/* LEGAL-REVIEW */}
        </p>
      </li>
      {/* NATIONALITY-SLOT: hold a fourth "A Canadian company." card until S Cubed Canadian registration confirmed. Do not render. */}
    </ul>
  </SectionShell>

  {/* ============ 02 WHAT YOU ARE EXPOSED TO TODAY ============ */}
  <SectionShell numeral="02" label="What a third-party cloud carries with it" id="exposure-h2">
    <h2 class="section-h2 section-h2--lead">
      Three exposures come bundled with every cloud AI subscription.
    </h2>
    <p class="prose prose--single">
      When your team puts a contract, a patient record, or a quarter's
      unpublished numbers into a third-party AI tool, three things follow
      that most buyers never priced in. None of them is about a bad provider.
      All three are properties of sending data outside your own control.
    </p>

    <ul class="ds__bleed exposures" role="list">
      <li class="exposure__item">
        <span class="exposure__index font-mono" data-numeric>01</span>
        <p class="exposure__text">
          <span class="exposure__lead">Reach that follows the provider, not the data.</span>
          Any cloud provider can be compelled, under the law of the jurisdiction
          it answers to, to produce customer data regardless of where that data
          physically sits. The US CLOUD Act is one well-known example of this
          general principle: it lets a US-controlled provider be ordered to hand
          over data even when that data lives in another country. The point is
          not specific to one law or one country. Residency is where the bytes
          live. Sovereignty is who can be ordered to produce them. They are not
          the same thing, in any jurisdiction.
          {/* LEGAL-REVIEW */}
        </p>
      </li>
      <li class="exposure__item">
        <span class="exposure__index font-mono" data-numeric>02</span>
        <p class="exposure__text">
          <span class="exposure__lead">Your prompts become records someone else holds.</span>
          A US court has already ordered a major AI provider to preserve chat
          logs. Prompts and responses sent to a third-party AI service are
          electronically stored information held by another party. They can be
          subpoenaed, preserved by order, and pulled into discovery, and a "we
          do not retain" promise gives way the moment a preservation order
          lands. The data you cannot see is the data you cannot govern.
          {/* LEGAL-REVIEW */}
        </p>
      </li>
      <li class="exposure__item">
        <span class="exposure__index font-mono" data-numeric>03</span>
        <p class="exposure__text">
          <span class="exposure__lead">A bill that grows with use.</span>
          A third-party AI API meters you per token. Light, occasional usage
          stays cheap. But once AI runs inside long-running agents, automations,
          and sustained daily work, the meter never stops and the bill grows
          with every win. The more value you get, the more it costs to keep
          getting it.
        </p>
      </li>
    </ul>

    <p class="prose__after">
      The first two exposures are about who can reach your data. The third is
      about who controls your cost. Running the model yourself addresses all
      three.
    </p>

    <div class="ds__bleed">
      <Figure
        fig="Fig. 1"
        svg={dataBoundary}
        caption="With Hive on-prem, your data stays inside your network boundary. Data sent to a third-party cloud crosses that boundary, and once it has crossed, the jurisdiction that governs it is no longer only yours."
      />
    </div>
  </SectionShell>

  {/* ============ 03 THREE-TIER SPECTRUM ============ */}
  <SectionShell numeral="03" label="A spectrum of control" id="spectrum-h2">
    <h2 class="section-h2">
      Run Hive at the level of control your data demands.
    </h2>
    <p class="prose prose--single">
      The same Hive software runs three ways, from maximum control to lowest
      cost. You choose the point that fits your data, your team, and your
      budget. Every hosted option runs only on infrastructure that sits
      in-region and under your own jurisdiction, because hosting you under a
      jurisdiction other than your own would reintroduce the exact exposure
      you came here to remove.
      {/* LEGAL-REVIEW */}
    </p>

    <div class="ds__bleed">
      <Figure
        fig="Fig. 2"
        svg={tierSpectrum}
        caption="The spectrum of control"
        ariaLabel="The control spectrum: Own on-prem, Colocate, Shared Cloud"
      />
    </div>

    <ul class="ds__bleed cards cards--3" role="list">
      <li class="card">
        <h3 class="card__h3">Own it (on-prem).</h3>
        <p class="card__body">
          A Hive deployment on your premises, run by your team. Data never
          leaves the building. The strongest position on the spectrum.
          {/* LEGAL-REVIEW */}
        </p>
      </li>
      <li class="card">
        <h3 class="card__h3">Colocate it.</h3>
        <p class="card__body">
          A dedicated Hive box, hosted by us in-region, on infrastructure under
          your own jurisdiction. We manage the hardware. Residency stays
          in-region.
        </p>
      </li>
      <li class="card">
        <h3 class="card__h3">Shared cloud.</h3>
        <p class="card__body">
          A region-locked shared instance with a full audit trail. Lowest cost,
          fastest start. Honestly the weakest tier on the spectrum, and we grade
          it that way.
        </p>
      </li>
    </ul>

    <p class="prose__after">
      <a class="textlink" href="/how-you-run-hive">Compare the three tiers</a>
    </p>
  </SectionShell>

  {/* ============ 04 THE OWNERSHIP COST STORY ============ */}
  <SectionShell numeral="04" label="Own the model, stop renting the meter" id="cost-h2">
    <h2 class="section-h2 section-h2--lead">
      An open-weight model you own costs a fraction of the API you rent.
    </h2>
    <div class="prose">
      <p>
        Open-weight models have closed much of the quality gap with the large
        proprietary APIs, and they run on hardware you can buy. Run the same
        workload on a model you own and the cost changes shape. There is a
        mostly fixed cost up front for the box, and after that inference is
        roughly the cost of the electricity it draws.
      </p>
      <p>
        Measured on output, open-weight inference runs roughly
        <span class="font-mono" data-numeric>10x</span> to
        <span class="font-mono" data-numeric>30x</span> cheaper than the
        proprietary APIs. For an entry-tier box handling sustained daily
        work, the box pays for itself in about
        <span class="font-mono" data-numeric>seven months</span>, then keeps
        saving every month after. For light, occasional use, a public API is
        a reasonable choice. For long-running agents, automations, and heavy
        daily usage, ownership is the cheaper position over time.
      </p>
    </div>

    <div class="ds__bleed cost__faceline">
      <span class="cost__facefig font-mono" data-numeric>~10x to ~30x</span>
      <span class="cost__facelabel">cheaper on output than proprietary APIs</span>
    </div>

    <div class="ds__bleed">
      <Figure
        fig="Fig. 3"
        svg={costCrossover}
        caption="Cumulative cost over time. The box you own passes metered cloud at breakeven, then keeps costing less."
      />
    </div>

    <p class="cost__citation">
      <span class="font-mono" data-numeric>Rates as of June 2026</span>.
      Hosted-API pricing, self-hosting economics differ.
    </p>

    <div class="cost__proof">
      <CalloutCard
        accent="teal"
        statement="Pinterest cut AI costs roughly 90 percent and raised accuracy roughly 30 percent by running and customising the open-weight Qwen3-VL model on its own infrastructure."
        asOf="as of June 2026"
      >
        <p slot="disclaimer" class="callout-disclaimer">
          Results reflect Pinterest's specific implementation. Your outcomes
          will depend on your hardware, model choice, and workload.
        </p>
      </CalloutCard>
    </div>

    <p class="prose__after">
      <a class="textlink" href="/pricing">See the full cost model</a>
    </p>
  </SectionShell>

  {/* ============ 05 WHO IT IS FOR ============ */}
  <SectionShell numeral="05" label="Built for confidential work" id="who-h2">
    <h2 class="section-h2 section-h2--lead">
      If your data cannot go to a third-party AI service, Hive runs in your
      own.
    </h2>
    <p class="prose prose--single">
      Hive is built for organisations whose data is the one thing they cannot
      put at risk. That covers a wide range of work, in many jurisdictions.
    </p>

    <ul class="ds__bleed cards cards--4" role="list">
      <li class="card">
        <h3 class="card__h3">Financial institutions.</h3>
        <p class="card__body">
          Banks, credit unions, insurers, and FinTech firms handling material
          non-public information, internal agents, and recordkeeping
          obligations that an external endpoint cannot satisfy.
        </p>
      </li>
      <li class="card">
        <h3 class="card__h3">Healthcare and health-data custodians.</h3>
        <p class="card__body">
          Personal health information stays under the custodian's direct
          control, with no external service provider in the data path.
        </p>
      </li>
      <li class="card">
        <h3 class="card__h3">Government and defence.</h3>
        <p class="card__body">
          Public-sector bodies and military programmes, including air-gapped
          and offline deployments where no outbound network path is permitted.
          {/* LEGAL-REVIEW */}
        </p>
      </li>
      <li class="card">
        <h3 class="card__h3">Legal.</h3>
        <p class="card__body">
          Privilege does not survive a third-party processor. Client matters
          and document review stay inside the firm.
        </p>
      </li>
      <li class="card">
        <h3 class="card__h3">Any sensitive-data organisation.</h3>
        <p class="card__body">
          If "do not send our data to a third party" is a hard rule, on-prem
          inference is the posture that keeps it. Trade secrets, source code,
          R&D, and competitive plans included.
          {/* LEGAL-REVIEW */}
        </p>
      </li>
    </ul>

    <p class="prose__after">
      <a class="textlink" href="/use-cases">See use cases by industry</a>
    </p>
  </SectionShell>

  {/* ============ 06 CONTINUOUS IMPROVEMENT ============ */}
  <SectionShell numeral="06" label="A platform, not a frozen box" id="improve-h2">
    <h2 class="section-h2 section-h2--lead">
      The box you buy keeps getting better.
    </h2>
    <div class="prose">
      <p>
        Owning your hardware does not mean owning a snapshot. Under the annual
        licence, the Hive software and the supported open-weight models keep
        improving. New and stronger open models are validated and made
        available as they ship, so your deployment tracks the state of the art
        instead of falling behind it. When a workload outgrows a single box,
        capacity is added: one box, then multiple GPUs, then multiple nodes.
        You are buying a platform that keeps improving, on hardware you can
        extend.
      </p>
    </div>

    <div class="ds__bleed">
      <Figure fig="Fig. 4" caption="Capacity you add, not a box you replace">
        <Image
          src={sectionImg}
          alt="Engineers extending a Hive deployment with additional GPU capacity."
          widths={[768, 1140]}
          sizes="(max-width: 1140px) 100vw, 1140px"
          width={1140}
          height={636}
          loading="lazy"
          class="improve__img"
        />
      </Figure>
    </div>

    <p class="prose__after">
      <a class="textlink" href="/how-you-run-hive">See how you run Hive</a>
    </p>
  </SectionShell>

  {/* ============ 07 COMPETITIVE ONE-LINER (the one Fraunces pull quote) ============ */}
  <SectionShell numeral="07" label="The difference in one line" id="oneliner-h2">
    <blockquote class="pullquote">
      Glean, Writer, Cohere North, and Copilot route every query through their
      cloud. Hive runs in yours.
    </blockquote>
  </SectionShell>

  {/* ============ 08 PROOF SPINE ============ */}
  <SectionShell numeral="08" label="What is actually true here" id="proof-h2">
    <ul class="ds__bleed proofs" role="list">
      <li class="proof__item">
        <span class="proof__label">Architecture</span>
        <p class="proof__text">
          Open-weight model weights run on your hardware. Every inference
          executes locally. No prompt, no response, no document calls an
          external API or leaves your network boundary.
          {/* LEGAL-REVIEW */}
        </p>
      </li>
      <li class="proof__item">
        <span class="proof__label">Perplexity</span>
        <p class="proof__text">
          Perplexity runs DeepSeek R1 on its own servers in US and EU data
          centres. User data never leaves Western servers.
          <span class="proof__stamp font-mono" data-numeric>(as of January 2025)</span>
        </p>
      </li>
      <li class="proof__item">
        <span class="proof__label">Regulatory category</span>
        <p class="proof__text">
          Finance, healthcare, and legal teams often cannot route sensitive
          data through an external AI endpoint. Self-hosted open weights keep
          every inference inside the network boundary. This is general
          information, not legal advice.
          {/* LEGAL-REVIEW */}
        </p>
      </li>
    </ul>
  </SectionShell>

  {/* ============ CLOSING CTA (ink, the second deliberate dark surface) ============ */}
  <section class="closing" aria-labelledby="closing-h2">
    <div class="shell narrow closing__inner">
      <h2 id="closing-h2" class="section-h2 section-h2--lead closing__h2">
        See it run inside your own network.
      </h2>
      <p class="closing__line">
        Book a short demo and we will show Hive running locally, with no data
        leaving the box.
      </p>
      <div class="cta-row">
        <a class="cta cta--on-ink tap-target" href="/book-a-demo">Book a Demo</a>
        <a class="cta cta--ghost-ink tap-target" href="/pricing">See Pricing</a>
      </div>
    </div>
  </section>
</Base>

<style>
  /* =========================================================================
     SHARED TYPE ROLES
     global.css forces h1 to Fraunces and h2..h6 to Inter. The dossier wants
     Fraunces for the H2 section titles and the pull quote, so each explicitly
     re-declares the display family to override the global Inter rule.
     ========================================================================= */
  .section-h2 {
    margin: 0 0 var(--space-6);
    font-family: var(--font-display);
    font-weight: 600;
    font-size: var(--text-h2-dossier);
    line-height: var(--lh-heading);
    letter-spacing: -0.01em;
    color: var(--text);
    max-width: 22ch;
  }

  .section-h2--lead {
    max-width: 24ch;
  }

  .prose {
    color: var(--text);
    font-size: var(--text-base);
    line-height: var(--lh-body);
  }

  .prose p {
    margin: 0 0 var(--space-5);
    max-width: 68ch;
  }

  .prose p:last-child {
    margin-bottom: 0;
  }

  .prose--single {
    margin: var(--space-5) 0 0;
    max-width: 66ch;
  }

  .prose__after {
    margin: var(--space-7) 0 0;
    font-size: var(--text-base);
    color: var(--text-muted);
    line-height: var(--lh-body);
    max-width: 68ch;
  }

  .textlink {
    color: var(--accent);
    font-weight: 600;
    border-bottom: 1px solid var(--accent-line);
    padding-bottom: 1px;
    transition: border-color var(--dur-fast) var(--ease-out);
  }

  .textlink:hover {
    border-bottom-color: var(--accent);
  }

  .narrow {
    max-width: 800px;
  }

  /* =========================================================================
     CTA BUTTONS
     Base buttons live on off-white. The closing section is ink, so it gets
     two ink-legible variants (verified: off-white on accent = 11.65:1,
     ink on off-white fill = 17.66:1, off-white ghost border passes).
     ========================================================================= */
  .cta-row {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-top: var(--space-7);
  }

  .cta {
    display: inline-flex;
    align-items: center;
    cursor: pointer;
    font-size: var(--text-sm);
    font-weight: 600;
    letter-spacing: 0.01em;
    padding: 14px 26px;
    border-radius: var(--radius-sm);
    transition: background-color var(--dur-base) var(--ease-out),
                border-color var(--dur-base) var(--ease-out),
                transform var(--dur-base) var(--ease-out);
  }

  .cta--primary {
    background-color: var(--accent);
    color: var(--bg);
    border: 1px solid var(--accent);
  }

  .cta--primary:hover {
    background-color: var(--accent-hover);
    border-color: var(--accent-hover);
    transform: translateY(-1px);
  }

  .cta--ghost {
    background-color: transparent;
    color: var(--text);
    border: 1px solid var(--border-strong);
  }

  .cta--ghost:hover {
    border-color: var(--text-faint);
    transform: translateY(-1px);
  }

  /* On-ink primary: off-white fill, ink label (17.66:1). */
  .cta--on-ink {
    background-color: var(--on-ink);
    color: var(--ink);
    border: 1px solid var(--on-ink);
  }

  .cta--on-ink:hover {
    background-color: #ffffff;
    border-color: #ffffff;
    transform: translateY(-1px);
  }

  /* On-ink ghost: off-white hairline border, off-white label. */
  .cta--ghost-ink {
    background-color: transparent;
    color: var(--on-ink);
    border: 1px solid var(--on-ink-border);
  }

  .cta--ghost-ink:hover {
    border-color: var(--on-ink);
    transform: translateY(-1px);
  }

  /* =========================================================================
     CARDS  (almost flat; one restrained hover-raise, no layout shift)
     ========================================================================= */
  .cards {
    list-style: none;
    margin: var(--space-8) 0 0;
    padding: 0;
    display: grid;
    gap: var(--space-5);
  }

  .cards--3 {
    grid-template-columns: repeat(3, 1fr);
  }

  .cards--4 {
    grid-template-columns: repeat(2, 1fr);
  }

  .card {
    background-color: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-card);
    padding: var(--space-6);
    transition: box-shadow var(--dur-base) var(--ease-out),
                transform var(--dur-base) var(--ease-out),
                border-color var(--dur-base) var(--ease-out);
  }

  .card:hover {
    box-shadow: var(--shadow-raise);
    transform: translateY(-2px);
    border-color: var(--border-strong);
  }

  .card__h3 {
    margin: 0 0 var(--space-3);
    font-size: var(--text-h3);
    line-height: var(--lh-heading);
    color: var(--text);
  }

  .card__body {
    margin: 0;
    color: var(--text-muted);
    font-size: var(--text-base);
    line-height: var(--lh-snug);
  }

  /* =========================================================================
     DIAGRAM COLOUR HOOKS (consumed by the SVGs inside Figure.astro)
     Inline attributes in the SVGs already paint the correct palette; these
     :global hooks let the assets re-tint cleanly from the host palette and keep
     ink line-work on currentColor. Red lives only inside data-boundary.svg.
     ========================================================================= */
  :global(.fig__svg .diagram) {
    color: var(--text);
  }
  :global(.fig__svg .d-faint) {
    stroke: var(--border-strong);
  }
  :global(.fig__svg .d-axis) {
    stroke: var(--border-strong);
  }
  :global(.fig__svg .d-muted) {
    fill: var(--text-muted);
  }
  :global(.fig__svg .d-grey) {
    stroke: var(--grey);
    fill: var(--grey);
  }
  :global(.fig__svg .d-accent) {
    stroke: var(--accent);
    fill: var(--accent);
  }
  :global(.fig__svg .d-accent-line),
  :global(.fig__svg .d-accent-ring),
  :global(.fig__svg .d-accent-dash) {
    stroke: var(--accent);
  }
  :global(.fig__svg .d-accent-text) {
    fill: var(--accent);
  }
  :global(.fig__svg .d-surface) {
    fill: var(--bg-card);
  }
  /* Residency-risk red lives only inside the data-boundary diagram. */
  :global(.fig__svg .d-risk) {
    stroke: var(--risk);
    fill: var(--risk);
  }
  :global(.fig__svg .d-risk-text) {
    fill: var(--risk);
  }
  :global(.fig__svg .diagram text) {
    font-family: var(--font-sans);
  }
  :global(.fig__svg .d-num) {
    font-family: var(--font-mono);
  }

  /* =========================================================================
     HERO  (asymmetric editorial + full-bleed ink right panel)
     Left column rides the shell; right panel escapes to the viewport edge via
     a 50vw absolute panel on desktop. Balanced top, no dead gap above the
     meta-row.
     ========================================================================= */
  .hero {
    position: relative;
    /* Clip the 50vw absolute media panel so it can bleed to the right viewport
       edge without ever creating horizontal scroll. Clip (not hidden) so the
       page never traps scroll position. */
    overflow-x: clip;
    padding-block: var(--space-6) var(--space-8);
  }

  /* Desktop: a single content column on the left at ~52% of the shell; the
     hero media is taken out of flow as an absolute full-bleed panel that owns
     the right half. Vertically centred so the lede reads as a balanced
     composition against the appliance. */
  .hero__grid {
    display: grid;
    grid-template-columns: minmax(0, 52%);
    gap: var(--space-8);
    align-items: center;
  }

  .hero__col {
    position: relative;
    z-index: 1;
    max-width: 36rem;
    padding-block: 0 var(--space-6);
  }

  /* Thin meta-row, hairline separators, small uppercase tags. No fake badges. */
  .hero__meta {
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-3);
    padding-bottom: var(--space-4);
    border-bottom: 1px solid var(--border);
  }

  .hero__tag {
    color: var(--text-muted);
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .hero__metasep {
    color: var(--text-faint);
  }

  /* Hero-specific H1 scale, raised toward 64-80 without touching the global
     h1 base used by other pages. */
  .hero__h1 {
    margin: var(--space-6) 0 0;
    font-size: clamp(44px, 6vw, 76px);
    line-height: var(--lh-tight);
    letter-spacing: -0.02em;
    max-width: 15ch;
  }

  .hero__points {
    list-style: none;
    margin: var(--space-7) 0 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .hero__point {
    position: relative;
    padding-left: var(--space-6);
    font-size: var(--text-md);
    line-height: var(--lh-snug);
    color: var(--text);
  }

  /* Each subhead fragment leads with a forest tick, not a plain dash. */
  .hero__point::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.62em;
    width: 14px;
    height: 2px;
    background-color: var(--accent);
  }

  .hero__scope {
    margin: var(--space-6) 0 0;
    color: var(--text-faint);
    font-size: var(--text-sm);
    line-height: var(--lh-snug);
    max-width: 48ch;
  }

  /* Desktop full-bleed right panel: pinned to the top, right, and bottom of the
     hero and sized slightly wider than the empty grid track so it always reaches
     the viewport edge. Sits behind the lede (z-index 0 vs the col's z-index 1).
     The literal #efece8 matches the render's own warm corner colour so any
     sub-pixel gap at the feathered edge blends into the image, not the page.
     A left-to-right mask feathers the inner ~22% from transparent to opaque so
     there is no hard vertical seam against the off-white page. */
  .hero__media {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    width: 52vw;
    z-index: 0;
    background-color: #efece8;
    transform-origin: center;
    -webkit-mask-image: linear-gradient(
      to right,
      transparent 0%,
      rgba(0, 0, 0, 0) 4%,
      #000 26%,
      #000 100%
    );
    mask-image: linear-gradient(
      to right,
      transparent 0%,
      rgba(0, 0, 0, 0) 4%,
      #000 26%,
      #000 100%
    );
  }

  /* The render fills the tall panel via object-fit:cover, so the appliance
     reads large and confident rather than as a floating thumbnail. No radius,
     no drop shadow: a full bleed has no floating-card edge. */
  .hero__img {
    display: block;
    width: 100%;
    height: 100%;
    max-width: none;
    object-fit: cover;
    object-position: center;
    opacity: 1;
  }

  /* =========================================================================
     02 EXPOSURE  (numbered stack)
     ========================================================================= */
  .exposures {
    list-style: none;
    margin: var(--space-8) 0 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
  }

  .exposure__item {
    border-top: 1px solid var(--border-strong);
    padding-top: var(--space-4);
  }

  .exposure__index {
    display: block;
    margin-bottom: var(--space-3);
    color: var(--accent);
    font-size: var(--text-sm);
    font-weight: 500;
    letter-spacing: 0.04em;
  }

  .exposure__text {
    margin: 0;
    color: var(--text);
    font-size: var(--text-base);
    line-height: var(--lh-snug);
  }

  .exposure__lead {
    font-weight: 600;
    color: var(--text);
  }

  /* =========================================================================
     04 COST  (on-face figure callout)
     ========================================================================= */
  .cost__faceline {
    margin: var(--space-7) 0 0;
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: var(--space-4);
    padding: var(--space-6) var(--space-7);
    background-color: var(--accent-wash);
    border: 1px solid var(--accent-line);
    border-radius: var(--radius-card);
  }

  .cost__facefig {
    color: var(--accent);
    font-size: clamp(32px, 4.5vw, 48px);
    font-weight: 500;
    line-height: 1;
    letter-spacing: -0.01em;
  }

  .cost__facelabel {
    color: var(--text-muted);
    font-size: var(--text-md);
    line-height: var(--lh-snug);
  }

  .cost__citation {
    margin: var(--space-5) 0 0;
    color: var(--text-faint);
    font-size: var(--text-sm);
    line-height: var(--lh-snug);
  }

  .cost__proof {
    margin: var(--space-7) 0 0;
    max-width: 640px;
  }

  .callout-disclaimer {
    margin: 0;
    color: var(--text-faint);
    font-size: var(--text-xs);
    line-height: var(--lh-snug);
  }

  /* =========================================================================
     06 CONTINUOUS IMPROVEMENT  (photo inside Figure frame)
     ========================================================================= */
  .improve__img {
    display: block;
    width: 100%;
    height: auto;
  }

  /* =========================================================================
     07 COMPETITIVE PULL QUOTE  (a Fraunces exception)
     ========================================================================= */
  .pullquote {
    margin: 0;
    padding: 0;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: clamp(28px, 3.4vw, 38px);
    line-height: var(--lh-snug);
    letter-spacing: -0.01em;
    color: var(--text);
    max-width: 22ch;
  }

  /* =========================================================================
     08 PROOF SPINE
     ========================================================================= */
  .proofs {
    list-style: none;
    margin: var(--space-7) 0 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
  }

  .proof__item {
    border-top: 1px solid var(--border-strong);
    padding-top: var(--space-4);
  }

  .proof__label {
    display: block;
    margin-bottom: var(--space-2);
    color: var(--accent);
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .proof__text {
    margin: 0;
    color: var(--text-muted);
    font-size: var(--text-sm);
    line-height: var(--lh-body);
  }

  .proof__stamp {
    color: var(--text-faint);
    font-size: 12.5px;
    white-space: nowrap;
  }

  /* =========================================================================
     CLOSING  (ink, the second deliberate dark surface)
     ========================================================================= */
  .closing {
    background-color: var(--ink);
    color: var(--on-ink);
    border-top: 1px solid var(--on-ink-border);
    padding-block: var(--space-9);
    position: relative;
    overflow: hidden;
  }

  .closing__inner {
    position: relative;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    z-index: 1;
  }

  .closing__h2 {
    color: var(--on-ink);
    max-width: 20ch;
  }

  .closing__line {
    margin: 0;
    color: var(--on-ink-muted);
    font-size: var(--text-md);
    line-height: var(--lh-body);
    max-width: 52ch;
  }

  .closing .cta-row {
    justify-content: center;
  }

  /* =========================================================================
     PAGE-LOAD REVEAL  (one orchestrated, staggered entrance on hero)
     Guarded by prefers-reduced-motion: no-preference. Content is visible by
     default (the keyframes start near full opacity and end at full), so any
     browser that ignores the animation still shows everything. No layout shift.
     ========================================================================= */
  @media (prefers-reduced-motion: no-preference) {
    .load-rise {
      animation: load-rise var(--dur-reveal) var(--ease-out) both;
    }
    .load-rise > * {
      animation: load-rise var(--dur-reveal) var(--ease-out) both;
    }
    .load-rise > *:nth-child(1) { animation-delay: 40ms; }
    .load-rise > *:nth-child(2) { animation-delay: 110ms; }
    .load-rise > *:nth-child(3) { animation-delay: 180ms; }
    .load-rise > *:nth-child(4) { animation-delay: 250ms; }
    .load-rise > *:nth-child(5) { animation-delay: 320ms; }
    .load-rise > *:nth-child(6) { animation-delay: 390ms; }

    .load-fade {
      animation: load-fade var(--dur-reveal) var(--ease-out) 120ms both;
    }

    /* Product render settles in: a subtle fade and scale-up. Starts near-full
       opacity (0.85) and a barely-scaled 0.98, so it is never hidden if the
       animation does not run. transform-origin is set on the base rule. The
       panel stays full-bleed correct: scale animates transform only, which does
       not affect the absolute pin, and the final state is scale(1) opacity(1). */
    .hero__media {
      animation: hero-img-settle var(--dur-reveal) var(--ease-out) 140ms both;
    }
  }

  /* Entrance shifts position and fades from near-full opacity. Because the
     start opacity is high (0.6) and there is no persistent opacity:0 state,
     content is never hidden if the animation does not run. */
  @keyframes load-rise {
    from {
      transform: translateY(var(--reveal-shift));
      opacity: 0.6;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  @keyframes load-fade {
    from {
      opacity: 0.7;
    }
    to {
      opacity: 1;
    }
  }

  /* Subtle, premium settle for the product render. No persistent hidden state. */
  @keyframes hero-img-settle {
    from {
      opacity: 0.85;
      transform: scale(0.98);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  /* =========================================================================
     RESPONSIVE
     ========================================================================= */
  @media (max-width: 1024px) {
    .hero__grid {
      gap: var(--space-7);
    }
  }

  @media (max-width: 880px) {
    /* Hero collapses to a single column. The media leaves the absolute panel,
       returns to normal flow above the lede, and runs full width on off-white
       at its natural height. The desktop seam mask and corner-colour backdrop
       are both dropped so a contain image is never clipped or fringed. */
    .hero {
      padding-block: var(--space-7) var(--space-8);
    }
    .hero__grid {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }
    .hero__media {
      position: static;
      width: 100%;
      height: auto;
      order: -1;
      background-color: transparent;
      -webkit-mask-image: none;
      mask-image: none;
    }
    .hero__img {
      width: 100%;
      height: auto;
      max-width: 100%;
      object-fit: contain;
    }
    .hero__col {
      max-width: none;
    }

    .cards--3,
    .cards--4,
    .exposures,
    .proofs {
      grid-template-columns: 1fr;
    }
  }
</style>
